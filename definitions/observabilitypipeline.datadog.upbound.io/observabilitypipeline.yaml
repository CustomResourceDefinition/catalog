---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: observabilitypipelines.observabilitypipeline.datadog.upbound.io
spec:
  group: observabilitypipeline.datadog.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - datadog
    kind: ObservabilityPipeline
    listKind: ObservabilityPipelineList
    plural: observabilitypipelines
    singular: observabilitypipeline
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ObservabilityPipeline is the Schema for the ObservabilityPipelines
          API. Provides a Datadog Observability Pipeline resource. Observability Pipelines
          allows you to collect and process logs within your own infrastructure, and
          then route them to downstream integrations. Datadog recommends using the
          -parallelism=1 option to apply this resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ObservabilityPipelineSpec defines the desired state of ObservabilityPipeline
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  config:
                    description: |-
                      (Block List) Configuration for the pipeline. (see below for nested schema)
                      Configuration for the pipeline.
                    items:
                      properties:
                        destination:
                          description: |-
                            (Block List) List of destinations. (see below for nested schema)
                            List of destinations.
                          items:
                            properties:
                              amazonOpensearch:
                                description: |-
                                  (Block List) The amazon_opensearch destination writes logs to Amazon OpenSearch. (see below for nested schema)
                                  The `amazon_opensearch` destination writes logs to Amazon OpenSearch.
                                items:
                                  properties:
                                    auth:
                                      description: (Block List) (see below for nested
                                        schema)
                                      items:
                                        properties:
                                          assumeRole:
                                            description: |-
                                              (String) ARN of the role to assume.
                                              ARN of the role to assume.
                                            type: string
                                          awsRegion:
                                            description: |-
                                              (String) AWS region override (if applicable).
                                              AWS region override (if applicable).
                                            type: string
                                          externalId:
                                            description: |-
                                              (String) External ID for assumed role.
                                              External ID for assumed role.
                                            type: string
                                          sessionName:
                                            description: |-
                                              (String) Session name for assumed role.
                                              Session name for assumed role.
                                            type: string
                                          strategy:
                                            description: |-
                                              (String) The authentication strategy to use (e.g. aws or basic).
                                              The authentication strategy to use (e.g. aws or basic).
                                            type: string
                                        type: object
                                      type: array
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    bulkIndex:
                                      description: |-
                                        (String) The index or datastream to write logs to.
                                        The index or datastream to write logs to.
                                      type: string
                                  type: object
                                type: array
                              amazonS3:
                                description: |-
                                  rehydratable format to an Amazon S3 bucket for archiving. (see below for nested schema)
                                  The `amazon_s3` destination sends your logs in Datadog-rehydratable format to an Amazon S3 bucket for archiving.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        AWS authentication credentials used for accessing AWS services. If omitted, the system's default credentials are used (for example, the IAM role and environment variables).
                                      items:
                                        properties:
                                          assumeRole:
                                            description: |-
                                              (String) ARN of the role to assume.
                                              The Amazon Resource Name (ARN) of the role to assume.
                                            type: string
                                          externalId:
                                            description: |-
                                              (String) External ID for assumed role.
                                              A unique identifier for cross-account role assumption.
                                            type: string
                                          sessionName:
                                            description: |-
                                              (String) Session name for assumed role.
                                              A session identifier used for logging and tracing the assumed role session.
                                            type: string
                                        type: object
                                      type: array
                                    bucket:
                                      description: |-
                                        (String) S3 bucket name.
                                        S3 bucket name.
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression configuration for archived logs. When omitted, the worker default (gzip) is used.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              Compression algorithm. One of `gzip` or `zstd`. Valid values are `gzip`, `zstd`.
                                            type: string
                                          level:
                                            description: |-
                                              9) and zstd (1-21).
                                              Compression level. Applies to `gzip` (1-9) and `zstd` (1-21).
                                            type: number
                                        type: object
                                      type: array
                                    keyPrefix:
                                      description: |-
                                        (String) Prefix for object keys.
                                        Prefix for object keys.
                                      type: string
                                    region:
                                      description: |-
                                        (String) AWS region of the S3 bucket.
                                        AWS region of the S3 bucket.
                                      type: string
                                    serverSideEncryption:
                                      description: |-
                                        side encryption algorithm used when storing objects in S3. Valid values: aws:kms, AES256. Valid values are aws:kms, AES256.
                                        The server-side encryption algorithm used when storing objects in S3. Valid values: `aws:kms`, `AES256`. Valid values are `aws:kms`, `AES256`.
                                      type: string
                                    ssekmsKeyId:
                                      description: |-
                                        KMS encryption. Only applies when server_side_encryption is aws:kms.
                                        ID of the AWS KMS key to use for SSE-KMS encryption. Only applies when `server_side_encryption` is `aws:kms`.
                                      type: string
                                    storageClass:
                                      description: |-
                                        (String) S3 storage class. Valid values are STANDARD, REDUCED_REDUNDANCY, INTELLIGENT_TIERING, STANDARD_IA, EXPRESS_ONEZONE, ONEZONE_IA, GLACIER, GLACIER_IR, DEEP_ARCHIVE.
                                        S3 storage class. Valid values are `STANDARD`, `REDUCED_REDUNDANCY`, `INTELLIGENT_TIERING`, `STANDARD_IA`, `EXPRESS_ONEZONE`, `ONEZONE_IA`, `GLACIER`, `GLACIER_IR`, `DEEP_ARCHIVE`.
                                      type: string
                                  type: object
                                type: array
                              amazonS3Generic:
                                description: |-
                                  (Block List) The amazon_s3_generic destination sends your logs to an Amazon S3 bucket. (see below for nested schema)
                                  The `amazon_s3_generic` destination sends your logs to an Amazon S3 bucket.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        AWS authentication credentials used for accessing AWS services. If omitted, the system's default credentials are used (for example, the IAM role and environment variables).
                                      items:
                                        properties:
                                          assumeRole:
                                            description: |-
                                              (String) ARN of the role to assume.
                                              The Amazon Resource Name (ARN) of the role to assume.
                                            type: string
                                          externalId:
                                            description: |-
                                              (String) External ID for assumed role.
                                              A unique identifier for cross-account role assumption.
                                            type: string
                                          sessionName:
                                            description: |-
                                              (String) Session name for assumed role.
                                              A session identifier used for logging and tracing the assumed role session.
                                            type: string
                                        type: object
                                      type: array
                                    batchSettings:
                                      description: |-
                                        (Block List) Event batching settings. (see below for nested schema)
                                        Event batching settings.
                                      items:
                                        properties:
                                          batchSize:
                                            description: |-
                                              (Number) Maximum batch size in bytes.
                                              Maximum batch size in bytes.
                                            type: number
                                          timeoutSecs:
                                            description: |-
                                              (Number) Maximum number of seconds to wait before flushing the batch.
                                              Maximum number of seconds to wait before flushing the batch.
                                            type: number
                                        type: object
                                      type: array
                                    bucket:
                                      description: |-
                                        (String) S3 bucket name.
                                        S3 bucket name.
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression configuration.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              Compression algorithm. Valid values are `gzip`, `zstd`, `snappy`.
                                            type: string
                                          level:
                                            description: |-
                                              9) and zstd (1-21).
                                              Compression level.
                                            type: number
                                        type: object
                                      type: array
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        Encoding format for the destination.
                                      items:
                                        properties:
                                          type:
                                            description: |-
                                              (String) The encoding type. Valid values are json, parquet.
                                              The encoding type. Valid values are `json`, `parquet`.
                                            type: string
                                        type: object
                                      type: array
                                    keyPrefix:
                                      description: |-
                                        (String) Prefix for object keys.
                                        Optional prefix for object keys.
                                      type: string
                                    region:
                                      description: |-
                                        (String) AWS region of the S3 bucket.
                                        AWS region of the S3 bucket.
                                      type: string
                                    serverSideEncryption:
                                      description: |-
                                        side encryption algorithm used when storing objects in S3. Valid values: aws:kms, AES256. Valid values are aws:kms, AES256.
                                        The server-side encryption algorithm used when storing objects in S3. Valid values: `aws:kms`, `AES256`. Valid values are `aws:kms`, `AES256`.
                                      type: string
                                    ssekmsKeyId:
                                      description: |-
                                        KMS encryption. Only applies when server_side_encryption is aws:kms.
                                        ID of the AWS KMS key to use for SSE-KMS encryption. Only applies when `server_side_encryption` is `aws:kms`.
                                      type: string
                                    storageClass:
                                      description: |-
                                        (String) S3 storage class. Valid values are STANDARD, REDUCED_REDUNDANCY, INTELLIGENT_TIERING, STANDARD_IA, EXPRESS_ONEZONE, ONEZONE_IA, GLACIER, GLACIER_IR, DEEP_ARCHIVE.
                                        S3 storage class. Valid values are `STANDARD`, `REDUCED_REDUNDANCY`, `INTELLIGENT_TIERING`, `STANDARD_IA`, `EXPRESS_ONEZONE`, `ONEZONE_IA`, `GLACIER`, `GLACIER_IR`, `DEEP_ARCHIVE`.
                                      type: string
                                  type: object
                                type: array
                              amazonSecurityLake:
                                description: |-
                                  (Block List) The amazon_security_lake destination sends your logs to Amazon Security Lake. (see below for nested schema)
                                  The `amazon_security_lake` destination sends your logs to Amazon Security Lake.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        AWS authentication credentials used for accessing AWS services. If omitted, the system's default credentials are used (for example, the IAM role and environment variables).
                                      items:
                                        properties:
                                          assumeRole:
                                            description: |-
                                              (String) ARN of the role to assume.
                                              The Amazon Resource Name (ARN) of the role to assume.
                                            type: string
                                          externalId:
                                            description: |-
                                              (String) External ID for assumed role.
                                              A unique identifier for cross-account role assumption.
                                            type: string
                                          sessionName:
                                            description: |-
                                              (String) Session name for assumed role.
                                              A session identifier used for logging and tracing the assumed role session.
                                            type: string
                                        type: object
                                      type: array
                                    bucket:
                                      description: |-
                                        (String) S3 bucket name.
                                        Name of the Amazon S3 bucket in Security Lake (3-63 characters).
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    customSourceName:
                                      description: |-
                                        (String) Custom source name for the logs in Security Lake.
                                        Custom source name for the logs in Security Lake.
                                      type: string
                                    region:
                                      description: |-
                                        (String) AWS region of the S3 bucket.
                                        AWS region of the Security Lake bucket.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              azureStorage:
                                description: |-
                                  (Block List) The azure_storage destination forwards logs to an Azure Blob Storage container. (see below for nested schema)
                                  The `azure_storage` destination forwards logs to an Azure Blob Storage container.
                                items:
                                  properties:
                                    blobPrefix:
                                      description: |-
                                        (String) Optional prefix for blobs written to the container.
                                        Optional prefix for blobs written to the container.
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression configuration for archived logs. When omitted, the worker default (gzip) is used.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              Compression algorithm. One of `gzip` or `zstd`. Valid values are `gzip`, `zstd`.
                                            type: string
                                          level:
                                            description: |-
                                              9) and zstd (1-21).
                                              Compression level. Applies to `gzip` (1-9) and `zstd` (1-21).
                                            type: number
                                        type: object
                                      type: array
                                    connectionStringKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the Azure Storage connection string.
                                        Name of the environment variable or secret that holds the Azure Storage connection string.
                                      type: string
                                    containerName:
                                      description: |-
                                        (String) The name of the Azure Blob Storage container to store logs in.
                                        The name of the Azure Blob Storage container to store logs in.
                                      type: string
                                  type: object
                                type: array
                              clickhouse:
                                description: |-
                                  (Block List) The clickhouse destination forwards logs to a ClickHouse server via HTTP. (see below for nested schema)
                                  The `clickhouse` destination forwards logs to a ClickHouse server via HTTP.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        Authentication strategy for ClickHouse HTTP requests. Only `basic` strategy is supported.
                                      items:
                                        properties:
                                          passwordKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                              Name of the environment variable or secret that holds the ClickHouse password. Defaults to `DESTINATION_CLICKHOUSE_PASSWORD`.
                                            type: string
                                          strategy:
                                            description: |-
                                              (String) The authentication strategy to use (e.g. aws or basic).
                                              Authentication strategy. Must be `basic`. Valid values are `basic`.
                                            type: string
                                          usernameKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                              Name of the environment variable or secret that holds the ClickHouse username. Defaults to `DESTINATION_CLICKHOUSE_USERNAME`.
                                            type: string
                                        type: object
                                      type: array
                                    batch:
                                      description: |-
                                        (Block List) Batching configuration for ClickHouse inserts. (see below for nested schema)
                                        Batching configuration for ClickHouse inserts.
                                      items:
                                        properties:
                                          maxEvents:
                                            description: |-
                                              (Number) Maximum events for the memory buffer.
                                              Maximum number of events per batch. Value must be at least 1.
                                            type: number
                                          timeoutSecs:
                                            description: |-
                                              (Number) Maximum number of seconds to wait before flushing the batch.
                                              Maximum time in seconds before a partial batch is flushed. Value must be between 1 and 65535.
                                            type: number
                                        type: object
                                      type: array
                                    batchEncoding:
                                      description: |-
                                        (Block List) Batch encoding configuration. Required when format is arrow_stream. (see below for nested schema)
                                        Batch encoding configuration. Required when `format` is `arrow_stream`.
                                      items:
                                        properties:
                                          allowNullableFields:
                                            description: |-
                                              nullable fields in the ClickHouse schema. Defaults to false.
                                              If `true`, allows null values for non-nullable fields in the ClickHouse schema. Defaults to `false`.
                                            type: boolean
                                          codec:
                                            description: |-
                                              (String) Batch encoding codec. Must be arrow_stream. Valid values are arrow_stream.
                                              Batch encoding codec. Must be `arrow_stream`. Valid values are `arrow_stream`.
                                            type: string
                                        type: object
                                      type: array
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression for outbound HTTP requests. Use `algorithm = "gzip"` or `algorithm = "none"`.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              Compression algorithm. Valid values are `gzip` and `none`. Valid values are `gzip`, `none`.
                                            type: string
                                          level:
                                            description: |-
                                              9) and zstd (1-21).
                                              Compression level (1–9). Only valid when `algorithm` is `gzip`. Value must be between 1 and 9.
                                            type: number
                                        type: object
                                      type: array
                                    database:
                                      description: |-
                                        (String) Optional name of the ClickHouse database to write to. When omitted, the user's default database is used.
                                        Optional name of the ClickHouse database to write to. When omitted, the user's default database is used.
                                      type: string
                                    dateTimeBestEffort:
                                      description: |-
                                        (Boolean) If true, enables flexible DateTime parsing on the server side.
                                        If `true`, enables flexible DateTime parsing on the server side.
                                      type: boolean
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to `DESTINATION_CLICKHOUSE_ENDPOINT_URL`.
                                      type: string
                                    format:
                                      description: |-
                                        (String) Insert format for events. json_each_row maps event fields to columns by name. json_as_object and json_as_string insert each event into a single JSON or String column. arrow_stream batches events with Apache Arrow IPC streaming and requires batch_encoding. Valid values are json_each_row, json_as_object, json_as_string, arrow_stream.
                                        Insert format for events. `json_each_row` maps event fields to columns by name. `json_as_object` and `json_as_string` insert each event into a single JSON or String column. `arrow_stream` batches events with Apache Arrow IPC streaming and requires `batch_encoding`. Valid values are `json_each_row`, `json_as_object`, `json_as_string`, `arrow_stream`.
                                      type: string
                                    skipUnknownFields:
                                      description: |-
                                        (Boolean) If true, fields not present in the target table schema are dropped instead of causing insert errors. When unset, the ClickHouse server's own input_format_skip_unknown_fields setting applies.
                                        If `true`, fields not present in the target table schema are dropped instead of causing insert errors. When unset, the ClickHouse server's own `input_format_skip_unknown_fields` setting applies.
                                      type: boolean
                                    table:
                                      description: |-
                                        (String) Target ClickHouse table name.
                                        Target ClickHouse table name.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              cloudPrem:
                                description: |-
                                  (Block List) The cloud_prem destination sends logs to Datadog CloudPrem. (see below for nested schema)
                                  The `cloud_prem` destination sends logs to Datadog CloudPrem.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the endpoint URL.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          serverName:
                                            description: |-
                                              (String) Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                              Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              crowdstrikeNextGenSiem:
                                description: |-
                                  (Block List) The crowdstrike_next_gen_siem destination forwards logs to CrowdStrike Next Gen SIEM. (see below for nested schema)
                                  The `crowdstrike_next_gen_siem` destination forwards logs to CrowdStrike Next Gen SIEM.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression configuration for log events.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              Compression algorithm for log events.
                                            type: string
                                          level:
                                            description: |-
                                              9) and zstd (1-21).
                                              Compression level.
                                            type: number
                                        type: object
                                      type: array
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        Encoding format for log events. Valid values are `json`, `raw_message`.
                                      type: string
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the endpoint URL.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                    tokenKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the authentication token.
                                        Name of the environment variable or secret that holds the authentication token.
                                      type: string
                                  type: object
                                type: array
                              databricksZerobus:
                                description: |-
                                  (Block List) The databricks_zerobus destination sends logs to Databricks via the Zerobus ingestion API. (see below for nested schema)
                                  The `databricks_zerobus` destination sends logs to Databricks via the Zerobus ingestion API.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        OAuth client credentials used to authenticate with Databricks.
                                      items:
                                        properties:
                                          clientId:
                                            description: |-
                                              (String) The OAuth client ID used to authenticate with Databricks.
                                              The OAuth client ID used to authenticate with Databricks.
                                            type: string
                                          clientSecretKey:
                                            description: |-
                                              (String) The name of the secret or environment variable holding the OAuth client secret. Defaults to DESTINATION_DATABRICKS_ZEROBUS_OAUTH_CLIENT_SECRET.
                                              The name of the secret or environment variable holding the OAuth client secret. Defaults to `DESTINATION_DATABRICKS_ZEROBUS_OAUTH_CLIENT_SECRET`.
                                            type: string
                                        type: object
                                      type: array
                                    ingestionEndpointKey:
                                      description: |-
                                        (String) The name of the secret or environment variable holding the Databricks Zerobus ingestion endpoint URL.
                                        The name of the secret or environment variable holding the Databricks Zerobus ingestion endpoint URL.
                                      type: string
                                    tableName:
                                      description: |-
                                        (String) The name of the Databricks table to ingest logs into.
                                        The name of the Databricks table to ingest logs into.
                                      type: string
                                    unityCatalogEndpointKey:
                                      description: |-
                                        (String) The name of the secret or environment variable holding the Databricks Unity Catalog endpoint URL.
                                        The name of the secret or environment variable holding the Databricks Unity Catalog endpoint URL.
                                      type: string
                                  type: object
                                type: array
                              datadogLogs:
                                description: |-
                                  (Block List) The datadog_logs destination forwards logs to Datadog Log Management. (see below for nested schema)
                                  The `datadog_logs` destination forwards logs to Datadog Log Management.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    routes:
                                      description: |-
                                        (Block List) A list of routing rules that forward matching logs to Datadog using dedicated API keys. (see below for nested schema)
                                        A list of routing rules that forward matching logs to Datadog using dedicated API keys.
                                      items:
                                        properties:
                                          apiKeyKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that stores the Datadog API key used by this route.
                                              Name of the environment variable or secret that stores the Datadog API key used by this route.
                                            type: string
                                          buffer:
                                            description: |-
                                              (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                              Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                            items:
                                              properties:
                                                disk:
                                                  description: |-
                                                    (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                                    Options for configuring a disk buffer. Cannot be used with `memory`.
                                                  items:
                                                    properties:
                                                      maxSize:
                                                        description: |-
                                                          (Number) Maximum size of the disk buffer (in bytes).
                                                          Maximum size of the disk buffer (in bytes).
                                                        type: number
                                                      whenFull:
                                                        description: |-
                                                          (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                          Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                        type: string
                                                    type: object
                                                  type: array
                                                memory:
                                                  description: |-
                                                    (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                                    Options for configuring a memory buffer. Cannot be used with `disk`.
                                                  items:
                                                    properties:
                                                      maxEvents:
                                                        description: |-
                                                          (Number) Maximum events for the memory buffer.
                                                          Maximum events for the memory buffer.
                                                        type: number
                                                      maxSize:
                                                        description: |-
                                                          (Number) Maximum size of the disk buffer (in bytes).
                                                          Maximum size of the memory buffer (in bytes).
                                                        type: number
                                                      whenFull:
                                                        description: |-
                                                          (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                          Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                        type: string
                                                    type: object
                                                  type: array
                                              type: object
                                            type: array
                                          include:
                                            description: |-
                                              (String) A Datadog search query that determines which logs are forwarded using this route.
                                              A Datadog search query that determines which logs are forwarded using this route.
                                            type: string
                                          routeId:
                                            description: |-
                                              (String) Unique identifier for this route within the destination.
                                              Unique identifier for this route within the destination.
                                            type: string
                                          site:
                                            description: |-
                                              (String) Datadog site where matching logs are sent (for example, us1).
                                              Datadog site where matching logs are sent (for example, `us1`).
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              datadogMetrics:
                                description: |-
                                  (Block List) The datadog_metrics destination forwards metrics to Datadog. (see below for nested schema)
                                  The `datadog_metrics` destination forwards metrics to Datadog.
                                items:
                                  type: object
                                type: array
                              elasticsearch:
                                description: |-
                                  (Block List) The elasticsearch destination writes logs or metrics to an Elasticsearch cluster. (see below for nested schema)
                                  The `elasticsearch` destination writes logs or metrics to an Elasticsearch cluster.
                                items:
                                  properties:
                                    apiVersion:
                                      description: |-
                                        detect. Valid values are auto, v6, v7, v8.
                                        The Elasticsearch API version to use. Set to `auto` to auto-detect. Valid values are `auto`, `v6`, `v7`, `v8`.
                                      type: string
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        Authentication settings for the Elasticsearch destination.
                                      items:
                                        properties:
                                          passwordKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                              Name of the environment variable or secret that holds the Elasticsearch password (used when `strategy` is `basic`).
                                            type: string
                                          strategy:
                                            description: |-
                                              (String) The authentication strategy to use (e.g. aws or basic).
                                              The authentication strategy to use. Valid values are `basic`, `aws`.
                                            type: string
                                          usernameKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                              Name of the environment variable or secret that holds the Elasticsearch username (used when `strategy` is `basic`).
                                            type: string
                                        type: object
                                      type: array
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    bulkIndex:
                                      description: |-
                                        (String) The index or datastream to write logs to.
                                        The name of the index to write events to in Elasticsearch.
                                      type: string
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression configuration for the Elasticsearch destination.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              The compression algorithm applied when sending data to Elasticsearch. Valid values are `none`, `gzip`, `zlib`, `zstd`, `snappy`.
                                            type: string
                                          level:
                                            description: |-
                                              9) and zstd (1-21).
                                              The compression level. Only applicable for `gzip`, `zlib`, and `zstd` algorithms.
                                            type: number
                                        type: object
                                      type: array
                                    dataStream:
                                      description: |-
                                        (Block List) Configuration options for writing to Elasticsearch Data Streams instead of a fixed index. (see below for nested schema)
                                        Configuration options for writing to Elasticsearch Data Streams instead of a fixed index.
                                      items:
                                        properties:
                                          autoRouting:
                                            description: |-
                                              (Boolean) When true, automatically routes events to the appropriate data stream based on the event content.
                                              When `true`, automatically routes events to the appropriate data stream based on the event content.
                                            type: boolean
                                          dataset:
                                            description: |-
                                              (String) The data stream dataset. This groups events by their source or application.
                                              The data stream dataset. This groups events by their source or application.
                                            type: string
                                          dtype:
                                            description: |-
                                              (String) The data stream type. This determines how events are categorized within the data stream.
                                              The data stream type. This determines how events are categorized within the data stream.
                                            type: string
                                          namespace:
                                            description: |-
                                              (String) The data stream namespace. This separates events into different environments or domains.
                                              The data stream namespace. This separates events into different environments or domains.
                                            type: string
                                          syncFields:
                                            description: |-
                                              (Boolean) When true, synchronizes data stream fields with the Elasticsearch index mapping.
                                              When `true`, synchronizes data stream fields with the Elasticsearch index mapping.
                                            type: boolean
                                        type: object
                                      type: array
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the Elasticsearch endpoint URL.
                                      type: string
                                    idKey:
                                      description: |-
                                        (String) The name of the field used as the document ID in Elasticsearch.
                                        The name of the field used as the document ID in Elasticsearch.
                                      type: string
                                    pipeline:
                                      description: |-
                                        (String) The name of an Elasticsearch ingest pipeline to apply to events before indexing.
                                        The name of an Elasticsearch ingest pipeline to apply to events before indexing.
                                      type: string
                                    requestRetryPartial:
                                      description: |-
                                        (Boolean) When true, retries failed partial bulk requests when some events in a batch fail while others succeed.
                                        When `true`, retries failed partial bulk requests when some events in a batch fail while others succeed.
                                      type: boolean
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              googleCloudStorage:
                                description: |-
                                  (Block List) The google_cloud_storage destination stores logs in a Google Cloud Storage (GCS) bucket. (see below for nested schema)
                                  The `google_cloud_storage` destination stores logs in a Google Cloud Storage (GCS) bucket.
                                items:
                                  properties:
                                    acl:
                                      description: |-
                                        (String) Access control list setting for objects written to the bucket.
                                        Access control list setting for objects written to the bucket.
                                      type: string
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        Google Cloud credentials used to authenticate with Google Cloud services.
                                      items:
                                        properties:
                                          credentialsFile:
                                            description: |-
                                              (String) Path to the Google Cloud service account key file.
                                              Path to the Google Cloud service account key file.
                                            type: string
                                        type: object
                                      type: array
                                    bucket:
                                      description: |-
                                        (String) S3 bucket name.
                                        Name of the GCS bucket.
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression configuration for archived logs. When omitted, the worker default (gzip) is used.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              Compression algorithm. One of `gzip` or `zstd`. Valid values are `gzip`, `zstd`.
                                            type: string
                                          level:
                                            description: |-
                                              9) and zstd (1-21).
                                              Compression level. Applies to `gzip` (1-9) and `zstd` (1-21).
                                            type: number
                                        type: object
                                      type: array
                                    keyPrefix:
                                      description: |-
                                        (String) Prefix for object keys.
                                        Optional prefix for object keys within the GCS bucket.
                                      type: string
                                    metadata:
                                      description: |-
                                        value pairs added to each object. (see below for nested schema)
                                        Custom metadata key-value pairs added to each object.
                                      items:
                                        properties:
                                          name:
                                            description: |-
                                              (String) The pipeline name.
                                              The metadata key.
                                            type: string
                                          value:
                                            description: |-
                                              (String) The metadata value.
                                              The metadata value.
                                            type: string
                                        type: object
                                      type: array
                                    storageClass:
                                      description: |-
                                        (String) S3 storage class. Valid values are STANDARD, REDUCED_REDUNDANCY, INTELLIGENT_TIERING, STANDARD_IA, EXPRESS_ONEZONE, ONEZONE_IA, GLACIER, GLACIER_IR, DEEP_ARCHIVE.
                                        Storage class used for objects stored in GCS.
                                      type: string
                                  type: object
                                type: array
                              googlePubsub:
                                description: |-
                                  (Block List) The google_pubsub destination publishes logs to a Google Cloud Pub/Sub topic. (see below for nested schema)
                                  The `google_pubsub` destination publishes logs to a Google Cloud Pub/Sub topic.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        Google Cloud credentials used to authenticate with Google Cloud services.
                                      items:
                                        properties:
                                          credentialsFile:
                                            description: |-
                                              (String) Path to the Google Cloud service account key file.
                                              Path to the Google Cloud service account key file.
                                            type: string
                                        type: object
                                      type: array
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        Encoding format for log events. Valid values: `json`, `raw_message`.
                                      type: string
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the Google Cloud Pub/Sub endpoint URL.
                                      type: string
                                    project:
                                      description: |-
                                        (String) The Google Cloud project ID that owns the Pub/Sub topic.
                                        The Google Cloud project ID that owns the Pub/Sub topic.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                    topic:
                                      description: |-
                                        (String) The Pub/Sub topic name to publish logs to.
                                        The Pub/Sub topic name to publish logs to.
                                      type: string
                                  type: object
                                type: array
                              googleSecops:
                                description: |-
                                  (Block List) The google_chronicle destination sends logs to Google SecOps. (see below for nested schema)
                                  The `google_chronicle` destination sends logs to Google SecOps.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        Google Cloud credentials used to authenticate with Google Cloud services.
                                      items:
                                        properties:
                                          credentialsFile:
                                            description: |-
                                              (String) Path to the Google Cloud service account key file.
                                              Path to the Google Cloud service account key file.
                                            type: string
                                        type: object
                                      type: array
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    customerId:
                                      description: |-
                                        (String) The Google SecOps customer ID.
                                        The Google SecOps customer ID.
                                      type: string
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        The encoding format for the logs sent to Google SecOps. Valid values are `json`, `raw_message`.
                                      type: string
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the Google Chronicle endpoint URL.
                                      type: string
                                    logType:
                                      description: |-
                                        (String) The log type metadata associated with the Google SecOps destination.
                                        The log type metadata associated with the Google SecOps destination.
                                      type: string
                                  type: object
                                type: array
                              httpClient:
                                description: |-
                                  (Block List) The http_client destination sends data to an HTTP endpoint. (see below for nested schema)
                                  The `http_client` destination sends data to an HTTP endpoint.
                                items:
                                  properties:
                                    authStrategy:
                                      description: |-
                                        (String) HTTP authentication strategy. Valid values are none, basic, bearer.
                                        HTTP authentication strategy. Valid values are `none`, `basic`, `bearer`.
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression configuration for HTTP requests.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              Compression algorithm. Valid values are `gzip`.
                                            type: string
                                        type: object
                                      type: array
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        Encoding format for events. Valid values are `json`.
                                      type: string
                                    passwordKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                        Name of the environment variable or secret that holds the password.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          serverName:
                                            description: |-
                                              (String) Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                              Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                            type: string
                                        type: object
                                      type: array
                                    tokenKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the authentication token.
                                        Name of the environment variable or secret that holds the authentication token.
                                      type: string
                                    uriKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the request URI.
                                        Name of the environment variable or secret that holds the request URI.
                                      type: string
                                    usernameKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                        Name of the environment variable or secret that holds the username.
                                      type: string
                                  type: object
                                type: array
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  The unique identifier for this destination.
                                type: string
                              inputs:
                                description: |-
                                  (List of String) A list of component IDs whose output is used as the input for this component.
                                  A list of component IDs whose output is used as the `input` for this component.
                                items:
                                  type: string
                                type: array
                              kafka:
                                description: |-
                                  (Block List) The kafka destination sends logs to Apache Kafka topics. (see below for nested schema)
                                  The `kafka` destination sends logs to Apache Kafka topics.
                                items:
                                  properties:
                                    bootstrapServersKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the Kafka bootstrap servers.
                                        Name of the environment variable or secret that holds the Kafka bootstrap servers.
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression codec for Kafka messages. Valid values are `none`, `gzip`, `snappy`, `lz4`, `zstd`.
                                      type: string
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        Encoding format for log events. Valid values are `json`, `raw_message`.
                                      type: string
                                    headersKey:
                                      description: |-
                                        (String) The field name to use for Kafka message headers.
                                        The field name to use for Kafka message headers.
                                      type: string
                                    keyField:
                                      description: |-
                                        (String) The field name to use as the Kafka message key.
                                        The field name to use as the Kafka message key.
                                      type: string
                                    librdkafkaOption:
                                      description: |-
                                        value pairs. (see below for nested schema)
                                        Optional list of advanced Kafka producer configuration options, defined as key-value pairs.
                                      items:
                                        properties:
                                          name:
                                            description: |-
                                              (String) The pipeline name.
                                              The name of the librdkafka configuration option.
                                            type: string
                                          value:
                                            description: |-
                                              (String) The metadata value.
                                              The value of the librdkafka configuration option.
                                            type: string
                                        type: object
                                      type: array
                                    messageTimeoutMs:
                                      description: |-
                                        (Number) Maximum time in milliseconds to wait for message delivery confirmation.
                                        Maximum time in milliseconds to wait for message delivery confirmation.
                                      type: number
                                    rateLimitDurationSecs:
                                      description: |-
                                        (Number) Duration in seconds for the rate limit window.
                                        Duration in seconds for the rate limit window.
                                      type: number
                                    rateLimitNum:
                                      description: |-
                                        (Number) Maximum number of messages allowed per rate limit duration.
                                        Maximum number of messages allowed per rate limit duration.
                                      type: number
                                    sasl:
                                      description: |-
                                        (Block List) Specifies the SASL mechanism for authenticating with a Kafka cluster. (see below for nested schema)
                                        Specifies the SASL mechanism for authenticating with a Kafka cluster.
                                      items:
                                        properties:
                                          mechanism:
                                            description: |-
                                              SHA-256, SCRAM-SHA-512.
                                              SASL authentication mechanism. Valid values are `PLAIN`, `SCRAM-SHA-256`, `SCRAM-SHA-512`.
                                            type: string
                                          passwordKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                              Name of the environment variable or secret that holds the SASL password.
                                            type: string
                                          usernameKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                              Name of the environment variable or secret that holds the SASL username.
                                            type: string
                                        type: object
                                      type: array
                                    socketTimeoutMs:
                                      description: |-
                                        (Number) Socket timeout in milliseconds for network requests.
                                        Socket timeout in milliseconds for network requests.
                                      type: number
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                    topic:
                                      description: |-
                                        (String) The Pub/Sub topic name to publish logs to.
                                        The Kafka topic name to publish logs to.
                                      type: string
                                  type: object
                                type: array
                              microsoftSentinel:
                                description: |-
                                  (Block List) The microsoft_sentinel destination forwards logs to Microsoft Sentinel. (see below for nested schema)
                                  The `microsoft_sentinel` destination forwards logs to Microsoft Sentinel.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    clientId:
                                      description: |-
                                        (String) The OAuth client ID used to authenticate with Databricks.
                                        Azure AD client ID used for authentication.
                                      type: string
                                    clientSecretKey:
                                      description: |-
                                        (String) The name of the secret or environment variable holding the OAuth client secret. Defaults to DESTINATION_DATABRICKS_ZEROBUS_OAUTH_CLIENT_SECRET.
                                        Name of the environment variable or secret that holds the Azure AD client secret.
                                      type: string
                                    dceUriKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the Data Collection Endpoint (DCE) URI.
                                        Name of the environment variable or secret that holds the Data Collection Endpoint (DCE) URI.
                                      type: string
                                    dcrImmutableId:
                                      description: |-
                                        (String) The immutable ID of the Data Collection Rule (DCR).
                                        The immutable ID of the Data Collection Rule (DCR).
                                      type: string
                                    table:
                                      description: |-
                                        (String) Target ClickHouse table name.
                                        The name of the Log Analytics table where logs will be sent.
                                      type: string
                                    tenantId:
                                      description: |-
                                        (String) Azure AD tenant ID.
                                        Azure AD tenant ID.
                                      type: string
                                  type: object
                                type: array
                              newRelic:
                                description: |-
                                  (Block List) The new_relic destination sends logs to the New Relic platform. (see below for nested schema)
                                  The `new_relic` destination sends logs to the New Relic platform.
                                items:
                                  properties:
                                    accountIdKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the New Relic account ID.
                                        Name of the environment variable or secret that holds the New Relic account ID.
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    licenseKeyKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the New Relic license key.
                                        Name of the environment variable or secret that holds the New Relic license key.
                                      type: string
                                    region:
                                      description: |-
                                        (String) AWS region of the S3 bucket.
                                        The New Relic region.
                                      type: string
                                  type: object
                                type: array
                              opensearch:
                                description: |-
                                  (Block List) The opensearch destination writes logs to an OpenSearch cluster. (see below for nested schema)
                                  The `opensearch` destination writes logs to an OpenSearch cluster.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        Authentication settings for the OpenSearch destination.
                                      items:
                                        properties:
                                          passwordKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                              Name of the environment variable or secret that holds the OpenSearch password (used when `strategy` is `basic`).
                                            type: string
                                          strategy:
                                            description: |-
                                              (String) The authentication strategy to use (e.g. aws or basic).
                                              The authentication strategy to use. Valid values are `basic`, `aws`.
                                            type: string
                                          usernameKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                              Name of the environment variable or secret that holds the OpenSearch username (used when `strategy` is `basic`).
                                            type: string
                                        type: object
                                      type: array
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    bulkIndex:
                                      description: |-
                                        (String) The index or datastream to write logs to.
                                        The index or datastream to write logs to.
                                      type: string
                                    dataStream:
                                      description: |-
                                        (Block List) Configuration options for writing to Elasticsearch Data Streams instead of a fixed index. (see below for nested schema)
                                        Configuration options for writing to OpenSearch Data Streams instead of a fixed index.
                                      items:
                                        properties:
                                          dataset:
                                            description: |-
                                              (String) The data stream dataset. This groups events by their source or application.
                                              The data stream dataset for your logs. This groups logs by their source or application.
                                            type: string
                                          dtype:
                                            description: |-
                                              (String) The data stream type. This determines how events are categorized within the data stream.
                                              The data stream type for your logs. This determines how logs are categorized within the data stream.
                                            type: string
                                          namespace:
                                            description: |-
                                              (String) The data stream namespace. This separates events into different environments or domains.
                                              The data stream namespace for your logs. This separates logs into different environments or domains.
                                            type: string
                                        type: object
                                      type: array
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the OpenSearch endpoint URL.
                                      type: string
                                  type: object
                                type: array
                              opentelemetry:
                                description: |-
                                  (Block List) The opentelemetry destination forwards metrics using the OpenTelemetry Protocol (OTLP) over HTTP. (see below for nested schema)
                                  The `opentelemetry` destination forwards metrics using the OpenTelemetry Protocol (OTLP) over HTTP.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    httpClientUriKey:
                                      description: |-
                                        (String) Environment variable name containing the URI of the OTLP HTTP endpoint to send metrics to.
                                        Environment variable name containing the URI of the OTLP HTTP endpoint to send metrics to.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              rsyslog:
                                description: |-
                                  (Block List) The rsyslog destination forwards logs to an external rsyslog server over TCP or UDP using the syslog protocol. (see below for nested schema)
                                  The `rsyslog` destination forwards logs to an external `rsyslog` server over TCP or UDP using the syslog protocol.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the rsyslog endpoint URL.
                                      type: string
                                    keepalive:
                                      description: |-
                                        (Number) Optional socket keepalive duration in milliseconds.
                                        Optional socket keepalive duration in milliseconds.
                                      type: number
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              sentinelOne:
                                description: |-
                                  (Block List) The sentinel_one destination sends logs to SentinelOne. (see below for nested schema)
                                  The `sentinel_one` destination sends logs to SentinelOne.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    region:
                                      description: |-
                                        (String) AWS region of the S3 bucket.
                                        The SentinelOne region to send logs to.
                                      type: string
                                    tokenKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the authentication token.
                                        Name of the environment variable or secret that holds the SentinelOne API token.
                                      type: string
                                  type: object
                                type: array
                              socket:
                                description: |-
                                  (Block List) The socket destination sends logs over TCP or UDP to a remote server. (see below for nested schema)
                                  The `socket` destination sends logs over TCP or UDP to a remote server.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the socket address (host:port).
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        Encoding format for log events. Valid values are `json`, `raw_message`.
                                      type: string
                                    framing:
                                      description: |-
                                        (Block List) Defines the framing method for outgoing messages. (see below for nested schema)
                                        Defines the framing method for outgoing messages.
                                      items:
                                        properties:
                                          characterDelimited:
                                            description: |-
                                              (Block List) Used when method is character_delimited. Specifies the delimiter character. (see below for nested schema)
                                              Used when `method` is `character_delimited`. Specifies the delimiter character.
                                            items:
                                              properties:
                                                delimiter:
                                                  description: |-
                                                    (String) A single ASCII character used as a delimiter.
                                                    A single ASCII character used as a delimiter.
                                                  type: string
                                              type: object
                                            type: array
                                          method:
                                            description: |-
                                              (String) The framing method. Valid values are newline_delimited, bytes, character_delimited.
                                              The framing method. Valid values are `newline_delimited`, `bytes`, `character_delimited`.
                                            type: string
                                        type: object
                                      type: array
                                    mode:
                                      description: |-
                                        (String) The protocol used to send logs. Valid values are tcp, udp.
                                        The protocol used to send logs. Valid values are `tcp`, `udp`.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          serverName:
                                            description: |-
                                              (String) Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                              Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              splunkHec:
                                description: |-
                                  (Block List) The splunk_hec destination forwards logs to Splunk using the HTTP Event Collector (HEC). (see below for nested schema)
                                  The `splunk_hec` destination forwards logs to Splunk using the HTTP Event Collector (HEC).
                                items:
                                  properties:
                                    autoExtractTimestamp:
                                      description: |-
                                        (Boolean) If true, Splunk tries to extract timestamps from incoming log events. If false, Splunk assigns the time the event was received. Only applies when endpoint_target is event; cannot be true when endpoint_target is raw.
                                        If `true`, Splunk tries to extract timestamps from incoming log events. If `false`, Splunk assigns the time the event was received. Only applies when `endpoint_target` is `event`; cannot be `true` when `endpoint_target` is `raw`.
                                      type: boolean
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        Encoding format for log events. Valid values are `json`, `raw_message`.
                                      type: string
                                    endpointTarget:
                                      description: |-
                                        (String) The Splunk HEC endpoint to send events to. Use event to send structured events to the /event endpoint, or raw to send the raw message to the /raw endpoint. Valid values are event, raw.
                                        The Splunk HEC endpoint to send events to. Use `event` to send structured events to the `/event` endpoint, or `raw` to send the raw message to the `/raw` endpoint. Valid values are `event`, `raw`.
                                      type: string
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the Splunk HEC endpoint URL.
                                      type: string
                                    index:
                                      description: |-
                                        (String) Optional name of the Splunk index where logs are written.
                                        Optional name of the Splunk index where logs are written.
                                      type: string
                                    indexedFields:
                                      description: |-
                                        (List of String) List of log field names to send as indexed fields to Splunk HEC. Available only when encoding is json.
                                        List of log field names to send as indexed fields to Splunk HEC. Available only when `encoding` is `json`.
                                      items:
                                        type: string
                                      type: array
                                    sourcetype:
                                      description: |-
                                        (String) The Splunk sourcetype to assign to log events.
                                        The Splunk sourcetype to assign to log events.
                                      type: string
                                    tokenKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the authentication token.
                                        Name of the environment variable or secret that holds the Splunk HEC token.
                                      type: string
                                    tokenStrategy:
                                      description: |-
                                        (String) Controls how the Splunk HEC token is supplied. Use custom to provide a token via token_key, or from_source to forward the token received from an upstream Splunk HEC source. Valid values are custom, from_source.
                                        Controls how the Splunk HEC token is supplied. Use `custom` to provide a token via `token_key`, or `from_source` to forward the token received from an upstream Splunk HEC source. Valid values are `custom`, `from_source`.
                                      type: string
                                  type: object
                                type: array
                              splunkHecMetrics:
                                description: |-
                                  (Block List) The splunk_hec_metrics destination forwards metrics to Splunk using the HTTP Event Collector (HEC). (see below for nested schema)
                                  The `splunk_hec_metrics` destination forwards metrics to Splunk using the HTTP Event Collector (HEC).
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression algorithm applied when sending metrics to Splunk HEC. Valid values are `none`, `gzip`.
                                      type: string
                                    defaultNamespace:
                                      description: |-
                                        (String) Optional default namespace for metrics sent to Splunk HEC.
                                        Optional default namespace for metrics sent to Splunk HEC.
                                      type: string
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the Splunk HEC endpoint URL.
                                      type: string
                                    index:
                                      description: |-
                                        (String) Optional name of the Splunk index where logs are written.
                                        Optional name of the Splunk index where metrics are written.
                                      type: string
                                    source:
                                      description: |-
                                        (Block List) List of sources. (see below for nested schema)
                                        The Splunk source field value for metric events.
                                      type: string
                                    sourcetype:
                                      description: |-
                                        (String) The Splunk sourcetype to assign to log events.
                                        The Splunk sourcetype to assign to metric events.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                    tokenKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the authentication token.
                                        Name of the environment variable or secret that holds the Splunk HEC token.
                                      type: string
                                  type: object
                                type: array
                              sumoLogic:
                                description: |-
                                  (Block List) The sumo_logic destination forwards logs to Sumo Logic. (see below for nested schema)
                                  The `sumo_logic` destination forwards logs to Sumo Logic.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        The output encoding format.
                                      type: string
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the Sumo Logic endpoint URL.
                                      type: string
                                    headerCustomField:
                                      description: |-
                                        (Block List) A list of custom headers to include in the request to Sumo Logic. (see below for nested schema)
                                        A list of custom headers to include in the request to Sumo Logic.
                                      items:
                                        properties:
                                          name:
                                            description: |-
                                              (String) The pipeline name.
                                              The header field name.
                                            type: string
                                          value:
                                            description: |-
                                              (String) The metadata value.
                                              The header field value.
                                            type: string
                                        type: object
                                      type: array
                                    headerHostName:
                                      description: |-
                                        (String) Optional override for the host name header.
                                        Optional override for the host name header.
                                      type: string
                                    headerSourceCategory:
                                      description: |-
                                        (String) Optional override for the source category header.
                                        Optional override for the source category header.
                                      type: string
                                    headerSourceName:
                                      description: |-
                                        (String) Optional override for the source name header.
                                        Optional override for the source name header.
                                      type: string
                                  type: object
                                type: array
                              syslogNg:
                                description: |-
                                  ng server over TCP or UDP using the syslog protocol. (see below for nested schema)
                                  The `syslog_ng` destination forwards logs to an external `syslog-ng` server over TCP or UDP using the syslog protocol.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the syslog-ng endpoint URL.
                                      type: string
                                    keepalive:
                                      description: |-
                                        (Number) Optional socket keepalive duration in milliseconds.
                                        Optional socket keepalive duration in milliseconds.
                                      type: number
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          serverName:
                                            description: |-
                                              (String) Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                              Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                            type: object
                          type: array
                        pipelineType:
                          description: |-
                            (String) The type of data being ingested. Defaults to logs if not specified. Valid values are logs, metrics.
                            The type of data being ingested. Defaults to `logs` if not specified. Valid values are `logs`, `metrics`.
                          type: string
                        processorGroup:
                          description: |-
                            (Block List) A processor group containing common configuration and nested processors. (see below for nested schema)
                            A processor group containing common configuration and nested processors.
                          items:
                            properties:
                              displayName:
                                description: |-
                                  friendly name of the processor group.
                                  A human-friendly name of the processor group.
                                type: string
                              enabled:
                                description: |-
                                  (Boolean) Whether this processor group is enabled.
                                  Whether this processor group is enabled.
                                type: boolean
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  The unique ID of the processor group.
                                type: string
                              include:
                                description: |-
                                  (String) A Datadog search query that determines which logs are forwarded using this route.
                                  A Datadog search query used to determine which logs this processor group targets.
                                type: string
                              inputs:
                                description: |-
                                  (List of String) A list of component IDs whose output is used as the input for this component.
                                  A list of component IDs whose output is used as the input for this processor group.
                                items:
                                  type: string
                                type: array
                              processor:
                                description: |-
                                  (Block List) The processor contained in this group. (see below for nested schema)
                                  The processor contained in this group.
                                items:
                                  properties:
                                    addEnvVars:
                                      description: |-
                                        (Block List) The add_env_vars processor adds environment variable values to log events. (see below for nested schema)
                                        The `add_env_vars` processor adds environment variable values to log events.
                                      items:
                                        properties:
                                          variable:
                                            description: |-
                                              (Block List) A list of environment variable mappings to apply to log fields. (see below for nested schema)
                                              A list of environment variable mappings to apply to log fields.
                                            items:
                                              properties:
                                                field:
                                                  description: |-
                                                    (String) The target field in the log event.
                                                    The target field in the log event.
                                                  type: string
                                                name:
                                                  description: |-
                                                    (String) The pipeline name.
                                                    The name of the environment variable to read.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    addFields:
                                      description: |-
                                        value fields to logs. (see below for nested schema)
                                        The `add_fields` processor adds static key-value fields to logs.
                                      items:
                                        properties:
                                          field:
                                            description: |-
                                              (String) The target field in the log event.
                                              A list of static fields (key-value pairs) that is added to each log event processed by this component.
                                            items:
                                              properties:
                                                name:
                                                  description: |-
                                                    (String) The pipeline name.
                                                    The field name to add.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The metadata value.
                                                    The value to assign to the field.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    addHostname:
                                      description: |-
                                        (Block List) The add_hostname processor adds the hostname to log events. (see below for nested schema)
                                        The `add_hostname` processor adds the hostname to log events.
                                      items:
                                        type: object
                                      type: array
                                    addMetricTags:
                                      description: |-
                                        (Block List) The add_metric_tags processor adds static tags to metrics. (see below for nested schema)
                                        The `add_metric_tags` processor adds static tags to metrics.
                                      items:
                                        properties:
                                          tag:
                                            description: |-
                                              (Block List) A list of static tags to add to each metric. Up to 15 tags may be defined. (see below for nested schema)
                                              A list of static tags to add to each metric. Up to 15 tags may be defined.
                                            items:
                                              properties:
                                                name:
                                                  description: |-
                                                    (String) The pipeline name.
                                                    The tag name.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The metadata value.
                                                    The tag value.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    aggregate:
                                      description: |-
                                        (Block List) The aggregate processor combines metrics that share the same name and tags into a single metric over a configurable interval. (see below for nested schema)
                                        The `aggregate` processor combines metrics that share the same name and tags into a single metric over a configurable interval.
                                      items:
                                        properties:
                                          intervalSecs:
                                            description: |-
                                              (Number) The interval, in seconds, over which metrics are aggregated. Must be between 1 and 60. Value must be between 1 and 60.
                                              The interval, in seconds, over which metrics are aggregated. Must be between 1 and 60. Value must be between 1 and 60.
                                            type: number
                                          mode:
                                            description: |-
                                              (String) The protocol used to send logs. Valid values are tcp, udp.
                                              The aggregation mode. One of `auto`, `sum`, `latest`, `count`, `max`, `min`, `mean`. Valid values are `auto`, `sum`, `latest`, `count`, `max`, `min`, `mean`.
                                            type: string
                                        type: object
                                      type: array
                                    customProcessor:
                                      description: |-
                                        (Block List) The custom_processor processor transforms events using Vector Remap Language (VRL) scripts with advanced filtering capabilities. (see below for nested schema)
                                        The `custom_processor` processor transforms events using Vector Remap Language (VRL) scripts with advanced filtering capabilities.
                                      items:
                                        properties:
                                          remap:
                                            description: |-
                                              (Block List) Array of VRL remap configurations. Each remap defines a transformation rule with its own filter and VRL script. (see below for nested schema)
                                              Array of VRL remap configurations. Each remap defines a transformation rule with its own filter and VRL script.
                                            items:
                                              properties:
                                                dropOnError:
                                                  description: |-
                                                    (Boolean) Whether to drop events that cause errors during transformation.
                                                    Whether to drop events that cause errors during transformation.
                                                  type: boolean
                                                enabled:
                                                  description: |-
                                                    (Boolean) Whether this processor group is enabled.
                                                    Whether this remap rule is enabled.
                                                  type: boolean
                                                include:
                                                  description: |-
                                                    (String) A Datadog search query that determines which logs are forwarded using this route.
                                                    A Datadog search query used to filter events for this specific remap rule.
                                                  type: string
                                                name:
                                                  description: |-
                                                    (String) The pipeline name.
                                                    A descriptive name for this remap rule.
                                                  type: string
                                                source:
                                                  description: |-
                                                    (Block List) List of sources. (see below for nested schema)
                                                    The VRL script source code that defines the transformation logic.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    datadogTags:
                                      description: (Block List) (see below for nested
                                        schema)
                                      items:
                                        properties:
                                          action:
                                            description: |-
                                              (String) Valid values are include, exclude.
                                              Valid values are `include`, `exclude`.
                                            type: string
                                          keys:
                                            description: (List of String)
                                            items:
                                              type: string
                                            type: array
                                          mode:
                                            description: |-
                                              (String) The protocol used to send logs. Valid values are tcp, udp.
                                              Valid values are `filter`.
                                            type: string
                                        type: object
                                      type: array
                                    dedupe:
                                      description: |-
                                        (Block List) The dedupe processor removes duplicate fields in log events. (see below for nested schema)
                                        The `dedupe` processor removes duplicate fields in log events.
                                      items:
                                        properties:
                                          fields:
                                            description: |-
                                              (List of String) A list of log field paths to check for duplicates.
                                              A list of log field paths to check for duplicates.
                                            items:
                                              type: string
                                            type: array
                                          mode:
                                            description: |-
                                              (String) The protocol used to send logs. Valid values are tcp, udp.
                                              The deduplication mode to apply to the fields.
                                            type: string
                                        type: object
                                      type: array
                                    displayName:
                                      description: |-
                                        friendly name of the processor group.
                                        A human-friendly name for this processor.
                                      type: string
                                    enabled:
                                      description: |-
                                        (Boolean) Whether this processor group is enabled.
                                        Whether this processor is enabled.
                                      type: boolean
                                    enrichmentTable:
                                      description: |-
                                        (Block List) The enrichment_table processor enriches logs using a static CSV file or GeoIP database. (see below for nested schema)
                                        The `enrichment_table` processor enriches logs using a static CSV file or GeoIP database.
                                      items:
                                        properties:
                                          file:
                                            description: |-
                                              (Block List) Defines a static enrichment table loaded from a CSV file. (see below for nested schema)
                                              Defines a static enrichment table loaded from a CSV file.
                                            items:
                                              properties:
                                                encoding:
                                                  description: (Block List) Encoding
                                                    format for the destination. (see
                                                    below for nested schema)
                                                  items:
                                                    properties:
                                                      delimiter:
                                                        description: |-
                                                          (String) A single ASCII character used as a delimiter.
                                                          The `encoding` `delimiter`.
                                                        type: string
                                                      includesHeaders:
                                                        description: |-
                                                          (Boolean) The encoding includes_headers.
                                                          The `encoding` `includes_headers`.
                                                        type: boolean
                                                      type:
                                                        description: |-
                                                          (String) The encoding type. Valid values are json, parquet.
                                                          File encoding format.
                                                        type: string
                                                    type: object
                                                  type: array
                                                key:
                                                  description: |-
                                                    (Block List) Key fields used to look up enrichment values. (see below for nested schema)
                                                    Key fields used to look up enrichment values.
                                                  items:
                                                    properties:
                                                      column:
                                                        description: |-
                                                          (String) The items column.
                                                          The `items` `column`.
                                                        type: string
                                                      comparison:
                                                        description: |-
                                                          (String) The comparison method (e.g. equals).
                                                          The comparison method (e.g. equals).
                                                        type: string
                                                      field:
                                                        description: |-
                                                          (String) The target field in the log event.
                                                          Specifies the source of the key value for enrichment table lookups. Set exactly one of `string_path`, `event`, `vrl`, or `secret`.
                                                        items:
                                                          properties:
                                                            event:
                                                              description: |-
                                                                (String) The path to the field in the log event to use as the lookup key.
                                                                The path to the field in the log event to use as the lookup key.
                                                              type: string
                                                            secret:
                                                              description: |-
                                                                (String) The name of the secret containing the lookup key value.
                                                                The name of the secret containing the lookup key value.
                                                              type: string
                                                            stringPath:
                                                              description: |-
                                                                (String) A plain field path in the log event (for example, log.user.id).
                                                                A plain field path in the log event (for example, `log.user.id`).
                                                              type: string
                                                            vrl:
                                                              description: |-
                                                                (String) A VRL expression that returns the value to use as the lookup key.
                                                                A VRL expression that returns the value to use as the lookup key.
                                                              type: string
                                                          type: object
                                                        type: array
                                                    type: object
                                                  type: array
                                                path:
                                                  description: |-
                                                    (String) Path to the CSV file.
                                                    Path to the CSV file.
                                                  type: string
                                              type: object
                                            type: array
                                          geoip:
                                            description: |-
                                              (Block List) Uses a GeoIP database to enrich logs based on an IP field. (see below for nested schema)
                                              Uses a GeoIP database to enrich logs based on an IP field.
                                            items:
                                              properties:
                                                keyField:
                                                  description: |-
                                                    (String) The field name to use as the Kafka message key.
                                                    Path to the IP field in the log.
                                                  type: string
                                                locale:
                                                  description: |-
                                                    (String) Locale used to resolve geographical names.
                                                    Locale used to resolve geographical names.
                                                  type: string
                                                path:
                                                  description: |-
                                                    (String) Path to the CSV file.
                                                    Path to the GeoIP database file.
                                                  type: string
                                              type: object
                                            type: array
                                          referenceTable:
                                            description: |-
                                              (Block List) Uses a Datadog reference table to enrich logs. (see below for nested schema)
                                              Uses a Datadog reference table to enrich logs.
                                            items:
                                              properties:
                                                appKeyKey:
                                                  description: |-
                                                    (String) Name of the environment variable or secret that holds the Datadog application key for the reference table.
                                                    Name of the environment variable or secret that holds the Datadog application key for the reference table.
                                                  type: string
                                                columns:
                                                  description: |-
                                                    (List of String) List of column names to include from the reference table. If not provided, all columns are included.
                                                    List of column names to include from the reference table. If not provided, all columns are included.
                                                  items:
                                                    type: string
                                                  type: array
                                                keyField:
                                                  description: |-
                                                    (String) The field name to use as the Kafka message key.
                                                    Path to the field in the log event to match against the reference table.
                                                  type: string
                                                tableId:
                                                  description: |-
                                                    (String) The unique identifier of the reference table.
                                                    The unique identifier of the reference table.
                                                  type: string
                                              type: object
                                            type: array
                                          target:
                                            description: |-
                                              (String) Path where enrichment results should be stored in the log.
                                              Path where enrichment results should be stored in the log.
                                            type: string
                                        type: object
                                      type: array
                                    filter:
                                      description: |-
                                        (Block List) The filter processor allows conditional processing of logs based on a Datadog search query. Logs that match the include query are passed through; others are discarded. (see below for nested schema)
                                        The `filter` processor allows conditional processing of logs based on a Datadog search query. Logs that match the `include` query are passed through; others are discarded.
                                      items:
                                        type: object
                                      type: array
                                    generateDatadogMetrics:
                                      description: |-
                                        (Block List) The generate_datadog_metrics processor creates custom metrics from logs. Metrics can be counters, gauges, or distributions and optionally grouped by log fields. (see below for nested schema)
                                        The `generate_datadog_metrics` processor creates custom metrics from logs. Metrics can be counters, gauges, or distributions and optionally grouped by log fields.
                                      items:
                                        properties:
                                          metric:
                                            description: |-
                                              (Block List) Configuration for generating individual metrics. (see below for nested schema)
                                              Configuration for generating individual metrics.
                                            items:
                                              properties:
                                                groupBy:
                                                  description: |-
                                                    (List of String) Optional fields used to group the metric series.
                                                    Optional fields used to group the metric series.
                                                  items:
                                                    type: string
                                                  type: array
                                                include:
                                                  description: |-
                                                    (String) A Datadog search query that determines which logs are forwarded using this route.
                                                    Datadog filter query to match logs for metric generation.
                                                  type: string
                                                metricType:
                                                  description: |-
                                                    (String) Type of metric to create.
                                                    Type of metric to create.
                                                  type: string
                                                name:
                                                  description: |-
                                                    (String) The pipeline name.
                                                    Name of the custom metric to be created.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The metadata value.
                                                    Specifies how the value of the generated metric is computed.
                                                  items:
                                                    properties:
                                                      field:
                                                        description: |-
                                                          (String) The target field in the log event.
                                                          Name of the log field containing the numeric value to increment the metric by (used only for `increment_by_field`).
                                                        type: string
                                                      strategy:
                                                        description: |-
                                                          (String) The authentication strategy to use (e.g. aws or basic).
                                                          Metric value strategy: `increment_by_one` or `increment_by_field`.
                                                        type: string
                                                    type: object
                                                  type: array
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    generateMetrics:
                                      description: |-
                                        id>.metrics. (see below for nested schema)
                                        The `generate_metrics` processor creates custom metrics from logs. The generated metrics must be routed to a metrics destination using the input `<processor-id>.metrics`.
                                      items:
                                        properties:
                                          metric:
                                            description: |-
                                              (Block List) Configuration for generating individual metrics. (see below for nested schema)
                                              Configuration for generating individual metrics.
                                            items:
                                              properties:
                                                groupBy:
                                                  description: |-
                                                    (List of String) Optional fields used to group the metric series.
                                                    Optional fields used to group the metric series.
                                                  items:
                                                    type: string
                                                  type: array
                                                include:
                                                  description: |-
                                                    (String) A Datadog search query that determines which logs are forwarded using this route.
                                                    Datadog filter query to match logs for metric generation.
                                                  type: string
                                                metricType:
                                                  description: |-
                                                    (String) Type of metric to create.
                                                    Type of metric to create.
                                                  type: string
                                                name:
                                                  description: |-
                                                    (String) The pipeline name.
                                                    Name of the custom metric to be created.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The metadata value.
                                                    Specifies how the value of the generated metric is computed.
                                                  items:
                                                    properties:
                                                      field:
                                                        description: |-
                                                          (String) The target field in the log event.
                                                          Name of the log field containing the numeric value to increment the metric by (used only for `increment_by_field`).
                                                        type: string
                                                      strategy:
                                                        description: |-
                                                          (String) The authentication strategy to use (e.g. aws or basic).
                                                          Metric value strategy: `increment_by_one` or `increment_by_field`.
                                                        type: string
                                                    type: object
                                                  type: array
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    id:
                                      description: |-
                                        (String) The ID of this resource.
                                        The unique identifier for this processor.
                                      type: string
                                    include:
                                      description: |-
                                        (String) A Datadog search query that determines which logs are forwarded using this route.
                                        A Datadog search query used to determine which logs this processor targets.
                                      type: string
                                    metricTags:
                                      description: |-
                                        (Block List) The metric_tags processor filters metrics based on their tags using Datadog tag key patterns. (see below for nested schema)
                                        The `metric_tags` processor filters metrics based on their tags using Datadog tag key patterns.
                                      items:
                                        properties:
                                          rule:
                                            description: |-
                                              (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                              A list of rules for filtering metric tags.
                                            items:
                                              properties:
                                                action:
                                                  description: |-
                                                    (String) Valid values are include, exclude.
                                                    The action to take on tags with matching keys. Valid values are `include`, `exclude`.
                                                  type: string
                                                include:
                                                  description: |-
                                                    (String) A Datadog search query that determines which logs are forwarded using this route.
                                                    A Datadog search query used to determine which metrics this rule targets.
                                                  type: string
                                                keys:
                                                  description: |-
                                                    (List of String)
                                                    A list of tag keys to include or exclude.
                                                  items:
                                                    type: string
                                                  type: array
                                                mode:
                                                  description: |-
                                                    (String) The protocol used to send logs. Valid values are tcp, udp.
                                                    The processing mode for tag filtering. Valid values are `filter`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    ocsfMapper:
                                      description: |-
                                        (Block List) The ocsf_mapper processor transforms logs into the OCSF schema using predefined library mappings or custom mapping configuration. (see below for nested schema)
                                        The `ocsf_mapper` processor transforms logs into the OCSF schema using predefined library mappings or custom mapping configuration.
                                      items:
                                        properties:
                                          keepUnmatched:
                                            description: |-
                                              (Boolean) Whether to keep an event that does not match any of the mapping filters.
                                              Whether to keep an event that does not match any of the mapping filters.
                                            type: boolean
                                          mapping:
                                            description: |-
                                              (Block List) List of OCSF mapping entries. Each entry uses either a library mapping or a custom mapping. (see below for nested schema)
                                              List of OCSF mapping entries. Each entry uses either a library mapping or a custom mapping.
                                            items:
                                              properties:
                                                customMapping:
                                                  description: |-
                                                    (Block List) Custom OCSF mapping configuration for transforming logs. (see below for nested schema)
                                                    Custom OCSF mapping configuration for transforming logs.
                                                  items:
                                                    properties:
                                                      mapping:
                                                        description: |-
                                                          (Block List) List of OCSF mapping entries. Each entry uses either a library mapping or a custom mapping. (see below for nested schema)
                                                          A list of field mapping rules for transforming log fields to OCSF schema fields.
                                                        items:
                                                          properties:
                                                            default:
                                                              description: |-
                                                                (String) The default value to use if the source field is missing or empty.
                                                                The default value to use if the source field is missing or empty.
                                                              type: string
                                                            dest:
                                                              description: |-
                                                                (String) The destination OCSF field path.
                                                                The destination OCSF field path.
                                                              type: string
                                                            lookup:
                                                              description: |-
                                                                (Block List) Lookup table configuration for mapping source values to destination values. (see below for nested schema)
                                                                Lookup table configuration for mapping source values to destination values.
                                                              items:
                                                                properties:
                                                                  default:
                                                                    description: |-
                                                                      (String) The default value to use if the source field is missing or empty.
                                                                      The default value to use if no lookup match is found.
                                                                    type: string
                                                                  table:
                                                                    description: |-
                                                                      (String) Target ClickHouse table name.
                                                                      A list of lookup table entries for value transformation.
                                                                    items:
                                                                      properties:
                                                                        contains:
                                                                          description: |-
                                                                            (String) The substring to match in the source value.
                                                                            The substring to match in the source value.
                                                                          type: string
                                                                        equals:
                                                                          description: |-
                                                                            (String) The exact value to match in the source.
                                                                            The exact value to match in the source.
                                                                          type: string
                                                                        equalsSource:
                                                                          description: |-
                                                                            (String) The source field to match against.
                                                                            The source field to match against.
                                                                          type: string
                                                                        matches:
                                                                          description: |-
                                                                            (String) A regex pattern to match in the source value.
                                                                            A regex pattern to match in the source value.
                                                                          type: string
                                                                        notMatches:
                                                                          description: |-
                                                                            (String) A regex pattern that must not match the source value.
                                                                            A regex pattern that must not match the source value.
                                                                          type: string
                                                                        value:
                                                                          description: |-
                                                                            (String) The metadata value.
                                                                            The value to use when a match is found.
                                                                          type: string
                                                                      type: object
                                                                    type: array
                                                                type: object
                                                              type: array
                                                            source:
                                                              description: |-
                                                                (Block List) List of sources. (see below for nested schema)
                                                                The source field path from the log event.
                                                              type: string
                                                            sources:
                                                              description: |-
                                                                (List of String) Multiple source field paths for combined mapping.
                                                                Multiple source field paths for combined mapping.
                                                              items:
                                                                type: string
                                                              type: array
                                                            value:
                                                              description: |-
                                                                (String) The metadata value.
                                                                A static value to use for the destination field.
                                                              type: string
                                                          type: object
                                                        type: array
                                                      metadata:
                                                        description: |-
                                                          value pairs added to each object. (see below for nested schema)
                                                          Metadata for the custom OCSF mapping.
                                                        items:
                                                          properties:
                                                            class:
                                                              description: |-
                                                                (String) The OCSF event class name.
                                                                The OCSF event class name.
                                                              type: string
                                                            profiles:
                                                              description: |-
                                                                (List of String) A list of OCSF profiles to apply.
                                                                A list of OCSF profiles to apply.
                                                              items:
                                                                type: string
                                                              type: array
                                                            version:
                                                              description: |-
                                                                (Number) The version of the custom mapping configuration.
                                                                The OCSF schema version.
                                                              type: string
                                                          type: object
                                                        type: array
                                                      version:
                                                        description: |-
                                                          (Number) The version of the custom mapping configuration.
                                                          The version of the custom mapping configuration.
                                                        type: number
                                                    type: object
                                                  type: array
                                                include:
                                                  description: |-
                                                    (String) A Datadog search query that determines which logs are forwarded using this route.
                                                    Search query for selecting which logs the mapping applies to.
                                                  type: string
                                                libraryMapping:
                                                  description: |-
                                                    (String) Predefined library mapping for log transformation. Use this or custom_mapping, not both.
                                                    Predefined library mapping for log transformation. Use this or custom_mapping, not both.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    parseGrok:
                                      description: |-
                                        (Block List) The parse_grok processor extracts structured fields from unstructured log messages using Grok patterns. (see below for nested schema)
                                        The `parse_grok` processor extracts structured fields from unstructured log messages using Grok patterns.
                                      items:
                                        properties:
                                          disableLibraryRules:
                                            description: |-
                                              (Boolean) If set to true, disables the default Grok rules provided by Datadog. Defaults to false.
                                              If set to `true`, disables the default Grok rules provided by Datadog. Defaults to `false`.
                                            type: boolean
                                          field:
                                            description: |-
                                              (String) The target field in the log event.
                                              The log field to parse with the Grok rules. Defaults to `"message"`.
                                            type: string
                                          includeRule:
                                            description: |-
                                              (Block List) A Grok parsing rule that targets logs matching a Datadog search query. (see below for nested schema)
                                              A Grok parsing rule that targets logs matching a Datadog search query.
                                            items:
                                              properties:
                                                include:
                                                  description: |-
                                                    (String) A Datadog search query that determines which logs are forwarded using this route.
                                                    A Datadog search query used to determine which logs this Grok rule targets.
                                                  type: string
                                                matchRule:
                                                  description: |-
                                                    (Block List) A list of Grok parsing rules that define how to extract fields. Each rule must contain a name and a valid Grok pattern. (see below for nested schema)
                                                    A list of Grok parsing rules that define how to extract fields. Each rule must contain a name and a valid Grok pattern.
                                                  items:
                                                    properties:
                                                      name:
                                                        description: |-
                                                          (String) The pipeline name.
                                                          The name of the rule.
                                                        type: string
                                                      rule:
                                                        description: |-
                                                          (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                                          The definition of the Grok rule.
                                                        type: string
                                                    type: object
                                                  type: array
                                                supportRule:
                                                  description: |-
                                                    (Block List) A list of helper Grok rules that can be referenced by the parsing rules. (see below for nested schema)
                                                    A list of helper Grok rules that can be referenced by the parsing rules.
                                                  items:
                                                    properties:
                                                      name:
                                                        description: |-
                                                          (String) The pipeline name.
                                                          The name of the helper Grok rule.
                                                        type: string
                                                      rule:
                                                        description: |-
                                                          (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                                          The definition of the helper Grok rule.
                                                        type: string
                                                    type: object
                                                  type: array
                                              type: object
                                            type: array
                                          rule:
                                            description: |-
                                              (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                              The list of Grok parsing rules. If multiple parsing rules are provided, they are evaluated in order. The first successful match is applied.
                                            items:
                                              properties:
                                                matchRule:
                                                  description: |-
                                                    (Block List) A list of Grok parsing rules that define how to extract fields. Each rule must contain a name and a valid Grok pattern. (see below for nested schema)
                                                    A list of Grok parsing rules that define how to extract fields. Each rule must contain a name and a valid Grok pattern.
                                                  items:
                                                    properties:
                                                      name:
                                                        description: |-
                                                          (String) The pipeline name.
                                                          The name of the rule.
                                                        type: string
                                                      rule:
                                                        description: |-
                                                          (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                                          The definition of the Grok rule.
                                                        type: string
                                                    type: object
                                                  type: array
                                                source:
                                                  description: |-
                                                    (Block List) List of sources. (see below for nested schema)
                                                    The value of the source field in log events which should be processed by the Grok rules.
                                                  type: string
                                                supportRule:
                                                  description: |-
                                                    (Block List) A list of helper Grok rules that can be referenced by the parsing rules. (see below for nested schema)
                                                    A list of helper Grok rules that can be referenced by the parsing rules.
                                                  items:
                                                    properties:
                                                      name:
                                                        description: |-
                                                          (String) The pipeline name.
                                                          The name of the helper Grok rule.
                                                        type: string
                                                      rule:
                                                        description: |-
                                                          (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                                          The definition of the helper Grok rule.
                                                        type: string
                                                    type: object
                                                  type: array
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    parseJson:
                                      description: |-
                                        (Block List) The parse_json processor extracts JSON from a specified field and flattens it into the event. This is useful when logs contain embedded JSON as a string. (see below for nested schema)
                                        The `parse_json` processor extracts JSON from a specified field and flattens it into the event. This is useful when logs contain embedded JSON as a string.
                                      items:
                                        properties:
                                          field:
                                            description: |-
                                              (String) The target field in the log event.
                                              The field to parse.
                                            type: string
                                        type: object
                                      type: array
                                    parseXml:
                                      description: |-
                                        (Block List) The parse_xml processor parses XML from a specified field and extracts it into the event. (see below for nested schema)
                                        The `parse_xml` processor parses XML from a specified field and extracts it into the event.
                                      items:
                                        properties:
                                          alwaysUseTextKey:
                                            description: |-
                                              (Boolean) Whether to always store text inside an object using the text key even when no attributes exist.
                                              Whether to always store text inside an object using the text key even when no attributes exist.
                                            type: boolean
                                          attrPrefix:
                                            description: |-
                                              (String) The prefix to use for XML attributes in the parsed output. If the field is left empty, the original attribute key is used.
                                              The prefix to use for XML attributes in the parsed output. If the field is left empty, the original attribute key is used.
                                            type: string
                                          field:
                                            description: |-
                                              (String) The target field in the log event.
                                              The path to the log field on which you want to parse XML.
                                            type: string
                                          includeAttr:
                                            description: |-
                                              (Boolean) Whether to include XML attributes in the parsed output.
                                              Whether to include XML attributes in the parsed output.
                                            type: boolean
                                          parseBool:
                                            description: |-
                                              (Boolean) Whether to parse boolean values from strings.
                                              Whether to parse boolean values from strings.
                                            type: boolean
                                          parseNull:
                                            description: |-
                                              (Boolean) Whether to parse null values.
                                              Whether to parse null values.
                                            type: boolean
                                          parseNumber:
                                            description: |-
                                              (Boolean) Whether to parse numeric values from strings.
                                              Whether to parse numeric values from strings.
                                            type: boolean
                                          textKey:
                                            description: |-
                                              (String) The key name to use for the text node when XML attributes are appended.
                                              The key name to use for the text node when XML attributes are appended.
                                            type: string
                                        type: object
                                      type: array
                                    quota:
                                      description: |-
                                        (Block List) The quota processor measures logging traffic for logs that match a specified filter. When the configured daily quota is met, the processor can drop or alert. (see below for nested schema)
                                        The `quota` processor measures logging traffic for logs that match a specified filter. When the configured daily quota is met, the processor can drop or alert.
                                      items:
                                        properties:
                                          dropEvents:
                                            description: |-
                                              (Boolean) Whether to drop events exceeding the limit.
                                              Whether to drop events exceeding the limit.
                                            type: boolean
                                          ignoreWhenMissingPartitions:
                                            description: |-
                                              (Boolean) Whether to ignore when partition fields are missing.
                                              Whether to ignore when partition fields are missing.
                                            type: boolean
                                          limit:
                                            description: (Block List) (see below for
                                              nested schema)
                                            items:
                                              properties:
                                                enforce:
                                                  description: |-
                                                    (String) Whether to enforce by 'bytes' or 'events'. Valid values are bytes, events.
                                                    Whether to enforce by 'bytes' or 'events'. Valid values are `bytes`, `events`.
                                                  type: string
                                                limit:
                                                  description: |-
                                                    (Block List) (see below for nested schema)
                                                    The daily quota limit.
                                                  type: number
                                              type: object
                                            type: array
                                          name:
                                            description: |-
                                              (String) The pipeline name.
                                              The name of the quota.
                                            type: string
                                          overflowAction:
                                            description: |-
                                              (String) The action to take when the quota is exceeded: drop, no_action, or overflow_routing.
                                              The action to take when the quota is exceeded: `drop`, `no_action`, or `overflow_routing`.
                                            type: string
                                          override:
                                            description: |-
                                              specific quotas. (see below for nested schema)
                                              The overrides for field-specific quotas.
                                            items:
                                              properties:
                                                field:
                                                  description: |-
                                                    (String) The target field in the log event.
                                                    Fields that trigger this override.
                                                  items:
                                                    properties:
                                                      name:
                                                        description: |-
                                                          (String) The pipeline name.
                                                          The field name.
                                                        type: string
                                                      value:
                                                        description: |-
                                                          (String) The metadata value.
                                                          The field value.
                                                        type: string
                                                    type: object
                                                  type: array
                                                limit:
                                                  description: (Block List) (see below
                                                    for nested schema)
                                                  items:
                                                    properties:
                                                      enforce:
                                                        description: |-
                                                          (String) Whether to enforce by 'bytes' or 'events'. Valid values are bytes, events.
                                                          Whether to enforce by 'bytes' or 'events'. Valid values are `bytes`, `events`.
                                                        type: string
                                                      limit:
                                                        description: |-
                                                          (Block List) (see below for nested schema)
                                                          The daily quota limit.
                                                        type: number
                                                    type: object
                                                  type: array
                                              type: object
                                            type: array
                                          partitionFields:
                                            description: |-
                                              (List of String) List of partition fields.
                                              List of partition fields.
                                            items:
                                              type: string
                                            type: array
                                          tooManyBucketsAction:
                                            description: |-
                                              (String) The action to take when the max number of buckets is exceeded: drop, no_action, or overflow_routing.
                                              The action to take when the max number of buckets is exceeded: `drop`, `no_action`, or `overflow_routing`.
                                            type: string
                                        type: object
                                      type: array
                                    reduce:
                                      description: |-
                                        (Block List) The reduce processor aggregates and merges logs based on matching keys and merge strategies. (see below for nested schema)
                                        The `reduce` processor aggregates and merges logs based on matching keys and merge strategies.
                                      items:
                                        properties:
                                          groupBy:
                                            description: |-
                                              (List of String) Optional fields used to group the metric series.
                                              A list of fields used to group log events for merging.
                                            items:
                                              type: string
                                            type: array
                                          mergeStrategy:
                                            description: |-
                                              (Block List) List of merge strategies defining how values from grouped events should be combined. (see below for nested schema)
                                              List of merge strategies defining how values from grouped events should be combined.
                                            items:
                                              properties:
                                                path:
                                                  description: |-
                                                    (String) Path to the CSV file.
                                                    The field path in the log event.
                                                  type: string
                                                strategy:
                                                  description: |-
                                                    (String) The authentication strategy to use (e.g. aws or basic).
                                                    The merge strategy to apply.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    removeFields:
                                      description: |-
                                        (Block List) The remove_fields processor deletes specified fields from logs. (see below for nested schema)
                                        The `remove_fields` processor deletes specified fields from logs.
                                      items:
                                        properties:
                                          fields:
                                            description: |-
                                              (List of String) A list of log field paths to check for duplicates.
                                              List of fields to remove from the events.
                                            items:
                                              type: string
                                            type: array
                                        type: object
                                      type: array
                                    renameFields:
                                      description: |-
                                        (Block List) The rename_fields processor changes field names. (see below for nested schema)
                                        The `rename_fields` processor changes field names.
                                      items:
                                        properties:
                                          field:
                                            description: |-
                                              (String) The target field in the log event.
                                              List of fields to rename.
                                            items:
                                              properties:
                                                destination:
                                                  description: |-
                                                    (Block List) List of destinations. (see below for nested schema)
                                                    Destination field name.
                                                  type: string
                                                preserveSource:
                                                  description: |-
                                                    (Boolean) Whether to keep the original field.
                                                    Whether to keep the original field.
                                                  type: boolean
                                                source:
                                                  description: |-
                                                    (Block List) List of sources. (see below for nested schema)
                                                    Source field to rename.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    renameMetricTags:
                                      description: |-
                                        (Block List) The rename_metric_tags processor changes the keys of tags on metrics. (see below for nested schema)
                                        The `rename_metric_tags` processor changes the keys of tags on metrics.
                                      items:
                                        properties:
                                          tag:
                                            description: |-
                                              (Block List) A list of static tags to add to each metric. Up to 15 tags may be defined. (see below for nested schema)
                                              A list of rename rules. Up to 15 tags may be defined.
                                            items:
                                              properties:
                                                renameTo:
                                                  description: |-
                                                    (String) The new tag key to assign in place of the original.
                                                    The new tag key to assign in place of the original.
                                                  type: string
                                                tag:
                                                  description: |-
                                                    (Block List) A list of static tags to add to each metric. Up to 15 tags may be defined. (see below for nested schema)
                                                    The original tag key on the metric event.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    sample:
                                      description: |-
                                        (Block List) The sample processor allows probabilistic sampling of logs at a fixed rate. (see below for nested schema)
                                        The `sample` processor allows probabilistic sampling of logs at a fixed rate.
                                      items:
                                        properties:
                                          groupBy:
                                            description: |-
                                              (List of String) Optional fields used to group the metric series.
                                              Optional list of fields to group events by. Each group is sampled independently.
                                            items:
                                              type: string
                                            type: array
                                          percentage:
                                            description: |-
                                              (Number) The percentage of logs to sample.
                                              The percentage of logs to sample.
                                            type: number
                                        type: object
                                      type: array
                                    sensitiveDataScanner:
                                      description: |-
                                        (Block List) The sensitive_data_scanner processor detects and optionally redacts sensitive data in log events. (see below for nested schema)
                                        The `sensitive_data_scanner` processor detects and optionally redacts sensitive data in log events.
                                      items:
                                        properties:
                                          rule:
                                            description: |-
                                              (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                              A list of rules for identifying and acting on sensitive data patterns.
                                            items:
                                              properties:
                                                keywordOptions:
                                                  description: |-
                                                    based proximity matching for sensitive data. (see below for nested schema)
                                                    Keyword-based proximity matching for sensitive data.
                                                  items:
                                                    properties:
                                                      keywords:
                                                        description: |-
                                                          (List of String) A list of keywords to match near the sensitive pattern.
                                                          A list of keywords to match near the sensitive pattern.
                                                        items:
                                                          type: string
                                                        type: array
                                                      proximity:
                                                        description: |-
                                                          (Number) Maximum number of tokens between a keyword and a sensitive value match.
                                                          Maximum number of tokens between a keyword and a sensitive value match.
                                                        type: number
                                                    type: object
                                                  type: array
                                                name:
                                                  description: |-
                                                    (String) The pipeline name.
                                                    A name identifying the rule.
                                                  type: string
                                                onMatch:
                                                  description: |-
                                                    (Block List) The action to take when a sensitive value is found. (see below for nested schema)
                                                    The action to take when a sensitive value is found.
                                                  items:
                                                    properties:
                                                      hash:
                                                        description: |-
                                                          (Block List) Hashes the matched value. (see below for nested schema)
                                                          Hashes the matched value.
                                                        items:
                                                          type: object
                                                        type: array
                                                      partialRedact:
                                                        description: |-
                                                          (Block List) Redacts part of the matched value (e.g., keep last 4 characters). (see below for nested schema)
                                                          Redacts part of the matched value (e.g., keep last 4 characters).
                                                        items:
                                                          properties:
                                                            characters:
                                                              description: |-
                                                                (Number) Number of characters to keep.
                                                                Number of characters to keep.
                                                              type: number
                                                            direction:
                                                              description: |-
                                                                (String) Direction from which to keep characters: first or last.
                                                                Direction from which to keep characters: `first` or `last`.
                                                              type: string
                                                          type: object
                                                        type: array
                                                      redact:
                                                        description: |-
                                                          (Block List) Redacts the matched value. Exactly one of redact, hash, or partial_redact must be specified. (see below for nested schema)
                                                          Redacts the matched value. Exactly one of `redact`, `hash`, or `partial_redact` must be specified.
                                                        items:
                                                          properties:
                                                            replace:
                                                              description: |-
                                                                (String) Replacement string for redacted values (e.g., ***).
                                                                Replacement string for redacted values (e.g., `***`).
                                                              type: string
                                                          type: object
                                                        type: array
                                                    type: object
                                                  type: array
                                                pattern:
                                                  description: |-
                                                    (Block List) Pattern detection configuration for identifying sensitive data using either a custom regex or a library reference. (see below for nested schema)
                                                    Pattern detection configuration for identifying sensitive data using either a custom regex or a library reference.
                                                  items:
                                                    properties:
                                                      custom:
                                                        description: |-
                                                          (Block List) Pattern detection using a custom regular expression. Exactly one of custom or library must be specified. (see below for nested schema)
                                                          Pattern detection using a custom regular expression. Exactly one of `custom` or `library` must be specified.
                                                        items:
                                                          properties:
                                                            description:
                                                              description: |-
                                                                readable description providing context about a sensitive data scanner rule.
                                                                Human-readable description providing context about a sensitive data scanner rule.
                                                              type: string
                                                            rule:
                                                              description: |-
                                                                (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                                                A regular expression used to detect sensitive values. Must be a valid regex.
                                                              type: string
                                                          type: object
                                                        type: array
                                                      library:
                                                        description: |-
                                                          (Block List) Pattern detection using a predefined pattern from the Sensitive Data Scanner library. (see below for nested schema)
                                                          Pattern detection using a predefined pattern from the Sensitive Data Scanner library.datadoghq.
                                                        items:
                                                          properties:
                                                            description:
                                                              description: |-
                                                                readable description providing context about a sensitive data scanner rule.
                                                                Human-readable description providing context about a sensitive data scanner rule.
                                                              type: string
                                                            id:
                                                              description: |-
                                                                (String) The ID of this resource.
                                                                Identifier for a predefined pattern from the sensitive data scanner pattern library.
                                                              type: string
                                                            useRecommendedKeywords:
                                                              description: |-
                                                                (Boolean) Whether to augment the pattern with recommended keywords .
                                                                Whether to augment the pattern with recommended keywords (optional).
                                                              type: boolean
                                                          type: object
                                                        type: array
                                                    type: object
                                                  type: array
                                                scope:
                                                  description: |-
                                                    level targeting options that determine where the scanner should operate. (see below for nested schema)
                                                    Field-level targeting options that determine where the scanner should operate.
                                                  items:
                                                    properties:
                                                      all:
                                                        description: |-
                                                          (Boolean) Scan all fields.
                                                          Scan all fields.
                                                        type: boolean
                                                      exclude:
                                                        description: |-
                                                          (Block List) Explicitly exclude these fields from scanning. (see below for nested schema)
                                                          Explicitly exclude these fields from scanning.
                                                        items:
                                                          properties:
                                                            fields:
                                                              description: |-
                                                                (List of String) A list of log field paths to check for duplicates.
                                                                The fields to exclude from scanning.
                                                              items:
                                                                type: string
                                                              type: array
                                                          type: object
                                                        type: array
                                                      include:
                                                        description: |-
                                                          (String) A Datadog search query that determines which logs are forwarded using this route.
                                                          Explicitly include these fields for scanning. Exactly one of `include`, `exclude`, or `all` must be specified.
                                                        items:
                                                          properties:
                                                            fields:
                                                              description: |-
                                                                (List of String) A list of log field paths to check for duplicates.
                                                                The fields to include in scanning.
                                                              items:
                                                                type: string
                                                              type: array
                                                          type: object
                                                        type: array
                                                    type: object
                                                  type: array
                                                tags:
                                                  description: |-
                                                    (List of String) Tags assigned to this rule for filtering and classification.
                                                    Tags assigned to this rule for filtering and classification.
                                                  items:
                                                    type: string
                                                  type: array
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    splitArray:
                                      description: |-
                                        (Block List) The split_array processor splits array fields into separate events based on configured rules. (see below for nested schema)
                                        The `split_array` processor splits array fields into separate events based on configured rules.
                                      items:
                                        properties:
                                          array:
                                            description: |-
                                              (Block List) A list of array split configurations. (see below for nested schema)
                                              A list of array split configurations.
                                            items:
                                              properties:
                                                field:
                                                  description: |-
                                                    (String) The target field in the log event.
                                                    The path to the array field to split.
                                                  type: string
                                                include:
                                                  description: |-
                                                    (String) A Datadog search query that determines which logs are forwarded using this route.
                                                    A Datadog search query used to determine which logs this array split operation targets.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    tagCardinalityLimit:
                                      description: |-
                                        (Block List) The tag_cardinality_limit processor caps the number of distinct tag value combinations on metrics, dropping tags or events once the limit is exceeded. (see below for nested schema)
                                        The `tag_cardinality_limit` processor caps the number of distinct tag value combinations on metrics, dropping tags or events once the limit is exceeded.
                                      items:
                                        properties:
                                          limitExceededAction:
                                            description: |-
                                              (String) The default action to take when the cardinality limit is exceeded. One of drop_tag, drop_event. Valid values are drop_tag, drop_event.
                                              The default action to take when the cardinality limit is exceeded. One of `drop_tag`, `drop_event`. Valid values are `drop_tag`, `drop_event`.
                                            type: string
                                          perMetricLimit:
                                            description: |-
                                              metric cardinality overrides that take precedence over the default value_limit. (see below for nested schema)
                                              Per-metric cardinality overrides that take precedence over the default `value_limit`.
                                            items:
                                              properties:
                                                limitExceededAction:
                                                  description: |-
                                                    (String) The default action to take when the cardinality limit is exceeded. One of drop_tag, drop_event. Valid values are drop_tag, drop_event.
                                                    The action to take on this metric when the limit is exceeded. Required when `override_type` is `limit_override`; must be omitted when `override_type` is `excluded`. Valid values are `drop_tag`, `drop_event`.
                                                  type: string
                                                metricName:
                                                  description: |-
                                                    (String) The metric name this override applies to.
                                                    The metric name this override applies to.
                                                  type: string
                                                overrideType:
                                                  description: |-
                                                    metric override is applied. One of limit_override, excluded. Valid values are limit_override, excluded.
                                                    How the per-metric override is applied. One of `limit_override`, `excluded`. Valid values are `limit_override`, `excluded`.
                                                  type: string
                                                perTagLimit:
                                                  description: |-
                                                    tag cardinality overrides that apply within this metric. Must be omitted when override_type is excluded. (see below for nested schema)
                                                    Per-tag cardinality overrides that apply within this metric. Must be omitted when `override_type` is `excluded`.
                                                  items:
                                                    properties:
                                                      overrideType:
                                                        description: |-
                                                          metric override is applied. One of limit_override, excluded. Valid values are limit_override, excluded.
                                                          How the per-tag override is applied. One of `limit_override`, `excluded`. Valid values are `limit_override`, `excluded`.
                                                        type: string
                                                      tagKey:
                                                        description: |-
                                                          (String) The tag key this override applies to.
                                                          The tag key this override applies to.
                                                        type: string
                                                      valueLimit:
                                                        description: |-
                                                          (Number) The default maximum number of distinct tag value combinations allowed per metric. Between 0 and 1000000. Value must be between 0 and 1000000.
                                                          The cardinality cap for this tag. Required when `override_type` is `limit_override`; must be omitted when `override_type` is `excluded`. Value must be between 0 and 1000000.
                                                        type: number
                                                    type: object
                                                  type: array
                                                valueLimit:
                                                  description: |-
                                                    (Number) The default maximum number of distinct tag value combinations allowed per metric. Between 0 and 1000000. Value must be between 0 and 1000000.
                                                    The cardinality cap for this metric. Required when `override_type` is `limit_override`; must be omitted when `override_type` is `excluded`. Value must be between 0 and 1000000.
                                                  type: number
                                              type: object
                                            type: array
                                          trackingMode:
                                            description: |-
                                              (Block List) Controls whether the processor uses exact or probabilistic tag tracking. (see below for nested schema)
                                              Controls whether the processor uses exact or probabilistic tag tracking.
                                            items:
                                              properties:
                                                mode:
                                                  description: |-
                                                    (String) The protocol used to send logs. Valid values are tcp, udp.
                                                    The cardinality tracking algorithm to use. One of `exact_fingerprint`, `probabilistic`. Valid values are `exact_fingerprint`, `probabilistic`.
                                                  type: string
                                              type: object
                                            type: array
                                          valueLimit:
                                            description: |-
                                              (Number) The default maximum number of distinct tag value combinations allowed per metric. Between 0 and 1000000. Value must be between 0 and 1000000.
                                              The default maximum number of distinct tag value combinations allowed per metric. Between 0 and 1000000. Value must be between 0 and 1000000.
                                            type: number
                                        type: object
                                      type: array
                                    throttle:
                                      description: |-
                                        (Block List) The throttle processor limits the number of events that pass through over a given time window. (see below for nested schema)
                                        The `throttle` processor limits the number of events that pass through over a given time window.
                                      items:
                                        properties:
                                          groupBy:
                                            description: |-
                                              (List of String) Optional fields used to group the metric series.
                                              Optional list of fields used to group events before applying throttling.
                                            items:
                                              type: string
                                            type: array
                                          threshold:
                                            description: |-
                                              (Number) The number of events to allow before throttling is applied.
                                              The number of events to allow before throttling is applied.
                                            type: number
                                          window:
                                            description: |-
                                              (Number) The time window in seconds over which the threshold applies.
                                              The time window in seconds over which the threshold applies.
                                            type: number
                                        type: object
                                      type: array
                                  type: object
                                type: array
                            type: object
                          type: array
                        source:
                          description: |-
                            (Block List) List of sources. (see below for nested schema)
                            List of sources.
                          items:
                            properties:
                              amazonDataFirehose:
                                description: |-
                                  (Block List) The amazon_data_firehose source ingests logs from AWS Data Firehose. (see below for nested schema)
                                  The `amazon_data_firehose` source ingests logs from AWS Data Firehose.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        AWS authentication credentials used for accessing AWS services. If omitted, the system's default credentials are used (for example, the IAM role and environment variables).
                                      items:
                                        properties:
                                          assumeRole:
                                            description: |-
                                              (String) ARN of the role to assume.
                                              The Amazon Resource Name (ARN) of the role to assume.
                                            type: string
                                          externalId:
                                            description: |-
                                              (String) External ID for assumed role.
                                              A unique identifier for cross-account role assumption.
                                            type: string
                                          sessionName:
                                            description: |-
                                              (String) Session name for assumed role.
                                              A session identifier used for logging and tracing the assumed role session.
                                            type: string
                                        type: object
                                      type: array
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              amazonS3:
                                description: |-
                                  rehydratable format to an Amazon S3 bucket for archiving. (see below for nested schema)
                                  The `amazon_s3` source ingests logs from an Amazon S3 bucket. It supports AWS authentication, TLS encryption, and configurable compression.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        AWS authentication credentials used for accessing AWS services. If omitted, the system's default credentials are used (for example, the IAM role and environment variables).
                                      items:
                                        properties:
                                          assumeRole:
                                            description: |-
                                              (String) ARN of the role to assume.
                                              The Amazon Resource Name (ARN) of the role to assume.
                                            type: string
                                          externalId:
                                            description: |-
                                              (String) External ID for assumed role.
                                              A unique identifier for cross-account role assumption.
                                            type: string
                                          sessionName:
                                            description: |-
                                              (String) Session name for assumed role.
                                              A session identifier used for logging and tracing the assumed role session.
                                            type: string
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression format for objects retrieved from the S3 bucket. Use `auto` to detect compression from the object's Content-Encoding header or file extension. Valid values are `auto`, `none`, `gzip`, `zstd`.
                                      type: string
                                    region:
                                      description: |-
                                        (String) AWS region of the S3 bucket.
                                        AWS region where the S3 bucket resides.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                    urlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the S3 bucket URL.
                                        Name of the environment variable or secret that holds the S3 bucket URL.
                                      type: string
                                  type: object
                                type: array
                              datadogAgent:
                                description: |-
                                  (Block List) The datadog_agent source collects logs from the Datadog Agent. (see below for nested schema)
                                  The `datadog_agent` source collects logs from the Datadog Agent.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address for the Datadog Agent source.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              fluentBit:
                                description: |-
                                  (Block List) The fluent_bit source ingests logs from Fluent Bit. (see below for nested schema)
                                  The `fluent_bit` source ingests logs from Fluent Bit.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              fluentd:
                                description: |-
                                  compatible service. (see below for nested schema)
                                  The `fluentd` source ingests logs from a Fluentd-compatible service.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              googlePubsub:
                                description: |-
                                  (Block List) The google_pubsub destination publishes logs to a Google Cloud Pub/Sub topic. (see below for nested schema)
                                  The `google_pubsub` source ingests logs from a Google Cloud Pub/Sub subscription.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        Google Cloud credentials used to authenticate with Google Cloud services.
                                      items:
                                        properties:
                                          credentialsFile:
                                            description: |-
                                              (String) Path to the Google Cloud service account key file.
                                              Path to the Google Cloud service account key file.
                                            type: string
                                        type: object
                                      type: array
                                    decoding:
                                      description: |-
                                        (String) The decoding format used to interpret incoming logs.
                                        The decoding format used to interpret incoming logs.
                                      type: string
                                    project:
                                      description: |-
                                        (String) The Google Cloud project ID that owns the Pub/Sub topic.
                                        The Google Cloud project ID that owns the Pub/Sub subscription.
                                      type: string
                                    subscription:
                                      description: |-
                                        (String) The Pub/Sub subscription name from which messages are consumed.
                                        The Pub/Sub subscription name from which messages are consumed.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              httpClient:
                                description: |-
                                  (Block List) The http_client destination sends data to an HTTP endpoint. (see below for nested schema)
                                  The `http_client` source scrapes logs from HTTP endpoints at regular intervals.
                                items:
                                  properties:
                                    authStrategy:
                                      description: |-
                                        (String) HTTP authentication strategy. Valid values are none, basic, bearer.
                                        Optional authentication strategy for HTTP requests. Valid values are `none`, `basic`, `bearer`, `custom`.
                                      type: string
                                    customKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds a custom header value (used with custom auth strategies).
                                        Name of the environment variable or secret that holds a custom header value (used with custom auth strategies).
                                      type: string
                                    decoding:
                                      description: |-
                                        (String) The decoding format used to interpret incoming logs.
                                        The decoding format used to interpret incoming logs.
                                      type: string
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the HTTP endpoint URL.
                                      type: string
                                    passwordKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                        Name of the environment variable or secret that holds the password.
                                      type: string
                                    scrapeIntervalSecs:
                                      description: |-
                                        (Number) The interval (in seconds) between HTTP scrape requests.
                                        The interval (in seconds) between HTTP scrape requests.
                                      type: number
                                    scrapeTimeoutSecs:
                                      description: |-
                                        (Number) The timeout (in seconds) for each scrape request.
                                        The timeout (in seconds) for each scrape request.
                                      type: number
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          serverName:
                                            description: |-
                                              (String) Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                              Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                            type: string
                                        type: object
                                      type: array
                                    tokenKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the authentication token.
                                        Name of the environment variable or secret that holds the authentication token.
                                      type: string
                                    usernameKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                        Name of the environment variable or secret that holds the username.
                                      type: string
                                  type: object
                                type: array
                              httpServer:
                                description: |-
                                  (Block List) The http_server source collects logs over HTTP POST from external services. (see below for nested schema)
                                  The `http_server` source collects logs over HTTP POST from external services.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                    authStrategy:
                                      description: |-
                                        (String) HTTP authentication strategy. Valid values are none, basic, bearer.
                                        HTTP authentication method. Valid values are `none`, `plain`.
                                      type: string
                                    decoding:
                                      description: |-
                                        (String) The decoding format used to interpret incoming logs.
                                        The decoding format used to interpret incoming logs. Valid values are `json`, `gelf`, `syslog`, `bytes`.
                                      type: string
                                    passwordKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                        Name of the environment variable or secret that holds the password.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                    usernameKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                        Name of the environment variable or secret that holds the username.
                                      type: string
                                    validToken:
                                      description: |-
                                        (Block List) A token accepted for authenticating incoming HTTP requests. Cannot be combined with the plain auth strategy. (see below for nested schema)
                                        A token accepted for authenticating incoming HTTP requests. Cannot be combined with the `plain` auth strategy.
                                      items:
                                        properties:
                                          enabled:
                                            description: |-
                                              (Boolean) Whether this processor group is enabled.
                                              Whether this token is currently accepted. Defaults to `true`.
                                            type: boolean
                                          fieldToAdd:
                                            description: |-
                                              Za-z0-9_]+$. (see below for nested schema)
                                              An optional metadata field attached to every event authenticated by the token. Both `key` and `value` must match `^[A-Za-z0-9_]+$`.
                                            items:
                                              properties:
                                                key:
                                                  description: |-
                                                    (Block List) Key fields used to look up enrichment values. (see below for nested schema)
                                                    The metadata field name to add to incoming events.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The metadata value.
                                                    The metadata field value to add to incoming events.
                                                  type: string
                                              type: object
                                            type: array
                                          pathToToken:
                                            description: |-
                                              in source or header to read it from a request header. (see below for nested schema)
                                              Specifies where the worker extracts the token from the incoming HTTP request. Set either `location` for a built-in source or `header` to read it from a request header.
                                            items:
                                              properties:
                                                header:
                                                  description: |-
                                                    (String) The name of the HTTP header that carries the token.
                                                    The name of the HTTP header that carries the token.
                                                  type: string
                                                location:
                                                  description: |-
                                                    in token location on the incoming HTTP request. One of path, address. Valid values are path, address.
                                                    Built-in token location on the incoming HTTP request. One of `path`, `address`. Valid values are `path`, `address`.
                                                  type: string
                                              type: object
                                            type: array
                                          tokenKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the authentication token.
                                              Name of the environment variable or secret that holds the expected token value.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  The unique identifier for this source.
                                type: string
                              kafka:
                                description: |-
                                  (Block List) The kafka destination sends logs to Apache Kafka topics. (see below for nested schema)
                                  The `kafka` source ingests data from Apache Kafka topics.
                                items:
                                  properties:
                                    bootstrapServersKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the Kafka bootstrap servers.
                                        Name of the environment variable or secret that holds the Kafka bootstrap servers connection string.
                                      type: string
                                    groupId:
                                      description: |-
                                        (String) The Kafka consumer group ID.
                                        The Kafka consumer group ID.
                                      type: string
                                    librdkafkaOption:
                                      description: |-
                                        value pairs. (see below for nested schema)
                                        Advanced librdkafka client configuration options.
                                      items:
                                        properties:
                                          name:
                                            description: |-
                                              (String) The pipeline name.
                                              The name of the librdkafka option.
                                            type: string
                                          value:
                                            description: |-
                                              (String) The metadata value.
                                              The value of the librdkafka option.
                                            type: string
                                        type: object
                                      type: array
                                    sasl:
                                      description: |-
                                        (Block List) Specifies the SASL mechanism for authenticating with a Kafka cluster. (see below for nested schema)
                                        SASL authentication settings.
                                      items:
                                        properties:
                                          mechanism:
                                            description: |-
                                              SHA-256, SCRAM-SHA-512.
                                              SASL mechanism to use (e.g., PLAIN, SCRAM-SHA-256, SCRAM-SHA-512). Valid values are `PLAIN`, `SCRAM-SHA-256`, `SCRAM-SHA-512`.
                                            type: string
                                          passwordKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                              Name of the environment variable or secret that holds the SASL password.
                                            type: string
                                          usernameKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                              Name of the environment variable or secret that holds the SASL username.
                                            type: string
                                        type: object
                                      type: array
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                    topics:
                                      description: |-
                                        (List of String) A list of Kafka topic names to subscribe to. The source ingests messages from each topic specified.
                                        A list of Kafka topic names to subscribe to. The source ingests messages from each topic specified.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                type: array
                              logstash:
                                description: |-
                                  (Block List) The logstash source ingests logs from a Logstash forwarder. (see below for nested schema)
                                  The `logstash` source ingests logs from a Logstash forwarder.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              opentelemetry:
                                description: |-
                                  (Block List) The opentelemetry destination forwards metrics using the OpenTelemetry Protocol (OTLP) over HTTP. (see below for nested schema)
                                  The `opentelemetry` source receives telemetry data using the OpenTelemetry Protocol (OTLP) over gRPC and HTTP.
                                items:
                                  properties:
                                    grpcAddressKey:
                                      description: |-
                                        (String) Environment variable name containing the gRPC server address for receiving OTLP data.
                                        Environment variable name containing the gRPC server address for receiving OTLP data.
                                      type: string
                                    httpAddressKey:
                                      description: |-
                                        (String) Environment variable name containing the HTTP server address for receiving OTLP data.
                                        Environment variable name containing the HTTP server address for receiving OTLP data.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              rsyslog:
                                description: |-
                                  (Block List) The rsyslog destination forwards logs to an external rsyslog server over TCP or UDP using the syslog protocol. (see below for nested schema)
                                  The `rsyslog` source listens for logs over TCP or UDP from an `rsyslog` server using the syslog protocol.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                    mode:
                                      description: |-
                                        (String) The protocol used to send logs. Valid values are tcp, udp.
                                        Protocol used by the syslog source to receive messages.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              socket:
                                description: |-
                                  (Block List) The socket destination sends logs over TCP or UDP to a remote server. (see below for nested schema)
                                  The `socket` source ingests logs over TCP or UDP.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address for the socket.
                                      type: string
                                    framing:
                                      description: |-
                                        (Block List) Defines the framing method for outgoing messages. (see below for nested schema)
                                        Defines the framing method for incoming messages.
                                      items:
                                        properties:
                                          characterDelimited:
                                            description: |-
                                              (Block List) Used when method is character_delimited. Specifies the delimiter character. (see below for nested schema)
                                              Used when `method` is `character_delimited`. Specifies the delimiter character.
                                            items:
                                              properties:
                                                delimiter:
                                                  description: |-
                                                    (String) A single ASCII character used as a delimiter.
                                                    A single ASCII character used as a delimiter.
                                                  type: string
                                              type: object
                                            type: array
                                          method:
                                            description: |-
                                              (String) The framing method. Valid values are newline_delimited, bytes, character_delimited.
                                              The framing method. Valid values are `newline_delimited`, `bytes`, `character_delimited`, `octet_counting`, `chunked_gelf`.
                                            type: string
                                        type: object
                                      type: array
                                    mode:
                                      description: |-
                                        (String) The protocol used to send logs. Valid values are tcp, udp.
                                        The protocol used to receive logs. Valid values are `tcp`, `udp`.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              splunkHec:
                                description: |-
                                  (Block List) The splunk_hec destination forwards logs to Splunk using the HTTP Event Collector (HEC). (see below for nested schema)
                                  The `splunk_hec` source implements the Splunk HTTP Event Collector (HEC) API.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address for the HEC API.
                                      type: string
                                    storeHecToken:
                                      description: |-
                                        (Boolean) When true, the Splunk HEC token from the incoming request is stored in the event, allowing downstream components to forward it to other Splunk HEC destinations.
                                        When `true`, the Splunk HEC token from the incoming request is stored in the event, allowing downstream components to forward it to other Splunk HEC destinations.
                                      type: boolean
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                    validToken:
                                      description: |-
                                        (Block List) A token accepted for authenticating incoming HTTP requests. Cannot be combined with the plain auth strategy. (see below for nested schema)
                                        A HEC token accepted for authenticating incoming Splunk HEC requests.
                                      items:
                                        properties:
                                          enabled:
                                            description: |-
                                              (Boolean) Whether this processor group is enabled.
                                              Whether this token is currently accepted. Defaults to `true`.
                                            type: boolean
                                          fieldToAdd:
                                            description: |-
                                              Za-z0-9_]+$. (see below for nested schema)
                                              An optional metadata field attached to every event authenticated by the token. Both `key` and `value` must match `^[A-Za-z0-9_]+$`.
                                            items:
                                              properties:
                                                key:
                                                  description: |-
                                                    (Block List) Key fields used to look up enrichment values. (see below for nested schema)
                                                    The metadata field name to add to incoming events.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The metadata value.
                                                    The metadata field value to add to incoming events.
                                                  type: string
                                              type: object
                                            type: array
                                          tokenKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the authentication token.
                                              Name of the environment variable or secret that holds the expected HEC token value.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              splunkTcp:
                                description: |-
                                  (Block List) The splunk_tcp source receives logs from a Splunk Universal Forwarder over TCP. TLS is supported for secure transmission. (see below for nested schema)
                                  The `splunk_tcp` source receives logs from a Splunk Universal Forwarder over TCP. TLS is supported for secure transmission.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address for the Splunk TCP receiver.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              sumoLogic:
                                description: |-
                                  (Block List) The sumo_logic destination forwards logs to Sumo Logic. (see below for nested schema)
                                  The `sumo_logic` source receives logs from Sumo Logic collectors.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                  type: object
                                type: array
                              syslogNg:
                                description: |-
                                  ng server over TCP or UDP using the syslog protocol. (see below for nested schema)
                                  The `syslog_ng` source listens for logs over TCP or UDP from a `syslog-ng` server using the syslog protocol.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                    mode:
                                      description: |-
                                        (String) The protocol used to send logs. Valid values are tcp, udp.
                                        Protocol used by the syslog source to receive messages.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              websocket:
                                description: |-
                                  (Block List) The websocket source establishes a persistent WebSocket connection to a remote endpoint and ingests log events as they are pushed by the server. (see below for nested schema)
                                  The `websocket` source establishes a persistent WebSocket connection to a remote endpoint and ingests log events as they are pushed by the server.
                                items:
                                  properties:
                                    authStrategy:
                                      description: |-
                                        (String) HTTP authentication strategy. Valid values are none, basic, bearer.
                                        The authentication strategy used when connecting to the WebSocket server. Valid values are `none`, `basic`, `bearer`, `custom`.
                                      type: string
                                    customKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds a custom header value (used with custom auth strategies).
                                        Name of the environment variable or secret that holds a custom header value. Used when `auth_strategy` is `custom`.
                                      type: string
                                    decoding:
                                      description: |-
                                        (String) The decoding format used to interpret incoming logs.
                                        The decoding format used to interpret incoming log events. Valid values are `bytes`, `gelf`, `json`, `syslog`.
                                      type: string
                                    passwordKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                        Name of the environment variable or secret that holds the password. Used when `auth_strategy` is `basic`.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        TLS configuration for the WebSocket connection. Set `mode` to `enabled` for server-certificate validation only, or `with_client_cert` to additionally present a client certificate.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the client certificate file. Required when `mode` is `with_client_cert`.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the client certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          mode:
                                            description: |-
                                              (String) The protocol used to send logs. Valid values are tcp, udp.
                                              The TLS mode. Use `enabled` for server-only TLS, or `with_client_cert` for mutual TLS with a client certificate. Valid values are `enabled`, `with_client_cert`.
                                            type: string
                                        type: object
                                      type: array
                                    tokenKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the authentication token.
                                        Name of the environment variable or secret that holds the bearer token. Used when `auth_strategy` is `bearer`.
                                      type: string
                                    uriKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the request URI.
                                        Name of the environment variable or secret that holds the WebSocket URI to connect to.
                                      type: string
                                    usernameKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                        Name of the environment variable or secret that holds the username. Used when `auth_strategy` is `basic`.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        useLegacySearchSyntax:
                          description: |-
                            (Boolean) Set to true to continue using the legacy search syntax while migrating filter queries. After migrating all queries to the new syntax, set to false. The legacy syntax is deprecated and will eventually be removed. Requires Observability Pipelines Worker 2.11 or later. Only applies to logs pipelines. This field is ignored for metrics pipelines. See https://docs.datadoghq.com/observability_pipelines/guide/upgrade_your_filter_queries_to_the_new_search_syntax/ for more information.
                            Set to `true` to continue using the legacy search syntax while migrating filter queries. After migrating all queries to the new syntax, set to `false`. The legacy syntax is deprecated and will eventually be removed. Requires Observability Pipelines Worker 2.11 or later. Only applies to `logs` pipelines. This field is ignored for `metrics` pipelines. See https://docs.datadoghq.com/observability_pipelines/guide/upgrade_your_filter_queries_to_the_new_search_syntax/ for more information.
                          type: boolean
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The pipeline name.
                      The pipeline name.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  config:
                    description: |-
                      (Block List) Configuration for the pipeline. (see below for nested schema)
                      Configuration for the pipeline.
                    items:
                      properties:
                        destination:
                          description: |-
                            (Block List) List of destinations. (see below for nested schema)
                            List of destinations.
                          items:
                            properties:
                              amazonOpensearch:
                                description: |-
                                  (Block List) The amazon_opensearch destination writes logs to Amazon OpenSearch. (see below for nested schema)
                                  The `amazon_opensearch` destination writes logs to Amazon OpenSearch.
                                items:
                                  properties:
                                    auth:
                                      description: (Block List) (see below for nested
                                        schema)
                                      items:
                                        properties:
                                          assumeRole:
                                            description: |-
                                              (String) ARN of the role to assume.
                                              ARN of the role to assume.
                                            type: string
                                          awsRegion:
                                            description: |-
                                              (String) AWS region override (if applicable).
                                              AWS region override (if applicable).
                                            type: string
                                          externalId:
                                            description: |-
                                              (String) External ID for assumed role.
                                              External ID for assumed role.
                                            type: string
                                          sessionName:
                                            description: |-
                                              (String) Session name for assumed role.
                                              Session name for assumed role.
                                            type: string
                                          strategy:
                                            description: |-
                                              (String) The authentication strategy to use (e.g. aws or basic).
                                              The authentication strategy to use (e.g. aws or basic).
                                            type: string
                                        type: object
                                      type: array
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    bulkIndex:
                                      description: |-
                                        (String) The index or datastream to write logs to.
                                        The index or datastream to write logs to.
                                      type: string
                                  type: object
                                type: array
                              amazonS3:
                                description: |-
                                  rehydratable format to an Amazon S3 bucket for archiving. (see below for nested schema)
                                  The `amazon_s3` destination sends your logs in Datadog-rehydratable format to an Amazon S3 bucket for archiving.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        AWS authentication credentials used for accessing AWS services. If omitted, the system's default credentials are used (for example, the IAM role and environment variables).
                                      items:
                                        properties:
                                          assumeRole:
                                            description: |-
                                              (String) ARN of the role to assume.
                                              The Amazon Resource Name (ARN) of the role to assume.
                                            type: string
                                          externalId:
                                            description: |-
                                              (String) External ID for assumed role.
                                              A unique identifier for cross-account role assumption.
                                            type: string
                                          sessionName:
                                            description: |-
                                              (String) Session name for assumed role.
                                              A session identifier used for logging and tracing the assumed role session.
                                            type: string
                                        type: object
                                      type: array
                                    bucket:
                                      description: |-
                                        (String) S3 bucket name.
                                        S3 bucket name.
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression configuration for archived logs. When omitted, the worker default (gzip) is used.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              Compression algorithm. One of `gzip` or `zstd`. Valid values are `gzip`, `zstd`.
                                            type: string
                                          level:
                                            description: |-
                                              9) and zstd (1-21).
                                              Compression level. Applies to `gzip` (1-9) and `zstd` (1-21).
                                            type: number
                                        type: object
                                      type: array
                                    keyPrefix:
                                      description: |-
                                        (String) Prefix for object keys.
                                        Prefix for object keys.
                                      type: string
                                    region:
                                      description: |-
                                        (String) AWS region of the S3 bucket.
                                        AWS region of the S3 bucket.
                                      type: string
                                    serverSideEncryption:
                                      description: |-
                                        side encryption algorithm used when storing objects in S3. Valid values: aws:kms, AES256. Valid values are aws:kms, AES256.
                                        The server-side encryption algorithm used when storing objects in S3. Valid values: `aws:kms`, `AES256`. Valid values are `aws:kms`, `AES256`.
                                      type: string
                                    ssekmsKeyId:
                                      description: |-
                                        KMS encryption. Only applies when server_side_encryption is aws:kms.
                                        ID of the AWS KMS key to use for SSE-KMS encryption. Only applies when `server_side_encryption` is `aws:kms`.
                                      type: string
                                    storageClass:
                                      description: |-
                                        (String) S3 storage class. Valid values are STANDARD, REDUCED_REDUNDANCY, INTELLIGENT_TIERING, STANDARD_IA, EXPRESS_ONEZONE, ONEZONE_IA, GLACIER, GLACIER_IR, DEEP_ARCHIVE.
                                        S3 storage class. Valid values are `STANDARD`, `REDUCED_REDUNDANCY`, `INTELLIGENT_TIERING`, `STANDARD_IA`, `EXPRESS_ONEZONE`, `ONEZONE_IA`, `GLACIER`, `GLACIER_IR`, `DEEP_ARCHIVE`.
                                      type: string
                                  type: object
                                type: array
                              amazonS3Generic:
                                description: |-
                                  (Block List) The amazon_s3_generic destination sends your logs to an Amazon S3 bucket. (see below for nested schema)
                                  The `amazon_s3_generic` destination sends your logs to an Amazon S3 bucket.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        AWS authentication credentials used for accessing AWS services. If omitted, the system's default credentials are used (for example, the IAM role and environment variables).
                                      items:
                                        properties:
                                          assumeRole:
                                            description: |-
                                              (String) ARN of the role to assume.
                                              The Amazon Resource Name (ARN) of the role to assume.
                                            type: string
                                          externalId:
                                            description: |-
                                              (String) External ID for assumed role.
                                              A unique identifier for cross-account role assumption.
                                            type: string
                                          sessionName:
                                            description: |-
                                              (String) Session name for assumed role.
                                              A session identifier used for logging and tracing the assumed role session.
                                            type: string
                                        type: object
                                      type: array
                                    batchSettings:
                                      description: |-
                                        (Block List) Event batching settings. (see below for nested schema)
                                        Event batching settings.
                                      items:
                                        properties:
                                          batchSize:
                                            description: |-
                                              (Number) Maximum batch size in bytes.
                                              Maximum batch size in bytes.
                                            type: number
                                          timeoutSecs:
                                            description: |-
                                              (Number) Maximum number of seconds to wait before flushing the batch.
                                              Maximum number of seconds to wait before flushing the batch.
                                            type: number
                                        type: object
                                      type: array
                                    bucket:
                                      description: |-
                                        (String) S3 bucket name.
                                        S3 bucket name.
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression configuration.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              Compression algorithm. Valid values are `gzip`, `zstd`, `snappy`.
                                            type: string
                                          level:
                                            description: |-
                                              9) and zstd (1-21).
                                              Compression level.
                                            type: number
                                        type: object
                                      type: array
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        Encoding format for the destination.
                                      items:
                                        properties:
                                          type:
                                            description: |-
                                              (String) The encoding type. Valid values are json, parquet.
                                              The encoding type. Valid values are `json`, `parquet`.
                                            type: string
                                        type: object
                                      type: array
                                    keyPrefix:
                                      description: |-
                                        (String) Prefix for object keys.
                                        Optional prefix for object keys.
                                      type: string
                                    region:
                                      description: |-
                                        (String) AWS region of the S3 bucket.
                                        AWS region of the S3 bucket.
                                      type: string
                                    serverSideEncryption:
                                      description: |-
                                        side encryption algorithm used when storing objects in S3. Valid values: aws:kms, AES256. Valid values are aws:kms, AES256.
                                        The server-side encryption algorithm used when storing objects in S3. Valid values: `aws:kms`, `AES256`. Valid values are `aws:kms`, `AES256`.
                                      type: string
                                    ssekmsKeyId:
                                      description: |-
                                        KMS encryption. Only applies when server_side_encryption is aws:kms.
                                        ID of the AWS KMS key to use for SSE-KMS encryption. Only applies when `server_side_encryption` is `aws:kms`.
                                      type: string
                                    storageClass:
                                      description: |-
                                        (String) S3 storage class. Valid values are STANDARD, REDUCED_REDUNDANCY, INTELLIGENT_TIERING, STANDARD_IA, EXPRESS_ONEZONE, ONEZONE_IA, GLACIER, GLACIER_IR, DEEP_ARCHIVE.
                                        S3 storage class. Valid values are `STANDARD`, `REDUCED_REDUNDANCY`, `INTELLIGENT_TIERING`, `STANDARD_IA`, `EXPRESS_ONEZONE`, `ONEZONE_IA`, `GLACIER`, `GLACIER_IR`, `DEEP_ARCHIVE`.
                                      type: string
                                  type: object
                                type: array
                              amazonSecurityLake:
                                description: |-
                                  (Block List) The amazon_security_lake destination sends your logs to Amazon Security Lake. (see below for nested schema)
                                  The `amazon_security_lake` destination sends your logs to Amazon Security Lake.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        AWS authentication credentials used for accessing AWS services. If omitted, the system's default credentials are used (for example, the IAM role and environment variables).
                                      items:
                                        properties:
                                          assumeRole:
                                            description: |-
                                              (String) ARN of the role to assume.
                                              The Amazon Resource Name (ARN) of the role to assume.
                                            type: string
                                          externalId:
                                            description: |-
                                              (String) External ID for assumed role.
                                              A unique identifier for cross-account role assumption.
                                            type: string
                                          sessionName:
                                            description: |-
                                              (String) Session name for assumed role.
                                              A session identifier used for logging and tracing the assumed role session.
                                            type: string
                                        type: object
                                      type: array
                                    bucket:
                                      description: |-
                                        (String) S3 bucket name.
                                        Name of the Amazon S3 bucket in Security Lake (3-63 characters).
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    customSourceName:
                                      description: |-
                                        (String) Custom source name for the logs in Security Lake.
                                        Custom source name for the logs in Security Lake.
                                      type: string
                                    region:
                                      description: |-
                                        (String) AWS region of the S3 bucket.
                                        AWS region of the Security Lake bucket.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              azureStorage:
                                description: |-
                                  (Block List) The azure_storage destination forwards logs to an Azure Blob Storage container. (see below for nested schema)
                                  The `azure_storage` destination forwards logs to an Azure Blob Storage container.
                                items:
                                  properties:
                                    blobPrefix:
                                      description: |-
                                        (String) Optional prefix for blobs written to the container.
                                        Optional prefix for blobs written to the container.
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression configuration for archived logs. When omitted, the worker default (gzip) is used.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              Compression algorithm. One of `gzip` or `zstd`. Valid values are `gzip`, `zstd`.
                                            type: string
                                          level:
                                            description: |-
                                              9) and zstd (1-21).
                                              Compression level. Applies to `gzip` (1-9) and `zstd` (1-21).
                                            type: number
                                        type: object
                                      type: array
                                    connectionStringKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the Azure Storage connection string.
                                        Name of the environment variable or secret that holds the Azure Storage connection string.
                                      type: string
                                    containerName:
                                      description: |-
                                        (String) The name of the Azure Blob Storage container to store logs in.
                                        The name of the Azure Blob Storage container to store logs in.
                                      type: string
                                  type: object
                                type: array
                              clickhouse:
                                description: |-
                                  (Block List) The clickhouse destination forwards logs to a ClickHouse server via HTTP. (see below for nested schema)
                                  The `clickhouse` destination forwards logs to a ClickHouse server via HTTP.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        Authentication strategy for ClickHouse HTTP requests. Only `basic` strategy is supported.
                                      items:
                                        properties:
                                          passwordKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                              Name of the environment variable or secret that holds the ClickHouse password. Defaults to `DESTINATION_CLICKHOUSE_PASSWORD`.
                                            type: string
                                          strategy:
                                            description: |-
                                              (String) The authentication strategy to use (e.g. aws or basic).
                                              Authentication strategy. Must be `basic`. Valid values are `basic`.
                                            type: string
                                          usernameKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                              Name of the environment variable or secret that holds the ClickHouse username. Defaults to `DESTINATION_CLICKHOUSE_USERNAME`.
                                            type: string
                                        type: object
                                      type: array
                                    batch:
                                      description: |-
                                        (Block List) Batching configuration for ClickHouse inserts. (see below for nested schema)
                                        Batching configuration for ClickHouse inserts.
                                      items:
                                        properties:
                                          maxEvents:
                                            description: |-
                                              (Number) Maximum events for the memory buffer.
                                              Maximum number of events per batch. Value must be at least 1.
                                            type: number
                                          timeoutSecs:
                                            description: |-
                                              (Number) Maximum number of seconds to wait before flushing the batch.
                                              Maximum time in seconds before a partial batch is flushed. Value must be between 1 and 65535.
                                            type: number
                                        type: object
                                      type: array
                                    batchEncoding:
                                      description: |-
                                        (Block List) Batch encoding configuration. Required when format is arrow_stream. (see below for nested schema)
                                        Batch encoding configuration. Required when `format` is `arrow_stream`.
                                      items:
                                        properties:
                                          allowNullableFields:
                                            description: |-
                                              nullable fields in the ClickHouse schema. Defaults to false.
                                              If `true`, allows null values for non-nullable fields in the ClickHouse schema. Defaults to `false`.
                                            type: boolean
                                          codec:
                                            description: |-
                                              (String) Batch encoding codec. Must be arrow_stream. Valid values are arrow_stream.
                                              Batch encoding codec. Must be `arrow_stream`. Valid values are `arrow_stream`.
                                            type: string
                                        type: object
                                      type: array
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression for outbound HTTP requests. Use `algorithm = "gzip"` or `algorithm = "none"`.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              Compression algorithm. Valid values are `gzip` and `none`. Valid values are `gzip`, `none`.
                                            type: string
                                          level:
                                            description: |-
                                              9) and zstd (1-21).
                                              Compression level (1–9). Only valid when `algorithm` is `gzip`. Value must be between 1 and 9.
                                            type: number
                                        type: object
                                      type: array
                                    database:
                                      description: |-
                                        (String) Optional name of the ClickHouse database to write to. When omitted, the user's default database is used.
                                        Optional name of the ClickHouse database to write to. When omitted, the user's default database is used.
                                      type: string
                                    dateTimeBestEffort:
                                      description: |-
                                        (Boolean) If true, enables flexible DateTime parsing on the server side.
                                        If `true`, enables flexible DateTime parsing on the server side.
                                      type: boolean
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to `DESTINATION_CLICKHOUSE_ENDPOINT_URL`.
                                      type: string
                                    format:
                                      description: |-
                                        (String) Insert format for events. json_each_row maps event fields to columns by name. json_as_object and json_as_string insert each event into a single JSON or String column. arrow_stream batches events with Apache Arrow IPC streaming and requires batch_encoding. Valid values are json_each_row, json_as_object, json_as_string, arrow_stream.
                                        Insert format for events. `json_each_row` maps event fields to columns by name. `json_as_object` and `json_as_string` insert each event into a single JSON or String column. `arrow_stream` batches events with Apache Arrow IPC streaming and requires `batch_encoding`. Valid values are `json_each_row`, `json_as_object`, `json_as_string`, `arrow_stream`.
                                      type: string
                                    skipUnknownFields:
                                      description: |-
                                        (Boolean) If true, fields not present in the target table schema are dropped instead of causing insert errors. When unset, the ClickHouse server's own input_format_skip_unknown_fields setting applies.
                                        If `true`, fields not present in the target table schema are dropped instead of causing insert errors. When unset, the ClickHouse server's own `input_format_skip_unknown_fields` setting applies.
                                      type: boolean
                                    table:
                                      description: |-
                                        (String) Target ClickHouse table name.
                                        Target ClickHouse table name.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              cloudPrem:
                                description: |-
                                  (Block List) The cloud_prem destination sends logs to Datadog CloudPrem. (see below for nested schema)
                                  The `cloud_prem` destination sends logs to Datadog CloudPrem.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the endpoint URL.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          serverName:
                                            description: |-
                                              (String) Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                              Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              crowdstrikeNextGenSiem:
                                description: |-
                                  (Block List) The crowdstrike_next_gen_siem destination forwards logs to CrowdStrike Next Gen SIEM. (see below for nested schema)
                                  The `crowdstrike_next_gen_siem` destination forwards logs to CrowdStrike Next Gen SIEM.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression configuration for log events.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              Compression algorithm for log events.
                                            type: string
                                          level:
                                            description: |-
                                              9) and zstd (1-21).
                                              Compression level.
                                            type: number
                                        type: object
                                      type: array
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        Encoding format for log events. Valid values are `json`, `raw_message`.
                                      type: string
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the endpoint URL.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                    tokenKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the authentication token.
                                        Name of the environment variable or secret that holds the authentication token.
                                      type: string
                                  type: object
                                type: array
                              databricksZerobus:
                                description: |-
                                  (Block List) The databricks_zerobus destination sends logs to Databricks via the Zerobus ingestion API. (see below for nested schema)
                                  The `databricks_zerobus` destination sends logs to Databricks via the Zerobus ingestion API.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        OAuth client credentials used to authenticate with Databricks.
                                      items:
                                        properties:
                                          clientId:
                                            description: |-
                                              (String) The OAuth client ID used to authenticate with Databricks.
                                              The OAuth client ID used to authenticate with Databricks.
                                            type: string
                                          clientSecretKey:
                                            description: |-
                                              (String) The name of the secret or environment variable holding the OAuth client secret. Defaults to DESTINATION_DATABRICKS_ZEROBUS_OAUTH_CLIENT_SECRET.
                                              The name of the secret or environment variable holding the OAuth client secret. Defaults to `DESTINATION_DATABRICKS_ZEROBUS_OAUTH_CLIENT_SECRET`.
                                            type: string
                                        type: object
                                      type: array
                                    ingestionEndpointKey:
                                      description: |-
                                        (String) The name of the secret or environment variable holding the Databricks Zerobus ingestion endpoint URL.
                                        The name of the secret or environment variable holding the Databricks Zerobus ingestion endpoint URL.
                                      type: string
                                    tableName:
                                      description: |-
                                        (String) The name of the Databricks table to ingest logs into.
                                        The name of the Databricks table to ingest logs into.
                                      type: string
                                    unityCatalogEndpointKey:
                                      description: |-
                                        (String) The name of the secret or environment variable holding the Databricks Unity Catalog endpoint URL.
                                        The name of the secret or environment variable holding the Databricks Unity Catalog endpoint URL.
                                      type: string
                                  type: object
                                type: array
                              datadogLogs:
                                description: |-
                                  (Block List) The datadog_logs destination forwards logs to Datadog Log Management. (see below for nested schema)
                                  The `datadog_logs` destination forwards logs to Datadog Log Management.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    routes:
                                      description: |-
                                        (Block List) A list of routing rules that forward matching logs to Datadog using dedicated API keys. (see below for nested schema)
                                        A list of routing rules that forward matching logs to Datadog using dedicated API keys.
                                      items:
                                        properties:
                                          apiKeyKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that stores the Datadog API key used by this route.
                                              Name of the environment variable or secret that stores the Datadog API key used by this route.
                                            type: string
                                          buffer:
                                            description: |-
                                              (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                              Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                            items:
                                              properties:
                                                disk:
                                                  description: |-
                                                    (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                                    Options for configuring a disk buffer. Cannot be used with `memory`.
                                                  items:
                                                    properties:
                                                      maxSize:
                                                        description: |-
                                                          (Number) Maximum size of the disk buffer (in bytes).
                                                          Maximum size of the disk buffer (in bytes).
                                                        type: number
                                                      whenFull:
                                                        description: |-
                                                          (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                          Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                        type: string
                                                    type: object
                                                  type: array
                                                memory:
                                                  description: |-
                                                    (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                                    Options for configuring a memory buffer. Cannot be used with `disk`.
                                                  items:
                                                    properties:
                                                      maxEvents:
                                                        description: |-
                                                          (Number) Maximum events for the memory buffer.
                                                          Maximum events for the memory buffer.
                                                        type: number
                                                      maxSize:
                                                        description: |-
                                                          (Number) Maximum size of the disk buffer (in bytes).
                                                          Maximum size of the memory buffer (in bytes).
                                                        type: number
                                                      whenFull:
                                                        description: |-
                                                          (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                          Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                        type: string
                                                    type: object
                                                  type: array
                                              type: object
                                            type: array
                                          include:
                                            description: |-
                                              (String) A Datadog search query that determines which logs are forwarded using this route.
                                              A Datadog search query that determines which logs are forwarded using this route.
                                            type: string
                                          routeId:
                                            description: |-
                                              (String) Unique identifier for this route within the destination.
                                              Unique identifier for this route within the destination.
                                            type: string
                                          site:
                                            description: |-
                                              (String) Datadog site where matching logs are sent (for example, us1).
                                              Datadog site where matching logs are sent (for example, `us1`).
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              datadogMetrics:
                                description: |-
                                  (Block List) The datadog_metrics destination forwards metrics to Datadog. (see below for nested schema)
                                  The `datadog_metrics` destination forwards metrics to Datadog.
                                items:
                                  type: object
                                type: array
                              elasticsearch:
                                description: |-
                                  (Block List) The elasticsearch destination writes logs or metrics to an Elasticsearch cluster. (see below for nested schema)
                                  The `elasticsearch` destination writes logs or metrics to an Elasticsearch cluster.
                                items:
                                  properties:
                                    apiVersion:
                                      description: |-
                                        detect. Valid values are auto, v6, v7, v8.
                                        The Elasticsearch API version to use. Set to `auto` to auto-detect. Valid values are `auto`, `v6`, `v7`, `v8`.
                                      type: string
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        Authentication settings for the Elasticsearch destination.
                                      items:
                                        properties:
                                          passwordKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                              Name of the environment variable or secret that holds the Elasticsearch password (used when `strategy` is `basic`).
                                            type: string
                                          strategy:
                                            description: |-
                                              (String) The authentication strategy to use (e.g. aws or basic).
                                              The authentication strategy to use. Valid values are `basic`, `aws`.
                                            type: string
                                          usernameKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                              Name of the environment variable or secret that holds the Elasticsearch username (used when `strategy` is `basic`).
                                            type: string
                                        type: object
                                      type: array
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    bulkIndex:
                                      description: |-
                                        (String) The index or datastream to write logs to.
                                        The name of the index to write events to in Elasticsearch.
                                      type: string
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression configuration for the Elasticsearch destination.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              The compression algorithm applied when sending data to Elasticsearch. Valid values are `none`, `gzip`, `zlib`, `zstd`, `snappy`.
                                            type: string
                                          level:
                                            description: |-
                                              9) and zstd (1-21).
                                              The compression level. Only applicable for `gzip`, `zlib`, and `zstd` algorithms.
                                            type: number
                                        type: object
                                      type: array
                                    dataStream:
                                      description: |-
                                        (Block List) Configuration options for writing to Elasticsearch Data Streams instead of a fixed index. (see below for nested schema)
                                        Configuration options for writing to Elasticsearch Data Streams instead of a fixed index.
                                      items:
                                        properties:
                                          autoRouting:
                                            description: |-
                                              (Boolean) When true, automatically routes events to the appropriate data stream based on the event content.
                                              When `true`, automatically routes events to the appropriate data stream based on the event content.
                                            type: boolean
                                          dataset:
                                            description: |-
                                              (String) The data stream dataset. This groups events by their source or application.
                                              The data stream dataset. This groups events by their source or application.
                                            type: string
                                          dtype:
                                            description: |-
                                              (String) The data stream type. This determines how events are categorized within the data stream.
                                              The data stream type. This determines how events are categorized within the data stream.
                                            type: string
                                          namespace:
                                            description: |-
                                              (String) The data stream namespace. This separates events into different environments or domains.
                                              The data stream namespace. This separates events into different environments or domains.
                                            type: string
                                          syncFields:
                                            description: |-
                                              (Boolean) When true, synchronizes data stream fields with the Elasticsearch index mapping.
                                              When `true`, synchronizes data stream fields with the Elasticsearch index mapping.
                                            type: boolean
                                        type: object
                                      type: array
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the Elasticsearch endpoint URL.
                                      type: string
                                    idKey:
                                      description: |-
                                        (String) The name of the field used as the document ID in Elasticsearch.
                                        The name of the field used as the document ID in Elasticsearch.
                                      type: string
                                    pipeline:
                                      description: |-
                                        (String) The name of an Elasticsearch ingest pipeline to apply to events before indexing.
                                        The name of an Elasticsearch ingest pipeline to apply to events before indexing.
                                      type: string
                                    requestRetryPartial:
                                      description: |-
                                        (Boolean) When true, retries failed partial bulk requests when some events in a batch fail while others succeed.
                                        When `true`, retries failed partial bulk requests when some events in a batch fail while others succeed.
                                      type: boolean
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              googleCloudStorage:
                                description: |-
                                  (Block List) The google_cloud_storage destination stores logs in a Google Cloud Storage (GCS) bucket. (see below for nested schema)
                                  The `google_cloud_storage` destination stores logs in a Google Cloud Storage (GCS) bucket.
                                items:
                                  properties:
                                    acl:
                                      description: |-
                                        (String) Access control list setting for objects written to the bucket.
                                        Access control list setting for objects written to the bucket.
                                      type: string
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        Google Cloud credentials used to authenticate with Google Cloud services.
                                      items:
                                        properties:
                                          credentialsFile:
                                            description: |-
                                              (String) Path to the Google Cloud service account key file.
                                              Path to the Google Cloud service account key file.
                                            type: string
                                        type: object
                                      type: array
                                    bucket:
                                      description: |-
                                        (String) S3 bucket name.
                                        Name of the GCS bucket.
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression configuration for archived logs. When omitted, the worker default (gzip) is used.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              Compression algorithm. One of `gzip` or `zstd`. Valid values are `gzip`, `zstd`.
                                            type: string
                                          level:
                                            description: |-
                                              9) and zstd (1-21).
                                              Compression level. Applies to `gzip` (1-9) and `zstd` (1-21).
                                            type: number
                                        type: object
                                      type: array
                                    keyPrefix:
                                      description: |-
                                        (String) Prefix for object keys.
                                        Optional prefix for object keys within the GCS bucket.
                                      type: string
                                    metadata:
                                      description: |-
                                        value pairs added to each object. (see below for nested schema)
                                        Custom metadata key-value pairs added to each object.
                                      items:
                                        properties:
                                          name:
                                            description: |-
                                              (String) The pipeline name.
                                              The metadata key.
                                            type: string
                                          value:
                                            description: |-
                                              (String) The metadata value.
                                              The metadata value.
                                            type: string
                                        type: object
                                      type: array
                                    storageClass:
                                      description: |-
                                        (String) S3 storage class. Valid values are STANDARD, REDUCED_REDUNDANCY, INTELLIGENT_TIERING, STANDARD_IA, EXPRESS_ONEZONE, ONEZONE_IA, GLACIER, GLACIER_IR, DEEP_ARCHIVE.
                                        Storage class used for objects stored in GCS.
                                      type: string
                                  type: object
                                type: array
                              googlePubsub:
                                description: |-
                                  (Block List) The google_pubsub destination publishes logs to a Google Cloud Pub/Sub topic. (see below for nested schema)
                                  The `google_pubsub` destination publishes logs to a Google Cloud Pub/Sub topic.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        Google Cloud credentials used to authenticate with Google Cloud services.
                                      items:
                                        properties:
                                          credentialsFile:
                                            description: |-
                                              (String) Path to the Google Cloud service account key file.
                                              Path to the Google Cloud service account key file.
                                            type: string
                                        type: object
                                      type: array
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        Encoding format for log events. Valid values: `json`, `raw_message`.
                                      type: string
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the Google Cloud Pub/Sub endpoint URL.
                                      type: string
                                    project:
                                      description: |-
                                        (String) The Google Cloud project ID that owns the Pub/Sub topic.
                                        The Google Cloud project ID that owns the Pub/Sub topic.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                    topic:
                                      description: |-
                                        (String) The Pub/Sub topic name to publish logs to.
                                        The Pub/Sub topic name to publish logs to.
                                      type: string
                                  type: object
                                type: array
                              googleSecops:
                                description: |-
                                  (Block List) The google_chronicle destination sends logs to Google SecOps. (see below for nested schema)
                                  The `google_chronicle` destination sends logs to Google SecOps.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        Google Cloud credentials used to authenticate with Google Cloud services.
                                      items:
                                        properties:
                                          credentialsFile:
                                            description: |-
                                              (String) Path to the Google Cloud service account key file.
                                              Path to the Google Cloud service account key file.
                                            type: string
                                        type: object
                                      type: array
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    customerId:
                                      description: |-
                                        (String) The Google SecOps customer ID.
                                        The Google SecOps customer ID.
                                      type: string
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        The encoding format for the logs sent to Google SecOps. Valid values are `json`, `raw_message`.
                                      type: string
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the Google Chronicle endpoint URL.
                                      type: string
                                    logType:
                                      description: |-
                                        (String) The log type metadata associated with the Google SecOps destination.
                                        The log type metadata associated with the Google SecOps destination.
                                      type: string
                                  type: object
                                type: array
                              httpClient:
                                description: |-
                                  (Block List) The http_client destination sends data to an HTTP endpoint. (see below for nested schema)
                                  The `http_client` destination sends data to an HTTP endpoint.
                                items:
                                  properties:
                                    authStrategy:
                                      description: |-
                                        (String) HTTP authentication strategy. Valid values are none, basic, bearer.
                                        HTTP authentication strategy. Valid values are `none`, `basic`, `bearer`.
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression configuration for HTTP requests.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              Compression algorithm. Valid values are `gzip`.
                                            type: string
                                        type: object
                                      type: array
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        Encoding format for events. Valid values are `json`.
                                      type: string
                                    passwordKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                        Name of the environment variable or secret that holds the password.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          serverName:
                                            description: |-
                                              (String) Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                              Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                            type: string
                                        type: object
                                      type: array
                                    tokenKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the authentication token.
                                        Name of the environment variable or secret that holds the authentication token.
                                      type: string
                                    uriKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the request URI.
                                        Name of the environment variable or secret that holds the request URI.
                                      type: string
                                    usernameKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                        Name of the environment variable or secret that holds the username.
                                      type: string
                                  type: object
                                type: array
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  The unique identifier for this destination.
                                type: string
                              inputs:
                                description: |-
                                  (List of String) A list of component IDs whose output is used as the input for this component.
                                  A list of component IDs whose output is used as the `input` for this component.
                                items:
                                  type: string
                                type: array
                              kafka:
                                description: |-
                                  (Block List) The kafka destination sends logs to Apache Kafka topics. (see below for nested schema)
                                  The `kafka` destination sends logs to Apache Kafka topics.
                                items:
                                  properties:
                                    bootstrapServersKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the Kafka bootstrap servers.
                                        Name of the environment variable or secret that holds the Kafka bootstrap servers.
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression codec for Kafka messages. Valid values are `none`, `gzip`, `snappy`, `lz4`, `zstd`.
                                      type: string
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        Encoding format for log events. Valid values are `json`, `raw_message`.
                                      type: string
                                    headersKey:
                                      description: |-
                                        (String) The field name to use for Kafka message headers.
                                        The field name to use for Kafka message headers.
                                      type: string
                                    keyField:
                                      description: |-
                                        (String) The field name to use as the Kafka message key.
                                        The field name to use as the Kafka message key.
                                      type: string
                                    librdkafkaOption:
                                      description: |-
                                        value pairs. (see below for nested schema)
                                        Optional list of advanced Kafka producer configuration options, defined as key-value pairs.
                                      items:
                                        properties:
                                          name:
                                            description: |-
                                              (String) The pipeline name.
                                              The name of the librdkafka configuration option.
                                            type: string
                                          value:
                                            description: |-
                                              (String) The metadata value.
                                              The value of the librdkafka configuration option.
                                            type: string
                                        type: object
                                      type: array
                                    messageTimeoutMs:
                                      description: |-
                                        (Number) Maximum time in milliseconds to wait for message delivery confirmation.
                                        Maximum time in milliseconds to wait for message delivery confirmation.
                                      type: number
                                    rateLimitDurationSecs:
                                      description: |-
                                        (Number) Duration in seconds for the rate limit window.
                                        Duration in seconds for the rate limit window.
                                      type: number
                                    rateLimitNum:
                                      description: |-
                                        (Number) Maximum number of messages allowed per rate limit duration.
                                        Maximum number of messages allowed per rate limit duration.
                                      type: number
                                    sasl:
                                      description: |-
                                        (Block List) Specifies the SASL mechanism for authenticating with a Kafka cluster. (see below for nested schema)
                                        Specifies the SASL mechanism for authenticating with a Kafka cluster.
                                      items:
                                        properties:
                                          mechanism:
                                            description: |-
                                              SHA-256, SCRAM-SHA-512.
                                              SASL authentication mechanism. Valid values are `PLAIN`, `SCRAM-SHA-256`, `SCRAM-SHA-512`.
                                            type: string
                                          passwordKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                              Name of the environment variable or secret that holds the SASL password.
                                            type: string
                                          usernameKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                              Name of the environment variable or secret that holds the SASL username.
                                            type: string
                                        type: object
                                      type: array
                                    socketTimeoutMs:
                                      description: |-
                                        (Number) Socket timeout in milliseconds for network requests.
                                        Socket timeout in milliseconds for network requests.
                                      type: number
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                    topic:
                                      description: |-
                                        (String) The Pub/Sub topic name to publish logs to.
                                        The Kafka topic name to publish logs to.
                                      type: string
                                  type: object
                                type: array
                              microsoftSentinel:
                                description: |-
                                  (Block List) The microsoft_sentinel destination forwards logs to Microsoft Sentinel. (see below for nested schema)
                                  The `microsoft_sentinel` destination forwards logs to Microsoft Sentinel.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    clientId:
                                      description: |-
                                        (String) The OAuth client ID used to authenticate with Databricks.
                                        Azure AD client ID used for authentication.
                                      type: string
                                    clientSecretKey:
                                      description: |-
                                        (String) The name of the secret or environment variable holding the OAuth client secret. Defaults to DESTINATION_DATABRICKS_ZEROBUS_OAUTH_CLIENT_SECRET.
                                        Name of the environment variable or secret that holds the Azure AD client secret.
                                      type: string
                                    dceUriKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the Data Collection Endpoint (DCE) URI.
                                        Name of the environment variable or secret that holds the Data Collection Endpoint (DCE) URI.
                                      type: string
                                    dcrImmutableId:
                                      description: |-
                                        (String) The immutable ID of the Data Collection Rule (DCR).
                                        The immutable ID of the Data Collection Rule (DCR).
                                      type: string
                                    table:
                                      description: |-
                                        (String) Target ClickHouse table name.
                                        The name of the Log Analytics table where logs will be sent.
                                      type: string
                                    tenantId:
                                      description: |-
                                        (String) Azure AD tenant ID.
                                        Azure AD tenant ID.
                                      type: string
                                  type: object
                                type: array
                              newRelic:
                                description: |-
                                  (Block List) The new_relic destination sends logs to the New Relic platform. (see below for nested schema)
                                  The `new_relic` destination sends logs to the New Relic platform.
                                items:
                                  properties:
                                    accountIdKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the New Relic account ID.
                                        Name of the environment variable or secret that holds the New Relic account ID.
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    licenseKeyKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the New Relic license key.
                                        Name of the environment variable or secret that holds the New Relic license key.
                                      type: string
                                    region:
                                      description: |-
                                        (String) AWS region of the S3 bucket.
                                        The New Relic region.
                                      type: string
                                  type: object
                                type: array
                              opensearch:
                                description: |-
                                  (Block List) The opensearch destination writes logs to an OpenSearch cluster. (see below for nested schema)
                                  The `opensearch` destination writes logs to an OpenSearch cluster.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        Authentication settings for the OpenSearch destination.
                                      items:
                                        properties:
                                          passwordKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                              Name of the environment variable or secret that holds the OpenSearch password (used when `strategy` is `basic`).
                                            type: string
                                          strategy:
                                            description: |-
                                              (String) The authentication strategy to use (e.g. aws or basic).
                                              The authentication strategy to use. Valid values are `basic`, `aws`.
                                            type: string
                                          usernameKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                              Name of the environment variable or secret that holds the OpenSearch username (used when `strategy` is `basic`).
                                            type: string
                                        type: object
                                      type: array
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    bulkIndex:
                                      description: |-
                                        (String) The index or datastream to write logs to.
                                        The index or datastream to write logs to.
                                      type: string
                                    dataStream:
                                      description: |-
                                        (Block List) Configuration options for writing to Elasticsearch Data Streams instead of a fixed index. (see below for nested schema)
                                        Configuration options for writing to OpenSearch Data Streams instead of a fixed index.
                                      items:
                                        properties:
                                          dataset:
                                            description: |-
                                              (String) The data stream dataset. This groups events by their source or application.
                                              The data stream dataset for your logs. This groups logs by their source or application.
                                            type: string
                                          dtype:
                                            description: |-
                                              (String) The data stream type. This determines how events are categorized within the data stream.
                                              The data stream type for your logs. This determines how logs are categorized within the data stream.
                                            type: string
                                          namespace:
                                            description: |-
                                              (String) The data stream namespace. This separates events into different environments or domains.
                                              The data stream namespace for your logs. This separates logs into different environments or domains.
                                            type: string
                                        type: object
                                      type: array
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the OpenSearch endpoint URL.
                                      type: string
                                  type: object
                                type: array
                              opentelemetry:
                                description: |-
                                  (Block List) The opentelemetry destination forwards metrics using the OpenTelemetry Protocol (OTLP) over HTTP. (see below for nested schema)
                                  The `opentelemetry` destination forwards metrics using the OpenTelemetry Protocol (OTLP) over HTTP.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    httpClientUriKey:
                                      description: |-
                                        (String) Environment variable name containing the URI of the OTLP HTTP endpoint to send metrics to.
                                        Environment variable name containing the URI of the OTLP HTTP endpoint to send metrics to.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              rsyslog:
                                description: |-
                                  (Block List) The rsyslog destination forwards logs to an external rsyslog server over TCP or UDP using the syslog protocol. (see below for nested schema)
                                  The `rsyslog` destination forwards logs to an external `rsyslog` server over TCP or UDP using the syslog protocol.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the rsyslog endpoint URL.
                                      type: string
                                    keepalive:
                                      description: |-
                                        (Number) Optional socket keepalive duration in milliseconds.
                                        Optional socket keepalive duration in milliseconds.
                                      type: number
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              sentinelOne:
                                description: |-
                                  (Block List) The sentinel_one destination sends logs to SentinelOne. (see below for nested schema)
                                  The `sentinel_one` destination sends logs to SentinelOne.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    region:
                                      description: |-
                                        (String) AWS region of the S3 bucket.
                                        The SentinelOne region to send logs to.
                                      type: string
                                    tokenKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the authentication token.
                                        Name of the environment variable or secret that holds the SentinelOne API token.
                                      type: string
                                  type: object
                                type: array
                              socket:
                                description: |-
                                  (Block List) The socket destination sends logs over TCP or UDP to a remote server. (see below for nested schema)
                                  The `socket` destination sends logs over TCP or UDP to a remote server.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the socket address (host:port).
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        Encoding format for log events. Valid values are `json`, `raw_message`.
                                      type: string
                                    framing:
                                      description: |-
                                        (Block List) Defines the framing method for outgoing messages. (see below for nested schema)
                                        Defines the framing method for outgoing messages.
                                      items:
                                        properties:
                                          characterDelimited:
                                            description: |-
                                              (Block List) Used when method is character_delimited. Specifies the delimiter character. (see below for nested schema)
                                              Used when `method` is `character_delimited`. Specifies the delimiter character.
                                            items:
                                              properties:
                                                delimiter:
                                                  description: |-
                                                    (String) A single ASCII character used as a delimiter.
                                                    A single ASCII character used as a delimiter.
                                                  type: string
                                              type: object
                                            type: array
                                          method:
                                            description: |-
                                              (String) The framing method. Valid values are newline_delimited, bytes, character_delimited.
                                              The framing method. Valid values are `newline_delimited`, `bytes`, `character_delimited`.
                                            type: string
                                        type: object
                                      type: array
                                    mode:
                                      description: |-
                                        (String) The protocol used to send logs. Valid values are tcp, udp.
                                        The protocol used to send logs. Valid values are `tcp`, `udp`.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          serverName:
                                            description: |-
                                              (String) Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                              Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              splunkHec:
                                description: |-
                                  (Block List) The splunk_hec destination forwards logs to Splunk using the HTTP Event Collector (HEC). (see below for nested schema)
                                  The `splunk_hec` destination forwards logs to Splunk using the HTTP Event Collector (HEC).
                                items:
                                  properties:
                                    autoExtractTimestamp:
                                      description: |-
                                        (Boolean) If true, Splunk tries to extract timestamps from incoming log events. If false, Splunk assigns the time the event was received. Only applies when endpoint_target is event; cannot be true when endpoint_target is raw.
                                        If `true`, Splunk tries to extract timestamps from incoming log events. If `false`, Splunk assigns the time the event was received. Only applies when `endpoint_target` is `event`; cannot be `true` when `endpoint_target` is `raw`.
                                      type: boolean
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        Encoding format for log events. Valid values are `json`, `raw_message`.
                                      type: string
                                    endpointTarget:
                                      description: |-
                                        (String) The Splunk HEC endpoint to send events to. Use event to send structured events to the /event endpoint, or raw to send the raw message to the /raw endpoint. Valid values are event, raw.
                                        The Splunk HEC endpoint to send events to. Use `event` to send structured events to the `/event` endpoint, or `raw` to send the raw message to the `/raw` endpoint. Valid values are `event`, `raw`.
                                      type: string
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the Splunk HEC endpoint URL.
                                      type: string
                                    index:
                                      description: |-
                                        (String) Optional name of the Splunk index where logs are written.
                                        Optional name of the Splunk index where logs are written.
                                      type: string
                                    indexedFields:
                                      description: |-
                                        (List of String) List of log field names to send as indexed fields to Splunk HEC. Available only when encoding is json.
                                        List of log field names to send as indexed fields to Splunk HEC. Available only when `encoding` is `json`.
                                      items:
                                        type: string
                                      type: array
                                    sourcetype:
                                      description: |-
                                        (String) The Splunk sourcetype to assign to log events.
                                        The Splunk sourcetype to assign to log events.
                                      type: string
                                    tokenKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the authentication token.
                                        Name of the environment variable or secret that holds the Splunk HEC token.
                                      type: string
                                    tokenStrategy:
                                      description: |-
                                        (String) Controls how the Splunk HEC token is supplied. Use custom to provide a token via token_key, or from_source to forward the token received from an upstream Splunk HEC source. Valid values are custom, from_source.
                                        Controls how the Splunk HEC token is supplied. Use `custom` to provide a token via `token_key`, or `from_source` to forward the token received from an upstream Splunk HEC source. Valid values are `custom`, `from_source`.
                                      type: string
                                  type: object
                                type: array
                              splunkHecMetrics:
                                description: |-
                                  (Block List) The splunk_hec_metrics destination forwards metrics to Splunk using the HTTP Event Collector (HEC). (see below for nested schema)
                                  The `splunk_hec_metrics` destination forwards metrics to Splunk using the HTTP Event Collector (HEC).
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression algorithm applied when sending metrics to Splunk HEC. Valid values are `none`, `gzip`.
                                      type: string
                                    defaultNamespace:
                                      description: |-
                                        (String) Optional default namespace for metrics sent to Splunk HEC.
                                        Optional default namespace for metrics sent to Splunk HEC.
                                      type: string
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the Splunk HEC endpoint URL.
                                      type: string
                                    index:
                                      description: |-
                                        (String) Optional name of the Splunk index where logs are written.
                                        Optional name of the Splunk index where metrics are written.
                                      type: string
                                    source:
                                      description: |-
                                        (Block List) List of sources. (see below for nested schema)
                                        The Splunk source field value for metric events.
                                      type: string
                                    sourcetype:
                                      description: |-
                                        (String) The Splunk sourcetype to assign to log events.
                                        The Splunk sourcetype to assign to metric events.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                    tokenKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the authentication token.
                                        Name of the environment variable or secret that holds the Splunk HEC token.
                                      type: string
                                  type: object
                                type: array
                              sumoLogic:
                                description: |-
                                  (Block List) The sumo_logic destination forwards logs to Sumo Logic. (see below for nested schema)
                                  The `sumo_logic` destination forwards logs to Sumo Logic.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        The output encoding format.
                                      type: string
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the Sumo Logic endpoint URL.
                                      type: string
                                    headerCustomField:
                                      description: |-
                                        (Block List) A list of custom headers to include in the request to Sumo Logic. (see below for nested schema)
                                        A list of custom headers to include in the request to Sumo Logic.
                                      items:
                                        properties:
                                          name:
                                            description: |-
                                              (String) The pipeline name.
                                              The header field name.
                                            type: string
                                          value:
                                            description: |-
                                              (String) The metadata value.
                                              The header field value.
                                            type: string
                                        type: object
                                      type: array
                                    headerHostName:
                                      description: |-
                                        (String) Optional override for the host name header.
                                        Optional override for the host name header.
                                      type: string
                                    headerSourceCategory:
                                      description: |-
                                        (String) Optional override for the source category header.
                                        Optional override for the source category header.
                                      type: string
                                    headerSourceName:
                                      description: |-
                                        (String) Optional override for the source name header.
                                        Optional override for the source name header.
                                      type: string
                                  type: object
                                type: array
                              syslogNg:
                                description: |-
                                  ng server over TCP or UDP using the syslog protocol. (see below for nested schema)
                                  The `syslog_ng` destination forwards logs to an external `syslog-ng` server over TCP or UDP using the syslog protocol.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the syslog-ng endpoint URL.
                                      type: string
                                    keepalive:
                                      description: |-
                                        (Number) Optional socket keepalive duration in milliseconds.
                                        Optional socket keepalive duration in milliseconds.
                                      type: number
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          serverName:
                                            description: |-
                                              (String) Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                              Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                            type: object
                          type: array
                        pipelineType:
                          description: |-
                            (String) The type of data being ingested. Defaults to logs if not specified. Valid values are logs, metrics.
                            The type of data being ingested. Defaults to `logs` if not specified. Valid values are `logs`, `metrics`.
                          type: string
                        processorGroup:
                          description: |-
                            (Block List) A processor group containing common configuration and nested processors. (see below for nested schema)
                            A processor group containing common configuration and nested processors.
                          items:
                            properties:
                              displayName:
                                description: |-
                                  friendly name of the processor group.
                                  A human-friendly name of the processor group.
                                type: string
                              enabled:
                                description: |-
                                  (Boolean) Whether this processor group is enabled.
                                  Whether this processor group is enabled.
                                type: boolean
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  The unique ID of the processor group.
                                type: string
                              include:
                                description: |-
                                  (String) A Datadog search query that determines which logs are forwarded using this route.
                                  A Datadog search query used to determine which logs this processor group targets.
                                type: string
                              inputs:
                                description: |-
                                  (List of String) A list of component IDs whose output is used as the input for this component.
                                  A list of component IDs whose output is used as the input for this processor group.
                                items:
                                  type: string
                                type: array
                              processor:
                                description: |-
                                  (Block List) The processor contained in this group. (see below for nested schema)
                                  The processor contained in this group.
                                items:
                                  properties:
                                    addEnvVars:
                                      description: |-
                                        (Block List) The add_env_vars processor adds environment variable values to log events. (see below for nested schema)
                                        The `add_env_vars` processor adds environment variable values to log events.
                                      items:
                                        properties:
                                          variable:
                                            description: |-
                                              (Block List) A list of environment variable mappings to apply to log fields. (see below for nested schema)
                                              A list of environment variable mappings to apply to log fields.
                                            items:
                                              properties:
                                                field:
                                                  description: |-
                                                    (String) The target field in the log event.
                                                    The target field in the log event.
                                                  type: string
                                                name:
                                                  description: |-
                                                    (String) The pipeline name.
                                                    The name of the environment variable to read.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    addFields:
                                      description: |-
                                        value fields to logs. (see below for nested schema)
                                        The `add_fields` processor adds static key-value fields to logs.
                                      items:
                                        properties:
                                          field:
                                            description: |-
                                              (String) The target field in the log event.
                                              A list of static fields (key-value pairs) that is added to each log event processed by this component.
                                            items:
                                              properties:
                                                name:
                                                  description: |-
                                                    (String) The pipeline name.
                                                    The field name to add.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The metadata value.
                                                    The value to assign to the field.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    addHostname:
                                      description: |-
                                        (Block List) The add_hostname processor adds the hostname to log events. (see below for nested schema)
                                        The `add_hostname` processor adds the hostname to log events.
                                      items:
                                        type: object
                                      type: array
                                    addMetricTags:
                                      description: |-
                                        (Block List) The add_metric_tags processor adds static tags to metrics. (see below for nested schema)
                                        The `add_metric_tags` processor adds static tags to metrics.
                                      items:
                                        properties:
                                          tag:
                                            description: |-
                                              (Block List) A list of static tags to add to each metric. Up to 15 tags may be defined. (see below for nested schema)
                                              A list of static tags to add to each metric. Up to 15 tags may be defined.
                                            items:
                                              properties:
                                                name:
                                                  description: |-
                                                    (String) The pipeline name.
                                                    The tag name.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The metadata value.
                                                    The tag value.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    aggregate:
                                      description: |-
                                        (Block List) The aggregate processor combines metrics that share the same name and tags into a single metric over a configurable interval. (see below for nested schema)
                                        The `aggregate` processor combines metrics that share the same name and tags into a single metric over a configurable interval.
                                      items:
                                        properties:
                                          intervalSecs:
                                            description: |-
                                              (Number) The interval, in seconds, over which metrics are aggregated. Must be between 1 and 60. Value must be between 1 and 60.
                                              The interval, in seconds, over which metrics are aggregated. Must be between 1 and 60. Value must be between 1 and 60.
                                            type: number
                                          mode:
                                            description: |-
                                              (String) The protocol used to send logs. Valid values are tcp, udp.
                                              The aggregation mode. One of `auto`, `sum`, `latest`, `count`, `max`, `min`, `mean`. Valid values are `auto`, `sum`, `latest`, `count`, `max`, `min`, `mean`.
                                            type: string
                                        type: object
                                      type: array
                                    customProcessor:
                                      description: |-
                                        (Block List) The custom_processor processor transforms events using Vector Remap Language (VRL) scripts with advanced filtering capabilities. (see below for nested schema)
                                        The `custom_processor` processor transforms events using Vector Remap Language (VRL) scripts with advanced filtering capabilities.
                                      items:
                                        properties:
                                          remap:
                                            description: |-
                                              (Block List) Array of VRL remap configurations. Each remap defines a transformation rule with its own filter and VRL script. (see below for nested schema)
                                              Array of VRL remap configurations. Each remap defines a transformation rule with its own filter and VRL script.
                                            items:
                                              properties:
                                                dropOnError:
                                                  description: |-
                                                    (Boolean) Whether to drop events that cause errors during transformation.
                                                    Whether to drop events that cause errors during transformation.
                                                  type: boolean
                                                enabled:
                                                  description: |-
                                                    (Boolean) Whether this processor group is enabled.
                                                    Whether this remap rule is enabled.
                                                  type: boolean
                                                include:
                                                  description: |-
                                                    (String) A Datadog search query that determines which logs are forwarded using this route.
                                                    A Datadog search query used to filter events for this specific remap rule.
                                                  type: string
                                                name:
                                                  description: |-
                                                    (String) The pipeline name.
                                                    A descriptive name for this remap rule.
                                                  type: string
                                                source:
                                                  description: |-
                                                    (Block List) List of sources. (see below for nested schema)
                                                    The VRL script source code that defines the transformation logic.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    datadogTags:
                                      description: (Block List) (see below for nested
                                        schema)
                                      items:
                                        properties:
                                          action:
                                            description: |-
                                              (String) Valid values are include, exclude.
                                              Valid values are `include`, `exclude`.
                                            type: string
                                          keys:
                                            description: (List of String)
                                            items:
                                              type: string
                                            type: array
                                          mode:
                                            description: |-
                                              (String) The protocol used to send logs. Valid values are tcp, udp.
                                              Valid values are `filter`.
                                            type: string
                                        type: object
                                      type: array
                                    dedupe:
                                      description: |-
                                        (Block List) The dedupe processor removes duplicate fields in log events. (see below for nested schema)
                                        The `dedupe` processor removes duplicate fields in log events.
                                      items:
                                        properties:
                                          fields:
                                            description: |-
                                              (List of String) A list of log field paths to check for duplicates.
                                              A list of log field paths to check for duplicates.
                                            items:
                                              type: string
                                            type: array
                                          mode:
                                            description: |-
                                              (String) The protocol used to send logs. Valid values are tcp, udp.
                                              The deduplication mode to apply to the fields.
                                            type: string
                                        type: object
                                      type: array
                                    displayName:
                                      description: |-
                                        friendly name of the processor group.
                                        A human-friendly name for this processor.
                                      type: string
                                    enabled:
                                      description: |-
                                        (Boolean) Whether this processor group is enabled.
                                        Whether this processor is enabled.
                                      type: boolean
                                    enrichmentTable:
                                      description: |-
                                        (Block List) The enrichment_table processor enriches logs using a static CSV file or GeoIP database. (see below for nested schema)
                                        The `enrichment_table` processor enriches logs using a static CSV file or GeoIP database.
                                      items:
                                        properties:
                                          file:
                                            description: |-
                                              (Block List) Defines a static enrichment table loaded from a CSV file. (see below for nested schema)
                                              Defines a static enrichment table loaded from a CSV file.
                                            items:
                                              properties:
                                                encoding:
                                                  description: (Block List) Encoding
                                                    format for the destination. (see
                                                    below for nested schema)
                                                  items:
                                                    properties:
                                                      delimiter:
                                                        description: |-
                                                          (String) A single ASCII character used as a delimiter.
                                                          The `encoding` `delimiter`.
                                                        type: string
                                                      includesHeaders:
                                                        description: |-
                                                          (Boolean) The encoding includes_headers.
                                                          The `encoding` `includes_headers`.
                                                        type: boolean
                                                      type:
                                                        description: |-
                                                          (String) The encoding type. Valid values are json, parquet.
                                                          File encoding format.
                                                        type: string
                                                    type: object
                                                  type: array
                                                key:
                                                  description: |-
                                                    (Block List) Key fields used to look up enrichment values. (see below for nested schema)
                                                    Key fields used to look up enrichment values.
                                                  items:
                                                    properties:
                                                      column:
                                                        description: |-
                                                          (String) The items column.
                                                          The `items` `column`.
                                                        type: string
                                                      comparison:
                                                        description: |-
                                                          (String) The comparison method (e.g. equals).
                                                          The comparison method (e.g. equals).
                                                        type: string
                                                      field:
                                                        description: |-
                                                          (String) The target field in the log event.
                                                          Specifies the source of the key value for enrichment table lookups. Set exactly one of `string_path`, `event`, `vrl`, or `secret`.
                                                        items:
                                                          properties:
                                                            event:
                                                              description: |-
                                                                (String) The path to the field in the log event to use as the lookup key.
                                                                The path to the field in the log event to use as the lookup key.
                                                              type: string
                                                            secret:
                                                              description: |-
                                                                (String) The name of the secret containing the lookup key value.
                                                                The name of the secret containing the lookup key value.
                                                              type: string
                                                            stringPath:
                                                              description: |-
                                                                (String) A plain field path in the log event (for example, log.user.id).
                                                                A plain field path in the log event (for example, `log.user.id`).
                                                              type: string
                                                            vrl:
                                                              description: |-
                                                                (String) A VRL expression that returns the value to use as the lookup key.
                                                                A VRL expression that returns the value to use as the lookup key.
                                                              type: string
                                                          type: object
                                                        type: array
                                                    type: object
                                                  type: array
                                                path:
                                                  description: |-
                                                    (String) Path to the CSV file.
                                                    Path to the CSV file.
                                                  type: string
                                              type: object
                                            type: array
                                          geoip:
                                            description: |-
                                              (Block List) Uses a GeoIP database to enrich logs based on an IP field. (see below for nested schema)
                                              Uses a GeoIP database to enrich logs based on an IP field.
                                            items:
                                              properties:
                                                keyField:
                                                  description: |-
                                                    (String) The field name to use as the Kafka message key.
                                                    Path to the IP field in the log.
                                                  type: string
                                                locale:
                                                  description: |-
                                                    (String) Locale used to resolve geographical names.
                                                    Locale used to resolve geographical names.
                                                  type: string
                                                path:
                                                  description: |-
                                                    (String) Path to the CSV file.
                                                    Path to the GeoIP database file.
                                                  type: string
                                              type: object
                                            type: array
                                          referenceTable:
                                            description: |-
                                              (Block List) Uses a Datadog reference table to enrich logs. (see below for nested schema)
                                              Uses a Datadog reference table to enrich logs.
                                            items:
                                              properties:
                                                appKeyKey:
                                                  description: |-
                                                    (String) Name of the environment variable or secret that holds the Datadog application key for the reference table.
                                                    Name of the environment variable or secret that holds the Datadog application key for the reference table.
                                                  type: string
                                                columns:
                                                  description: |-
                                                    (List of String) List of column names to include from the reference table. If not provided, all columns are included.
                                                    List of column names to include from the reference table. If not provided, all columns are included.
                                                  items:
                                                    type: string
                                                  type: array
                                                keyField:
                                                  description: |-
                                                    (String) The field name to use as the Kafka message key.
                                                    Path to the field in the log event to match against the reference table.
                                                  type: string
                                                tableId:
                                                  description: |-
                                                    (String) The unique identifier of the reference table.
                                                    The unique identifier of the reference table.
                                                  type: string
                                              type: object
                                            type: array
                                          target:
                                            description: |-
                                              (String) Path where enrichment results should be stored in the log.
                                              Path where enrichment results should be stored in the log.
                                            type: string
                                        type: object
                                      type: array
                                    filter:
                                      description: |-
                                        (Block List) The filter processor allows conditional processing of logs based on a Datadog search query. Logs that match the include query are passed through; others are discarded. (see below for nested schema)
                                        The `filter` processor allows conditional processing of logs based on a Datadog search query. Logs that match the `include` query are passed through; others are discarded.
                                      items:
                                        type: object
                                      type: array
                                    generateDatadogMetrics:
                                      description: |-
                                        (Block List) The generate_datadog_metrics processor creates custom metrics from logs. Metrics can be counters, gauges, or distributions and optionally grouped by log fields. (see below for nested schema)
                                        The `generate_datadog_metrics` processor creates custom metrics from logs. Metrics can be counters, gauges, or distributions and optionally grouped by log fields.
                                      items:
                                        properties:
                                          metric:
                                            description: |-
                                              (Block List) Configuration for generating individual metrics. (see below for nested schema)
                                              Configuration for generating individual metrics.
                                            items:
                                              properties:
                                                groupBy:
                                                  description: |-
                                                    (List of String) Optional fields used to group the metric series.
                                                    Optional fields used to group the metric series.
                                                  items:
                                                    type: string
                                                  type: array
                                                include:
                                                  description: |-
                                                    (String) A Datadog search query that determines which logs are forwarded using this route.
                                                    Datadog filter query to match logs for metric generation.
                                                  type: string
                                                metricType:
                                                  description: |-
                                                    (String) Type of metric to create.
                                                    Type of metric to create.
                                                  type: string
                                                name:
                                                  description: |-
                                                    (String) The pipeline name.
                                                    Name of the custom metric to be created.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The metadata value.
                                                    Specifies how the value of the generated metric is computed.
                                                  items:
                                                    properties:
                                                      field:
                                                        description: |-
                                                          (String) The target field in the log event.
                                                          Name of the log field containing the numeric value to increment the metric by (used only for `increment_by_field`).
                                                        type: string
                                                      strategy:
                                                        description: |-
                                                          (String) The authentication strategy to use (e.g. aws or basic).
                                                          Metric value strategy: `increment_by_one` or `increment_by_field`.
                                                        type: string
                                                    type: object
                                                  type: array
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    generateMetrics:
                                      description: |-
                                        id>.metrics. (see below for nested schema)
                                        The `generate_metrics` processor creates custom metrics from logs. The generated metrics must be routed to a metrics destination using the input `<processor-id>.metrics`.
                                      items:
                                        properties:
                                          metric:
                                            description: |-
                                              (Block List) Configuration for generating individual metrics. (see below for nested schema)
                                              Configuration for generating individual metrics.
                                            items:
                                              properties:
                                                groupBy:
                                                  description: |-
                                                    (List of String) Optional fields used to group the metric series.
                                                    Optional fields used to group the metric series.
                                                  items:
                                                    type: string
                                                  type: array
                                                include:
                                                  description: |-
                                                    (String) A Datadog search query that determines which logs are forwarded using this route.
                                                    Datadog filter query to match logs for metric generation.
                                                  type: string
                                                metricType:
                                                  description: |-
                                                    (String) Type of metric to create.
                                                    Type of metric to create.
                                                  type: string
                                                name:
                                                  description: |-
                                                    (String) The pipeline name.
                                                    Name of the custom metric to be created.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The metadata value.
                                                    Specifies how the value of the generated metric is computed.
                                                  items:
                                                    properties:
                                                      field:
                                                        description: |-
                                                          (String) The target field in the log event.
                                                          Name of the log field containing the numeric value to increment the metric by (used only for `increment_by_field`).
                                                        type: string
                                                      strategy:
                                                        description: |-
                                                          (String) The authentication strategy to use (e.g. aws or basic).
                                                          Metric value strategy: `increment_by_one` or `increment_by_field`.
                                                        type: string
                                                    type: object
                                                  type: array
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    id:
                                      description: |-
                                        (String) The ID of this resource.
                                        The unique identifier for this processor.
                                      type: string
                                    include:
                                      description: |-
                                        (String) A Datadog search query that determines which logs are forwarded using this route.
                                        A Datadog search query used to determine which logs this processor targets.
                                      type: string
                                    metricTags:
                                      description: |-
                                        (Block List) The metric_tags processor filters metrics based on their tags using Datadog tag key patterns. (see below for nested schema)
                                        The `metric_tags` processor filters metrics based on their tags using Datadog tag key patterns.
                                      items:
                                        properties:
                                          rule:
                                            description: |-
                                              (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                              A list of rules for filtering metric tags.
                                            items:
                                              properties:
                                                action:
                                                  description: |-
                                                    (String) Valid values are include, exclude.
                                                    The action to take on tags with matching keys. Valid values are `include`, `exclude`.
                                                  type: string
                                                include:
                                                  description: |-
                                                    (String) A Datadog search query that determines which logs are forwarded using this route.
                                                    A Datadog search query used to determine which metrics this rule targets.
                                                  type: string
                                                keys:
                                                  description: |-
                                                    (List of String)
                                                    A list of tag keys to include or exclude.
                                                  items:
                                                    type: string
                                                  type: array
                                                mode:
                                                  description: |-
                                                    (String) The protocol used to send logs. Valid values are tcp, udp.
                                                    The processing mode for tag filtering. Valid values are `filter`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    ocsfMapper:
                                      description: |-
                                        (Block List) The ocsf_mapper processor transforms logs into the OCSF schema using predefined library mappings or custom mapping configuration. (see below for nested schema)
                                        The `ocsf_mapper` processor transforms logs into the OCSF schema using predefined library mappings or custom mapping configuration.
                                      items:
                                        properties:
                                          keepUnmatched:
                                            description: |-
                                              (Boolean) Whether to keep an event that does not match any of the mapping filters.
                                              Whether to keep an event that does not match any of the mapping filters.
                                            type: boolean
                                          mapping:
                                            description: |-
                                              (Block List) List of OCSF mapping entries. Each entry uses either a library mapping or a custom mapping. (see below for nested schema)
                                              List of OCSF mapping entries. Each entry uses either a library mapping or a custom mapping.
                                            items:
                                              properties:
                                                customMapping:
                                                  description: |-
                                                    (Block List) Custom OCSF mapping configuration for transforming logs. (see below for nested schema)
                                                    Custom OCSF mapping configuration for transforming logs.
                                                  items:
                                                    properties:
                                                      mapping:
                                                        description: |-
                                                          (Block List) List of OCSF mapping entries. Each entry uses either a library mapping or a custom mapping. (see below for nested schema)
                                                          A list of field mapping rules for transforming log fields to OCSF schema fields.
                                                        items:
                                                          properties:
                                                            default:
                                                              description: |-
                                                                (String) The default value to use if the source field is missing or empty.
                                                                The default value to use if the source field is missing or empty.
                                                              type: string
                                                            dest:
                                                              description: |-
                                                                (String) The destination OCSF field path.
                                                                The destination OCSF field path.
                                                              type: string
                                                            lookup:
                                                              description: |-
                                                                (Block List) Lookup table configuration for mapping source values to destination values. (see below for nested schema)
                                                                Lookup table configuration for mapping source values to destination values.
                                                              items:
                                                                properties:
                                                                  default:
                                                                    description: |-
                                                                      (String) The default value to use if the source field is missing or empty.
                                                                      The default value to use if no lookup match is found.
                                                                    type: string
                                                                  table:
                                                                    description: |-
                                                                      (String) Target ClickHouse table name.
                                                                      A list of lookup table entries for value transformation.
                                                                    items:
                                                                      properties:
                                                                        contains:
                                                                          description: |-
                                                                            (String) The substring to match in the source value.
                                                                            The substring to match in the source value.
                                                                          type: string
                                                                        equals:
                                                                          description: |-
                                                                            (String) The exact value to match in the source.
                                                                            The exact value to match in the source.
                                                                          type: string
                                                                        equalsSource:
                                                                          description: |-
                                                                            (String) The source field to match against.
                                                                            The source field to match against.
                                                                          type: string
                                                                        matches:
                                                                          description: |-
                                                                            (String) A regex pattern to match in the source value.
                                                                            A regex pattern to match in the source value.
                                                                          type: string
                                                                        notMatches:
                                                                          description: |-
                                                                            (String) A regex pattern that must not match the source value.
                                                                            A regex pattern that must not match the source value.
                                                                          type: string
                                                                        value:
                                                                          description: |-
                                                                            (String) The metadata value.
                                                                            The value to use when a match is found.
                                                                          type: string
                                                                      type: object
                                                                    type: array
                                                                type: object
                                                              type: array
                                                            source:
                                                              description: |-
                                                                (Block List) List of sources. (see below for nested schema)
                                                                The source field path from the log event.
                                                              type: string
                                                            sources:
                                                              description: |-
                                                                (List of String) Multiple source field paths for combined mapping.
                                                                Multiple source field paths for combined mapping.
                                                              items:
                                                                type: string
                                                              type: array
                                                            value:
                                                              description: |-
                                                                (String) The metadata value.
                                                                A static value to use for the destination field.
                                                              type: string
                                                          type: object
                                                        type: array
                                                      metadata:
                                                        description: |-
                                                          value pairs added to each object. (see below for nested schema)
                                                          Metadata for the custom OCSF mapping.
                                                        items:
                                                          properties:
                                                            class:
                                                              description: |-
                                                                (String) The OCSF event class name.
                                                                The OCSF event class name.
                                                              type: string
                                                            profiles:
                                                              description: |-
                                                                (List of String) A list of OCSF profiles to apply.
                                                                A list of OCSF profiles to apply.
                                                              items:
                                                                type: string
                                                              type: array
                                                            version:
                                                              description: |-
                                                                (Number) The version of the custom mapping configuration.
                                                                The OCSF schema version.
                                                              type: string
                                                          type: object
                                                        type: array
                                                      version:
                                                        description: |-
                                                          (Number) The version of the custom mapping configuration.
                                                          The version of the custom mapping configuration.
                                                        type: number
                                                    type: object
                                                  type: array
                                                include:
                                                  description: |-
                                                    (String) A Datadog search query that determines which logs are forwarded using this route.
                                                    Search query for selecting which logs the mapping applies to.
                                                  type: string
                                                libraryMapping:
                                                  description: |-
                                                    (String) Predefined library mapping for log transformation. Use this or custom_mapping, not both.
                                                    Predefined library mapping for log transformation. Use this or custom_mapping, not both.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    parseGrok:
                                      description: |-
                                        (Block List) The parse_grok processor extracts structured fields from unstructured log messages using Grok patterns. (see below for nested schema)
                                        The `parse_grok` processor extracts structured fields from unstructured log messages using Grok patterns.
                                      items:
                                        properties:
                                          disableLibraryRules:
                                            description: |-
                                              (Boolean) If set to true, disables the default Grok rules provided by Datadog. Defaults to false.
                                              If set to `true`, disables the default Grok rules provided by Datadog. Defaults to `false`.
                                            type: boolean
                                          field:
                                            description: |-
                                              (String) The target field in the log event.
                                              The log field to parse with the Grok rules. Defaults to `"message"`.
                                            type: string
                                          includeRule:
                                            description: |-
                                              (Block List) A Grok parsing rule that targets logs matching a Datadog search query. (see below for nested schema)
                                              A Grok parsing rule that targets logs matching a Datadog search query.
                                            items:
                                              properties:
                                                include:
                                                  description: |-
                                                    (String) A Datadog search query that determines which logs are forwarded using this route.
                                                    A Datadog search query used to determine which logs this Grok rule targets.
                                                  type: string
                                                matchRule:
                                                  description: |-
                                                    (Block List) A list of Grok parsing rules that define how to extract fields. Each rule must contain a name and a valid Grok pattern. (see below for nested schema)
                                                    A list of Grok parsing rules that define how to extract fields. Each rule must contain a name and a valid Grok pattern.
                                                  items:
                                                    properties:
                                                      name:
                                                        description: |-
                                                          (String) The pipeline name.
                                                          The name of the rule.
                                                        type: string
                                                      rule:
                                                        description: |-
                                                          (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                                          The definition of the Grok rule.
                                                        type: string
                                                    type: object
                                                  type: array
                                                supportRule:
                                                  description: |-
                                                    (Block List) A list of helper Grok rules that can be referenced by the parsing rules. (see below for nested schema)
                                                    A list of helper Grok rules that can be referenced by the parsing rules.
                                                  items:
                                                    properties:
                                                      name:
                                                        description: |-
                                                          (String) The pipeline name.
                                                          The name of the helper Grok rule.
                                                        type: string
                                                      rule:
                                                        description: |-
                                                          (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                                          The definition of the helper Grok rule.
                                                        type: string
                                                    type: object
                                                  type: array
                                              type: object
                                            type: array
                                          rule:
                                            description: |-
                                              (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                              The list of Grok parsing rules. If multiple parsing rules are provided, they are evaluated in order. The first successful match is applied.
                                            items:
                                              properties:
                                                matchRule:
                                                  description: |-
                                                    (Block List) A list of Grok parsing rules that define how to extract fields. Each rule must contain a name and a valid Grok pattern. (see below for nested schema)
                                                    A list of Grok parsing rules that define how to extract fields. Each rule must contain a name and a valid Grok pattern.
                                                  items:
                                                    properties:
                                                      name:
                                                        description: |-
                                                          (String) The pipeline name.
                                                          The name of the rule.
                                                        type: string
                                                      rule:
                                                        description: |-
                                                          (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                                          The definition of the Grok rule.
                                                        type: string
                                                    type: object
                                                  type: array
                                                source:
                                                  description: |-
                                                    (Block List) List of sources. (see below for nested schema)
                                                    The value of the source field in log events which should be processed by the Grok rules.
                                                  type: string
                                                supportRule:
                                                  description: |-
                                                    (Block List) A list of helper Grok rules that can be referenced by the parsing rules. (see below for nested schema)
                                                    A list of helper Grok rules that can be referenced by the parsing rules.
                                                  items:
                                                    properties:
                                                      name:
                                                        description: |-
                                                          (String) The pipeline name.
                                                          The name of the helper Grok rule.
                                                        type: string
                                                      rule:
                                                        description: |-
                                                          (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                                          The definition of the helper Grok rule.
                                                        type: string
                                                    type: object
                                                  type: array
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    parseJson:
                                      description: |-
                                        (Block List) The parse_json processor extracts JSON from a specified field and flattens it into the event. This is useful when logs contain embedded JSON as a string. (see below for nested schema)
                                        The `parse_json` processor extracts JSON from a specified field and flattens it into the event. This is useful when logs contain embedded JSON as a string.
                                      items:
                                        properties:
                                          field:
                                            description: |-
                                              (String) The target field in the log event.
                                              The field to parse.
                                            type: string
                                        type: object
                                      type: array
                                    parseXml:
                                      description: |-
                                        (Block List) The parse_xml processor parses XML from a specified field and extracts it into the event. (see below for nested schema)
                                        The `parse_xml` processor parses XML from a specified field and extracts it into the event.
                                      items:
                                        properties:
                                          alwaysUseTextKey:
                                            description: |-
                                              (Boolean) Whether to always store text inside an object using the text key even when no attributes exist.
                                              Whether to always store text inside an object using the text key even when no attributes exist.
                                            type: boolean
                                          attrPrefix:
                                            description: |-
                                              (String) The prefix to use for XML attributes in the parsed output. If the field is left empty, the original attribute key is used.
                                              The prefix to use for XML attributes in the parsed output. If the field is left empty, the original attribute key is used.
                                            type: string
                                          field:
                                            description: |-
                                              (String) The target field in the log event.
                                              The path to the log field on which you want to parse XML.
                                            type: string
                                          includeAttr:
                                            description: |-
                                              (Boolean) Whether to include XML attributes in the parsed output.
                                              Whether to include XML attributes in the parsed output.
                                            type: boolean
                                          parseBool:
                                            description: |-
                                              (Boolean) Whether to parse boolean values from strings.
                                              Whether to parse boolean values from strings.
                                            type: boolean
                                          parseNull:
                                            description: |-
                                              (Boolean) Whether to parse null values.
                                              Whether to parse null values.
                                            type: boolean
                                          parseNumber:
                                            description: |-
                                              (Boolean) Whether to parse numeric values from strings.
                                              Whether to parse numeric values from strings.
                                            type: boolean
                                          textKey:
                                            description: |-
                                              (String) The key name to use for the text node when XML attributes are appended.
                                              The key name to use for the text node when XML attributes are appended.
                                            type: string
                                        type: object
                                      type: array
                                    quota:
                                      description: |-
                                        (Block List) The quota processor measures logging traffic for logs that match a specified filter. When the configured daily quota is met, the processor can drop or alert. (see below for nested schema)
                                        The `quota` processor measures logging traffic for logs that match a specified filter. When the configured daily quota is met, the processor can drop or alert.
                                      items:
                                        properties:
                                          dropEvents:
                                            description: |-
                                              (Boolean) Whether to drop events exceeding the limit.
                                              Whether to drop events exceeding the limit.
                                            type: boolean
                                          ignoreWhenMissingPartitions:
                                            description: |-
                                              (Boolean) Whether to ignore when partition fields are missing.
                                              Whether to ignore when partition fields are missing.
                                            type: boolean
                                          limit:
                                            description: (Block List) (see below for
                                              nested schema)
                                            items:
                                              properties:
                                                enforce:
                                                  description: |-
                                                    (String) Whether to enforce by 'bytes' or 'events'. Valid values are bytes, events.
                                                    Whether to enforce by 'bytes' or 'events'. Valid values are `bytes`, `events`.
                                                  type: string
                                                limit:
                                                  description: |-
                                                    (Block List) (see below for nested schema)
                                                    The daily quota limit.
                                                  type: number
                                              type: object
                                            type: array
                                          name:
                                            description: |-
                                              (String) The pipeline name.
                                              The name of the quota.
                                            type: string
                                          overflowAction:
                                            description: |-
                                              (String) The action to take when the quota is exceeded: drop, no_action, or overflow_routing.
                                              The action to take when the quota is exceeded: `drop`, `no_action`, or `overflow_routing`.
                                            type: string
                                          override:
                                            description: |-
                                              specific quotas. (see below for nested schema)
                                              The overrides for field-specific quotas.
                                            items:
                                              properties:
                                                field:
                                                  description: |-
                                                    (String) The target field in the log event.
                                                    Fields that trigger this override.
                                                  items:
                                                    properties:
                                                      name:
                                                        description: |-
                                                          (String) The pipeline name.
                                                          The field name.
                                                        type: string
                                                      value:
                                                        description: |-
                                                          (String) The metadata value.
                                                          The field value.
                                                        type: string
                                                    type: object
                                                  type: array
                                                limit:
                                                  description: (Block List) (see below
                                                    for nested schema)
                                                  items:
                                                    properties:
                                                      enforce:
                                                        description: |-
                                                          (String) Whether to enforce by 'bytes' or 'events'. Valid values are bytes, events.
                                                          Whether to enforce by 'bytes' or 'events'. Valid values are `bytes`, `events`.
                                                        type: string
                                                      limit:
                                                        description: |-
                                                          (Block List) (see below for nested schema)
                                                          The daily quota limit.
                                                        type: number
                                                    type: object
                                                  type: array
                                              type: object
                                            type: array
                                          partitionFields:
                                            description: |-
                                              (List of String) List of partition fields.
                                              List of partition fields.
                                            items:
                                              type: string
                                            type: array
                                          tooManyBucketsAction:
                                            description: |-
                                              (String) The action to take when the max number of buckets is exceeded: drop, no_action, or overflow_routing.
                                              The action to take when the max number of buckets is exceeded: `drop`, `no_action`, or `overflow_routing`.
                                            type: string
                                        type: object
                                      type: array
                                    reduce:
                                      description: |-
                                        (Block List) The reduce processor aggregates and merges logs based on matching keys and merge strategies. (see below for nested schema)
                                        The `reduce` processor aggregates and merges logs based on matching keys and merge strategies.
                                      items:
                                        properties:
                                          groupBy:
                                            description: |-
                                              (List of String) Optional fields used to group the metric series.
                                              A list of fields used to group log events for merging.
                                            items:
                                              type: string
                                            type: array
                                          mergeStrategy:
                                            description: |-
                                              (Block List) List of merge strategies defining how values from grouped events should be combined. (see below for nested schema)
                                              List of merge strategies defining how values from grouped events should be combined.
                                            items:
                                              properties:
                                                path:
                                                  description: |-
                                                    (String) Path to the CSV file.
                                                    The field path in the log event.
                                                  type: string
                                                strategy:
                                                  description: |-
                                                    (String) The authentication strategy to use (e.g. aws or basic).
                                                    The merge strategy to apply.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    removeFields:
                                      description: |-
                                        (Block List) The remove_fields processor deletes specified fields from logs. (see below for nested schema)
                                        The `remove_fields` processor deletes specified fields from logs.
                                      items:
                                        properties:
                                          fields:
                                            description: |-
                                              (List of String) A list of log field paths to check for duplicates.
                                              List of fields to remove from the events.
                                            items:
                                              type: string
                                            type: array
                                        type: object
                                      type: array
                                    renameFields:
                                      description: |-
                                        (Block List) The rename_fields processor changes field names. (see below for nested schema)
                                        The `rename_fields` processor changes field names.
                                      items:
                                        properties:
                                          field:
                                            description: |-
                                              (String) The target field in the log event.
                                              List of fields to rename.
                                            items:
                                              properties:
                                                destination:
                                                  description: |-
                                                    (Block List) List of destinations. (see below for nested schema)
                                                    Destination field name.
                                                  type: string
                                                preserveSource:
                                                  description: |-
                                                    (Boolean) Whether to keep the original field.
                                                    Whether to keep the original field.
                                                  type: boolean
                                                source:
                                                  description: |-
                                                    (Block List) List of sources. (see below for nested schema)
                                                    Source field to rename.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    renameMetricTags:
                                      description: |-
                                        (Block List) The rename_metric_tags processor changes the keys of tags on metrics. (see below for nested schema)
                                        The `rename_metric_tags` processor changes the keys of tags on metrics.
                                      items:
                                        properties:
                                          tag:
                                            description: |-
                                              (Block List) A list of static tags to add to each metric. Up to 15 tags may be defined. (see below for nested schema)
                                              A list of rename rules. Up to 15 tags may be defined.
                                            items:
                                              properties:
                                                renameTo:
                                                  description: |-
                                                    (String) The new tag key to assign in place of the original.
                                                    The new tag key to assign in place of the original.
                                                  type: string
                                                tag:
                                                  description: |-
                                                    (Block List) A list of static tags to add to each metric. Up to 15 tags may be defined. (see below for nested schema)
                                                    The original tag key on the metric event.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    sample:
                                      description: |-
                                        (Block List) The sample processor allows probabilistic sampling of logs at a fixed rate. (see below for nested schema)
                                        The `sample` processor allows probabilistic sampling of logs at a fixed rate.
                                      items:
                                        properties:
                                          groupBy:
                                            description: |-
                                              (List of String) Optional fields used to group the metric series.
                                              Optional list of fields to group events by. Each group is sampled independently.
                                            items:
                                              type: string
                                            type: array
                                          percentage:
                                            description: |-
                                              (Number) The percentage of logs to sample.
                                              The percentage of logs to sample.
                                            type: number
                                        type: object
                                      type: array
                                    sensitiveDataScanner:
                                      description: |-
                                        (Block List) The sensitive_data_scanner processor detects and optionally redacts sensitive data in log events. (see below for nested schema)
                                        The `sensitive_data_scanner` processor detects and optionally redacts sensitive data in log events.
                                      items:
                                        properties:
                                          rule:
                                            description: |-
                                              (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                              A list of rules for identifying and acting on sensitive data patterns.
                                            items:
                                              properties:
                                                keywordOptions:
                                                  description: |-
                                                    based proximity matching for sensitive data. (see below for nested schema)
                                                    Keyword-based proximity matching for sensitive data.
                                                  items:
                                                    properties:
                                                      keywords:
                                                        description: |-
                                                          (List of String) A list of keywords to match near the sensitive pattern.
                                                          A list of keywords to match near the sensitive pattern.
                                                        items:
                                                          type: string
                                                        type: array
                                                      proximity:
                                                        description: |-
                                                          (Number) Maximum number of tokens between a keyword and a sensitive value match.
                                                          Maximum number of tokens between a keyword and a sensitive value match.
                                                        type: number
                                                    type: object
                                                  type: array
                                                name:
                                                  description: |-
                                                    (String) The pipeline name.
                                                    A name identifying the rule.
                                                  type: string
                                                onMatch:
                                                  description: |-
                                                    (Block List) The action to take when a sensitive value is found. (see below for nested schema)
                                                    The action to take when a sensitive value is found.
                                                  items:
                                                    properties:
                                                      hash:
                                                        description: |-
                                                          (Block List) Hashes the matched value. (see below for nested schema)
                                                          Hashes the matched value.
                                                        items:
                                                          type: object
                                                        type: array
                                                      partialRedact:
                                                        description: |-
                                                          (Block List) Redacts part of the matched value (e.g., keep last 4 characters). (see below for nested schema)
                                                          Redacts part of the matched value (e.g., keep last 4 characters).
                                                        items:
                                                          properties:
                                                            characters:
                                                              description: |-
                                                                (Number) Number of characters to keep.
                                                                Number of characters to keep.
                                                              type: number
                                                            direction:
                                                              description: |-
                                                                (String) Direction from which to keep characters: first or last.
                                                                Direction from which to keep characters: `first` or `last`.
                                                              type: string
                                                          type: object
                                                        type: array
                                                      redact:
                                                        description: |-
                                                          (Block List) Redacts the matched value. Exactly one of redact, hash, or partial_redact must be specified. (see below for nested schema)
                                                          Redacts the matched value. Exactly one of `redact`, `hash`, or `partial_redact` must be specified.
                                                        items:
                                                          properties:
                                                            replace:
                                                              description: |-
                                                                (String) Replacement string for redacted values (e.g., ***).
                                                                Replacement string for redacted values (e.g., `***`).
                                                              type: string
                                                          type: object
                                                        type: array
                                                    type: object
                                                  type: array
                                                pattern:
                                                  description: |-
                                                    (Block List) Pattern detection configuration for identifying sensitive data using either a custom regex or a library reference. (see below for nested schema)
                                                    Pattern detection configuration for identifying sensitive data using either a custom regex or a library reference.
                                                  items:
                                                    properties:
                                                      custom:
                                                        description: |-
                                                          (Block List) Pattern detection using a custom regular expression. Exactly one of custom or library must be specified. (see below for nested schema)
                                                          Pattern detection using a custom regular expression. Exactly one of `custom` or `library` must be specified.
                                                        items:
                                                          properties:
                                                            description:
                                                              description: |-
                                                                readable description providing context about a sensitive data scanner rule.
                                                                Human-readable description providing context about a sensitive data scanner rule.
                                                              type: string
                                                            rule:
                                                              description: |-
                                                                (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                                                A regular expression used to detect sensitive values. Must be a valid regex.
                                                              type: string
                                                          type: object
                                                        type: array
                                                      library:
                                                        description: |-
                                                          (Block List) Pattern detection using a predefined pattern from the Sensitive Data Scanner library. (see below for nested schema)
                                                          Pattern detection using a predefined pattern from the Sensitive Data Scanner library.datadoghq.
                                                        items:
                                                          properties:
                                                            description:
                                                              description: |-
                                                                readable description providing context about a sensitive data scanner rule.
                                                                Human-readable description providing context about a sensitive data scanner rule.
                                                              type: string
                                                            id:
                                                              description: |-
                                                                (String) The ID of this resource.
                                                                Identifier for a predefined pattern from the sensitive data scanner pattern library.
                                                              type: string
                                                            useRecommendedKeywords:
                                                              description: |-
                                                                (Boolean) Whether to augment the pattern with recommended keywords .
                                                                Whether to augment the pattern with recommended keywords (optional).
                                                              type: boolean
                                                          type: object
                                                        type: array
                                                    type: object
                                                  type: array
                                                scope:
                                                  description: |-
                                                    level targeting options that determine where the scanner should operate. (see below for nested schema)
                                                    Field-level targeting options that determine where the scanner should operate.
                                                  items:
                                                    properties:
                                                      all:
                                                        description: |-
                                                          (Boolean) Scan all fields.
                                                          Scan all fields.
                                                        type: boolean
                                                      exclude:
                                                        description: |-
                                                          (Block List) Explicitly exclude these fields from scanning. (see below for nested schema)
                                                          Explicitly exclude these fields from scanning.
                                                        items:
                                                          properties:
                                                            fields:
                                                              description: |-
                                                                (List of String) A list of log field paths to check for duplicates.
                                                                The fields to exclude from scanning.
                                                              items:
                                                                type: string
                                                              type: array
                                                          type: object
                                                        type: array
                                                      include:
                                                        description: |-
                                                          (String) A Datadog search query that determines which logs are forwarded using this route.
                                                          Explicitly include these fields for scanning. Exactly one of `include`, `exclude`, or `all` must be specified.
                                                        items:
                                                          properties:
                                                            fields:
                                                              description: |-
                                                                (List of String) A list of log field paths to check for duplicates.
                                                                The fields to include in scanning.
                                                              items:
                                                                type: string
                                                              type: array
                                                          type: object
                                                        type: array
                                                    type: object
                                                  type: array
                                                tags:
                                                  description: |-
                                                    (List of String) Tags assigned to this rule for filtering and classification.
                                                    Tags assigned to this rule for filtering and classification.
                                                  items:
                                                    type: string
                                                  type: array
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    splitArray:
                                      description: |-
                                        (Block List) The split_array processor splits array fields into separate events based on configured rules. (see below for nested schema)
                                        The `split_array` processor splits array fields into separate events based on configured rules.
                                      items:
                                        properties:
                                          array:
                                            description: |-
                                              (Block List) A list of array split configurations. (see below for nested schema)
                                              A list of array split configurations.
                                            items:
                                              properties:
                                                field:
                                                  description: |-
                                                    (String) The target field in the log event.
                                                    The path to the array field to split.
                                                  type: string
                                                include:
                                                  description: |-
                                                    (String) A Datadog search query that determines which logs are forwarded using this route.
                                                    A Datadog search query used to determine which logs this array split operation targets.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    tagCardinalityLimit:
                                      description: |-
                                        (Block List) The tag_cardinality_limit processor caps the number of distinct tag value combinations on metrics, dropping tags or events once the limit is exceeded. (see below for nested schema)
                                        The `tag_cardinality_limit` processor caps the number of distinct tag value combinations on metrics, dropping tags or events once the limit is exceeded.
                                      items:
                                        properties:
                                          limitExceededAction:
                                            description: |-
                                              (String) The default action to take when the cardinality limit is exceeded. One of drop_tag, drop_event. Valid values are drop_tag, drop_event.
                                              The default action to take when the cardinality limit is exceeded. One of `drop_tag`, `drop_event`. Valid values are `drop_tag`, `drop_event`.
                                            type: string
                                          perMetricLimit:
                                            description: |-
                                              metric cardinality overrides that take precedence over the default value_limit. (see below for nested schema)
                                              Per-metric cardinality overrides that take precedence over the default `value_limit`.
                                            items:
                                              properties:
                                                limitExceededAction:
                                                  description: |-
                                                    (String) The default action to take when the cardinality limit is exceeded. One of drop_tag, drop_event. Valid values are drop_tag, drop_event.
                                                    The action to take on this metric when the limit is exceeded. Required when `override_type` is `limit_override`; must be omitted when `override_type` is `excluded`. Valid values are `drop_tag`, `drop_event`.
                                                  type: string
                                                metricName:
                                                  description: |-
                                                    (String) The metric name this override applies to.
                                                    The metric name this override applies to.
                                                  type: string
                                                overrideType:
                                                  description: |-
                                                    metric override is applied. One of limit_override, excluded. Valid values are limit_override, excluded.
                                                    How the per-metric override is applied. One of `limit_override`, `excluded`. Valid values are `limit_override`, `excluded`.
                                                  type: string
                                                perTagLimit:
                                                  description: |-
                                                    tag cardinality overrides that apply within this metric. Must be omitted when override_type is excluded. (see below for nested schema)
                                                    Per-tag cardinality overrides that apply within this metric. Must be omitted when `override_type` is `excluded`.
                                                  items:
                                                    properties:
                                                      overrideType:
                                                        description: |-
                                                          metric override is applied. One of limit_override, excluded. Valid values are limit_override, excluded.
                                                          How the per-tag override is applied. One of `limit_override`, `excluded`. Valid values are `limit_override`, `excluded`.
                                                        type: string
                                                      tagKey:
                                                        description: |-
                                                          (String) The tag key this override applies to.
                                                          The tag key this override applies to.
                                                        type: string
                                                      valueLimit:
                                                        description: |-
                                                          (Number) The default maximum number of distinct tag value combinations allowed per metric. Between 0 and 1000000. Value must be between 0 and 1000000.
                                                          The cardinality cap for this tag. Required when `override_type` is `limit_override`; must be omitted when `override_type` is `excluded`. Value must be between 0 and 1000000.
                                                        type: number
                                                    type: object
                                                  type: array
                                                valueLimit:
                                                  description: |-
                                                    (Number) The default maximum number of distinct tag value combinations allowed per metric. Between 0 and 1000000. Value must be between 0 and 1000000.
                                                    The cardinality cap for this metric. Required when `override_type` is `limit_override`; must be omitted when `override_type` is `excluded`. Value must be between 0 and 1000000.
                                                  type: number
                                              type: object
                                            type: array
                                          trackingMode:
                                            description: |-
                                              (Block List) Controls whether the processor uses exact or probabilistic tag tracking. (see below for nested schema)
                                              Controls whether the processor uses exact or probabilistic tag tracking.
                                            items:
                                              properties:
                                                mode:
                                                  description: |-
                                                    (String) The protocol used to send logs. Valid values are tcp, udp.
                                                    The cardinality tracking algorithm to use. One of `exact_fingerprint`, `probabilistic`. Valid values are `exact_fingerprint`, `probabilistic`.
                                                  type: string
                                              type: object
                                            type: array
                                          valueLimit:
                                            description: |-
                                              (Number) The default maximum number of distinct tag value combinations allowed per metric. Between 0 and 1000000. Value must be between 0 and 1000000.
                                              The default maximum number of distinct tag value combinations allowed per metric. Between 0 and 1000000. Value must be between 0 and 1000000.
                                            type: number
                                        type: object
                                      type: array
                                    throttle:
                                      description: |-
                                        (Block List) The throttle processor limits the number of events that pass through over a given time window. (see below for nested schema)
                                        The `throttle` processor limits the number of events that pass through over a given time window.
                                      items:
                                        properties:
                                          groupBy:
                                            description: |-
                                              (List of String) Optional fields used to group the metric series.
                                              Optional list of fields used to group events before applying throttling.
                                            items:
                                              type: string
                                            type: array
                                          threshold:
                                            description: |-
                                              (Number) The number of events to allow before throttling is applied.
                                              The number of events to allow before throttling is applied.
                                            type: number
                                          window:
                                            description: |-
                                              (Number) The time window in seconds over which the threshold applies.
                                              The time window in seconds over which the threshold applies.
                                            type: number
                                        type: object
                                      type: array
                                  type: object
                                type: array
                            type: object
                          type: array
                        source:
                          description: |-
                            (Block List) List of sources. (see below for nested schema)
                            List of sources.
                          items:
                            properties:
                              amazonDataFirehose:
                                description: |-
                                  (Block List) The amazon_data_firehose source ingests logs from AWS Data Firehose. (see below for nested schema)
                                  The `amazon_data_firehose` source ingests logs from AWS Data Firehose.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        AWS authentication credentials used for accessing AWS services. If omitted, the system's default credentials are used (for example, the IAM role and environment variables).
                                      items:
                                        properties:
                                          assumeRole:
                                            description: |-
                                              (String) ARN of the role to assume.
                                              The Amazon Resource Name (ARN) of the role to assume.
                                            type: string
                                          externalId:
                                            description: |-
                                              (String) External ID for assumed role.
                                              A unique identifier for cross-account role assumption.
                                            type: string
                                          sessionName:
                                            description: |-
                                              (String) Session name for assumed role.
                                              A session identifier used for logging and tracing the assumed role session.
                                            type: string
                                        type: object
                                      type: array
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              amazonS3:
                                description: |-
                                  rehydratable format to an Amazon S3 bucket for archiving. (see below for nested schema)
                                  The `amazon_s3` source ingests logs from an Amazon S3 bucket. It supports AWS authentication, TLS encryption, and configurable compression.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        AWS authentication credentials used for accessing AWS services. If omitted, the system's default credentials are used (for example, the IAM role and environment variables).
                                      items:
                                        properties:
                                          assumeRole:
                                            description: |-
                                              (String) ARN of the role to assume.
                                              The Amazon Resource Name (ARN) of the role to assume.
                                            type: string
                                          externalId:
                                            description: |-
                                              (String) External ID for assumed role.
                                              A unique identifier for cross-account role assumption.
                                            type: string
                                          sessionName:
                                            description: |-
                                              (String) Session name for assumed role.
                                              A session identifier used for logging and tracing the assumed role session.
                                            type: string
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression format for objects retrieved from the S3 bucket. Use `auto` to detect compression from the object's Content-Encoding header or file extension. Valid values are `auto`, `none`, `gzip`, `zstd`.
                                      type: string
                                    region:
                                      description: |-
                                        (String) AWS region of the S3 bucket.
                                        AWS region where the S3 bucket resides.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                    urlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the S3 bucket URL.
                                        Name of the environment variable or secret that holds the S3 bucket URL.
                                      type: string
                                  type: object
                                type: array
                              datadogAgent:
                                description: |-
                                  (Block List) The datadog_agent source collects logs from the Datadog Agent. (see below for nested schema)
                                  The `datadog_agent` source collects logs from the Datadog Agent.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address for the Datadog Agent source.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              fluentBit:
                                description: |-
                                  (Block List) The fluent_bit source ingests logs from Fluent Bit. (see below for nested schema)
                                  The `fluent_bit` source ingests logs from Fluent Bit.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              fluentd:
                                description: |-
                                  compatible service. (see below for nested schema)
                                  The `fluentd` source ingests logs from a Fluentd-compatible service.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              googlePubsub:
                                description: |-
                                  (Block List) The google_pubsub destination publishes logs to a Google Cloud Pub/Sub topic. (see below for nested schema)
                                  The `google_pubsub` source ingests logs from a Google Cloud Pub/Sub subscription.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        Google Cloud credentials used to authenticate with Google Cloud services.
                                      items:
                                        properties:
                                          credentialsFile:
                                            description: |-
                                              (String) Path to the Google Cloud service account key file.
                                              Path to the Google Cloud service account key file.
                                            type: string
                                        type: object
                                      type: array
                                    decoding:
                                      description: |-
                                        (String) The decoding format used to interpret incoming logs.
                                        The decoding format used to interpret incoming logs.
                                      type: string
                                    project:
                                      description: |-
                                        (String) The Google Cloud project ID that owns the Pub/Sub topic.
                                        The Google Cloud project ID that owns the Pub/Sub subscription.
                                      type: string
                                    subscription:
                                      description: |-
                                        (String) The Pub/Sub subscription name from which messages are consumed.
                                        The Pub/Sub subscription name from which messages are consumed.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              httpClient:
                                description: |-
                                  (Block List) The http_client destination sends data to an HTTP endpoint. (see below for nested schema)
                                  The `http_client` source scrapes logs from HTTP endpoints at regular intervals.
                                items:
                                  properties:
                                    authStrategy:
                                      description: |-
                                        (String) HTTP authentication strategy. Valid values are none, basic, bearer.
                                        Optional authentication strategy for HTTP requests. Valid values are `none`, `basic`, `bearer`, `custom`.
                                      type: string
                                    customKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds a custom header value (used with custom auth strategies).
                                        Name of the environment variable or secret that holds a custom header value (used with custom auth strategies).
                                      type: string
                                    decoding:
                                      description: |-
                                        (String) The decoding format used to interpret incoming logs.
                                        The decoding format used to interpret incoming logs.
                                      type: string
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the HTTP endpoint URL.
                                      type: string
                                    passwordKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                        Name of the environment variable or secret that holds the password.
                                      type: string
                                    scrapeIntervalSecs:
                                      description: |-
                                        (Number) The interval (in seconds) between HTTP scrape requests.
                                        The interval (in seconds) between HTTP scrape requests.
                                      type: number
                                    scrapeTimeoutSecs:
                                      description: |-
                                        (Number) The timeout (in seconds) for each scrape request.
                                        The timeout (in seconds) for each scrape request.
                                      type: number
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          serverName:
                                            description: |-
                                              (String) Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                              Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                            type: string
                                        type: object
                                      type: array
                                    tokenKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the authentication token.
                                        Name of the environment variable or secret that holds the authentication token.
                                      type: string
                                    usernameKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                        Name of the environment variable or secret that holds the username.
                                      type: string
                                  type: object
                                type: array
                              httpServer:
                                description: |-
                                  (Block List) The http_server source collects logs over HTTP POST from external services. (see below for nested schema)
                                  The `http_server` source collects logs over HTTP POST from external services.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                    authStrategy:
                                      description: |-
                                        (String) HTTP authentication strategy. Valid values are none, basic, bearer.
                                        HTTP authentication method. Valid values are `none`, `plain`.
                                      type: string
                                    decoding:
                                      description: |-
                                        (String) The decoding format used to interpret incoming logs.
                                        The decoding format used to interpret incoming logs. Valid values are `json`, `gelf`, `syslog`, `bytes`.
                                      type: string
                                    passwordKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                        Name of the environment variable or secret that holds the password.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                    usernameKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                        Name of the environment variable or secret that holds the username.
                                      type: string
                                    validToken:
                                      description: |-
                                        (Block List) A token accepted for authenticating incoming HTTP requests. Cannot be combined with the plain auth strategy. (see below for nested schema)
                                        A token accepted for authenticating incoming HTTP requests. Cannot be combined with the `plain` auth strategy.
                                      items:
                                        properties:
                                          enabled:
                                            description: |-
                                              (Boolean) Whether this processor group is enabled.
                                              Whether this token is currently accepted. Defaults to `true`.
                                            type: boolean
                                          fieldToAdd:
                                            description: |-
                                              Za-z0-9_]+$. (see below for nested schema)
                                              An optional metadata field attached to every event authenticated by the token. Both `key` and `value` must match `^[A-Za-z0-9_]+$`.
                                            items:
                                              properties:
                                                key:
                                                  description: |-
                                                    (Block List) Key fields used to look up enrichment values. (see below for nested schema)
                                                    The metadata field name to add to incoming events.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The metadata value.
                                                    The metadata field value to add to incoming events.
                                                  type: string
                                              type: object
                                            type: array
                                          pathToToken:
                                            description: |-
                                              in source or header to read it from a request header. (see below for nested schema)
                                              Specifies where the worker extracts the token from the incoming HTTP request. Set either `location` for a built-in source or `header` to read it from a request header.
                                            items:
                                              properties:
                                                header:
                                                  description: |-
                                                    (String) The name of the HTTP header that carries the token.
                                                    The name of the HTTP header that carries the token.
                                                  type: string
                                                location:
                                                  description: |-
                                                    in token location on the incoming HTTP request. One of path, address. Valid values are path, address.
                                                    Built-in token location on the incoming HTTP request. One of `path`, `address`. Valid values are `path`, `address`.
                                                  type: string
                                              type: object
                                            type: array
                                          tokenKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the authentication token.
                                              Name of the environment variable or secret that holds the expected token value.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  The unique identifier for this source.
                                type: string
                              kafka:
                                description: |-
                                  (Block List) The kafka destination sends logs to Apache Kafka topics. (see below for nested schema)
                                  The `kafka` source ingests data from Apache Kafka topics.
                                items:
                                  properties:
                                    bootstrapServersKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the Kafka bootstrap servers.
                                        Name of the environment variable or secret that holds the Kafka bootstrap servers connection string.
                                      type: string
                                    groupId:
                                      description: |-
                                        (String) The Kafka consumer group ID.
                                        The Kafka consumer group ID.
                                      type: string
                                    librdkafkaOption:
                                      description: |-
                                        value pairs. (see below for nested schema)
                                        Advanced librdkafka client configuration options.
                                      items:
                                        properties:
                                          name:
                                            description: |-
                                              (String) The pipeline name.
                                              The name of the librdkafka option.
                                            type: string
                                          value:
                                            description: |-
                                              (String) The metadata value.
                                              The value of the librdkafka option.
                                            type: string
                                        type: object
                                      type: array
                                    sasl:
                                      description: |-
                                        (Block List) Specifies the SASL mechanism for authenticating with a Kafka cluster. (see below for nested schema)
                                        SASL authentication settings.
                                      items:
                                        properties:
                                          mechanism:
                                            description: |-
                                              SHA-256, SCRAM-SHA-512.
                                              SASL mechanism to use (e.g., PLAIN, SCRAM-SHA-256, SCRAM-SHA-512). Valid values are `PLAIN`, `SCRAM-SHA-256`, `SCRAM-SHA-512`.
                                            type: string
                                          passwordKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                              Name of the environment variable or secret that holds the SASL password.
                                            type: string
                                          usernameKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                              Name of the environment variable or secret that holds the SASL username.
                                            type: string
                                        type: object
                                      type: array
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                    topics:
                                      description: |-
                                        (List of String) A list of Kafka topic names to subscribe to. The source ingests messages from each topic specified.
                                        A list of Kafka topic names to subscribe to. The source ingests messages from each topic specified.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                type: array
                              logstash:
                                description: |-
                                  (Block List) The logstash source ingests logs from a Logstash forwarder. (see below for nested schema)
                                  The `logstash` source ingests logs from a Logstash forwarder.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              opentelemetry:
                                description: |-
                                  (Block List) The opentelemetry destination forwards metrics using the OpenTelemetry Protocol (OTLP) over HTTP. (see below for nested schema)
                                  The `opentelemetry` source receives telemetry data using the OpenTelemetry Protocol (OTLP) over gRPC and HTTP.
                                items:
                                  properties:
                                    grpcAddressKey:
                                      description: |-
                                        (String) Environment variable name containing the gRPC server address for receiving OTLP data.
                                        Environment variable name containing the gRPC server address for receiving OTLP data.
                                      type: string
                                    httpAddressKey:
                                      description: |-
                                        (String) Environment variable name containing the HTTP server address for receiving OTLP data.
                                        Environment variable name containing the HTTP server address for receiving OTLP data.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              rsyslog:
                                description: |-
                                  (Block List) The rsyslog destination forwards logs to an external rsyslog server over TCP or UDP using the syslog protocol. (see below for nested schema)
                                  The `rsyslog` source listens for logs over TCP or UDP from an `rsyslog` server using the syslog protocol.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                    mode:
                                      description: |-
                                        (String) The protocol used to send logs. Valid values are tcp, udp.
                                        Protocol used by the syslog source to receive messages.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              socket:
                                description: |-
                                  (Block List) The socket destination sends logs over TCP or UDP to a remote server. (see below for nested schema)
                                  The `socket` source ingests logs over TCP or UDP.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address for the socket.
                                      type: string
                                    framing:
                                      description: |-
                                        (Block List) Defines the framing method for outgoing messages. (see below for nested schema)
                                        Defines the framing method for incoming messages.
                                      items:
                                        properties:
                                          characterDelimited:
                                            description: |-
                                              (Block List) Used when method is character_delimited. Specifies the delimiter character. (see below for nested schema)
                                              Used when `method` is `character_delimited`. Specifies the delimiter character.
                                            items:
                                              properties:
                                                delimiter:
                                                  description: |-
                                                    (String) A single ASCII character used as a delimiter.
                                                    A single ASCII character used as a delimiter.
                                                  type: string
                                              type: object
                                            type: array
                                          method:
                                            description: |-
                                              (String) The framing method. Valid values are newline_delimited, bytes, character_delimited.
                                              The framing method. Valid values are `newline_delimited`, `bytes`, `character_delimited`, `octet_counting`, `chunked_gelf`.
                                            type: string
                                        type: object
                                      type: array
                                    mode:
                                      description: |-
                                        (String) The protocol used to send logs. Valid values are tcp, udp.
                                        The protocol used to receive logs. Valid values are `tcp`, `udp`.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              splunkHec:
                                description: |-
                                  (Block List) The splunk_hec destination forwards logs to Splunk using the HTTP Event Collector (HEC). (see below for nested schema)
                                  The `splunk_hec` source implements the Splunk HTTP Event Collector (HEC) API.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address for the HEC API.
                                      type: string
                                    storeHecToken:
                                      description: |-
                                        (Boolean) When true, the Splunk HEC token from the incoming request is stored in the event, allowing downstream components to forward it to other Splunk HEC destinations.
                                        When `true`, the Splunk HEC token from the incoming request is stored in the event, allowing downstream components to forward it to other Splunk HEC destinations.
                                      type: boolean
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                    validToken:
                                      description: |-
                                        (Block List) A token accepted for authenticating incoming HTTP requests. Cannot be combined with the plain auth strategy. (see below for nested schema)
                                        A HEC token accepted for authenticating incoming Splunk HEC requests.
                                      items:
                                        properties:
                                          enabled:
                                            description: |-
                                              (Boolean) Whether this processor group is enabled.
                                              Whether this token is currently accepted. Defaults to `true`.
                                            type: boolean
                                          fieldToAdd:
                                            description: |-
                                              Za-z0-9_]+$. (see below for nested schema)
                                              An optional metadata field attached to every event authenticated by the token. Both `key` and `value` must match `^[A-Za-z0-9_]+$`.
                                            items:
                                              properties:
                                                key:
                                                  description: |-
                                                    (Block List) Key fields used to look up enrichment values. (see below for nested schema)
                                                    The metadata field name to add to incoming events.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The metadata value.
                                                    The metadata field value to add to incoming events.
                                                  type: string
                                              type: object
                                            type: array
                                          tokenKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the authentication token.
                                              Name of the environment variable or secret that holds the expected HEC token value.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              splunkTcp:
                                description: |-
                                  (Block List) The splunk_tcp source receives logs from a Splunk Universal Forwarder over TCP. TLS is supported for secure transmission. (see below for nested schema)
                                  The `splunk_tcp` source receives logs from a Splunk Universal Forwarder over TCP. TLS is supported for secure transmission.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address for the Splunk TCP receiver.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              sumoLogic:
                                description: |-
                                  (Block List) The sumo_logic destination forwards logs to Sumo Logic. (see below for nested schema)
                                  The `sumo_logic` source receives logs from Sumo Logic collectors.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                  type: object
                                type: array
                              syslogNg:
                                description: |-
                                  ng server over TCP or UDP using the syslog protocol. (see below for nested schema)
                                  The `syslog_ng` source listens for logs over TCP or UDP from a `syslog-ng` server using the syslog protocol.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                    mode:
                                      description: |-
                                        (String) The protocol used to send logs. Valid values are tcp, udp.
                                        Protocol used by the syslog source to receive messages.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              websocket:
                                description: |-
                                  (Block List) The websocket source establishes a persistent WebSocket connection to a remote endpoint and ingests log events as they are pushed by the server. (see below for nested schema)
                                  The `websocket` source establishes a persistent WebSocket connection to a remote endpoint and ingests log events as they are pushed by the server.
                                items:
                                  properties:
                                    authStrategy:
                                      description: |-
                                        (String) HTTP authentication strategy. Valid values are none, basic, bearer.
                                        The authentication strategy used when connecting to the WebSocket server. Valid values are `none`, `basic`, `bearer`, `custom`.
                                      type: string
                                    customKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds a custom header value (used with custom auth strategies).
                                        Name of the environment variable or secret that holds a custom header value. Used when `auth_strategy` is `custom`.
                                      type: string
                                    decoding:
                                      description: |-
                                        (String) The decoding format used to interpret incoming logs.
                                        The decoding format used to interpret incoming log events. Valid values are `bytes`, `gelf`, `json`, `syslog`.
                                      type: string
                                    passwordKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                        Name of the environment variable or secret that holds the password. Used when `auth_strategy` is `basic`.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        TLS configuration for the WebSocket connection. Set `mode` to `enabled` for server-certificate validation only, or `with_client_cert` to additionally present a client certificate.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the client certificate file. Required when `mode` is `with_client_cert`.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the client certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          mode:
                                            description: |-
                                              (String) The protocol used to send logs. Valid values are tcp, udp.
                                              The TLS mode. Use `enabled` for server-only TLS, or `with_client_cert` for mutual TLS with a client certificate. Valid values are `enabled`, `with_client_cert`.
                                            type: string
                                        type: object
                                      type: array
                                    tokenKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the authentication token.
                                        Name of the environment variable or secret that holds the bearer token. Used when `auth_strategy` is `bearer`.
                                      type: string
                                    uriKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the request URI.
                                        Name of the environment variable or secret that holds the WebSocket URI to connect to.
                                      type: string
                                    usernameKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                        Name of the environment variable or secret that holds the username. Used when `auth_strategy` is `basic`.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        useLegacySearchSyntax:
                          description: |-
                            (Boolean) Set to true to continue using the legacy search syntax while migrating filter queries. After migrating all queries to the new syntax, set to false. The legacy syntax is deprecated and will eventually be removed. Requires Observability Pipelines Worker 2.11 or later. Only applies to logs pipelines. This field is ignored for metrics pipelines. See https://docs.datadoghq.com/observability_pipelines/guide/upgrade_your_filter_queries_to_the_new_search_syntax/ for more information.
                            Set to `true` to continue using the legacy search syntax while migrating filter queries. After migrating all queries to the new syntax, set to `false`. The legacy syntax is deprecated and will eventually be removed. Requires Observability Pipelines Worker 2.11 or later. Only applies to `logs` pipelines. This field is ignored for `metrics` pipelines. See https://docs.datadoghq.com/observability_pipelines/guide/upgrade_your_filter_queries_to_the_new_search_syntax/ for more information.
                          type: boolean
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The pipeline name.
                      The pipeline name.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: ObservabilityPipelineStatus defines the observed state of
              ObservabilityPipeline.
            properties:
              atProvider:
                properties:
                  config:
                    description: |-
                      (Block List) Configuration for the pipeline. (see below for nested schema)
                      Configuration for the pipeline.
                    items:
                      properties:
                        destination:
                          description: |-
                            (Block List) List of destinations. (see below for nested schema)
                            List of destinations.
                          items:
                            properties:
                              amazonOpensearch:
                                description: |-
                                  (Block List) The amazon_opensearch destination writes logs to Amazon OpenSearch. (see below for nested schema)
                                  The `amazon_opensearch` destination writes logs to Amazon OpenSearch.
                                items:
                                  properties:
                                    auth:
                                      description: (Block List) (see below for nested
                                        schema)
                                      items:
                                        properties:
                                          assumeRole:
                                            description: |-
                                              (String) ARN of the role to assume.
                                              ARN of the role to assume.
                                            type: string
                                          awsRegion:
                                            description: |-
                                              (String) AWS region override (if applicable).
                                              AWS region override (if applicable).
                                            type: string
                                          externalId:
                                            description: |-
                                              (String) External ID for assumed role.
                                              External ID for assumed role.
                                            type: string
                                          sessionName:
                                            description: |-
                                              (String) Session name for assumed role.
                                              Session name for assumed role.
                                            type: string
                                          strategy:
                                            description: |-
                                              (String) The authentication strategy to use (e.g. aws or basic).
                                              The authentication strategy to use (e.g. aws or basic).
                                            type: string
                                        type: object
                                      type: array
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    bulkIndex:
                                      description: |-
                                        (String) The index or datastream to write logs to.
                                        The index or datastream to write logs to.
                                      type: string
                                  type: object
                                type: array
                              amazonS3:
                                description: |-
                                  rehydratable format to an Amazon S3 bucket for archiving. (see below for nested schema)
                                  The `amazon_s3` destination sends your logs in Datadog-rehydratable format to an Amazon S3 bucket for archiving.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        AWS authentication credentials used for accessing AWS services. If omitted, the system's default credentials are used (for example, the IAM role and environment variables).
                                      items:
                                        properties:
                                          assumeRole:
                                            description: |-
                                              (String) ARN of the role to assume.
                                              The Amazon Resource Name (ARN) of the role to assume.
                                            type: string
                                          externalId:
                                            description: |-
                                              (String) External ID for assumed role.
                                              A unique identifier for cross-account role assumption.
                                            type: string
                                          sessionName:
                                            description: |-
                                              (String) Session name for assumed role.
                                              A session identifier used for logging and tracing the assumed role session.
                                            type: string
                                        type: object
                                      type: array
                                    bucket:
                                      description: |-
                                        (String) S3 bucket name.
                                        S3 bucket name.
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression configuration for archived logs. When omitted, the worker default (gzip) is used.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              Compression algorithm. One of `gzip` or `zstd`. Valid values are `gzip`, `zstd`.
                                            type: string
                                          level:
                                            description: |-
                                              9) and zstd (1-21).
                                              Compression level. Applies to `gzip` (1-9) and `zstd` (1-21).
                                            type: number
                                        type: object
                                      type: array
                                    keyPrefix:
                                      description: |-
                                        (String) Prefix for object keys.
                                        Prefix for object keys.
                                      type: string
                                    region:
                                      description: |-
                                        (String) AWS region of the S3 bucket.
                                        AWS region of the S3 bucket.
                                      type: string
                                    serverSideEncryption:
                                      description: |-
                                        side encryption algorithm used when storing objects in S3. Valid values: aws:kms, AES256. Valid values are aws:kms, AES256.
                                        The server-side encryption algorithm used when storing objects in S3. Valid values: `aws:kms`, `AES256`. Valid values are `aws:kms`, `AES256`.
                                      type: string
                                    ssekmsKeyId:
                                      description: |-
                                        KMS encryption. Only applies when server_side_encryption is aws:kms.
                                        ID of the AWS KMS key to use for SSE-KMS encryption. Only applies when `server_side_encryption` is `aws:kms`.
                                      type: string
                                    storageClass:
                                      description: |-
                                        (String) S3 storage class. Valid values are STANDARD, REDUCED_REDUNDANCY, INTELLIGENT_TIERING, STANDARD_IA, EXPRESS_ONEZONE, ONEZONE_IA, GLACIER, GLACIER_IR, DEEP_ARCHIVE.
                                        S3 storage class. Valid values are `STANDARD`, `REDUCED_REDUNDANCY`, `INTELLIGENT_TIERING`, `STANDARD_IA`, `EXPRESS_ONEZONE`, `ONEZONE_IA`, `GLACIER`, `GLACIER_IR`, `DEEP_ARCHIVE`.
                                      type: string
                                  type: object
                                type: array
                              amazonS3Generic:
                                description: |-
                                  (Block List) The amazon_s3_generic destination sends your logs to an Amazon S3 bucket. (see below for nested schema)
                                  The `amazon_s3_generic` destination sends your logs to an Amazon S3 bucket.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        AWS authentication credentials used for accessing AWS services. If omitted, the system's default credentials are used (for example, the IAM role and environment variables).
                                      items:
                                        properties:
                                          assumeRole:
                                            description: |-
                                              (String) ARN of the role to assume.
                                              The Amazon Resource Name (ARN) of the role to assume.
                                            type: string
                                          externalId:
                                            description: |-
                                              (String) External ID for assumed role.
                                              A unique identifier for cross-account role assumption.
                                            type: string
                                          sessionName:
                                            description: |-
                                              (String) Session name for assumed role.
                                              A session identifier used for logging and tracing the assumed role session.
                                            type: string
                                        type: object
                                      type: array
                                    batchSettings:
                                      description: |-
                                        (Block List) Event batching settings. (see below for nested schema)
                                        Event batching settings.
                                      items:
                                        properties:
                                          batchSize:
                                            description: |-
                                              (Number) Maximum batch size in bytes.
                                              Maximum batch size in bytes.
                                            type: number
                                          timeoutSecs:
                                            description: |-
                                              (Number) Maximum number of seconds to wait before flushing the batch.
                                              Maximum number of seconds to wait before flushing the batch.
                                            type: number
                                        type: object
                                      type: array
                                    bucket:
                                      description: |-
                                        (String) S3 bucket name.
                                        S3 bucket name.
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression configuration.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              Compression algorithm. Valid values are `gzip`, `zstd`, `snappy`.
                                            type: string
                                          level:
                                            description: |-
                                              9) and zstd (1-21).
                                              Compression level.
                                            type: number
                                        type: object
                                      type: array
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        Encoding format for the destination.
                                      items:
                                        properties:
                                          type:
                                            description: |-
                                              (String) The encoding type. Valid values are json, parquet.
                                              The encoding type. Valid values are `json`, `parquet`.
                                            type: string
                                        type: object
                                      type: array
                                    keyPrefix:
                                      description: |-
                                        (String) Prefix for object keys.
                                        Optional prefix for object keys.
                                      type: string
                                    region:
                                      description: |-
                                        (String) AWS region of the S3 bucket.
                                        AWS region of the S3 bucket.
                                      type: string
                                    serverSideEncryption:
                                      description: |-
                                        side encryption algorithm used when storing objects in S3. Valid values: aws:kms, AES256. Valid values are aws:kms, AES256.
                                        The server-side encryption algorithm used when storing objects in S3. Valid values: `aws:kms`, `AES256`. Valid values are `aws:kms`, `AES256`.
                                      type: string
                                    ssekmsKeyId:
                                      description: |-
                                        KMS encryption. Only applies when server_side_encryption is aws:kms.
                                        ID of the AWS KMS key to use for SSE-KMS encryption. Only applies when `server_side_encryption` is `aws:kms`.
                                      type: string
                                    storageClass:
                                      description: |-
                                        (String) S3 storage class. Valid values are STANDARD, REDUCED_REDUNDANCY, INTELLIGENT_TIERING, STANDARD_IA, EXPRESS_ONEZONE, ONEZONE_IA, GLACIER, GLACIER_IR, DEEP_ARCHIVE.
                                        S3 storage class. Valid values are `STANDARD`, `REDUCED_REDUNDANCY`, `INTELLIGENT_TIERING`, `STANDARD_IA`, `EXPRESS_ONEZONE`, `ONEZONE_IA`, `GLACIER`, `GLACIER_IR`, `DEEP_ARCHIVE`.
                                      type: string
                                  type: object
                                type: array
                              amazonSecurityLake:
                                description: |-
                                  (Block List) The amazon_security_lake destination sends your logs to Amazon Security Lake. (see below for nested schema)
                                  The `amazon_security_lake` destination sends your logs to Amazon Security Lake.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        AWS authentication credentials used for accessing AWS services. If omitted, the system's default credentials are used (for example, the IAM role and environment variables).
                                      items:
                                        properties:
                                          assumeRole:
                                            description: |-
                                              (String) ARN of the role to assume.
                                              The Amazon Resource Name (ARN) of the role to assume.
                                            type: string
                                          externalId:
                                            description: |-
                                              (String) External ID for assumed role.
                                              A unique identifier for cross-account role assumption.
                                            type: string
                                          sessionName:
                                            description: |-
                                              (String) Session name for assumed role.
                                              A session identifier used for logging and tracing the assumed role session.
                                            type: string
                                        type: object
                                      type: array
                                    bucket:
                                      description: |-
                                        (String) S3 bucket name.
                                        Name of the Amazon S3 bucket in Security Lake (3-63 characters).
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    customSourceName:
                                      description: |-
                                        (String) Custom source name for the logs in Security Lake.
                                        Custom source name for the logs in Security Lake.
                                      type: string
                                    region:
                                      description: |-
                                        (String) AWS region of the S3 bucket.
                                        AWS region of the Security Lake bucket.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              azureStorage:
                                description: |-
                                  (Block List) The azure_storage destination forwards logs to an Azure Blob Storage container. (see below for nested schema)
                                  The `azure_storage` destination forwards logs to an Azure Blob Storage container.
                                items:
                                  properties:
                                    blobPrefix:
                                      description: |-
                                        (String) Optional prefix for blobs written to the container.
                                        Optional prefix for blobs written to the container.
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression configuration for archived logs. When omitted, the worker default (gzip) is used.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              Compression algorithm. One of `gzip` or `zstd`. Valid values are `gzip`, `zstd`.
                                            type: string
                                          level:
                                            description: |-
                                              9) and zstd (1-21).
                                              Compression level. Applies to `gzip` (1-9) and `zstd` (1-21).
                                            type: number
                                        type: object
                                      type: array
                                    connectionStringKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the Azure Storage connection string.
                                        Name of the environment variable or secret that holds the Azure Storage connection string.
                                      type: string
                                    containerName:
                                      description: |-
                                        (String) The name of the Azure Blob Storage container to store logs in.
                                        The name of the Azure Blob Storage container to store logs in.
                                      type: string
                                  type: object
                                type: array
                              clickhouse:
                                description: |-
                                  (Block List) The clickhouse destination forwards logs to a ClickHouse server via HTTP. (see below for nested schema)
                                  The `clickhouse` destination forwards logs to a ClickHouse server via HTTP.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        Authentication strategy for ClickHouse HTTP requests. Only `basic` strategy is supported.
                                      items:
                                        properties:
                                          passwordKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                              Name of the environment variable or secret that holds the ClickHouse password. Defaults to `DESTINATION_CLICKHOUSE_PASSWORD`.
                                            type: string
                                          strategy:
                                            description: |-
                                              (String) The authentication strategy to use (e.g. aws or basic).
                                              Authentication strategy. Must be `basic`. Valid values are `basic`.
                                            type: string
                                          usernameKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                              Name of the environment variable or secret that holds the ClickHouse username. Defaults to `DESTINATION_CLICKHOUSE_USERNAME`.
                                            type: string
                                        type: object
                                      type: array
                                    batch:
                                      description: |-
                                        (Block List) Batching configuration for ClickHouse inserts. (see below for nested schema)
                                        Batching configuration for ClickHouse inserts.
                                      items:
                                        properties:
                                          maxEvents:
                                            description: |-
                                              (Number) Maximum events for the memory buffer.
                                              Maximum number of events per batch. Value must be at least 1.
                                            type: number
                                          timeoutSecs:
                                            description: |-
                                              (Number) Maximum number of seconds to wait before flushing the batch.
                                              Maximum time in seconds before a partial batch is flushed. Value must be between 1 and 65535.
                                            type: number
                                        type: object
                                      type: array
                                    batchEncoding:
                                      description: |-
                                        (Block List) Batch encoding configuration. Required when format is arrow_stream. (see below for nested schema)
                                        Batch encoding configuration. Required when `format` is `arrow_stream`.
                                      items:
                                        properties:
                                          allowNullableFields:
                                            description: |-
                                              nullable fields in the ClickHouse schema. Defaults to false.
                                              If `true`, allows null values for non-nullable fields in the ClickHouse schema. Defaults to `false`.
                                            type: boolean
                                          codec:
                                            description: |-
                                              (String) Batch encoding codec. Must be arrow_stream. Valid values are arrow_stream.
                                              Batch encoding codec. Must be `arrow_stream`. Valid values are `arrow_stream`.
                                            type: string
                                        type: object
                                      type: array
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression for outbound HTTP requests. Use `algorithm = "gzip"` or `algorithm = "none"`.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              Compression algorithm. Valid values are `gzip` and `none`. Valid values are `gzip`, `none`.
                                            type: string
                                          level:
                                            description: |-
                                              9) and zstd (1-21).
                                              Compression level (1–9). Only valid when `algorithm` is `gzip`. Value must be between 1 and 9.
                                            type: number
                                        type: object
                                      type: array
                                    database:
                                      description: |-
                                        (String) Optional name of the ClickHouse database to write to. When omitted, the user's default database is used.
                                        Optional name of the ClickHouse database to write to. When omitted, the user's default database is used.
                                      type: string
                                    dateTimeBestEffort:
                                      description: |-
                                        (Boolean) If true, enables flexible DateTime parsing on the server side.
                                        If `true`, enables flexible DateTime parsing on the server side.
                                      type: boolean
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to `DESTINATION_CLICKHOUSE_ENDPOINT_URL`.
                                      type: string
                                    format:
                                      description: |-
                                        (String) Insert format for events. json_each_row maps event fields to columns by name. json_as_object and json_as_string insert each event into a single JSON or String column. arrow_stream batches events with Apache Arrow IPC streaming and requires batch_encoding. Valid values are json_each_row, json_as_object, json_as_string, arrow_stream.
                                        Insert format for events. `json_each_row` maps event fields to columns by name. `json_as_object` and `json_as_string` insert each event into a single JSON or String column. `arrow_stream` batches events with Apache Arrow IPC streaming and requires `batch_encoding`. Valid values are `json_each_row`, `json_as_object`, `json_as_string`, `arrow_stream`.
                                      type: string
                                    skipUnknownFields:
                                      description: |-
                                        (Boolean) If true, fields not present in the target table schema are dropped instead of causing insert errors. When unset, the ClickHouse server's own input_format_skip_unknown_fields setting applies.
                                        If `true`, fields not present in the target table schema are dropped instead of causing insert errors. When unset, the ClickHouse server's own `input_format_skip_unknown_fields` setting applies.
                                      type: boolean
                                    table:
                                      description: |-
                                        (String) Target ClickHouse table name.
                                        Target ClickHouse table name.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              cloudPrem:
                                description: |-
                                  (Block List) The cloud_prem destination sends logs to Datadog CloudPrem. (see below for nested schema)
                                  The `cloud_prem` destination sends logs to Datadog CloudPrem.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the endpoint URL.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          serverName:
                                            description: |-
                                              (String) Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                              Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              crowdstrikeNextGenSiem:
                                description: |-
                                  (Block List) The crowdstrike_next_gen_siem destination forwards logs to CrowdStrike Next Gen SIEM. (see below for nested schema)
                                  The `crowdstrike_next_gen_siem` destination forwards logs to CrowdStrike Next Gen SIEM.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression configuration for log events.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              Compression algorithm for log events.
                                            type: string
                                          level:
                                            description: |-
                                              9) and zstd (1-21).
                                              Compression level.
                                            type: number
                                        type: object
                                      type: array
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        Encoding format for log events. Valid values are `json`, `raw_message`.
                                      type: string
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the endpoint URL.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                    tokenKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the authentication token.
                                        Name of the environment variable or secret that holds the authentication token.
                                      type: string
                                  type: object
                                type: array
                              databricksZerobus:
                                description: |-
                                  (Block List) The databricks_zerobus destination sends logs to Databricks via the Zerobus ingestion API. (see below for nested schema)
                                  The `databricks_zerobus` destination sends logs to Databricks via the Zerobus ingestion API.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        OAuth client credentials used to authenticate with Databricks.
                                      items:
                                        properties:
                                          clientId:
                                            description: |-
                                              (String) The OAuth client ID used to authenticate with Databricks.
                                              The OAuth client ID used to authenticate with Databricks.
                                            type: string
                                          clientSecretKey:
                                            description: |-
                                              (String) The name of the secret or environment variable holding the OAuth client secret. Defaults to DESTINATION_DATABRICKS_ZEROBUS_OAUTH_CLIENT_SECRET.
                                              The name of the secret or environment variable holding the OAuth client secret. Defaults to `DESTINATION_DATABRICKS_ZEROBUS_OAUTH_CLIENT_SECRET`.
                                            type: string
                                        type: object
                                      type: array
                                    ingestionEndpointKey:
                                      description: |-
                                        (String) The name of the secret or environment variable holding the Databricks Zerobus ingestion endpoint URL.
                                        The name of the secret or environment variable holding the Databricks Zerobus ingestion endpoint URL.
                                      type: string
                                    tableName:
                                      description: |-
                                        (String) The name of the Databricks table to ingest logs into.
                                        The name of the Databricks table to ingest logs into.
                                      type: string
                                    unityCatalogEndpointKey:
                                      description: |-
                                        (String) The name of the secret or environment variable holding the Databricks Unity Catalog endpoint URL.
                                        The name of the secret or environment variable holding the Databricks Unity Catalog endpoint URL.
                                      type: string
                                  type: object
                                type: array
                              datadogLogs:
                                description: |-
                                  (Block List) The datadog_logs destination forwards logs to Datadog Log Management. (see below for nested schema)
                                  The `datadog_logs` destination forwards logs to Datadog Log Management.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    routes:
                                      description: |-
                                        (Block List) A list of routing rules that forward matching logs to Datadog using dedicated API keys. (see below for nested schema)
                                        A list of routing rules that forward matching logs to Datadog using dedicated API keys.
                                      items:
                                        properties:
                                          apiKeyKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that stores the Datadog API key used by this route.
                                              Name of the environment variable or secret that stores the Datadog API key used by this route.
                                            type: string
                                          buffer:
                                            description: |-
                                              (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                              Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                            items:
                                              properties:
                                                disk:
                                                  description: |-
                                                    (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                                    Options for configuring a disk buffer. Cannot be used with `memory`.
                                                  items:
                                                    properties:
                                                      maxSize:
                                                        description: |-
                                                          (Number) Maximum size of the disk buffer (in bytes).
                                                          Maximum size of the disk buffer (in bytes).
                                                        type: number
                                                      whenFull:
                                                        description: |-
                                                          (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                          Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                        type: string
                                                    type: object
                                                  type: array
                                                memory:
                                                  description: |-
                                                    (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                                    Options for configuring a memory buffer. Cannot be used with `disk`.
                                                  items:
                                                    properties:
                                                      maxEvents:
                                                        description: |-
                                                          (Number) Maximum events for the memory buffer.
                                                          Maximum events for the memory buffer.
                                                        type: number
                                                      maxSize:
                                                        description: |-
                                                          (Number) Maximum size of the disk buffer (in bytes).
                                                          Maximum size of the memory buffer (in bytes).
                                                        type: number
                                                      whenFull:
                                                        description: |-
                                                          (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                          Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                        type: string
                                                    type: object
                                                  type: array
                                              type: object
                                            type: array
                                          include:
                                            description: |-
                                              (String) A Datadog search query that determines which logs are forwarded using this route.
                                              A Datadog search query that determines which logs are forwarded using this route.
                                            type: string
                                          routeId:
                                            description: |-
                                              (String) Unique identifier for this route within the destination.
                                              Unique identifier for this route within the destination.
                                            type: string
                                          site:
                                            description: |-
                                              (String) Datadog site where matching logs are sent (for example, us1).
                                              Datadog site where matching logs are sent (for example, `us1`).
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              datadogMetrics:
                                description: |-
                                  (Block List) The datadog_metrics destination forwards metrics to Datadog. (see below for nested schema)
                                  The `datadog_metrics` destination forwards metrics to Datadog.
                                items:
                                  type: object
                                type: array
                              elasticsearch:
                                description: |-
                                  (Block List) The elasticsearch destination writes logs or metrics to an Elasticsearch cluster. (see below for nested schema)
                                  The `elasticsearch` destination writes logs or metrics to an Elasticsearch cluster.
                                items:
                                  properties:
                                    apiVersion:
                                      description: |-
                                        detect. Valid values are auto, v6, v7, v8.
                                        The Elasticsearch API version to use. Set to `auto` to auto-detect. Valid values are `auto`, `v6`, `v7`, `v8`.
                                      type: string
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        Authentication settings for the Elasticsearch destination.
                                      items:
                                        properties:
                                          passwordKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                              Name of the environment variable or secret that holds the Elasticsearch password (used when `strategy` is `basic`).
                                            type: string
                                          strategy:
                                            description: |-
                                              (String) The authentication strategy to use (e.g. aws or basic).
                                              The authentication strategy to use. Valid values are `basic`, `aws`.
                                            type: string
                                          usernameKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                              Name of the environment variable or secret that holds the Elasticsearch username (used when `strategy` is `basic`).
                                            type: string
                                        type: object
                                      type: array
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    bulkIndex:
                                      description: |-
                                        (String) The index or datastream to write logs to.
                                        The name of the index to write events to in Elasticsearch.
                                      type: string
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression configuration for the Elasticsearch destination.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              The compression algorithm applied when sending data to Elasticsearch. Valid values are `none`, `gzip`, `zlib`, `zstd`, `snappy`.
                                            type: string
                                          level:
                                            description: |-
                                              9) and zstd (1-21).
                                              The compression level. Only applicable for `gzip`, `zlib`, and `zstd` algorithms.
                                            type: number
                                        type: object
                                      type: array
                                    dataStream:
                                      description: |-
                                        (Block List) Configuration options for writing to Elasticsearch Data Streams instead of a fixed index. (see below for nested schema)
                                        Configuration options for writing to Elasticsearch Data Streams instead of a fixed index.
                                      items:
                                        properties:
                                          autoRouting:
                                            description: |-
                                              (Boolean) When true, automatically routes events to the appropriate data stream based on the event content.
                                              When `true`, automatically routes events to the appropriate data stream based on the event content.
                                            type: boolean
                                          dataset:
                                            description: |-
                                              (String) The data stream dataset. This groups events by their source or application.
                                              The data stream dataset. This groups events by their source or application.
                                            type: string
                                          dtype:
                                            description: |-
                                              (String) The data stream type. This determines how events are categorized within the data stream.
                                              The data stream type. This determines how events are categorized within the data stream.
                                            type: string
                                          namespace:
                                            description: |-
                                              (String) The data stream namespace. This separates events into different environments or domains.
                                              The data stream namespace. This separates events into different environments or domains.
                                            type: string
                                          syncFields:
                                            description: |-
                                              (Boolean) When true, synchronizes data stream fields with the Elasticsearch index mapping.
                                              When `true`, synchronizes data stream fields with the Elasticsearch index mapping.
                                            type: boolean
                                        type: object
                                      type: array
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the Elasticsearch endpoint URL.
                                      type: string
                                    idKey:
                                      description: |-
                                        (String) The name of the field used as the document ID in Elasticsearch.
                                        The name of the field used as the document ID in Elasticsearch.
                                      type: string
                                    pipeline:
                                      description: |-
                                        (String) The name of an Elasticsearch ingest pipeline to apply to events before indexing.
                                        The name of an Elasticsearch ingest pipeline to apply to events before indexing.
                                      type: string
                                    requestRetryPartial:
                                      description: |-
                                        (Boolean) When true, retries failed partial bulk requests when some events in a batch fail while others succeed.
                                        When `true`, retries failed partial bulk requests when some events in a batch fail while others succeed.
                                      type: boolean
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              googleCloudStorage:
                                description: |-
                                  (Block List) The google_cloud_storage destination stores logs in a Google Cloud Storage (GCS) bucket. (see below for nested schema)
                                  The `google_cloud_storage` destination stores logs in a Google Cloud Storage (GCS) bucket.
                                items:
                                  properties:
                                    acl:
                                      description: |-
                                        (String) Access control list setting for objects written to the bucket.
                                        Access control list setting for objects written to the bucket.
                                      type: string
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        Google Cloud credentials used to authenticate with Google Cloud services.
                                      items:
                                        properties:
                                          credentialsFile:
                                            description: |-
                                              (String) Path to the Google Cloud service account key file.
                                              Path to the Google Cloud service account key file.
                                            type: string
                                        type: object
                                      type: array
                                    bucket:
                                      description: |-
                                        (String) S3 bucket name.
                                        Name of the GCS bucket.
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression configuration for archived logs. When omitted, the worker default (gzip) is used.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              Compression algorithm. One of `gzip` or `zstd`. Valid values are `gzip`, `zstd`.
                                            type: string
                                          level:
                                            description: |-
                                              9) and zstd (1-21).
                                              Compression level. Applies to `gzip` (1-9) and `zstd` (1-21).
                                            type: number
                                        type: object
                                      type: array
                                    keyPrefix:
                                      description: |-
                                        (String) Prefix for object keys.
                                        Optional prefix for object keys within the GCS bucket.
                                      type: string
                                    metadata:
                                      description: |-
                                        value pairs added to each object. (see below for nested schema)
                                        Custom metadata key-value pairs added to each object.
                                      items:
                                        properties:
                                          name:
                                            description: |-
                                              (String) The pipeline name.
                                              The metadata key.
                                            type: string
                                          value:
                                            description: |-
                                              (String) The metadata value.
                                              The metadata value.
                                            type: string
                                        type: object
                                      type: array
                                    storageClass:
                                      description: |-
                                        (String) S3 storage class. Valid values are STANDARD, REDUCED_REDUNDANCY, INTELLIGENT_TIERING, STANDARD_IA, EXPRESS_ONEZONE, ONEZONE_IA, GLACIER, GLACIER_IR, DEEP_ARCHIVE.
                                        Storage class used for objects stored in GCS.
                                      type: string
                                  type: object
                                type: array
                              googlePubsub:
                                description: |-
                                  (Block List) The google_pubsub destination publishes logs to a Google Cloud Pub/Sub topic. (see below for nested schema)
                                  The `google_pubsub` destination publishes logs to a Google Cloud Pub/Sub topic.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        Google Cloud credentials used to authenticate with Google Cloud services.
                                      items:
                                        properties:
                                          credentialsFile:
                                            description: |-
                                              (String) Path to the Google Cloud service account key file.
                                              Path to the Google Cloud service account key file.
                                            type: string
                                        type: object
                                      type: array
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        Encoding format for log events. Valid values: `json`, `raw_message`.
                                      type: string
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the Google Cloud Pub/Sub endpoint URL.
                                      type: string
                                    project:
                                      description: |-
                                        (String) The Google Cloud project ID that owns the Pub/Sub topic.
                                        The Google Cloud project ID that owns the Pub/Sub topic.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                    topic:
                                      description: |-
                                        (String) The Pub/Sub topic name to publish logs to.
                                        The Pub/Sub topic name to publish logs to.
                                      type: string
                                  type: object
                                type: array
                              googleSecops:
                                description: |-
                                  (Block List) The google_chronicle destination sends logs to Google SecOps. (see below for nested schema)
                                  The `google_chronicle` destination sends logs to Google SecOps.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        Google Cloud credentials used to authenticate with Google Cloud services.
                                      items:
                                        properties:
                                          credentialsFile:
                                            description: |-
                                              (String) Path to the Google Cloud service account key file.
                                              Path to the Google Cloud service account key file.
                                            type: string
                                        type: object
                                      type: array
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    customerId:
                                      description: |-
                                        (String) The Google SecOps customer ID.
                                        The Google SecOps customer ID.
                                      type: string
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        The encoding format for the logs sent to Google SecOps. Valid values are `json`, `raw_message`.
                                      type: string
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the Google Chronicle endpoint URL.
                                      type: string
                                    logType:
                                      description: |-
                                        (String) The log type metadata associated with the Google SecOps destination.
                                        The log type metadata associated with the Google SecOps destination.
                                      type: string
                                  type: object
                                type: array
                              httpClient:
                                description: |-
                                  (Block List) The http_client destination sends data to an HTTP endpoint. (see below for nested schema)
                                  The `http_client` destination sends data to an HTTP endpoint.
                                items:
                                  properties:
                                    authStrategy:
                                      description: |-
                                        (String) HTTP authentication strategy. Valid values are none, basic, bearer.
                                        HTTP authentication strategy. Valid values are `none`, `basic`, `bearer`.
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression configuration for HTTP requests.
                                      items:
                                        properties:
                                          algorithm:
                                            description: |-
                                              (String) Compression algorithm. One of gzip or zstd. Valid values are gzip, zstd.
                                              Compression algorithm. Valid values are `gzip`.
                                            type: string
                                        type: object
                                      type: array
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        Encoding format for events. Valid values are `json`.
                                      type: string
                                    passwordKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                        Name of the environment variable or secret that holds the password.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          serverName:
                                            description: |-
                                              (String) Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                              Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                            type: string
                                        type: object
                                      type: array
                                    tokenKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the authentication token.
                                        Name of the environment variable or secret that holds the authentication token.
                                      type: string
                                    uriKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the request URI.
                                        Name of the environment variable or secret that holds the request URI.
                                      type: string
                                    usernameKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                        Name of the environment variable or secret that holds the username.
                                      type: string
                                  type: object
                                type: array
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  The unique identifier for this destination.
                                type: string
                              inputs:
                                description: |-
                                  (List of String) A list of component IDs whose output is used as the input for this component.
                                  A list of component IDs whose output is used as the `input` for this component.
                                items:
                                  type: string
                                type: array
                              kafka:
                                description: |-
                                  (Block List) The kafka destination sends logs to Apache Kafka topics. (see below for nested schema)
                                  The `kafka` destination sends logs to Apache Kafka topics.
                                items:
                                  properties:
                                    bootstrapServersKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the Kafka bootstrap servers.
                                        Name of the environment variable or secret that holds the Kafka bootstrap servers.
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression codec for Kafka messages. Valid values are `none`, `gzip`, `snappy`, `lz4`, `zstd`.
                                      type: string
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        Encoding format for log events. Valid values are `json`, `raw_message`.
                                      type: string
                                    headersKey:
                                      description: |-
                                        (String) The field name to use for Kafka message headers.
                                        The field name to use for Kafka message headers.
                                      type: string
                                    keyField:
                                      description: |-
                                        (String) The field name to use as the Kafka message key.
                                        The field name to use as the Kafka message key.
                                      type: string
                                    librdkafkaOption:
                                      description: |-
                                        value pairs. (see below for nested schema)
                                        Optional list of advanced Kafka producer configuration options, defined as key-value pairs.
                                      items:
                                        properties:
                                          name:
                                            description: |-
                                              (String) The pipeline name.
                                              The name of the librdkafka configuration option.
                                            type: string
                                          value:
                                            description: |-
                                              (String) The metadata value.
                                              The value of the librdkafka configuration option.
                                            type: string
                                        type: object
                                      type: array
                                    messageTimeoutMs:
                                      description: |-
                                        (Number) Maximum time in milliseconds to wait for message delivery confirmation.
                                        Maximum time in milliseconds to wait for message delivery confirmation.
                                      type: number
                                    rateLimitDurationSecs:
                                      description: |-
                                        (Number) Duration in seconds for the rate limit window.
                                        Duration in seconds for the rate limit window.
                                      type: number
                                    rateLimitNum:
                                      description: |-
                                        (Number) Maximum number of messages allowed per rate limit duration.
                                        Maximum number of messages allowed per rate limit duration.
                                      type: number
                                    sasl:
                                      description: |-
                                        (Block List) Specifies the SASL mechanism for authenticating with a Kafka cluster. (see below for nested schema)
                                        Specifies the SASL mechanism for authenticating with a Kafka cluster.
                                      items:
                                        properties:
                                          mechanism:
                                            description: |-
                                              SHA-256, SCRAM-SHA-512.
                                              SASL authentication mechanism. Valid values are `PLAIN`, `SCRAM-SHA-256`, `SCRAM-SHA-512`.
                                            type: string
                                          passwordKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                              Name of the environment variable or secret that holds the SASL password.
                                            type: string
                                          usernameKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                              Name of the environment variable or secret that holds the SASL username.
                                            type: string
                                        type: object
                                      type: array
                                    socketTimeoutMs:
                                      description: |-
                                        (Number) Socket timeout in milliseconds for network requests.
                                        Socket timeout in milliseconds for network requests.
                                      type: number
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                    topic:
                                      description: |-
                                        (String) The Pub/Sub topic name to publish logs to.
                                        The Kafka topic name to publish logs to.
                                      type: string
                                  type: object
                                type: array
                              microsoftSentinel:
                                description: |-
                                  (Block List) The microsoft_sentinel destination forwards logs to Microsoft Sentinel. (see below for nested schema)
                                  The `microsoft_sentinel` destination forwards logs to Microsoft Sentinel.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    clientId:
                                      description: |-
                                        (String) The OAuth client ID used to authenticate with Databricks.
                                        Azure AD client ID used for authentication.
                                      type: string
                                    clientSecretKey:
                                      description: |-
                                        (String) The name of the secret or environment variable holding the OAuth client secret. Defaults to DESTINATION_DATABRICKS_ZEROBUS_OAUTH_CLIENT_SECRET.
                                        Name of the environment variable or secret that holds the Azure AD client secret.
                                      type: string
                                    dceUriKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the Data Collection Endpoint (DCE) URI.
                                        Name of the environment variable or secret that holds the Data Collection Endpoint (DCE) URI.
                                      type: string
                                    dcrImmutableId:
                                      description: |-
                                        (String) The immutable ID of the Data Collection Rule (DCR).
                                        The immutable ID of the Data Collection Rule (DCR).
                                      type: string
                                    table:
                                      description: |-
                                        (String) Target ClickHouse table name.
                                        The name of the Log Analytics table where logs will be sent.
                                      type: string
                                    tenantId:
                                      description: |-
                                        (String) Azure AD tenant ID.
                                        Azure AD tenant ID.
                                      type: string
                                  type: object
                                type: array
                              newRelic:
                                description: |-
                                  (Block List) The new_relic destination sends logs to the New Relic platform. (see below for nested schema)
                                  The `new_relic` destination sends logs to the New Relic platform.
                                items:
                                  properties:
                                    accountIdKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the New Relic account ID.
                                        Name of the environment variable or secret that holds the New Relic account ID.
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    licenseKeyKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the New Relic license key.
                                        Name of the environment variable or secret that holds the New Relic license key.
                                      type: string
                                    region:
                                      description: |-
                                        (String) AWS region of the S3 bucket.
                                        The New Relic region.
                                      type: string
                                  type: object
                                type: array
                              opensearch:
                                description: |-
                                  (Block List) The opensearch destination writes logs to an OpenSearch cluster. (see below for nested schema)
                                  The `opensearch` destination writes logs to an OpenSearch cluster.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        Authentication settings for the OpenSearch destination.
                                      items:
                                        properties:
                                          passwordKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                              Name of the environment variable or secret that holds the OpenSearch password (used when `strategy` is `basic`).
                                            type: string
                                          strategy:
                                            description: |-
                                              (String) The authentication strategy to use (e.g. aws or basic).
                                              The authentication strategy to use. Valid values are `basic`, `aws`.
                                            type: string
                                          usernameKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                              Name of the environment variable or secret that holds the OpenSearch username (used when `strategy` is `basic`).
                                            type: string
                                        type: object
                                      type: array
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    bulkIndex:
                                      description: |-
                                        (String) The index or datastream to write logs to.
                                        The index or datastream to write logs to.
                                      type: string
                                    dataStream:
                                      description: |-
                                        (Block List) Configuration options for writing to Elasticsearch Data Streams instead of a fixed index. (see below for nested schema)
                                        Configuration options for writing to OpenSearch Data Streams instead of a fixed index.
                                      items:
                                        properties:
                                          dataset:
                                            description: |-
                                              (String) The data stream dataset. This groups events by their source or application.
                                              The data stream dataset for your logs. This groups logs by their source or application.
                                            type: string
                                          dtype:
                                            description: |-
                                              (String) The data stream type. This determines how events are categorized within the data stream.
                                              The data stream type for your logs. This determines how logs are categorized within the data stream.
                                            type: string
                                          namespace:
                                            description: |-
                                              (String) The data stream namespace. This separates events into different environments or domains.
                                              The data stream namespace for your logs. This separates logs into different environments or domains.
                                            type: string
                                        type: object
                                      type: array
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the OpenSearch endpoint URL.
                                      type: string
                                  type: object
                                type: array
                              opentelemetry:
                                description: |-
                                  (Block List) The opentelemetry destination forwards metrics using the OpenTelemetry Protocol (OTLP) over HTTP. (see below for nested schema)
                                  The `opentelemetry` destination forwards metrics using the OpenTelemetry Protocol (OTLP) over HTTP.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    httpClientUriKey:
                                      description: |-
                                        (String) Environment variable name containing the URI of the OTLP HTTP endpoint to send metrics to.
                                        Environment variable name containing the URI of the OTLP HTTP endpoint to send metrics to.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              rsyslog:
                                description: |-
                                  (Block List) The rsyslog destination forwards logs to an external rsyslog server over TCP or UDP using the syslog protocol. (see below for nested schema)
                                  The `rsyslog` destination forwards logs to an external `rsyslog` server over TCP or UDP using the syslog protocol.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the rsyslog endpoint URL.
                                      type: string
                                    keepalive:
                                      description: |-
                                        (Number) Optional socket keepalive duration in milliseconds.
                                        Optional socket keepalive duration in milliseconds.
                                      type: number
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              sentinelOne:
                                description: |-
                                  (Block List) The sentinel_one destination sends logs to SentinelOne. (see below for nested schema)
                                  The `sentinel_one` destination sends logs to SentinelOne.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    region:
                                      description: |-
                                        (String) AWS region of the S3 bucket.
                                        The SentinelOne region to send logs to.
                                      type: string
                                    tokenKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the authentication token.
                                        Name of the environment variable or secret that holds the SentinelOne API token.
                                      type: string
                                  type: object
                                type: array
                              socket:
                                description: |-
                                  (Block List) The socket destination sends logs over TCP or UDP to a remote server. (see below for nested schema)
                                  The `socket` destination sends logs over TCP or UDP to a remote server.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the socket address (host:port).
                                      type: string
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        Encoding format for log events. Valid values are `json`, `raw_message`.
                                      type: string
                                    framing:
                                      description: |-
                                        (Block List) Defines the framing method for outgoing messages. (see below for nested schema)
                                        Defines the framing method for outgoing messages.
                                      items:
                                        properties:
                                          characterDelimited:
                                            description: |-
                                              (Block List) Used when method is character_delimited. Specifies the delimiter character. (see below for nested schema)
                                              Used when `method` is `character_delimited`. Specifies the delimiter character.
                                            items:
                                              properties:
                                                delimiter:
                                                  description: |-
                                                    (String) A single ASCII character used as a delimiter.
                                                    A single ASCII character used as a delimiter.
                                                  type: string
                                              type: object
                                            type: array
                                          method:
                                            description: |-
                                              (String) The framing method. Valid values are newline_delimited, bytes, character_delimited.
                                              The framing method. Valid values are `newline_delimited`, `bytes`, `character_delimited`.
                                            type: string
                                        type: object
                                      type: array
                                    mode:
                                      description: |-
                                        (String) The protocol used to send logs. Valid values are tcp, udp.
                                        The protocol used to send logs. Valid values are `tcp`, `udp`.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          serverName:
                                            description: |-
                                              (String) Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                              Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              splunkHec:
                                description: |-
                                  (Block List) The splunk_hec destination forwards logs to Splunk using the HTTP Event Collector (HEC). (see below for nested schema)
                                  The `splunk_hec` destination forwards logs to Splunk using the HTTP Event Collector (HEC).
                                items:
                                  properties:
                                    autoExtractTimestamp:
                                      description: |-
                                        (Boolean) If true, Splunk tries to extract timestamps from incoming log events. If false, Splunk assigns the time the event was received. Only applies when endpoint_target is event; cannot be true when endpoint_target is raw.
                                        If `true`, Splunk tries to extract timestamps from incoming log events. If `false`, Splunk assigns the time the event was received. Only applies when `endpoint_target` is `event`; cannot be `true` when `endpoint_target` is `raw`.
                                      type: boolean
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        Encoding format for log events. Valid values are `json`, `raw_message`.
                                      type: string
                                    endpointTarget:
                                      description: |-
                                        (String) The Splunk HEC endpoint to send events to. Use event to send structured events to the /event endpoint, or raw to send the raw message to the /raw endpoint. Valid values are event, raw.
                                        The Splunk HEC endpoint to send events to. Use `event` to send structured events to the `/event` endpoint, or `raw` to send the raw message to the `/raw` endpoint. Valid values are `event`, `raw`.
                                      type: string
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the Splunk HEC endpoint URL.
                                      type: string
                                    index:
                                      description: |-
                                        (String) Optional name of the Splunk index where logs are written.
                                        Optional name of the Splunk index where logs are written.
                                      type: string
                                    indexedFields:
                                      description: |-
                                        (List of String) List of log field names to send as indexed fields to Splunk HEC. Available only when encoding is json.
                                        List of log field names to send as indexed fields to Splunk HEC. Available only when `encoding` is `json`.
                                      items:
                                        type: string
                                      type: array
                                    sourcetype:
                                      description: |-
                                        (String) The Splunk sourcetype to assign to log events.
                                        The Splunk sourcetype to assign to log events.
                                      type: string
                                    tokenKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the authentication token.
                                        Name of the environment variable or secret that holds the Splunk HEC token.
                                      type: string
                                    tokenStrategy:
                                      description: |-
                                        (String) Controls how the Splunk HEC token is supplied. Use custom to provide a token via token_key, or from_source to forward the token received from an upstream Splunk HEC source. Valid values are custom, from_source.
                                        Controls how the Splunk HEC token is supplied. Use `custom` to provide a token via `token_key`, or `from_source` to forward the token received from an upstream Splunk HEC source. Valid values are `custom`, `from_source`.
                                      type: string
                                  type: object
                                type: array
                              splunkHecMetrics:
                                description: |-
                                  (Block List) The splunk_hec_metrics destination forwards metrics to Splunk using the HTTP Event Collector (HEC). (see below for nested schema)
                                  The `splunk_hec_metrics` destination forwards metrics to Splunk using the HTTP Event Collector (HEC).
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression algorithm applied when sending metrics to Splunk HEC. Valid values are `none`, `gzip`.
                                      type: string
                                    defaultNamespace:
                                      description: |-
                                        (String) Optional default namespace for metrics sent to Splunk HEC.
                                        Optional default namespace for metrics sent to Splunk HEC.
                                      type: string
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the Splunk HEC endpoint URL.
                                      type: string
                                    index:
                                      description: |-
                                        (String) Optional name of the Splunk index where logs are written.
                                        Optional name of the Splunk index where metrics are written.
                                      type: string
                                    source:
                                      description: |-
                                        (Block List) List of sources. (see below for nested schema)
                                        The Splunk source field value for metric events.
                                      type: string
                                    sourcetype:
                                      description: |-
                                        (String) The Splunk sourcetype to assign to log events.
                                        The Splunk sourcetype to assign to metric events.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                    tokenKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the authentication token.
                                        Name of the environment variable or secret that holds the Splunk HEC token.
                                      type: string
                                  type: object
                                type: array
                              sumoLogic:
                                description: |-
                                  (Block List) The sumo_logic destination forwards logs to Sumo Logic. (see below for nested schema)
                                  The `sumo_logic` destination forwards logs to Sumo Logic.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    encoding:
                                      description: |-
                                        (Block List) Encoding format for the destination. (see below for nested schema)
                                        The output encoding format.
                                      type: string
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the Sumo Logic endpoint URL.
                                      type: string
                                    headerCustomField:
                                      description: |-
                                        (Block List) A list of custom headers to include in the request to Sumo Logic. (see below for nested schema)
                                        A list of custom headers to include in the request to Sumo Logic.
                                      items:
                                        properties:
                                          name:
                                            description: |-
                                              (String) The pipeline name.
                                              The header field name.
                                            type: string
                                          value:
                                            description: |-
                                              (String) The metadata value.
                                              The header field value.
                                            type: string
                                        type: object
                                      type: array
                                    headerHostName:
                                      description: |-
                                        (String) Optional override for the host name header.
                                        Optional override for the host name header.
                                      type: string
                                    headerSourceCategory:
                                      description: |-
                                        (String) Optional override for the source category header.
                                        Optional override for the source category header.
                                      type: string
                                    headerSourceName:
                                      description: |-
                                        (String) Optional override for the source name header.
                                        Optional override for the source name header.
                                      type: string
                                  type: object
                                type: array
                              syslogNg:
                                description: |-
                                  ng server over TCP or UDP using the syslog protocol. (see below for nested schema)
                                  The `syslog_ng` destination forwards logs to an external `syslog-ng` server over TCP or UDP using the syslog protocol.
                                items:
                                  properties:
                                    buffer:
                                      description: |-
                                        (Block List) Configuration for buffer settings on destination components. Exactly one of disk or memory must be specified. (see below for nested schema)
                                        Configuration for buffer settings on destination components. Exactly one of `disk` or `memory` must be specified.
                                      items:
                                        properties:
                                          disk:
                                            description: |-
                                              (Block List) Options for configuring a disk buffer. Cannot be used with memory. (see below for nested schema)
                                              Options for configuring a disk buffer. Cannot be used with `memory`.
                                            items:
                                              properties:
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the disk buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                          memory:
                                            description: |-
                                              (Block List) Options for configuring a memory buffer. Cannot be used with disk. (see below for nested schema)
                                              Options for configuring a memory buffer. Cannot be used with `disk`.
                                            items:
                                              properties:
                                                maxEvents:
                                                  description: |-
                                                    (Number) Maximum events for the memory buffer.
                                                    Maximum events for the memory buffer.
                                                  type: number
                                                maxSize:
                                                  description: |-
                                                    (Number) Maximum size of the disk buffer (in bytes).
                                                    Maximum size of the memory buffer (in bytes).
                                                  type: number
                                                whenFull:
                                                  description: |-
                                                    (String) Behavior when the buffer is full. Valid values are block or drop_newest. Defaults to "block".
                                                    Behavior when the buffer is full. Valid values are `block` or `drop_newest`. Defaults to `"block"`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the syslog-ng endpoint URL.
                                      type: string
                                    keepalive:
                                      description: |-
                                        (Number) Optional socket keepalive duration in milliseconds.
                                        Optional socket keepalive duration in milliseconds.
                                      type: number
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          serverName:
                                            description: |-
                                              (String) Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                              Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                            type: object
                          type: array
                        pipelineType:
                          description: |-
                            (String) The type of data being ingested. Defaults to logs if not specified. Valid values are logs, metrics.
                            The type of data being ingested. Defaults to `logs` if not specified. Valid values are `logs`, `metrics`.
                          type: string
                        processorGroup:
                          description: |-
                            (Block List) A processor group containing common configuration and nested processors. (see below for nested schema)
                            A processor group containing common configuration and nested processors.
                          items:
                            properties:
                              displayName:
                                description: |-
                                  friendly name of the processor group.
                                  A human-friendly name of the processor group.
                                type: string
                              enabled:
                                description: |-
                                  (Boolean) Whether this processor group is enabled.
                                  Whether this processor group is enabled.
                                type: boolean
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  The unique ID of the processor group.
                                type: string
                              include:
                                description: |-
                                  (String) A Datadog search query that determines which logs are forwarded using this route.
                                  A Datadog search query used to determine which logs this processor group targets.
                                type: string
                              inputs:
                                description: |-
                                  (List of String) A list of component IDs whose output is used as the input for this component.
                                  A list of component IDs whose output is used as the input for this processor group.
                                items:
                                  type: string
                                type: array
                              processor:
                                description: |-
                                  (Block List) The processor contained in this group. (see below for nested schema)
                                  The processor contained in this group.
                                items:
                                  properties:
                                    addEnvVars:
                                      description: |-
                                        (Block List) The add_env_vars processor adds environment variable values to log events. (see below for nested schema)
                                        The `add_env_vars` processor adds environment variable values to log events.
                                      items:
                                        properties:
                                          variable:
                                            description: |-
                                              (Block List) A list of environment variable mappings to apply to log fields. (see below for nested schema)
                                              A list of environment variable mappings to apply to log fields.
                                            items:
                                              properties:
                                                field:
                                                  description: |-
                                                    (String) The target field in the log event.
                                                    The target field in the log event.
                                                  type: string
                                                name:
                                                  description: |-
                                                    (String) The pipeline name.
                                                    The name of the environment variable to read.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    addFields:
                                      description: |-
                                        value fields to logs. (see below for nested schema)
                                        The `add_fields` processor adds static key-value fields to logs.
                                      items:
                                        properties:
                                          field:
                                            description: |-
                                              (String) The target field in the log event.
                                              A list of static fields (key-value pairs) that is added to each log event processed by this component.
                                            items:
                                              properties:
                                                name:
                                                  description: |-
                                                    (String) The pipeline name.
                                                    The field name to add.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The metadata value.
                                                    The value to assign to the field.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    addHostname:
                                      description: |-
                                        (Block List) The add_hostname processor adds the hostname to log events. (see below for nested schema)
                                        The `add_hostname` processor adds the hostname to log events.
                                      items:
                                        type: object
                                      type: array
                                    addMetricTags:
                                      description: |-
                                        (Block List) The add_metric_tags processor adds static tags to metrics. (see below for nested schema)
                                        The `add_metric_tags` processor adds static tags to metrics.
                                      items:
                                        properties:
                                          tag:
                                            description: |-
                                              (Block List) A list of static tags to add to each metric. Up to 15 tags may be defined. (see below for nested schema)
                                              A list of static tags to add to each metric. Up to 15 tags may be defined.
                                            items:
                                              properties:
                                                name:
                                                  description: |-
                                                    (String) The pipeline name.
                                                    The tag name.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The metadata value.
                                                    The tag value.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    aggregate:
                                      description: |-
                                        (Block List) The aggregate processor combines metrics that share the same name and tags into a single metric over a configurable interval. (see below for nested schema)
                                        The `aggregate` processor combines metrics that share the same name and tags into a single metric over a configurable interval.
                                      items:
                                        properties:
                                          intervalSecs:
                                            description: |-
                                              (Number) The interval, in seconds, over which metrics are aggregated. Must be between 1 and 60. Value must be between 1 and 60.
                                              The interval, in seconds, over which metrics are aggregated. Must be between 1 and 60. Value must be between 1 and 60.
                                            type: number
                                          mode:
                                            description: |-
                                              (String) The protocol used to send logs. Valid values are tcp, udp.
                                              The aggregation mode. One of `auto`, `sum`, `latest`, `count`, `max`, `min`, `mean`. Valid values are `auto`, `sum`, `latest`, `count`, `max`, `min`, `mean`.
                                            type: string
                                        type: object
                                      type: array
                                    customProcessor:
                                      description: |-
                                        (Block List) The custom_processor processor transforms events using Vector Remap Language (VRL) scripts with advanced filtering capabilities. (see below for nested schema)
                                        The `custom_processor` processor transforms events using Vector Remap Language (VRL) scripts with advanced filtering capabilities.
                                      items:
                                        properties:
                                          remap:
                                            description: |-
                                              (Block List) Array of VRL remap configurations. Each remap defines a transformation rule with its own filter and VRL script. (see below for nested schema)
                                              Array of VRL remap configurations. Each remap defines a transformation rule with its own filter and VRL script.
                                            items:
                                              properties:
                                                dropOnError:
                                                  description: |-
                                                    (Boolean) Whether to drop events that cause errors during transformation.
                                                    Whether to drop events that cause errors during transformation.
                                                  type: boolean
                                                enabled:
                                                  description: |-
                                                    (Boolean) Whether this processor group is enabled.
                                                    Whether this remap rule is enabled.
                                                  type: boolean
                                                include:
                                                  description: |-
                                                    (String) A Datadog search query that determines which logs are forwarded using this route.
                                                    A Datadog search query used to filter events for this specific remap rule.
                                                  type: string
                                                name:
                                                  description: |-
                                                    (String) The pipeline name.
                                                    A descriptive name for this remap rule.
                                                  type: string
                                                source:
                                                  description: |-
                                                    (Block List) List of sources. (see below for nested schema)
                                                    The VRL script source code that defines the transformation logic.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    datadogTags:
                                      description: (Block List) (see below for nested
                                        schema)
                                      items:
                                        properties:
                                          action:
                                            description: |-
                                              (String) Valid values are include, exclude.
                                              Valid values are `include`, `exclude`.
                                            type: string
                                          keys:
                                            description: (List of String)
                                            items:
                                              type: string
                                            type: array
                                          mode:
                                            description: |-
                                              (String) The protocol used to send logs. Valid values are tcp, udp.
                                              Valid values are `filter`.
                                            type: string
                                        type: object
                                      type: array
                                    dedupe:
                                      description: |-
                                        (Block List) The dedupe processor removes duplicate fields in log events. (see below for nested schema)
                                        The `dedupe` processor removes duplicate fields in log events.
                                      items:
                                        properties:
                                          fields:
                                            description: |-
                                              (List of String) A list of log field paths to check for duplicates.
                                              A list of log field paths to check for duplicates.
                                            items:
                                              type: string
                                            type: array
                                          mode:
                                            description: |-
                                              (String) The protocol used to send logs. Valid values are tcp, udp.
                                              The deduplication mode to apply to the fields.
                                            type: string
                                        type: object
                                      type: array
                                    displayName:
                                      description: |-
                                        friendly name of the processor group.
                                        A human-friendly name for this processor.
                                      type: string
                                    enabled:
                                      description: |-
                                        (Boolean) Whether this processor group is enabled.
                                        Whether this processor is enabled.
                                      type: boolean
                                    enrichmentTable:
                                      description: |-
                                        (Block List) The enrichment_table processor enriches logs using a static CSV file or GeoIP database. (see below for nested schema)
                                        The `enrichment_table` processor enriches logs using a static CSV file or GeoIP database.
                                      items:
                                        properties:
                                          file:
                                            description: |-
                                              (Block List) Defines a static enrichment table loaded from a CSV file. (see below for nested schema)
                                              Defines a static enrichment table loaded from a CSV file.
                                            items:
                                              properties:
                                                encoding:
                                                  description: (Block List) Encoding
                                                    format for the destination. (see
                                                    below for nested schema)
                                                  items:
                                                    properties:
                                                      delimiter:
                                                        description: |-
                                                          (String) A single ASCII character used as a delimiter.
                                                          The `encoding` `delimiter`.
                                                        type: string
                                                      includesHeaders:
                                                        description: |-
                                                          (Boolean) The encoding includes_headers.
                                                          The `encoding` `includes_headers`.
                                                        type: boolean
                                                      type:
                                                        description: |-
                                                          (String) The encoding type. Valid values are json, parquet.
                                                          File encoding format.
                                                        type: string
                                                    type: object
                                                  type: array
                                                key:
                                                  description: |-
                                                    (Block List) Key fields used to look up enrichment values. (see below for nested schema)
                                                    Key fields used to look up enrichment values.
                                                  items:
                                                    properties:
                                                      column:
                                                        description: |-
                                                          (String) The items column.
                                                          The `items` `column`.
                                                        type: string
                                                      comparison:
                                                        description: |-
                                                          (String) The comparison method (e.g. equals).
                                                          The comparison method (e.g. equals).
                                                        type: string
                                                      field:
                                                        description: |-
                                                          (String) The target field in the log event.
                                                          Specifies the source of the key value for enrichment table lookups. Set exactly one of `string_path`, `event`, `vrl`, or `secret`.
                                                        items:
                                                          properties:
                                                            event:
                                                              description: |-
                                                                (String) The path to the field in the log event to use as the lookup key.
                                                                The path to the field in the log event to use as the lookup key.
                                                              type: string
                                                            secret:
                                                              description: |-
                                                                (String) The name of the secret containing the lookup key value.
                                                                The name of the secret containing the lookup key value.
                                                              type: string
                                                            stringPath:
                                                              description: |-
                                                                (String) A plain field path in the log event (for example, log.user.id).
                                                                A plain field path in the log event (for example, `log.user.id`).
                                                              type: string
                                                            vrl:
                                                              description: |-
                                                                (String) A VRL expression that returns the value to use as the lookup key.
                                                                A VRL expression that returns the value to use as the lookup key.
                                                              type: string
                                                          type: object
                                                        type: array
                                                    type: object
                                                  type: array
                                                path:
                                                  description: |-
                                                    (String) Path to the CSV file.
                                                    Path to the CSV file.
                                                  type: string
                                              type: object
                                            type: array
                                          geoip:
                                            description: |-
                                              (Block List) Uses a GeoIP database to enrich logs based on an IP field. (see below for nested schema)
                                              Uses a GeoIP database to enrich logs based on an IP field.
                                            items:
                                              properties:
                                                keyField:
                                                  description: |-
                                                    (String) The field name to use as the Kafka message key.
                                                    Path to the IP field in the log.
                                                  type: string
                                                locale:
                                                  description: |-
                                                    (String) Locale used to resolve geographical names.
                                                    Locale used to resolve geographical names.
                                                  type: string
                                                path:
                                                  description: |-
                                                    (String) Path to the CSV file.
                                                    Path to the GeoIP database file.
                                                  type: string
                                              type: object
                                            type: array
                                          referenceTable:
                                            description: |-
                                              (Block List) Uses a Datadog reference table to enrich logs. (see below for nested schema)
                                              Uses a Datadog reference table to enrich logs.
                                            items:
                                              properties:
                                                appKeyKey:
                                                  description: |-
                                                    (String) Name of the environment variable or secret that holds the Datadog application key for the reference table.
                                                    Name of the environment variable or secret that holds the Datadog application key for the reference table.
                                                  type: string
                                                columns:
                                                  description: |-
                                                    (List of String) List of column names to include from the reference table. If not provided, all columns are included.
                                                    List of column names to include from the reference table. If not provided, all columns are included.
                                                  items:
                                                    type: string
                                                  type: array
                                                keyField:
                                                  description: |-
                                                    (String) The field name to use as the Kafka message key.
                                                    Path to the field in the log event to match against the reference table.
                                                  type: string
                                                tableId:
                                                  description: |-
                                                    (String) The unique identifier of the reference table.
                                                    The unique identifier of the reference table.
                                                  type: string
                                              type: object
                                            type: array
                                          target:
                                            description: |-
                                              (String) Path where enrichment results should be stored in the log.
                                              Path where enrichment results should be stored in the log.
                                            type: string
                                        type: object
                                      type: array
                                    filter:
                                      description: |-
                                        (Block List) The filter processor allows conditional processing of logs based on a Datadog search query. Logs that match the include query are passed through; others are discarded. (see below for nested schema)
                                        The `filter` processor allows conditional processing of logs based on a Datadog search query. Logs that match the `include` query are passed through; others are discarded.
                                      items:
                                        type: object
                                      type: array
                                    generateDatadogMetrics:
                                      description: |-
                                        (Block List) The generate_datadog_metrics processor creates custom metrics from logs. Metrics can be counters, gauges, or distributions and optionally grouped by log fields. (see below for nested schema)
                                        The `generate_datadog_metrics` processor creates custom metrics from logs. Metrics can be counters, gauges, or distributions and optionally grouped by log fields.
                                      items:
                                        properties:
                                          metric:
                                            description: |-
                                              (Block List) Configuration for generating individual metrics. (see below for nested schema)
                                              Configuration for generating individual metrics.
                                            items:
                                              properties:
                                                groupBy:
                                                  description: |-
                                                    (List of String) Optional fields used to group the metric series.
                                                    Optional fields used to group the metric series.
                                                  items:
                                                    type: string
                                                  type: array
                                                include:
                                                  description: |-
                                                    (String) A Datadog search query that determines which logs are forwarded using this route.
                                                    Datadog filter query to match logs for metric generation.
                                                  type: string
                                                metricType:
                                                  description: |-
                                                    (String) Type of metric to create.
                                                    Type of metric to create.
                                                  type: string
                                                name:
                                                  description: |-
                                                    (String) The pipeline name.
                                                    Name of the custom metric to be created.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The metadata value.
                                                    Specifies how the value of the generated metric is computed.
                                                  items:
                                                    properties:
                                                      field:
                                                        description: |-
                                                          (String) The target field in the log event.
                                                          Name of the log field containing the numeric value to increment the metric by (used only for `increment_by_field`).
                                                        type: string
                                                      strategy:
                                                        description: |-
                                                          (String) The authentication strategy to use (e.g. aws or basic).
                                                          Metric value strategy: `increment_by_one` or `increment_by_field`.
                                                        type: string
                                                    type: object
                                                  type: array
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    generateMetrics:
                                      description: |-
                                        id>.metrics. (see below for nested schema)
                                        The `generate_metrics` processor creates custom metrics from logs. The generated metrics must be routed to a metrics destination using the input `<processor-id>.metrics`.
                                      items:
                                        properties:
                                          metric:
                                            description: |-
                                              (Block List) Configuration for generating individual metrics. (see below for nested schema)
                                              Configuration for generating individual metrics.
                                            items:
                                              properties:
                                                groupBy:
                                                  description: |-
                                                    (List of String) Optional fields used to group the metric series.
                                                    Optional fields used to group the metric series.
                                                  items:
                                                    type: string
                                                  type: array
                                                include:
                                                  description: |-
                                                    (String) A Datadog search query that determines which logs are forwarded using this route.
                                                    Datadog filter query to match logs for metric generation.
                                                  type: string
                                                metricType:
                                                  description: |-
                                                    (String) Type of metric to create.
                                                    Type of metric to create.
                                                  type: string
                                                name:
                                                  description: |-
                                                    (String) The pipeline name.
                                                    Name of the custom metric to be created.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The metadata value.
                                                    Specifies how the value of the generated metric is computed.
                                                  items:
                                                    properties:
                                                      field:
                                                        description: |-
                                                          (String) The target field in the log event.
                                                          Name of the log field containing the numeric value to increment the metric by (used only for `increment_by_field`).
                                                        type: string
                                                      strategy:
                                                        description: |-
                                                          (String) The authentication strategy to use (e.g. aws or basic).
                                                          Metric value strategy: `increment_by_one` or `increment_by_field`.
                                                        type: string
                                                    type: object
                                                  type: array
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    id:
                                      description: |-
                                        (String) The ID of this resource.
                                        The unique identifier for this processor.
                                      type: string
                                    include:
                                      description: |-
                                        (String) A Datadog search query that determines which logs are forwarded using this route.
                                        A Datadog search query used to determine which logs this processor targets.
                                      type: string
                                    metricTags:
                                      description: |-
                                        (Block List) The metric_tags processor filters metrics based on their tags using Datadog tag key patterns. (see below for nested schema)
                                        The `metric_tags` processor filters metrics based on their tags using Datadog tag key patterns.
                                      items:
                                        properties:
                                          rule:
                                            description: |-
                                              (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                              A list of rules for filtering metric tags.
                                            items:
                                              properties:
                                                action:
                                                  description: |-
                                                    (String) Valid values are include, exclude.
                                                    The action to take on tags with matching keys. Valid values are `include`, `exclude`.
                                                  type: string
                                                include:
                                                  description: |-
                                                    (String) A Datadog search query that determines which logs are forwarded using this route.
                                                    A Datadog search query used to determine which metrics this rule targets.
                                                  type: string
                                                keys:
                                                  description: |-
                                                    (List of String)
                                                    A list of tag keys to include or exclude.
                                                  items:
                                                    type: string
                                                  type: array
                                                mode:
                                                  description: |-
                                                    (String) The protocol used to send logs. Valid values are tcp, udp.
                                                    The processing mode for tag filtering. Valid values are `filter`.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    ocsfMapper:
                                      description: |-
                                        (Block List) The ocsf_mapper processor transforms logs into the OCSF schema using predefined library mappings or custom mapping configuration. (see below for nested schema)
                                        The `ocsf_mapper` processor transforms logs into the OCSF schema using predefined library mappings or custom mapping configuration.
                                      items:
                                        properties:
                                          keepUnmatched:
                                            description: |-
                                              (Boolean) Whether to keep an event that does not match any of the mapping filters.
                                              Whether to keep an event that does not match any of the mapping filters.
                                            type: boolean
                                          mapping:
                                            description: |-
                                              (Block List) List of OCSF mapping entries. Each entry uses either a library mapping or a custom mapping. (see below for nested schema)
                                              List of OCSF mapping entries. Each entry uses either a library mapping or a custom mapping.
                                            items:
                                              properties:
                                                customMapping:
                                                  description: |-
                                                    (Block List) Custom OCSF mapping configuration for transforming logs. (see below for nested schema)
                                                    Custom OCSF mapping configuration for transforming logs.
                                                  items:
                                                    properties:
                                                      mapping:
                                                        description: |-
                                                          (Block List) List of OCSF mapping entries. Each entry uses either a library mapping or a custom mapping. (see below for nested schema)
                                                          A list of field mapping rules for transforming log fields to OCSF schema fields.
                                                        items:
                                                          properties:
                                                            default:
                                                              description: |-
                                                                (String) The default value to use if the source field is missing or empty.
                                                                The default value to use if the source field is missing or empty.
                                                              type: string
                                                            dest:
                                                              description: |-
                                                                (String) The destination OCSF field path.
                                                                The destination OCSF field path.
                                                              type: string
                                                            lookup:
                                                              description: |-
                                                                (Block List) Lookup table configuration for mapping source values to destination values. (see below for nested schema)
                                                                Lookup table configuration for mapping source values to destination values.
                                                              items:
                                                                properties:
                                                                  default:
                                                                    description: |-
                                                                      (String) The default value to use if the source field is missing or empty.
                                                                      The default value to use if no lookup match is found.
                                                                    type: string
                                                                  table:
                                                                    description: |-
                                                                      (String) Target ClickHouse table name.
                                                                      A list of lookup table entries for value transformation.
                                                                    items:
                                                                      properties:
                                                                        contains:
                                                                          description: |-
                                                                            (String) The substring to match in the source value.
                                                                            The substring to match in the source value.
                                                                          type: string
                                                                        equals:
                                                                          description: |-
                                                                            (String) The exact value to match in the source.
                                                                            The exact value to match in the source.
                                                                          type: string
                                                                        equalsSource:
                                                                          description: |-
                                                                            (String) The source field to match against.
                                                                            The source field to match against.
                                                                          type: string
                                                                        matches:
                                                                          description: |-
                                                                            (String) A regex pattern to match in the source value.
                                                                            A regex pattern to match in the source value.
                                                                          type: string
                                                                        notMatches:
                                                                          description: |-
                                                                            (String) A regex pattern that must not match the source value.
                                                                            A regex pattern that must not match the source value.
                                                                          type: string
                                                                        value:
                                                                          description: |-
                                                                            (String) The metadata value.
                                                                            The value to use when a match is found.
                                                                          type: string
                                                                      type: object
                                                                    type: array
                                                                type: object
                                                              type: array
                                                            source:
                                                              description: |-
                                                                (Block List) List of sources. (see below for nested schema)
                                                                The source field path from the log event.
                                                              type: string
                                                            sources:
                                                              description: |-
                                                                (List of String) Multiple source field paths for combined mapping.
                                                                Multiple source field paths for combined mapping.
                                                              items:
                                                                type: string
                                                              type: array
                                                            value:
                                                              description: |-
                                                                (String) The metadata value.
                                                                A static value to use for the destination field.
                                                              type: string
                                                          type: object
                                                        type: array
                                                      metadata:
                                                        description: |-
                                                          value pairs added to each object. (see below for nested schema)
                                                          Metadata for the custom OCSF mapping.
                                                        items:
                                                          properties:
                                                            class:
                                                              description: |-
                                                                (String) The OCSF event class name.
                                                                The OCSF event class name.
                                                              type: string
                                                            profiles:
                                                              description: |-
                                                                (List of String) A list of OCSF profiles to apply.
                                                                A list of OCSF profiles to apply.
                                                              items:
                                                                type: string
                                                              type: array
                                                            version:
                                                              description: |-
                                                                (Number) The version of the custom mapping configuration.
                                                                The OCSF schema version.
                                                              type: string
                                                          type: object
                                                        type: array
                                                      version:
                                                        description: |-
                                                          (Number) The version of the custom mapping configuration.
                                                          The version of the custom mapping configuration.
                                                        type: number
                                                    type: object
                                                  type: array
                                                include:
                                                  description: |-
                                                    (String) A Datadog search query that determines which logs are forwarded using this route.
                                                    Search query for selecting which logs the mapping applies to.
                                                  type: string
                                                libraryMapping:
                                                  description: |-
                                                    (String) Predefined library mapping for log transformation. Use this or custom_mapping, not both.
                                                    Predefined library mapping for log transformation. Use this or custom_mapping, not both.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    parseGrok:
                                      description: |-
                                        (Block List) The parse_grok processor extracts structured fields from unstructured log messages using Grok patterns. (see below for nested schema)
                                        The `parse_grok` processor extracts structured fields from unstructured log messages using Grok patterns.
                                      items:
                                        properties:
                                          disableLibraryRules:
                                            description: |-
                                              (Boolean) If set to true, disables the default Grok rules provided by Datadog. Defaults to false.
                                              If set to `true`, disables the default Grok rules provided by Datadog. Defaults to `false`.
                                            type: boolean
                                          field:
                                            description: |-
                                              (String) The target field in the log event.
                                              The log field to parse with the Grok rules. Defaults to `"message"`.
                                            type: string
                                          includeRule:
                                            description: |-
                                              (Block List) A Grok parsing rule that targets logs matching a Datadog search query. (see below for nested schema)
                                              A Grok parsing rule that targets logs matching a Datadog search query.
                                            items:
                                              properties:
                                                include:
                                                  description: |-
                                                    (String) A Datadog search query that determines which logs are forwarded using this route.
                                                    A Datadog search query used to determine which logs this Grok rule targets.
                                                  type: string
                                                matchRule:
                                                  description: |-
                                                    (Block List) A list of Grok parsing rules that define how to extract fields. Each rule must contain a name and a valid Grok pattern. (see below for nested schema)
                                                    A list of Grok parsing rules that define how to extract fields. Each rule must contain a name and a valid Grok pattern.
                                                  items:
                                                    properties:
                                                      name:
                                                        description: |-
                                                          (String) The pipeline name.
                                                          The name of the rule.
                                                        type: string
                                                      rule:
                                                        description: |-
                                                          (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                                          The definition of the Grok rule.
                                                        type: string
                                                    type: object
                                                  type: array
                                                supportRule:
                                                  description: |-
                                                    (Block List) A list of helper Grok rules that can be referenced by the parsing rules. (see below for nested schema)
                                                    A list of helper Grok rules that can be referenced by the parsing rules.
                                                  items:
                                                    properties:
                                                      name:
                                                        description: |-
                                                          (String) The pipeline name.
                                                          The name of the helper Grok rule.
                                                        type: string
                                                      rule:
                                                        description: |-
                                                          (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                                          The definition of the helper Grok rule.
                                                        type: string
                                                    type: object
                                                  type: array
                                              type: object
                                            type: array
                                          rule:
                                            description: |-
                                              (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                              The list of Grok parsing rules. If multiple parsing rules are provided, they are evaluated in order. The first successful match is applied.
                                            items:
                                              properties:
                                                matchRule:
                                                  description: |-
                                                    (Block List) A list of Grok parsing rules that define how to extract fields. Each rule must contain a name and a valid Grok pattern. (see below for nested schema)
                                                    A list of Grok parsing rules that define how to extract fields. Each rule must contain a name and a valid Grok pattern.
                                                  items:
                                                    properties:
                                                      name:
                                                        description: |-
                                                          (String) The pipeline name.
                                                          The name of the rule.
                                                        type: string
                                                      rule:
                                                        description: |-
                                                          (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                                          The definition of the Grok rule.
                                                        type: string
                                                    type: object
                                                  type: array
                                                source:
                                                  description: |-
                                                    (Block List) List of sources. (see below for nested schema)
                                                    The value of the source field in log events which should be processed by the Grok rules.
                                                  type: string
                                                supportRule:
                                                  description: |-
                                                    (Block List) A list of helper Grok rules that can be referenced by the parsing rules. (see below for nested schema)
                                                    A list of helper Grok rules that can be referenced by the parsing rules.
                                                  items:
                                                    properties:
                                                      name:
                                                        description: |-
                                                          (String) The pipeline name.
                                                          The name of the helper Grok rule.
                                                        type: string
                                                      rule:
                                                        description: |-
                                                          (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                                          The definition of the helper Grok rule.
                                                        type: string
                                                    type: object
                                                  type: array
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    parseJson:
                                      description: |-
                                        (Block List) The parse_json processor extracts JSON from a specified field and flattens it into the event. This is useful when logs contain embedded JSON as a string. (see below for nested schema)
                                        The `parse_json` processor extracts JSON from a specified field and flattens it into the event. This is useful when logs contain embedded JSON as a string.
                                      items:
                                        properties:
                                          field:
                                            description: |-
                                              (String) The target field in the log event.
                                              The field to parse.
                                            type: string
                                        type: object
                                      type: array
                                    parseXml:
                                      description: |-
                                        (Block List) The parse_xml processor parses XML from a specified field and extracts it into the event. (see below for nested schema)
                                        The `parse_xml` processor parses XML from a specified field and extracts it into the event.
                                      items:
                                        properties:
                                          alwaysUseTextKey:
                                            description: |-
                                              (Boolean) Whether to always store text inside an object using the text key even when no attributes exist.
                                              Whether to always store text inside an object using the text key even when no attributes exist.
                                            type: boolean
                                          attrPrefix:
                                            description: |-
                                              (String) The prefix to use for XML attributes in the parsed output. If the field is left empty, the original attribute key is used.
                                              The prefix to use for XML attributes in the parsed output. If the field is left empty, the original attribute key is used.
                                            type: string
                                          field:
                                            description: |-
                                              (String) The target field in the log event.
                                              The path to the log field on which you want to parse XML.
                                            type: string
                                          includeAttr:
                                            description: |-
                                              (Boolean) Whether to include XML attributes in the parsed output.
                                              Whether to include XML attributes in the parsed output.
                                            type: boolean
                                          parseBool:
                                            description: |-
                                              (Boolean) Whether to parse boolean values from strings.
                                              Whether to parse boolean values from strings.
                                            type: boolean
                                          parseNull:
                                            description: |-
                                              (Boolean) Whether to parse null values.
                                              Whether to parse null values.
                                            type: boolean
                                          parseNumber:
                                            description: |-
                                              (Boolean) Whether to parse numeric values from strings.
                                              Whether to parse numeric values from strings.
                                            type: boolean
                                          textKey:
                                            description: |-
                                              (String) The key name to use for the text node when XML attributes are appended.
                                              The key name to use for the text node when XML attributes are appended.
                                            type: string
                                        type: object
                                      type: array
                                    quota:
                                      description: |-
                                        (Block List) The quota processor measures logging traffic for logs that match a specified filter. When the configured daily quota is met, the processor can drop or alert. (see below for nested schema)
                                        The `quota` processor measures logging traffic for logs that match a specified filter. When the configured daily quota is met, the processor can drop or alert.
                                      items:
                                        properties:
                                          dropEvents:
                                            description: |-
                                              (Boolean) Whether to drop events exceeding the limit.
                                              Whether to drop events exceeding the limit.
                                            type: boolean
                                          ignoreWhenMissingPartitions:
                                            description: |-
                                              (Boolean) Whether to ignore when partition fields are missing.
                                              Whether to ignore when partition fields are missing.
                                            type: boolean
                                          limit:
                                            description: (Block List) (see below for
                                              nested schema)
                                            items:
                                              properties:
                                                enforce:
                                                  description: |-
                                                    (String) Whether to enforce by 'bytes' or 'events'. Valid values are bytes, events.
                                                    Whether to enforce by 'bytes' or 'events'. Valid values are `bytes`, `events`.
                                                  type: string
                                                limit:
                                                  description: |-
                                                    (Block List) (see below for nested schema)
                                                    The daily quota limit.
                                                  type: number
                                              type: object
                                            type: array
                                          name:
                                            description: |-
                                              (String) The pipeline name.
                                              The name of the quota.
                                            type: string
                                          overflowAction:
                                            description: |-
                                              (String) The action to take when the quota is exceeded: drop, no_action, or overflow_routing.
                                              The action to take when the quota is exceeded: `drop`, `no_action`, or `overflow_routing`.
                                            type: string
                                          override:
                                            description: |-
                                              specific quotas. (see below for nested schema)
                                              The overrides for field-specific quotas.
                                            items:
                                              properties:
                                                field:
                                                  description: |-
                                                    (String) The target field in the log event.
                                                    Fields that trigger this override.
                                                  items:
                                                    properties:
                                                      name:
                                                        description: |-
                                                          (String) The pipeline name.
                                                          The field name.
                                                        type: string
                                                      value:
                                                        description: |-
                                                          (String) The metadata value.
                                                          The field value.
                                                        type: string
                                                    type: object
                                                  type: array
                                                limit:
                                                  description: (Block List) (see below
                                                    for nested schema)
                                                  items:
                                                    properties:
                                                      enforce:
                                                        description: |-
                                                          (String) Whether to enforce by 'bytes' or 'events'. Valid values are bytes, events.
                                                          Whether to enforce by 'bytes' or 'events'. Valid values are `bytes`, `events`.
                                                        type: string
                                                      limit:
                                                        description: |-
                                                          (Block List) (see below for nested schema)
                                                          The daily quota limit.
                                                        type: number
                                                    type: object
                                                  type: array
                                              type: object
                                            type: array
                                          partitionFields:
                                            description: |-
                                              (List of String) List of partition fields.
                                              List of partition fields.
                                            items:
                                              type: string
                                            type: array
                                          tooManyBucketsAction:
                                            description: |-
                                              (String) The action to take when the max number of buckets is exceeded: drop, no_action, or overflow_routing.
                                              The action to take when the max number of buckets is exceeded: `drop`, `no_action`, or `overflow_routing`.
                                            type: string
                                        type: object
                                      type: array
                                    reduce:
                                      description: |-
                                        (Block List) The reduce processor aggregates and merges logs based on matching keys and merge strategies. (see below for nested schema)
                                        The `reduce` processor aggregates and merges logs based on matching keys and merge strategies.
                                      items:
                                        properties:
                                          groupBy:
                                            description: |-
                                              (List of String) Optional fields used to group the metric series.
                                              A list of fields used to group log events for merging.
                                            items:
                                              type: string
                                            type: array
                                          mergeStrategy:
                                            description: |-
                                              (Block List) List of merge strategies defining how values from grouped events should be combined. (see below for nested schema)
                                              List of merge strategies defining how values from grouped events should be combined.
                                            items:
                                              properties:
                                                path:
                                                  description: |-
                                                    (String) Path to the CSV file.
                                                    The field path in the log event.
                                                  type: string
                                                strategy:
                                                  description: |-
                                                    (String) The authentication strategy to use (e.g. aws or basic).
                                                    The merge strategy to apply.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    removeFields:
                                      description: |-
                                        (Block List) The remove_fields processor deletes specified fields from logs. (see below for nested schema)
                                        The `remove_fields` processor deletes specified fields from logs.
                                      items:
                                        properties:
                                          fields:
                                            description: |-
                                              (List of String) A list of log field paths to check for duplicates.
                                              List of fields to remove from the events.
                                            items:
                                              type: string
                                            type: array
                                        type: object
                                      type: array
                                    renameFields:
                                      description: |-
                                        (Block List) The rename_fields processor changes field names. (see below for nested schema)
                                        The `rename_fields` processor changes field names.
                                      items:
                                        properties:
                                          field:
                                            description: |-
                                              (String) The target field in the log event.
                                              List of fields to rename.
                                            items:
                                              properties:
                                                destination:
                                                  description: |-
                                                    (Block List) List of destinations. (see below for nested schema)
                                                    Destination field name.
                                                  type: string
                                                preserveSource:
                                                  description: |-
                                                    (Boolean) Whether to keep the original field.
                                                    Whether to keep the original field.
                                                  type: boolean
                                                source:
                                                  description: |-
                                                    (Block List) List of sources. (see below for nested schema)
                                                    Source field to rename.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    renameMetricTags:
                                      description: |-
                                        (Block List) The rename_metric_tags processor changes the keys of tags on metrics. (see below for nested schema)
                                        The `rename_metric_tags` processor changes the keys of tags on metrics.
                                      items:
                                        properties:
                                          tag:
                                            description: |-
                                              (Block List) A list of static tags to add to each metric. Up to 15 tags may be defined. (see below for nested schema)
                                              A list of rename rules. Up to 15 tags may be defined.
                                            items:
                                              properties:
                                                renameTo:
                                                  description: |-
                                                    (String) The new tag key to assign in place of the original.
                                                    The new tag key to assign in place of the original.
                                                  type: string
                                                tag:
                                                  description: |-
                                                    (Block List) A list of static tags to add to each metric. Up to 15 tags may be defined. (see below for nested schema)
                                                    The original tag key on the metric event.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    sample:
                                      description: |-
                                        (Block List) The sample processor allows probabilistic sampling of logs at a fixed rate. (see below for nested schema)
                                        The `sample` processor allows probabilistic sampling of logs at a fixed rate.
                                      items:
                                        properties:
                                          groupBy:
                                            description: |-
                                              (List of String) Optional fields used to group the metric series.
                                              Optional list of fields to group events by. Each group is sampled independently.
                                            items:
                                              type: string
                                            type: array
                                          percentage:
                                            description: |-
                                              (Number) The percentage of logs to sample.
                                              The percentage of logs to sample.
                                            type: number
                                        type: object
                                      type: array
                                    sensitiveDataScanner:
                                      description: |-
                                        (Block List) The sensitive_data_scanner processor detects and optionally redacts sensitive data in log events. (see below for nested schema)
                                        The `sensitive_data_scanner` processor detects and optionally redacts sensitive data in log events.
                                      items:
                                        properties:
                                          rule:
                                            description: |-
                                              (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                              A list of rules for identifying and acting on sensitive data patterns.
                                            items:
                                              properties:
                                                keywordOptions:
                                                  description: |-
                                                    based proximity matching for sensitive data. (see below for nested schema)
                                                    Keyword-based proximity matching for sensitive data.
                                                  items:
                                                    properties:
                                                      keywords:
                                                        description: |-
                                                          (List of String) A list of keywords to match near the sensitive pattern.
                                                          A list of keywords to match near the sensitive pattern.
                                                        items:
                                                          type: string
                                                        type: array
                                                      proximity:
                                                        description: |-
                                                          (Number) Maximum number of tokens between a keyword and a sensitive value match.
                                                          Maximum number of tokens between a keyword and a sensitive value match.
                                                        type: number
                                                    type: object
                                                  type: array
                                                name:
                                                  description: |-
                                                    (String) The pipeline name.
                                                    A name identifying the rule.
                                                  type: string
                                                onMatch:
                                                  description: |-
                                                    (Block List) The action to take when a sensitive value is found. (see below for nested schema)
                                                    The action to take when a sensitive value is found.
                                                  items:
                                                    properties:
                                                      hash:
                                                        description: |-
                                                          (Block List) Hashes the matched value. (see below for nested schema)
                                                          Hashes the matched value.
                                                        items:
                                                          type: object
                                                        type: array
                                                      partialRedact:
                                                        description: |-
                                                          (Block List) Redacts part of the matched value (e.g., keep last 4 characters). (see below for nested schema)
                                                          Redacts part of the matched value (e.g., keep last 4 characters).
                                                        items:
                                                          properties:
                                                            characters:
                                                              description: |-
                                                                (Number) Number of characters to keep.
                                                                Number of characters to keep.
                                                              type: number
                                                            direction:
                                                              description: |-
                                                                (String) Direction from which to keep characters: first or last.
                                                                Direction from which to keep characters: `first` or `last`.
                                                              type: string
                                                          type: object
                                                        type: array
                                                      redact:
                                                        description: |-
                                                          (Block List) Redacts the matched value. Exactly one of redact, hash, or partial_redact must be specified. (see below for nested schema)
                                                          Redacts the matched value. Exactly one of `redact`, `hash`, or `partial_redact` must be specified.
                                                        items:
                                                          properties:
                                                            replace:
                                                              description: |-
                                                                (String) Replacement string for redacted values (e.g., ***).
                                                                Replacement string for redacted values (e.g., `***`).
                                                              type: string
                                                          type: object
                                                        type: array
                                                    type: object
                                                  type: array
                                                pattern:
                                                  description: |-
                                                    (Block List) Pattern detection configuration for identifying sensitive data using either a custom regex or a library reference. (see below for nested schema)
                                                    Pattern detection configuration for identifying sensitive data using either a custom regex or a library reference.
                                                  items:
                                                    properties:
                                                      custom:
                                                        description: |-
                                                          (Block List) Pattern detection using a custom regular expression. Exactly one of custom or library must be specified. (see below for nested schema)
                                                          Pattern detection using a custom regular expression. Exactly one of `custom` or `library` must be specified.
                                                        items:
                                                          properties:
                                                            description:
                                                              description: |-
                                                                readable description providing context about a sensitive data scanner rule.
                                                                Human-readable description providing context about a sensitive data scanner rule.
                                                              type: string
                                                            rule:
                                                              description: |-
                                                                (Block List) A list of rules for filtering metric tags. (see below for nested schema)
                                                                A regular expression used to detect sensitive values. Must be a valid regex.
                                                              type: string
                                                          type: object
                                                        type: array
                                                      library:
                                                        description: |-
                                                          (Block List) Pattern detection using a predefined pattern from the Sensitive Data Scanner library. (see below for nested schema)
                                                          Pattern detection using a predefined pattern from the Sensitive Data Scanner library.datadoghq.
                                                        items:
                                                          properties:
                                                            description:
                                                              description: |-
                                                                readable description providing context about a sensitive data scanner rule.
                                                                Human-readable description providing context about a sensitive data scanner rule.
                                                              type: string
                                                            id:
                                                              description: |-
                                                                (String) The ID of this resource.
                                                                Identifier for a predefined pattern from the sensitive data scanner pattern library.
                                                              type: string
                                                            useRecommendedKeywords:
                                                              description: |-
                                                                (Boolean) Whether to augment the pattern with recommended keywords .
                                                                Whether to augment the pattern with recommended keywords (optional).
                                                              type: boolean
                                                          type: object
                                                        type: array
                                                    type: object
                                                  type: array
                                                scope:
                                                  description: |-
                                                    level targeting options that determine where the scanner should operate. (see below for nested schema)
                                                    Field-level targeting options that determine where the scanner should operate.
                                                  items:
                                                    properties:
                                                      all:
                                                        description: |-
                                                          (Boolean) Scan all fields.
                                                          Scan all fields.
                                                        type: boolean
                                                      exclude:
                                                        description: |-
                                                          (Block List) Explicitly exclude these fields from scanning. (see below for nested schema)
                                                          Explicitly exclude these fields from scanning.
                                                        items:
                                                          properties:
                                                            fields:
                                                              description: |-
                                                                (List of String) A list of log field paths to check for duplicates.
                                                                The fields to exclude from scanning.
                                                              items:
                                                                type: string
                                                              type: array
                                                          type: object
                                                        type: array
                                                      include:
                                                        description: |-
                                                          (String) A Datadog search query that determines which logs are forwarded using this route.
                                                          Explicitly include these fields for scanning. Exactly one of `include`, `exclude`, or `all` must be specified.
                                                        items:
                                                          properties:
                                                            fields:
                                                              description: |-
                                                                (List of String) A list of log field paths to check for duplicates.
                                                                The fields to include in scanning.
                                                              items:
                                                                type: string
                                                              type: array
                                                          type: object
                                                        type: array
                                                    type: object
                                                  type: array
                                                tags:
                                                  description: |-
                                                    (List of String) Tags assigned to this rule for filtering and classification.
                                                    Tags assigned to this rule for filtering and classification.
                                                  items:
                                                    type: string
                                                  type: array
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    splitArray:
                                      description: |-
                                        (Block List) The split_array processor splits array fields into separate events based on configured rules. (see below for nested schema)
                                        The `split_array` processor splits array fields into separate events based on configured rules.
                                      items:
                                        properties:
                                          array:
                                            description: |-
                                              (Block List) A list of array split configurations. (see below for nested schema)
                                              A list of array split configurations.
                                            items:
                                              properties:
                                                field:
                                                  description: |-
                                                    (String) The target field in the log event.
                                                    The path to the array field to split.
                                                  type: string
                                                include:
                                                  description: |-
                                                    (String) A Datadog search query that determines which logs are forwarded using this route.
                                                    A Datadog search query used to determine which logs this array split operation targets.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    tagCardinalityLimit:
                                      description: |-
                                        (Block List) The tag_cardinality_limit processor caps the number of distinct tag value combinations on metrics, dropping tags or events once the limit is exceeded. (see below for nested schema)
                                        The `tag_cardinality_limit` processor caps the number of distinct tag value combinations on metrics, dropping tags or events once the limit is exceeded.
                                      items:
                                        properties:
                                          limitExceededAction:
                                            description: |-
                                              (String) The default action to take when the cardinality limit is exceeded. One of drop_tag, drop_event. Valid values are drop_tag, drop_event.
                                              The default action to take when the cardinality limit is exceeded. One of `drop_tag`, `drop_event`. Valid values are `drop_tag`, `drop_event`.
                                            type: string
                                          perMetricLimit:
                                            description: |-
                                              metric cardinality overrides that take precedence over the default value_limit. (see below for nested schema)
                                              Per-metric cardinality overrides that take precedence over the default `value_limit`.
                                            items:
                                              properties:
                                                limitExceededAction:
                                                  description: |-
                                                    (String) The default action to take when the cardinality limit is exceeded. One of drop_tag, drop_event. Valid values are drop_tag, drop_event.
                                                    The action to take on this metric when the limit is exceeded. Required when `override_type` is `limit_override`; must be omitted when `override_type` is `excluded`. Valid values are `drop_tag`, `drop_event`.
                                                  type: string
                                                metricName:
                                                  description: |-
                                                    (String) The metric name this override applies to.
                                                    The metric name this override applies to.
                                                  type: string
                                                overrideType:
                                                  description: |-
                                                    metric override is applied. One of limit_override, excluded. Valid values are limit_override, excluded.
                                                    How the per-metric override is applied. One of `limit_override`, `excluded`. Valid values are `limit_override`, `excluded`.
                                                  type: string
                                                perTagLimit:
                                                  description: |-
                                                    tag cardinality overrides that apply within this metric. Must be omitted when override_type is excluded. (see below for nested schema)
                                                    Per-tag cardinality overrides that apply within this metric. Must be omitted when `override_type` is `excluded`.
                                                  items:
                                                    properties:
                                                      overrideType:
                                                        description: |-
                                                          metric override is applied. One of limit_override, excluded. Valid values are limit_override, excluded.
                                                          How the per-tag override is applied. One of `limit_override`, `excluded`. Valid values are `limit_override`, `excluded`.
                                                        type: string
                                                      tagKey:
                                                        description: |-
                                                          (String) The tag key this override applies to.
                                                          The tag key this override applies to.
                                                        type: string
                                                      valueLimit:
                                                        description: |-
                                                          (Number) The default maximum number of distinct tag value combinations allowed per metric. Between 0 and 1000000. Value must be between 0 and 1000000.
                                                          The cardinality cap for this tag. Required when `override_type` is `limit_override`; must be omitted when `override_type` is `excluded`. Value must be between 0 and 1000000.
                                                        type: number
                                                    type: object
                                                  type: array
                                                valueLimit:
                                                  description: |-
                                                    (Number) The default maximum number of distinct tag value combinations allowed per metric. Between 0 and 1000000. Value must be between 0 and 1000000.
                                                    The cardinality cap for this metric. Required when `override_type` is `limit_override`; must be omitted when `override_type` is `excluded`. Value must be between 0 and 1000000.
                                                  type: number
                                              type: object
                                            type: array
                                          trackingMode:
                                            description: |-
                                              (Block List) Controls whether the processor uses exact or probabilistic tag tracking. (see below for nested schema)
                                              Controls whether the processor uses exact or probabilistic tag tracking.
                                            items:
                                              properties:
                                                mode:
                                                  description: |-
                                                    (String) The protocol used to send logs. Valid values are tcp, udp.
                                                    The cardinality tracking algorithm to use. One of `exact_fingerprint`, `probabilistic`. Valid values are `exact_fingerprint`, `probabilistic`.
                                                  type: string
                                              type: object
                                            type: array
                                          valueLimit:
                                            description: |-
                                              (Number) The default maximum number of distinct tag value combinations allowed per metric. Between 0 and 1000000. Value must be between 0 and 1000000.
                                              The default maximum number of distinct tag value combinations allowed per metric. Between 0 and 1000000. Value must be between 0 and 1000000.
                                            type: number
                                        type: object
                                      type: array
                                    throttle:
                                      description: |-
                                        (Block List) The throttle processor limits the number of events that pass through over a given time window. (see below for nested schema)
                                        The `throttle` processor limits the number of events that pass through over a given time window.
                                      items:
                                        properties:
                                          groupBy:
                                            description: |-
                                              (List of String) Optional fields used to group the metric series.
                                              Optional list of fields used to group events before applying throttling.
                                            items:
                                              type: string
                                            type: array
                                          threshold:
                                            description: |-
                                              (Number) The number of events to allow before throttling is applied.
                                              The number of events to allow before throttling is applied.
                                            type: number
                                          window:
                                            description: |-
                                              (Number) The time window in seconds over which the threshold applies.
                                              The time window in seconds over which the threshold applies.
                                            type: number
                                        type: object
                                      type: array
                                  type: object
                                type: array
                            type: object
                          type: array
                        source:
                          description: |-
                            (Block List) List of sources. (see below for nested schema)
                            List of sources.
                          items:
                            properties:
                              amazonDataFirehose:
                                description: |-
                                  (Block List) The amazon_data_firehose source ingests logs from AWS Data Firehose. (see below for nested schema)
                                  The `amazon_data_firehose` source ingests logs from AWS Data Firehose.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        AWS authentication credentials used for accessing AWS services. If omitted, the system's default credentials are used (for example, the IAM role and environment variables).
                                      items:
                                        properties:
                                          assumeRole:
                                            description: |-
                                              (String) ARN of the role to assume.
                                              The Amazon Resource Name (ARN) of the role to assume.
                                            type: string
                                          externalId:
                                            description: |-
                                              (String) External ID for assumed role.
                                              A unique identifier for cross-account role assumption.
                                            type: string
                                          sessionName:
                                            description: |-
                                              (String) Session name for assumed role.
                                              A session identifier used for logging and tracing the assumed role session.
                                            type: string
                                        type: object
                                      type: array
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              amazonS3:
                                description: |-
                                  rehydratable format to an Amazon S3 bucket for archiving. (see below for nested schema)
                                  The `amazon_s3` source ingests logs from an Amazon S3 bucket. It supports AWS authentication, TLS encryption, and configurable compression.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        AWS authentication credentials used for accessing AWS services. If omitted, the system's default credentials are used (for example, the IAM role and environment variables).
                                      items:
                                        properties:
                                          assumeRole:
                                            description: |-
                                              (String) ARN of the role to assume.
                                              The Amazon Resource Name (ARN) of the role to assume.
                                            type: string
                                          externalId:
                                            description: |-
                                              (String) External ID for assumed role.
                                              A unique identifier for cross-account role assumption.
                                            type: string
                                          sessionName:
                                            description: |-
                                              (String) Session name for assumed role.
                                              A session identifier used for logging and tracing the assumed role session.
                                            type: string
                                        type: object
                                      type: array
                                    compression:
                                      description: |-
                                        (Block List) Compression configuration for archived logs. When omitted, the worker default (gzip) is used. (see below for nested schema)
                                        Compression format for objects retrieved from the S3 bucket. Use `auto` to detect compression from the object's Content-Encoding header or file extension. Valid values are `auto`, `none`, `gzip`, `zstd`.
                                      type: string
                                    region:
                                      description: |-
                                        (String) AWS region of the S3 bucket.
                                        AWS region where the S3 bucket resides.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                    urlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the S3 bucket URL.
                                        Name of the environment variable or secret that holds the S3 bucket URL.
                                      type: string
                                  type: object
                                type: array
                              datadogAgent:
                                description: |-
                                  (Block List) The datadog_agent source collects logs from the Datadog Agent. (see below for nested schema)
                                  The `datadog_agent` source collects logs from the Datadog Agent.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address for the Datadog Agent source.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              fluentBit:
                                description: |-
                                  (Block List) The fluent_bit source ingests logs from Fluent Bit. (see below for nested schema)
                                  The `fluent_bit` source ingests logs from Fluent Bit.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              fluentd:
                                description: |-
                                  compatible service. (see below for nested schema)
                                  The `fluentd` source ingests logs from a Fluentd-compatible service.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              googlePubsub:
                                description: |-
                                  (Block List) The google_pubsub destination publishes logs to a Google Cloud Pub/Sub topic. (see below for nested schema)
                                  The `google_pubsub` source ingests logs from a Google Cloud Pub/Sub subscription.
                                items:
                                  properties:
                                    auth:
                                      description: |-
                                        (Block List) (see below for nested schema)
                                        Google Cloud credentials used to authenticate with Google Cloud services.
                                      items:
                                        properties:
                                          credentialsFile:
                                            description: |-
                                              (String) Path to the Google Cloud service account key file.
                                              Path to the Google Cloud service account key file.
                                            type: string
                                        type: object
                                      type: array
                                    decoding:
                                      description: |-
                                        (String) The decoding format used to interpret incoming logs.
                                        The decoding format used to interpret incoming logs.
                                      type: string
                                    project:
                                      description: |-
                                        (String) The Google Cloud project ID that owns the Pub/Sub topic.
                                        The Google Cloud project ID that owns the Pub/Sub subscription.
                                      type: string
                                    subscription:
                                      description: |-
                                        (String) The Pub/Sub subscription name from which messages are consumed.
                                        The Pub/Sub subscription name from which messages are consumed.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              httpClient:
                                description: |-
                                  (Block List) The http_client destination sends data to an HTTP endpoint. (see below for nested schema)
                                  The `http_client` source scrapes logs from HTTP endpoints at regular intervals.
                                items:
                                  properties:
                                    authStrategy:
                                      description: |-
                                        (String) HTTP authentication strategy. Valid values are none, basic, bearer.
                                        Optional authentication strategy for HTTP requests. Valid values are `none`, `basic`, `bearer`, `custom`.
                                      type: string
                                    customKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds a custom header value (used with custom auth strategies).
                                        Name of the environment variable or secret that holds a custom header value (used with custom auth strategies).
                                      type: string
                                    decoding:
                                      description: |-
                                        (String) The decoding format used to interpret incoming logs.
                                        The decoding format used to interpret incoming logs.
                                      type: string
                                    endpointUrlKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse HTTP endpoint URL. Defaults to DESTINATION_CLICKHOUSE_ENDPOINT_URL.
                                        Name of the environment variable or secret that holds the HTTP endpoint URL.
                                      type: string
                                    passwordKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                        Name of the environment variable or secret that holds the password.
                                      type: string
                                    scrapeIntervalSecs:
                                      description: |-
                                        (Number) The interval (in seconds) between HTTP scrape requests.
                                        The interval (in seconds) between HTTP scrape requests.
                                      type: number
                                    scrapeTimeoutSecs:
                                      description: |-
                                        (Number) The timeout (in seconds) for each scrape request.
                                        The timeout (in seconds) for each scrape request.
                                      type: number
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          serverName:
                                            description: |-
                                              (String) Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                              Server name to use for Server Name Indication (SNI) and to verify against the certificate presented by the remote host. Use this when the address you connect to doesn't match the certificate's Common Name or Subject Alternative Name.
                                            type: string
                                        type: object
                                      type: array
                                    tokenKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the authentication token.
                                        Name of the environment variable or secret that holds the authentication token.
                                      type: string
                                    usernameKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                        Name of the environment variable or secret that holds the username.
                                      type: string
                                  type: object
                                type: array
                              httpServer:
                                description: |-
                                  (Block List) The http_server source collects logs over HTTP POST from external services. (see below for nested schema)
                                  The `http_server` source collects logs over HTTP POST from external services.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                    authStrategy:
                                      description: |-
                                        (String) HTTP authentication strategy. Valid values are none, basic, bearer.
                                        HTTP authentication method. Valid values are `none`, `plain`.
                                      type: string
                                    decoding:
                                      description: |-
                                        (String) The decoding format used to interpret incoming logs.
                                        The decoding format used to interpret incoming logs. Valid values are `json`, `gelf`, `syslog`, `bytes`.
                                      type: string
                                    passwordKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                        Name of the environment variable or secret that holds the password.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                    usernameKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                        Name of the environment variable or secret that holds the username.
                                      type: string
                                    validToken:
                                      description: |-
                                        (Block List) A token accepted for authenticating incoming HTTP requests. Cannot be combined with the plain auth strategy. (see below for nested schema)
                                        A token accepted for authenticating incoming HTTP requests. Cannot be combined with the `plain` auth strategy.
                                      items:
                                        properties:
                                          enabled:
                                            description: |-
                                              (Boolean) Whether this processor group is enabled.
                                              Whether this token is currently accepted. Defaults to `true`.
                                            type: boolean
                                          fieldToAdd:
                                            description: |-
                                              Za-z0-9_]+$. (see below for nested schema)
                                              An optional metadata field attached to every event authenticated by the token. Both `key` and `value` must match `^[A-Za-z0-9_]+$`.
                                            items:
                                              properties:
                                                key:
                                                  description: |-
                                                    (Block List) Key fields used to look up enrichment values. (see below for nested schema)
                                                    The metadata field name to add to incoming events.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The metadata value.
                                                    The metadata field value to add to incoming events.
                                                  type: string
                                              type: object
                                            type: array
                                          pathToToken:
                                            description: |-
                                              in source or header to read it from a request header. (see below for nested schema)
                                              Specifies where the worker extracts the token from the incoming HTTP request. Set either `location` for a built-in source or `header` to read it from a request header.
                                            items:
                                              properties:
                                                header:
                                                  description: |-
                                                    (String) The name of the HTTP header that carries the token.
                                                    The name of the HTTP header that carries the token.
                                                  type: string
                                                location:
                                                  description: |-
                                                    in token location on the incoming HTTP request. One of path, address. Valid values are path, address.
                                                    Built-in token location on the incoming HTTP request. One of `path`, `address`. Valid values are `path`, `address`.
                                                  type: string
                                              type: object
                                            type: array
                                          tokenKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the authentication token.
                                              Name of the environment variable or secret that holds the expected token value.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  The unique identifier for this source.
                                type: string
                              kafka:
                                description: |-
                                  (Block List) The kafka destination sends logs to Apache Kafka topics. (see below for nested schema)
                                  The `kafka` source ingests data from Apache Kafka topics.
                                items:
                                  properties:
                                    bootstrapServersKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the Kafka bootstrap servers.
                                        Name of the environment variable or secret that holds the Kafka bootstrap servers connection string.
                                      type: string
                                    groupId:
                                      description: |-
                                        (String) The Kafka consumer group ID.
                                        The Kafka consumer group ID.
                                      type: string
                                    librdkafkaOption:
                                      description: |-
                                        value pairs. (see below for nested schema)
                                        Advanced librdkafka client configuration options.
                                      items:
                                        properties:
                                          name:
                                            description: |-
                                              (String) The pipeline name.
                                              The name of the librdkafka option.
                                            type: string
                                          value:
                                            description: |-
                                              (String) The metadata value.
                                              The value of the librdkafka option.
                                            type: string
                                        type: object
                                      type: array
                                    sasl:
                                      description: |-
                                        (Block List) Specifies the SASL mechanism for authenticating with a Kafka cluster. (see below for nested schema)
                                        SASL authentication settings.
                                      items:
                                        properties:
                                          mechanism:
                                            description: |-
                                              SHA-256, SCRAM-SHA-512.
                                              SASL mechanism to use (e.g., PLAIN, SCRAM-SHA-256, SCRAM-SHA-512). Valid values are `PLAIN`, `SCRAM-SHA-256`, `SCRAM-SHA-512`.
                                            type: string
                                          passwordKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                              Name of the environment variable or secret that holds the SASL password.
                                            type: string
                                          usernameKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                              Name of the environment variable or secret that holds the SASL username.
                                            type: string
                                        type: object
                                      type: array
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external services.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                        type: object
                                      type: array
                                    topics:
                                      description: |-
                                        (List of String) A list of Kafka topic names to subscribe to. The source ingests messages from each topic specified.
                                        A list of Kafka topic names to subscribe to. The source ingests messages from each topic specified.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                type: array
                              logstash:
                                description: |-
                                  (Block List) The logstash source ingests logs from a Logstash forwarder. (see below for nested schema)
                                  The `logstash` source ingests logs from a Logstash forwarder.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              opentelemetry:
                                description: |-
                                  (Block List) The opentelemetry destination forwards metrics using the OpenTelemetry Protocol (OTLP) over HTTP. (see below for nested schema)
                                  The `opentelemetry` source receives telemetry data using the OpenTelemetry Protocol (OTLP) over gRPC and HTTP.
                                items:
                                  properties:
                                    grpcAddressKey:
                                      description: |-
                                        (String) Environment variable name containing the gRPC server address for receiving OTLP data.
                                        Environment variable name containing the gRPC server address for receiving OTLP data.
                                      type: string
                                    httpAddressKey:
                                      description: |-
                                        (String) Environment variable name containing the HTTP server address for receiving OTLP data.
                                        Environment variable name containing the HTTP server address for receiving OTLP data.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              rsyslog:
                                description: |-
                                  (Block List) The rsyslog destination forwards logs to an external rsyslog server over TCP or UDP using the syslog protocol. (see below for nested schema)
                                  The `rsyslog` source listens for logs over TCP or UDP from an `rsyslog` server using the syslog protocol.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                    mode:
                                      description: |-
                                        (String) The protocol used to send logs. Valid values are tcp, udp.
                                        Protocol used by the syslog source to receive messages.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              socket:
                                description: |-
                                  (Block List) The socket destination sends logs over TCP or UDP to a remote server. (see below for nested schema)
                                  The `socket` source ingests logs over TCP or UDP.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address for the socket.
                                      type: string
                                    framing:
                                      description: |-
                                        (Block List) Defines the framing method for outgoing messages. (see below for nested schema)
                                        Defines the framing method for incoming messages.
                                      items:
                                        properties:
                                          characterDelimited:
                                            description: |-
                                              (Block List) Used when method is character_delimited. Specifies the delimiter character. (see below for nested schema)
                                              Used when `method` is `character_delimited`. Specifies the delimiter character.
                                            items:
                                              properties:
                                                delimiter:
                                                  description: |-
                                                    (String) A single ASCII character used as a delimiter.
                                                    A single ASCII character used as a delimiter.
                                                  type: string
                                              type: object
                                            type: array
                                          method:
                                            description: |-
                                              (String) The framing method. Valid values are newline_delimited, bytes, character_delimited.
                                              The framing method. Valid values are `newline_delimited`, `bytes`, `character_delimited`, `octet_counting`, `chunked_gelf`.
                                            type: string
                                        type: object
                                      type: array
                                    mode:
                                      description: |-
                                        (String) The protocol used to send logs. Valid values are tcp, udp.
                                        The protocol used to receive logs. Valid values are `tcp`, `udp`.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              splunkHec:
                                description: |-
                                  (Block List) The splunk_hec destination forwards logs to Splunk using the HTTP Event Collector (HEC). (see below for nested schema)
                                  The `splunk_hec` source implements the Splunk HTTP Event Collector (HEC) API.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address for the HEC API.
                                      type: string
                                    storeHecToken:
                                      description: |-
                                        (Boolean) When true, the Splunk HEC token from the incoming request is stored in the event, allowing downstream components to forward it to other Splunk HEC destinations.
                                        When `true`, the Splunk HEC token from the incoming request is stored in the event, allowing downstream components to forward it to other Splunk HEC destinations.
                                      type: boolean
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                    validToken:
                                      description: |-
                                        (Block List) A token accepted for authenticating incoming HTTP requests. Cannot be combined with the plain auth strategy. (see below for nested schema)
                                        A HEC token accepted for authenticating incoming Splunk HEC requests.
                                      items:
                                        properties:
                                          enabled:
                                            description: |-
                                              (Boolean) Whether this processor group is enabled.
                                              Whether this token is currently accepted. Defaults to `true`.
                                            type: boolean
                                          fieldToAdd:
                                            description: |-
                                              Za-z0-9_]+$. (see below for nested schema)
                                              An optional metadata field attached to every event authenticated by the token. Both `key` and `value` must match `^[A-Za-z0-9_]+$`.
                                            items:
                                              properties:
                                                key:
                                                  description: |-
                                                    (Block List) Key fields used to look up enrichment values. (see below for nested schema)
                                                    The metadata field name to add to incoming events.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The metadata value.
                                                    The metadata field value to add to incoming events.
                                                  type: string
                                              type: object
                                            type: array
                                          tokenKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the authentication token.
                                              Name of the environment variable or secret that holds the expected HEC token value.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              splunkTcp:
                                description: |-
                                  (Block List) The splunk_tcp source receives logs from a Splunk Universal Forwarder over TCP. TLS is supported for secure transmission. (see below for nested schema)
                                  The `splunk_tcp` source receives logs from a Splunk Universal Forwarder over TCP. TLS is supported for secure transmission.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address for the Splunk TCP receiver.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              sumoLogic:
                                description: |-
                                  (Block List) The sumo_logic destination forwards logs to Sumo Logic. (see below for nested schema)
                                  The `sumo_logic` source receives logs from Sumo Logic collectors.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                  type: object
                                type: array
                              syslogNg:
                                description: |-
                                  ng server over TCP or UDP using the syslog protocol. (see below for nested schema)
                                  The `syslog_ng` source listens for logs over TCP or UDP from a `syslog-ng` server using the syslog protocol.
                                items:
                                  properties:
                                    addressKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the socket address (host:port).
                                        Name of the environment variable or secret that holds the listen address.
                                      type: string
                                    mode:
                                      description: |-
                                        (String) The protocol used to send logs. Valid values are tcp, udp.
                                        Protocol used by the syslog source to receive messages.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        Configuration for enabling TLS encryption between the pipeline component and external connecting clients.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate connecting clients' TLS certificates.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the TLS server certificate file used to identify the pipeline component to connecting clients.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the TLS server certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          verifyCertificate:
                                            description: |-
                                              (Boolean) When true, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                              When `true`, requires client connections to present a valid certificate, enabling mutual TLS authentication.
                                            type: boolean
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              websocket:
                                description: |-
                                  (Block List) The websocket source establishes a persistent WebSocket connection to a remote endpoint and ingests log events as they are pushed by the server. (see below for nested schema)
                                  The `websocket` source establishes a persistent WebSocket connection to a remote endpoint and ingests log events as they are pushed by the server.
                                items:
                                  properties:
                                    authStrategy:
                                      description: |-
                                        (String) HTTP authentication strategy. Valid values are none, basic, bearer.
                                        The authentication strategy used when connecting to the WebSocket server. Valid values are `none`, `basic`, `bearer`, `custom`.
                                      type: string
                                    customKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds a custom header value (used with custom auth strategies).
                                        Name of the environment variable or secret that holds a custom header value. Used when `auth_strategy` is `custom`.
                                      type: string
                                    decoding:
                                      description: |-
                                        (String) The decoding format used to interpret incoming logs.
                                        The decoding format used to interpret incoming log events. Valid values are `bytes`, `gelf`, `json`, `syslog`.
                                      type: string
                                    passwordKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse password. Defaults to DESTINATION_CLICKHOUSE_PASSWORD.
                                        Name of the environment variable or secret that holds the password. Used when `auth_strategy` is `basic`.
                                      type: string
                                    tls:
                                      description: |-
                                        (Block List) Configuration for enabling TLS encryption between the pipeline component and external services. (see below for nested schema)
                                        TLS configuration for the WebSocket connection. Set `mode` to `enabled` for server-certificate validation only, or `with_client_cert` to additionally present a client certificate.
                                      items:
                                        properties:
                                          caFile:
                                            description: |-
                                              (String) Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                              Path to the Certificate Authority (CA) file used to validate the server's TLS certificate.
                                            type: string
                                          crtFile:
                                            description: |-
                                              (String) Path to the TLS client certificate file used to authenticate the pipeline component with upstream or downstream services.
                                              Path to the client certificate file. Required when `mode` is `with_client_cert`.
                                            type: string
                                          keyFile:
                                            description: |-
                                              (String) Path to the private key file associated with the TLS client certificate. Used for mutual TLS authentication.
                                              Path to the private key file associated with the client certificate.
                                            type: string
                                          keyPassKey:
                                            description: |-
                                              (String) Name of the environment variable or secret that holds the passphrase for the private key file.
                                              Name of the environment variable or secret that holds the passphrase for the private key file.
                                            type: string
                                          mode:
                                            description: |-
                                              (String) The protocol used to send logs. Valid values are tcp, udp.
                                              The TLS mode. Use `enabled` for server-only TLS, or `with_client_cert` for mutual TLS with a client certificate. Valid values are `enabled`, `with_client_cert`.
                                            type: string
                                        type: object
                                      type: array
                                    tokenKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the authentication token.
                                        Name of the environment variable or secret that holds the bearer token. Used when `auth_strategy` is `bearer`.
                                      type: string
                                    uriKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the request URI.
                                        Name of the environment variable or secret that holds the WebSocket URI to connect to.
                                      type: string
                                    usernameKey:
                                      description: |-
                                        (String) Name of the environment variable or secret that holds the ClickHouse username. Defaults to DESTINATION_CLICKHOUSE_USERNAME.
                                        Name of the environment variable or secret that holds the username. Used when `auth_strategy` is `basic`.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        useLegacySearchSyntax:
                          description: |-
                            (Boolean) Set to true to continue using the legacy search syntax while migrating filter queries. After migrating all queries to the new syntax, set to false. The legacy syntax is deprecated and will eventually be removed. Requires Observability Pipelines Worker 2.11 or later. Only applies to logs pipelines. This field is ignored for metrics pipelines. See https://docs.datadoghq.com/observability_pipelines/guide/upgrade_your_filter_queries_to_the_new_search_syntax/ for more information.
                            Set to `true` to continue using the legacy search syntax while migrating filter queries. After migrating all queries to the new syntax, set to `false`. The legacy syntax is deprecated and will eventually be removed. Requires Observability Pipelines Worker 2.11 or later. Only applies to `logs` pipelines. This field is ignored for `metrics` pipelines. See https://docs.datadoghq.com/observability_pipelines/guide/upgrade_your_filter_queries_to_the_new_search_syntax/ for more information.
                          type: boolean
                      type: object
                    type: array
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) The pipeline name.
                      The pipeline name.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

