---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: costbudgets.cloudcost.datadog.upbound.io
spec:
  group: cloudcost.datadog.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - datadog
    kind: CostBudget
    listKind: CostBudgetList
    plural: costbudgets
    singular: costbudget
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CostBudget is the Schema for the CostBudgets API. Provides a
          Datadog Cost Budget resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CostBudgetSpec defines the desired state of CostBudget
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  budgetLine:
                    description: |-
                      (Block Set) Budget lines that group monthly amounts by tag combination. Use this instead of entries for a more convenient schema. Note: The order of budget_line blocks does not matter. (see below for nested schema)
                      Budget lines that group monthly amounts by tag combination. Use this instead of `entries` for a more convenient schema. **Note:** The order of budget_line blocks does not matter.
                    items:
                      properties:
                        amounts:
                          additionalProperties:
                            type: number
                          description: |-
                            (Map of Number) Map of month (YYYYMM) to budget amount. Example: {"202601": 1000.0, "202602": 1200.0}
                            Map of month (YYYYMM) to budget amount. Example: {"202601": 1000.0, "202602": 1200.0}
                          type: object
                          x-kubernetes-map-type: granular
                        childTagFilters:
                          description: |-
                            (Block List) Child tag filters for hierarchical budgets. Note: Must be used with parent_tag_filters. Cannot be used with tag_filters. (see below for nested schema)
                            Child tag filters for hierarchical budgets. **Note:** Must be used with parent_tag_filters. Cannot be used with tag_filters.
                          items:
                            properties:
                              tagKey:
                                description: |-
                                  (String) Must be one of the tags from the metrics_query.
                                  Must be one of the tags from the `metrics_query`.
                                type: string
                              tagValue:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        parentTagFilters:
                          description: |-
                            (Block List) Parent tag filters for hierarchical budgets. Note: Must be used with child_tag_filters. Cannot be used with tag_filters. (see below for nested schema)
                            Parent tag filters for hierarchical budgets. **Note:** Must be used with child_tag_filters. Cannot be used with tag_filters.
                          items:
                            properties:
                              tagKey:
                                description: |-
                                  (String) Must be one of the tags from the metrics_query.
                                  Must be one of the tags from the `metrics_query`.
                                type: string
                              tagValue:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        tagFilters:
                          description: |-
                            hierarchical budgets. Note: Cannot be used with parent_tag_filters/child_tag_filters. (see below for nested schema)
                            Tag filters for non-hierarchical budgets. **Note:** Cannot be used with parent_tag_filters/child_tag_filters.
                          items:
                            properties:
                              tagKey:
                                description: |-
                                  (String) Must be one of the tags from the metrics_query.
                                  Must be one of the tags from the `metrics_query`.
                                type: string
                              tagValue:
                                description: (String)
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  endMonth:
                    description: |-
                      (Number) The month when the budget ends (YYYYMM).
                      The month when the budget ends (YYYYMM).
                    type: number
                  entries:
                    description: |-
                      (Block List, Deprecated) The entries of the budget. Note: You must provide entries for all months in the budget period. For hierarchical budgets, each unique tag combination must have entries for all months. (see below for nested schema)
                      The entries of the budget. **Note:** You must provide entries for all months in the budget period. For hierarchical budgets, each unique tag combination must have entries for all months.
                    items:
                      properties:
                        amount:
                          description: (Number)
                          type: number
                        month:
                          description: (Number)
                          type: number
                        tagFilters:
                          description: 'hierarchical budgets. Note: Cannot be used
                            with parent_tag_filters/child_tag_filters. (see below
                            for nested schema)'
                          items:
                            properties:
                              tagKey:
                                description: |-
                                  (String) Must be one of the tags from the metrics_query.
                                  **Note:** Must be one of the tags from the `metrics_query`.
                                type: string
                              tagValue:
                                description: (String)
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  metricsQuery:
                    description: |-
                      (String) The cost query used to track against the budget. Note: For hierarchical budgets using by {tag1,tag2}, the order of tags determines the UI hierarchy (parent, child).
                      The cost query used to track against the budget. **Note:** For hierarchical budgets using `by {tag1,tag2}`, the order of tags determines the UI hierarchy (parent, child).
                    type: string
                  name:
                    description: |-
                      (String) The name of the budget.
                      The name of the budget.
                    type: string
                  startMonth:
                    description: |-
                      (Number) The month when the budget starts (YYYYMM).
                      The month when the budget starts (YYYYMM).
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  budgetLine:
                    description: |-
                      (Block Set) Budget lines that group monthly amounts by tag combination. Use this instead of entries for a more convenient schema. Note: The order of budget_line blocks does not matter. (see below for nested schema)
                      Budget lines that group monthly amounts by tag combination. Use this instead of `entries` for a more convenient schema. **Note:** The order of budget_line blocks does not matter.
                    items:
                      properties:
                        amounts:
                          additionalProperties:
                            type: number
                          description: |-
                            (Map of Number) Map of month (YYYYMM) to budget amount. Example: {"202601": 1000.0, "202602": 1200.0}
                            Map of month (YYYYMM) to budget amount. Example: {"202601": 1000.0, "202602": 1200.0}
                          type: object
                          x-kubernetes-map-type: granular
                        childTagFilters:
                          description: |-
                            (Block List) Child tag filters for hierarchical budgets. Note: Must be used with parent_tag_filters. Cannot be used with tag_filters. (see below for nested schema)
                            Child tag filters for hierarchical budgets. **Note:** Must be used with parent_tag_filters. Cannot be used with tag_filters.
                          items:
                            properties:
                              tagKey:
                                description: |-
                                  (String) Must be one of the tags from the metrics_query.
                                  Must be one of the tags from the `metrics_query`.
                                type: string
                              tagValue:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        parentTagFilters:
                          description: |-
                            (Block List) Parent tag filters for hierarchical budgets. Note: Must be used with child_tag_filters. Cannot be used with tag_filters. (see below for nested schema)
                            Parent tag filters for hierarchical budgets. **Note:** Must be used with child_tag_filters. Cannot be used with tag_filters.
                          items:
                            properties:
                              tagKey:
                                description: |-
                                  (String) Must be one of the tags from the metrics_query.
                                  Must be one of the tags from the `metrics_query`.
                                type: string
                              tagValue:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        tagFilters:
                          description: |-
                            hierarchical budgets. Note: Cannot be used with parent_tag_filters/child_tag_filters. (see below for nested schema)
                            Tag filters for non-hierarchical budgets. **Note:** Cannot be used with parent_tag_filters/child_tag_filters.
                          items:
                            properties:
                              tagKey:
                                description: |-
                                  (String) Must be one of the tags from the metrics_query.
                                  Must be one of the tags from the `metrics_query`.
                                type: string
                              tagValue:
                                description: (String)
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  endMonth:
                    description: |-
                      (Number) The month when the budget ends (YYYYMM).
                      The month when the budget ends (YYYYMM).
                    type: number
                  entries:
                    description: |-
                      (Block List, Deprecated) The entries of the budget. Note: You must provide entries for all months in the budget period. For hierarchical budgets, each unique tag combination must have entries for all months. (see below for nested schema)
                      The entries of the budget. **Note:** You must provide entries for all months in the budget period. For hierarchical budgets, each unique tag combination must have entries for all months.
                    items:
                      properties:
                        amount:
                          description: (Number)
                          type: number
                        month:
                          description: (Number)
                          type: number
                        tagFilters:
                          description: 'hierarchical budgets. Note: Cannot be used
                            with parent_tag_filters/child_tag_filters. (see below
                            for nested schema)'
                          items:
                            properties:
                              tagKey:
                                description: |-
                                  (String) Must be one of the tags from the metrics_query.
                                  **Note:** Must be one of the tags from the `metrics_query`.
                                type: string
                              tagValue:
                                description: (String)
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  metricsQuery:
                    description: |-
                      (String) The cost query used to track against the budget. Note: For hierarchical budgets using by {tag1,tag2}, the order of tags determines the UI hierarchy (parent, child).
                      The cost query used to track against the budget. **Note:** For hierarchical budgets using `by {tag1,tag2}`, the order of tags determines the UI hierarchy (parent, child).
                    type: string
                  name:
                    description: |-
                      (String) The name of the budget.
                      The name of the budget.
                    type: string
                  startMonth:
                    description: |-
                      (Number) The month when the budget starts (YYYYMM).
                      The month when the budget starts (YYYYMM).
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.endMonth is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.endMonth)
                || (has(self.initProvider) && has(self.initProvider.endMonth))'
            - message: spec.forProvider.metricsQuery is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metricsQuery)
                || (has(self.initProvider) && has(self.initProvider.metricsQuery))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.startMonth is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.startMonth)
                || (has(self.initProvider) && has(self.initProvider.startMonth))'
          status:
            description: CostBudgetStatus defines the observed state of CostBudget.
            properties:
              atProvider:
                properties:
                  budgetLine:
                    description: |-
                      (Block Set) Budget lines that group monthly amounts by tag combination. Use this instead of entries for a more convenient schema. Note: The order of budget_line blocks does not matter. (see below for nested schema)
                      Budget lines that group monthly amounts by tag combination. Use this instead of `entries` for a more convenient schema. **Note:** The order of budget_line blocks does not matter.
                    items:
                      properties:
                        amounts:
                          additionalProperties:
                            type: number
                          description: |-
                            (Map of Number) Map of month (YYYYMM) to budget amount. Example: {"202601": 1000.0, "202602": 1200.0}
                            Map of month (YYYYMM) to budget amount. Example: {"202601": 1000.0, "202602": 1200.0}
                          type: object
                          x-kubernetes-map-type: granular
                        childTagFilters:
                          description: |-
                            (Block List) Child tag filters for hierarchical budgets. Note: Must be used with parent_tag_filters. Cannot be used with tag_filters. (see below for nested schema)
                            Child tag filters for hierarchical budgets. **Note:** Must be used with parent_tag_filters. Cannot be used with tag_filters.
                          items:
                            properties:
                              tagKey:
                                description: |-
                                  (String) Must be one of the tags from the metrics_query.
                                  Must be one of the tags from the `metrics_query`.
                                type: string
                              tagValue:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        parentTagFilters:
                          description: |-
                            (Block List) Parent tag filters for hierarchical budgets. Note: Must be used with child_tag_filters. Cannot be used with tag_filters. (see below for nested schema)
                            Parent tag filters for hierarchical budgets. **Note:** Must be used with child_tag_filters. Cannot be used with tag_filters.
                          items:
                            properties:
                              tagKey:
                                description: |-
                                  (String) Must be one of the tags from the metrics_query.
                                  Must be one of the tags from the `metrics_query`.
                                type: string
                              tagValue:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        tagFilters:
                          description: |-
                            hierarchical budgets. Note: Cannot be used with parent_tag_filters/child_tag_filters. (see below for nested schema)
                            Tag filters for non-hierarchical budgets. **Note:** Cannot be used with parent_tag_filters/child_tag_filters.
                          items:
                            properties:
                              tagKey:
                                description: |-
                                  (String) Must be one of the tags from the metrics_query.
                                  Must be one of the tags from the `metrics_query`.
                                type: string
                              tagValue:
                                description: (String)
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  endMonth:
                    description: |-
                      (Number) The month when the budget ends (YYYYMM).
                      The month when the budget ends (YYYYMM).
                    type: number
                  entries:
                    description: |-
                      (Block List, Deprecated) The entries of the budget. Note: You must provide entries for all months in the budget period. For hierarchical budgets, each unique tag combination must have entries for all months. (see below for nested schema)
                      The entries of the budget. **Note:** You must provide entries for all months in the budget period. For hierarchical budgets, each unique tag combination must have entries for all months.
                    items:
                      properties:
                        amount:
                          description: (Number)
                          type: number
                        month:
                          description: (Number)
                          type: number
                        tagFilters:
                          description: 'hierarchical budgets. Note: Cannot be used
                            with parent_tag_filters/child_tag_filters. (see below
                            for nested schema)'
                          items:
                            properties:
                              tagKey:
                                description: |-
                                  (String) Must be one of the tags from the metrics_query.
                                  **Note:** Must be one of the tags from the `metrics_query`.
                                type: string
                              tagValue:
                                description: (String)
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  id:
                    description: (String) The ID of the budget.
                    type: string
                  metricsQuery:
                    description: |-
                      (String) The cost query used to track against the budget. Note: For hierarchical budgets using by {tag1,tag2}, the order of tags determines the UI hierarchy (parent, child).
                      The cost query used to track against the budget. **Note:** For hierarchical budgets using `by {tag1,tag2}`, the order of tags determines the UI hierarchy (parent, child).
                    type: string
                  name:
                    description: |-
                      (String) The name of the budget.
                      The name of the budget.
                    type: string
                  startMonth:
                    description: |-
                      (Number) The month when the budget starts (YYYYMM).
                      The month when the budget starts (YYYYMM).
                    type: number
                  totalAmount:
                    description: |-
                      (Number) The sum of all budget entries' amounts.
                      The sum of all budget entries' amounts.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

