---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: notificationrules.securitymonitoring.datadog.upbound.io
spec:
  group: securitymonitoring.datadog.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - datadog
    kind: NotificationRule
    listKind: NotificationRuleList
    plural: notificationrules
    singular: notificationrule
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NotificationRule is the Schema for the NotificationRules API.
          Provides a Datadog Security Monitoring Notification Rule API resource for
          creating and managing Datadog security notification rules.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NotificationRuleSpec defines the desired state of NotificationRule
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  enabled:
                    description: |-
                      (Boolean) Indicates whether the rule is enabled. Defaults to true.
                      Indicates whether the rule is enabled. Defaults to `true`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the rule (must be unique).
                      The name of the rule (must be unique).
                    type: string
                  selectors:
                    description: |-
                      (Block, Optional) Defines selectors to filter security issues that generate notifications. (see below for nested schema)
                      Defines selectors to filter security issues that generate notifications.
                    properties:
                      query:
                        description: |-
                          (String) Comprises one or several key:value pairs for filtering security issues based on tags and attributes. Defaults to "".
                          Comprises one or several key:value pairs for filtering security issues based on tags and attributes. Defaults to `""`.
                        type: string
                      ruleTypes:
                        description: |-
                          (Set of String) Specifies security rule types for filtering signals and vulnerabilities that generate notifications.
                          Specifies security rule types for filtering signals and vulnerabilities that generate notifications.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      severities:
                        description: |-
                          (Set of String) The security rules severities to consider.
                          The security rules severities to consider.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      triggerSource:
                        description: |-
                          (String) The type of security issues the rule applies to. Use security_signals for rules based on security signals and security_findings for those based on vulnerabilities.
                          The type of security issues the rule applies to. Use `security_signals` for rules based on security signals and `security_findings` for those based on vulnerabilities.
                        type: string
                    type: object
                  targets:
                    description: |-
                      ops), or a Teams recipient (@teams-ops).
                      The list of handle targets for the notifications. A target must be prefixed with an @. It can be an email address (@bob@email.com), or any installed integration. For example, a Slack recipient (@slack-ops), or a Teams recipient (@teams-ops).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  timeAggregation:
                    description: |-
                      (Number) Specifies the time period, in seconds, used to aggregate the notification. Defaults to 0.
                      Specifies the time period, in seconds, used to aggregate the notification. Defaults to `0`.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  enabled:
                    description: |-
                      (Boolean) Indicates whether the rule is enabled. Defaults to true.
                      Indicates whether the rule is enabled. Defaults to `true`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the rule (must be unique).
                      The name of the rule (must be unique).
                    type: string
                  selectors:
                    description: |-
                      (Block, Optional) Defines selectors to filter security issues that generate notifications. (see below for nested schema)
                      Defines selectors to filter security issues that generate notifications.
                    properties:
                      query:
                        description: |-
                          (String) Comprises one or several key:value pairs for filtering security issues based on tags and attributes. Defaults to "".
                          Comprises one or several key:value pairs for filtering security issues based on tags and attributes. Defaults to `""`.
                        type: string
                      ruleTypes:
                        description: |-
                          (Set of String) Specifies security rule types for filtering signals and vulnerabilities that generate notifications.
                          Specifies security rule types for filtering signals and vulnerabilities that generate notifications.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      severities:
                        description: |-
                          (Set of String) The security rules severities to consider.
                          The security rules severities to consider.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      triggerSource:
                        description: |-
                          (String) The type of security issues the rule applies to. Use security_signals for rules based on security signals and security_findings for those based on vulnerabilities.
                          The type of security issues the rule applies to. Use `security_signals` for rules based on security signals and `security_findings` for those based on vulnerabilities.
                        type: string
                    type: object
                  targets:
                    description: |-
                      ops), or a Teams recipient (@teams-ops).
                      The list of handle targets for the notifications. A target must be prefixed with an @. It can be an email address (@bob@email.com), or any installed integration. For example, a Slack recipient (@slack-ops), or a Teams recipient (@teams-ops).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  timeAggregation:
                    description: |-
                      (Number) Specifies the time period, in seconds, used to aggregate the notification. Defaults to 0.
                      Specifies the time period, in seconds, used to aggregate the notification. Defaults to `0`.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.selectors is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.selectors)
                || (has(self.initProvider) && has(self.initProvider.selectors))'
            - message: spec.forProvider.targets is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.targets)
                || (has(self.initProvider) && has(self.initProvider.targets))'
          status:
            description: NotificationRuleStatus defines the observed state of NotificationRule.
            properties:
              atProvider:
                properties:
                  createdAt:
                    description: |-
                      (Number) Indicates when this rule was created.
                      Indicates when this rule was created.
                    type: number
                  createdByHandle:
                    description: |-
                      (String) The handle of the rule creator.
                      The handle of the rule creator.
                    type: string
                  createdByName:
                    description: |-
                      (String) The name of the rule creator.
                      The name of the rule creator.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Indicates whether the rule is enabled. Defaults to true.
                      Indicates whether the rule is enabled. Defaults to `true`.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  modifiedAt:
                    description: |-
                      (Number) Indicates when this rule was last modified.
                      Indicates when this rule was last modified.
                    type: number
                  modifiedByHandle:
                    description: |-
                      (String) The handle of the rule last modifier.
                      The handle of the rule last modifier.
                    type: string
                  modifiedByName:
                    description: |-
                      (String) The name of the rule last modifier.
                      The name of the rule last modifier.
                    type: string
                  name:
                    description: |-
                      (String) The name of the rule (must be unique).
                      The name of the rule (must be unique).
                    type: string
                  selectors:
                    description: |-
                      (Block, Optional) Defines selectors to filter security issues that generate notifications. (see below for nested schema)
                      Defines selectors to filter security issues that generate notifications.
                    properties:
                      query:
                        description: |-
                          (String) Comprises one or several key:value pairs for filtering security issues based on tags and attributes. Defaults to "".
                          Comprises one or several key:value pairs for filtering security issues based on tags and attributes. Defaults to `""`.
                        type: string
                      ruleTypes:
                        description: |-
                          (Set of String) Specifies security rule types for filtering signals and vulnerabilities that generate notifications.
                          Specifies security rule types for filtering signals and vulnerabilities that generate notifications.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      severities:
                        description: |-
                          (Set of String) The security rules severities to consider.
                          The security rules severities to consider.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      triggerSource:
                        description: |-
                          (String) The type of security issues the rule applies to. Use security_signals for rules based on security signals and security_findings for those based on vulnerabilities.
                          The type of security issues the rule applies to. Use `security_signals` for rules based on security signals and `security_findings` for those based on vulnerabilities.
                        type: string
                    type: object
                  targets:
                    description: |-
                      ops), or a Teams recipient (@teams-ops).
                      The list of handle targets for the notifications. A target must be prefixed with an @. It can be an email address (@bob@email.com), or any installed integration. For example, a Slack recipient (@slack-ops), or a Teams recipient (@teams-ops).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  timeAggregation:
                    description: |-
                      (Number) Specifies the time period, in seconds, used to aggregate the notification. Defaults to 0.
                      Specifies the time period, in seconds, used to aggregate the notification. Defaults to `0`.
                    type: number
                  version:
                    description: |-
                      (Number) The rule version (incremented at each update).
                      The rule version (incremented at each update).
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

