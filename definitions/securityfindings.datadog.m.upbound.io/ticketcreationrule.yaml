---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: ticketcreationrules.securityfindings.datadog.m.upbound.io
spec:
  group: securityfindings.datadog.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - datadog
    kind: TicketCreationRule
    listKind: TicketCreationRuleList
    plural: ticketcreationrules
    singular: ticketcreationrule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TicketCreationRule is the Schema for the TicketCreationRules
          API. Provides a Datadog security findings automation ticket creation rule
          resource. This can be used to create and manage rules that automatically
          open tickets for matching security findings. Use the datadog_security_findings_ticket_creation_rules_order
          resource to manage the evaluation order of ticket creation rules.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TicketCreationRuleSpec defines the desired state of TicketCreationRule
            properties:
              forProvider:
                properties:
                  action:
                    description: (Attributes) The action to take when the ticket creation
                      rule matches a finding. (see below for nested schema)
                    properties:
                      assigneeId:
                        description: |-
                          (String) The UUID of the default assignee for created tickets. Must be a valid UUID.
                          The UUID of the default assignee for created tickets. Must be a valid UUID.
                        type: string
                      fields:
                        description: |-
                          encoded object of custom fields of the Jira issue to create. For the list of available fields, see the Jira documentation.
                          A JSON-encoded object of custom fields of the Jira issue to create. For the list of available fields, see the [Jira documentation](https://developer.atlassian.com/cloud/jira/platform/rest/v2/api-group-issues/#api-rest-api-2-issue-createmeta-projectidorkey-issuetypes-issuetypeid-get).
                        type: string
                      maxTicketsPerDay:
                        description: |-
                          (Number) The maximum number of tickets the rule may create per day. If exceeded, one final ticket will be created, explaining the limit was hit and linking back to the responsible rule. Value must be between 1 and 500.
                          The maximum number of tickets the rule may create per day. If exceeded, one final ticket will be created, explaining the limit was hit and linking back to the responsible rule. Value must be between 1 and 500.
                        type: number
                      projectId:
                        description: |-
                          (String) The UUID of the Case Management project. Must be a valid UUID.
                          The UUID of the Case Management project. Must be a valid UUID.
                        type: string
                      target:
                        description: |-
                          (String) The ticketing system to create tickets in. Valid values are jira, case_management.
                          The ticketing system to create tickets in. Valid values are `jira`, `case_management`.
                        type: string
                    type: object
                  enabled:
                    description: |-
                      (Boolean) Whether the ticket creation rule is enabled. Defaults to true.
                      Whether the ticket creation rule is enabled. Defaults to `true`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the ticket creation rule.
                      The name of the ticket creation rule.
                    type: string
                  rule:
                    description: (Attributes) Defines the scope of findings to which
                      the automation rule applies. (see below for nested schema)
                    properties:
                      findingTypes:
                        description: |-
                          (List of String) The list of security finding types that the automation rule applies to. Valid values are api_security, attack_path, host_and_container_vulnerability, iac_misconfiguration, identity_risk, library_vulnerability, misconfiguration, runtime_code_vulnerability, secret, static_code_vulnerability, workload_activity.
                          The list of security finding types that the automation rule applies to. Valid values are `api_security`, `attack_path`, `host_and_container_vulnerability`, `iac_misconfiguration`, `identity_risk`, `library_vulnerability`, `misconfiguration`, `runtime_code_vulnerability`, `secret`, `static_code_vulnerability`, `workload_activity`.
                        items:
                          type: string
                        type: array
                      query:
                        description: |-
                          (String) A search query to further filter the findings matched by this rule. The @workflow.* namespace and @status fields are not permitted. For a reference of available fields, see the Security Findings schema documentation.
                          A search query to further filter the findings matched by this rule. The `@workflow.*` namespace and `@status` fields are not permitted. For a reference of available fields, see the [Security Findings schema documentation](https://docs.datadoghq.com/security/guide/findings-schema/).
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  action:
                    description: (Attributes) The action to take when the ticket creation
                      rule matches a finding. (see below for nested schema)
                    properties:
                      assigneeId:
                        description: |-
                          (String) The UUID of the default assignee for created tickets. Must be a valid UUID.
                          The UUID of the default assignee for created tickets. Must be a valid UUID.
                        type: string
                      fields:
                        description: |-
                          encoded object of custom fields of the Jira issue to create. For the list of available fields, see the Jira documentation.
                          A JSON-encoded object of custom fields of the Jira issue to create. For the list of available fields, see the [Jira documentation](https://developer.atlassian.com/cloud/jira/platform/rest/v2/api-group-issues/#api-rest-api-2-issue-createmeta-projectidorkey-issuetypes-issuetypeid-get).
                        type: string
                      maxTicketsPerDay:
                        description: |-
                          (Number) The maximum number of tickets the rule may create per day. If exceeded, one final ticket will be created, explaining the limit was hit and linking back to the responsible rule. Value must be between 1 and 500.
                          The maximum number of tickets the rule may create per day. If exceeded, one final ticket will be created, explaining the limit was hit and linking back to the responsible rule. Value must be between 1 and 500.
                        type: number
                      projectId:
                        description: |-
                          (String) The UUID of the Case Management project. Must be a valid UUID.
                          The UUID of the Case Management project. Must be a valid UUID.
                        type: string
                      target:
                        description: |-
                          (String) The ticketing system to create tickets in. Valid values are jira, case_management.
                          The ticketing system to create tickets in. Valid values are `jira`, `case_management`.
                        type: string
                    type: object
                  enabled:
                    description: |-
                      (Boolean) Whether the ticket creation rule is enabled. Defaults to true.
                      Whether the ticket creation rule is enabled. Defaults to `true`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the ticket creation rule.
                      The name of the ticket creation rule.
                    type: string
                  rule:
                    description: (Attributes) Defines the scope of findings to which
                      the automation rule applies. (see below for nested schema)
                    properties:
                      findingTypes:
                        description: |-
                          (List of String) The list of security finding types that the automation rule applies to. Valid values are api_security, attack_path, host_and_container_vulnerability, iac_misconfiguration, identity_risk, library_vulnerability, misconfiguration, runtime_code_vulnerability, secret, static_code_vulnerability, workload_activity.
                          The list of security finding types that the automation rule applies to. Valid values are `api_security`, `attack_path`, `host_and_container_vulnerability`, `iac_misconfiguration`, `identity_risk`, `library_vulnerability`, `misconfiguration`, `runtime_code_vulnerability`, `secret`, `static_code_vulnerability`, `workload_activity`.
                        items:
                          type: string
                        type: array
                      query:
                        description: |-
                          (String) A search query to further filter the findings matched by this rule. The @workflow.* namespace and @status fields are not permitted. For a reference of available fields, see the Security Findings schema documentation.
                          A search query to further filter the findings matched by this rule. The `@workflow.*` namespace and `@status` fields are not permitted. For a reference of available fields, see the [Security Findings schema documentation](https://docs.datadoghq.com/security/guide/findings-schema/).
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.action is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.action)
                || (has(self.initProvider) && has(self.initProvider.action))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.rule is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.rule)
                || (has(self.initProvider) && has(self.initProvider.rule))'
          status:
            description: TicketCreationRuleStatus defines the observed state of TicketCreationRule.
            properties:
              atProvider:
                properties:
                  action:
                    description: (Attributes) The action to take when the ticket creation
                      rule matches a finding. (see below for nested schema)
                    properties:
                      assigneeId:
                        description: |-
                          (String) The UUID of the default assignee for created tickets. Must be a valid UUID.
                          The UUID of the default assignee for created tickets. Must be a valid UUID.
                        type: string
                      autoDisabledReason:
                        description: |-
                          only.
                          The reason the rule was automatically disabled by the system due to a ticketing integration error. This field is read-only.
                        type: string
                      fields:
                        description: |-
                          encoded object of custom fields of the Jira issue to create. For the list of available fields, see the Jira documentation.
                          A JSON-encoded object of custom fields of the Jira issue to create. For the list of available fields, see the [Jira documentation](https://developer.atlassian.com/cloud/jira/platform/rest/v2/api-group-issues/#api-rest-api-2-issue-createmeta-projectidorkey-issuetypes-issuetypeid-get).
                        type: string
                      maxTicketsPerDay:
                        description: |-
                          (Number) The maximum number of tickets the rule may create per day. If exceeded, one final ticket will be created, explaining the limit was hit and linking back to the responsible rule. Value must be between 1 and 500.
                          The maximum number of tickets the rule may create per day. If exceeded, one final ticket will be created, explaining the limit was hit and linking back to the responsible rule. Value must be between 1 and 500.
                        type: number
                      projectId:
                        description: |-
                          (String) The UUID of the Case Management project. Must be a valid UUID.
                          The UUID of the Case Management project. Must be a valid UUID.
                        type: string
                      target:
                        description: |-
                          (String) The ticketing system to create tickets in. Valid values are jira, case_management.
                          The ticketing system to create tickets in. Valid values are `jira`, `case_management`.
                        type: string
                    type: object
                  enabled:
                    description: |-
                      (Boolean) Whether the ticket creation rule is enabled. Defaults to true.
                      Whether the ticket creation rule is enabled. Defaults to `true`.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) The name of the ticket creation rule.
                      The name of the ticket creation rule.
                    type: string
                  rule:
                    description: (Attributes) Defines the scope of findings to which
                      the automation rule applies. (see below for nested schema)
                    properties:
                      findingTypes:
                        description: |-
                          (List of String) The list of security finding types that the automation rule applies to. Valid values are api_security, attack_path, host_and_container_vulnerability, iac_misconfiguration, identity_risk, library_vulnerability, misconfiguration, runtime_code_vulnerability, secret, static_code_vulnerability, workload_activity.
                          The list of security finding types that the automation rule applies to. Valid values are `api_security`, `attack_path`, `host_and_container_vulnerability`, `iac_misconfiguration`, `identity_risk`, `library_vulnerability`, `misconfiguration`, `runtime_code_vulnerability`, `secret`, `static_code_vulnerability`, `workload_activity`.
                        items:
                          type: string
                        type: array
                      query:
                        description: |-
                          (String) A search query to further filter the findings matched by this rule. The @workflow.* namespace and @status fields are not permitted. For a reference of available fields, see the Security Findings schema documentation.
                          A search query to further filter the findings matched by this rule. The `@workflow.*` namespace and `@status` fields are not permitted. For a reference of available fields, see the [Security Findings schema documentation](https://docs.datadoghq.com/security/guide/findings-schema/).
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

