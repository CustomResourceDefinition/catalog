---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: userdefinedfields.incident.datadog.m.upbound.io
spec:
  group: incident.datadog.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - datadog
    kind: UserDefinedField
    listKind: UserDefinedFieldList
    plural: userdefinedfields
    singular: userdefinedfield
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'UserDefinedField is the Schema for the UserDefinedFields API.
          Provides a Datadog incident user-defined field resource. This can be used
          to create and manage custom fields on Datadog incidents. Note: This resource
          targets an endpoint that is in preview and is subject to change.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UserDefinedFieldSpec defines the desired state of UserDefinedField
            properties:
              forProvider:
                properties:
                  category:
                    description: |-
                      (String) The section in which the field appears: what_happened or why_it_happened. When unset, the field appears in the Attributes section.
                      The section in which the field appears: `what_happened` or `why_it_happened`. When unset, the field appears in the Attributes section.
                    type: string
                  defaultValue:
                    description: |-
                      (String) The default value for the field. Must be one of the valid values when valid_values is set.
                      The default value for the field. Must be one of the valid values when `valid_values` is set.
                    type: string
                  displayName:
                    description: |-
                      readable name shown in the UI. Defaults to a formatted version of the name if not provided.
                      The human-readable name shown in the UI. Defaults to a formatted version of the name if not provided.
                    type: string
                  incidentType:
                    description: |-
                      (String) The ID of the incident type this field is associated with. Changing the incident type forces a new resource.
                      The ID of the incident type this field is associated with. Changing the incident type forces a new resource.
                    type: string
                  incidentTypeRef:
                    description: Reference to a IncidentType in incident.datadog to
                      populate incidentType.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  incidentTypeSelector:
                    description: Selector for a IncidentType in incident.datadog to
                      populate incidentType.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  name:
                    description: |-
                      (String) The unique identifier of the field. Must start with a letter or digit and contain only letters, digits, underscores, or periods. Changing the name forces a new resource.
                      The unique identifier of the field. Must start with a letter or digit and contain only letters, digits, underscores, or periods. Changing the name forces a new resource.
                    type: string
                  ordinal:
                    description: |-
                      (String) A decimal string representing the field's display order in the UI. Assigned by the server when not provided.
                      A decimal string representing the field's display order in the UI. Assigned by the server when not provided.
                    type: string
                  required:
                    description: |-
                      (Boolean) When true, users must fill out this field on incidents. Defaults to false.
                      When true, users must fill out this field on incidents. Defaults to `false`.
                    type: boolean
                  tagKey:
                    description: |-
                      type fields only, the metric tag key that powers the autocomplete options. Changing the tag key forces a new resource.
                      For metric tag-type fields only, the metric tag key that powers the autocomplete options. Changing the tag key forces a new resource.
                    type: string
                  type:
                    description: |-
                      (String) The data type of the field. Changing the type forces a new resource. Valid values are dropdown, multiselect, textbox, textarray, metrictag, autocomplete, number, datetime.
                      The data type of the field. Changing the type forces a new resource. Valid values are `dropdown`, `multiselect`, `textbox`, `textarray`, `metrictag`, `autocomplete`, `number`, `datetime`.
                    type: string
                  validValue:
                    description: |-
                      (Block Set) A set of allowed values for dropdown, multiselect, and autocomplete fields; specify one block per value. Limited to 1000 values. The API does not preserve ordering, so this is modeled as an unordered set. (see below for nested schema)
                      A set of allowed values for dropdown, multiselect, and autocomplete fields; specify one block per value. Limited to 1000 values. The API does not preserve ordering, so this is modeled as an unordered set.
                    items:
                      properties:
                        description:
                          description: |-
                            (String) A detailed description of the valid value.
                            A detailed description of the valid value.
                          type: string
                        displayName:
                          description: |-
                            readable name shown in the UI. Defaults to a formatted version of the name if not provided.
                            The human-readable display name for this value.
                          type: string
                        shortDescription:
                          description: |-
                            (String) A short description of the valid value.
                            A short description of the valid value.
                          type: string
                        value:
                          description: |-
                            (String) The identifier that is stored when this option is selected.
                            The identifier that is stored when this option is selected.
                          type: string
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  category:
                    description: |-
                      (String) The section in which the field appears: what_happened or why_it_happened. When unset, the field appears in the Attributes section.
                      The section in which the field appears: `what_happened` or `why_it_happened`. When unset, the field appears in the Attributes section.
                    type: string
                  defaultValue:
                    description: |-
                      (String) The default value for the field. Must be one of the valid values when valid_values is set.
                      The default value for the field. Must be one of the valid values when `valid_values` is set.
                    type: string
                  displayName:
                    description: |-
                      readable name shown in the UI. Defaults to a formatted version of the name if not provided.
                      The human-readable name shown in the UI. Defaults to a formatted version of the name if not provided.
                    type: string
                  incidentType:
                    description: |-
                      (String) The ID of the incident type this field is associated with. Changing the incident type forces a new resource.
                      The ID of the incident type this field is associated with. Changing the incident type forces a new resource.
                    type: string
                  incidentTypeRef:
                    description: Reference to a IncidentType in incident.datadog to
                      populate incidentType.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  incidentTypeSelector:
                    description: Selector for a IncidentType in incident.datadog to
                      populate incidentType.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  name:
                    description: |-
                      (String) The unique identifier of the field. Must start with a letter or digit and contain only letters, digits, underscores, or periods. Changing the name forces a new resource.
                      The unique identifier of the field. Must start with a letter or digit and contain only letters, digits, underscores, or periods. Changing the name forces a new resource.
                    type: string
                  ordinal:
                    description: |-
                      (String) A decimal string representing the field's display order in the UI. Assigned by the server when not provided.
                      A decimal string representing the field's display order in the UI. Assigned by the server when not provided.
                    type: string
                  required:
                    description: |-
                      (Boolean) When true, users must fill out this field on incidents. Defaults to false.
                      When true, users must fill out this field on incidents. Defaults to `false`.
                    type: boolean
                  tagKey:
                    description: |-
                      type fields only, the metric tag key that powers the autocomplete options. Changing the tag key forces a new resource.
                      For metric tag-type fields only, the metric tag key that powers the autocomplete options. Changing the tag key forces a new resource.
                    type: string
                  type:
                    description: |-
                      (String) The data type of the field. Changing the type forces a new resource. Valid values are dropdown, multiselect, textbox, textarray, metrictag, autocomplete, number, datetime.
                      The data type of the field. Changing the type forces a new resource. Valid values are `dropdown`, `multiselect`, `textbox`, `textarray`, `metrictag`, `autocomplete`, `number`, `datetime`.
                    type: string
                  validValue:
                    description: |-
                      (Block Set) A set of allowed values for dropdown, multiselect, and autocomplete fields; specify one block per value. Limited to 1000 values. The API does not preserve ordering, so this is modeled as an unordered set. (see below for nested schema)
                      A set of allowed values for dropdown, multiselect, and autocomplete fields; specify one block per value. Limited to 1000 values. The API does not preserve ordering, so this is modeled as an unordered set.
                    items:
                      properties:
                        description:
                          description: |-
                            (String) A detailed description of the valid value.
                            A detailed description of the valid value.
                          type: string
                        displayName:
                          description: |-
                            readable name shown in the UI. Defaults to a formatted version of the name if not provided.
                            The human-readable display name for this value.
                          type: string
                        shortDescription:
                          description: |-
                            (String) A short description of the valid value.
                            A short description of the valid value.
                          type: string
                        value:
                          description: |-
                            (String) The identifier that is stored when this option is selected.
                            The identifier that is stored when this option is selected.
                          type: string
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: UserDefinedFieldStatus defines the observed state of UserDefinedField.
            properties:
              atProvider:
                properties:
                  category:
                    description: |-
                      (String) The section in which the field appears: what_happened or why_it_happened. When unset, the field appears in the Attributes section.
                      The section in which the field appears: `what_happened` or `why_it_happened`. When unset, the field appears in the Attributes section.
                    type: string
                  created:
                    description: |-
                      (String) Timestamp when the field was created.
                      Timestamp when the field was created.
                    type: string
                  defaultValue:
                    description: |-
                      (String) The default value for the field. Must be one of the valid values when valid_values is set.
                      The default value for the field. Must be one of the valid values when `valid_values` is set.
                    type: string
                  deleted:
                    description: |-
                      deleted, or null if not deleted.
                      Timestamp when the field was soft-deleted, or null if not deleted.
                    type: string
                  displayName:
                    description: |-
                      readable name shown in the UI. Defaults to a formatted version of the name if not provided.
                      The human-readable name shown in the UI. Defaults to a formatted version of the name if not provided.
                    type: string
                  id:
                    description: defined field.
                    type: string
                  incidentType:
                    description: |-
                      (String) The ID of the incident type this field is associated with. Changing the incident type forces a new resource.
                      The ID of the incident type this field is associated with. Changing the incident type forces a new resource.
                    type: string
                  metadata:
                    description: type fields, describing how to populate autocomplete
                      options. Populated by the server for supported fields. (see
                      below for nested schema)
                    properties:
                      category:
                        description: |-
                          (String) The section in which the field appears: what_happened or why_it_happened. When unset, the field appears in the Attributes section.
                          The category of the autocomplete source.
                        type: string
                      searchLimitParam:
                        description: |-
                          (String) The query parameter used to limit the number of autocomplete results.
                          The query parameter used to limit the number of autocomplete results.
                        type: string
                      searchParams:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Additional query parameters to include in the search URL.
                          Additional query parameters to include in the search URL.
                        type: object
                        x-kubernetes-map-type: granular
                      searchQueryParam:
                        description: |-
                          (String) The query parameter used to pass typed input to the search URL.
                          The query parameter used to pass typed input to the search URL.
                        type: string
                      searchResultPath:
                        description: |-
                          (String) The JSON path to the results in the response body.
                          The JSON path to the results in the response body.
                        type: string
                      searchUrl:
                        description: |-
                          (String) The URL used to populate autocomplete options.
                          The URL used to populate autocomplete options.
                        type: string
                    type: object
                  modified:
                    description: |-
                      (String) Timestamp when the field was last modified.
                      Timestamp when the field was last modified.
                    type: string
                  name:
                    description: |-
                      (String) The unique identifier of the field. Must start with a letter or digit and contain only letters, digits, underscores, or periods. Changing the name forces a new resource.
                      The unique identifier of the field. Must start with a letter or digit and contain only letters, digits, underscores, or periods. Changing the name forces a new resource.
                    type: string
                  ordinal:
                    description: |-
                      (String) A decimal string representing the field's display order in the UI. Assigned by the server when not provided.
                      A decimal string representing the field's display order in the UI. Assigned by the server when not provided.
                    type: string
                  required:
                    description: |-
                      (Boolean) When true, users must fill out this field on incidents. Defaults to false.
                      When true, users must fill out this field on incidents. Defaults to `false`.
                    type: boolean
                  reserved:
                    description: |-
                      (Boolean) When true, this field is reserved for system use and cannot be deleted.
                      When true, this field is reserved for system use and cannot be deleted.
                    type: boolean
                  tagKey:
                    description: |-
                      type fields only, the metric tag key that powers the autocomplete options. Changing the tag key forces a new resource.
                      For metric tag-type fields only, the metric tag key that powers the autocomplete options. Changing the tag key forces a new resource.
                    type: string
                  type:
                    description: |-
                      (String) The data type of the field. Changing the type forces a new resource. Valid values are dropdown, multiselect, textbox, textarray, metrictag, autocomplete, number, datetime.
                      The data type of the field. Changing the type forces a new resource. Valid values are `dropdown`, `multiselect`, `textbox`, `textarray`, `metrictag`, `autocomplete`, `number`, `datetime`.
                    type: string
                  validValue:
                    description: |-
                      (Block Set) A set of allowed values for dropdown, multiselect, and autocomplete fields; specify one block per value. Limited to 1000 values. The API does not preserve ordering, so this is modeled as an unordered set. (see below for nested schema)
                      A set of allowed values for dropdown, multiselect, and autocomplete fields; specify one block per value. Limited to 1000 values. The API does not preserve ordering, so this is modeled as an unordered set.
                    items:
                      properties:
                        description:
                          description: |-
                            (String) A detailed description of the valid value.
                            A detailed description of the valid value.
                          type: string
                        displayName:
                          description: |-
                            readable name shown in the UI. Defaults to a formatted version of the name if not provided.
                            The human-readable display name for this value.
                          type: string
                        shortDescription:
                          description: |-
                            (String) A short description of the valid value.
                            A short description of the valid value.
                          type: string
                        value:
                          description: |-
                            (String) The identifier that is stored when this option is selected.
                            The identifier that is stored when this option is selected.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

