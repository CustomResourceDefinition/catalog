---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: workerresourcetemplates.temporal.io
spec:
  group: temporal.io
  names:
    kind: WorkerResourceTemplate
    listKind: WorkerResourceTemplateList
    plural: workerresourcetemplates
    shortNames:
    - wrt
    singular: workerresourcetemplate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Referenced WorkerDeployment
      jsonPath: .spec.workerDeploymentRef.name
      name: Worker
      type: string
    - description: Kind of resource that is being templated
      jsonPath: .spec.template.kind
      name: Kind
      type: string
    - description: Age
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              template:
                type: object
                x-kubernetes-preserve-unknown-fields: true
              temporalWorkerDeploymentRef:
                properties:
                  name:
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                required:
                - name
                type: object
              workerDeploymentRef:
                properties:
                  name:
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                required:
                - name
                type: object
            required:
            - template
            type: object
            x-kubernetes-validations:
            - message: workerDeploymentRef must be set (or temporalWorkerDeploymentRef
                for migration compatibility)
              rule: has(self.workerDeploymentRef) || has(self.temporalWorkerDeploymentRef)
            - message: only one of workerDeploymentRef or temporalWorkerDeploymentRef
                may be set
              rule: '!(has(self.workerDeploymentRef) && has(self.temporalWorkerDeploymentRef))'
          status:
            properties:
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              versions:
                items:
                  properties:
                    applyError:
                      type: string
                    buildID:
                      type: string
                    lastAppliedGeneration:
                      format: int64
                      type: integer
                    lastAppliedHash:
                      type: string
                    lastTransitionTime:
                      format: date-time
                      type: string
                    resourceName:
                      type: string
                  required:
                  - buildID
                  - lastTransitionTime
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

