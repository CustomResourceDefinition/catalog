---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: wafcustomrules.appsec.datadog.m.upbound.io
spec:
  group: appsec.datadog.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - datadog
    kind: WAFCustomRule
    listKind: WAFCustomRuleList
    plural: wafcustomrules
    singular: wafcustomrule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WAFCustomRule is the Schema for the WAFCustomRules API. Provides
          a Datadog AppsecWafCustomRule resource. This can be used to create and manage
          Datadog appsec_waf_custom_rule.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WAFCustomRuleSpec defines the desired state of WAFCustomRule
            properties:
              forProvider:
                properties:
                  action:
                    description: (Block, Optional) (see below for nested schema)
                    properties:
                      action:
                        description: |-
                          (Block, Optional) (see below for nested schema)
                          Override the default action to take when the WAF custom rule would block.
                        type: string
                      parameters:
                        description: (Block, Optional) (see below for nested schema)
                        properties:
                          location:
                            description: |-
                              (String) The location to redirect to when the WAF custom rule triggers.
                              The location to redirect to when the WAF custom rule triggers.
                            type: string
                          statusCode:
                            description: |-
                              (Number) The status code to return when the WAF custom rule triggers.
                              The status code to return when the WAF custom rule triggers.
                            type: number
                        type: object
                    type: object
                  blocking:
                    description: |-
                      (Boolean) Indicates whether the WAF custom rule will block the request.
                      Indicates whether the WAF custom rule will block the request.
                    type: boolean
                  condition:
                    description: (Block List) (see below for nested schema)
                    items:
                      properties:
                        operator:
                          description: |-
                            (String) Operator to use for the WAF Condition.
                            Operator to use for the WAF Condition.
                          type: string
                        parameters:
                          description: (Block, Optional) (see below for nested schema)
                          properties:
                            data:
                              description: |-
                                (String) Identifier of a list of data from the denylist. Can only be used as substitution from the list parameter.
                                Identifier of a list of data from the denylist. Can only be used as substitution from the list parameter.
                              type: string
                            input:
                              description: (Block List) (see below for nested schema)
                              items:
                                properties:
                                  address:
                                    description: |-
                                      (String) Input from the request on which the condition should apply.
                                      Input from the request on which the condition should apply.
                                    type: string
                                  keyPath:
                                    description: |-
                                      (List of String) Specific path for the input.
                                      Specific path for the input.
                                    items:
                                      type: string
                                    type: array
                                type: object
                              type: array
                            list:
                              description: |-
                                (List of String) List of value to use with the condition. Only used with the phrase_match, !phrase_match, exact_match and !exact_match operator.
                                List of value to use with the condition. Only used with the phrase_match, !phrase_match, exact_match and !exact_match operator.
                              items:
                                type: string
                              type: array
                            options:
                              description: (Block, Optional) (see below for nested
                                schema)
                              properties:
                                caseSensitive:
                                  description: |-
                                    (Boolean) Evaluate the value as case sensitive.
                                    Evaluate the value as case sensitive.
                                  type: boolean
                                minLength:
                                  description: |-
                                    (Number) Only evaluate this condition if the value has a minimum amount of characters.
                                    Only evaluate this condition if the value has a minimum amount of characters.
                                  type: number
                              type: object
                            regex:
                              description: |-
                                (String) Regex to use with the condition. Only used with match_regex and !match_regex operator.
                                Regex to use with the condition. Only used with match_regex and !match_regex operator.
                              type: string
                            value:
                              description: |-
                                (String) Store the captured value in the specified tag name. Only used with the capture_data operator.
                                Store the captured value in the specified tag name. Only used with the capture_data operator.
                              type: string
                          type: object
                      type: object
                    type: array
                  enabled:
                    description: |-
                      (Boolean) Indicates whether the WAF custom rule is enabled.
                      Indicates whether the WAF custom rule is enabled.
                    type: boolean
                  name:
                    description: |-
                      (String) The Name of the WAF custom rule.
                      The Name of the WAF custom rule.
                    type: string
                  pathGlob:
                    description: |-
                      (String) The path glob for the WAF custom rule.
                      The path glob for the WAF custom rule.
                    type: string
                  scope:
                    description: (Block List) (see below for nested schema)
                    items:
                      properties:
                        env:
                          description: |-
                            (String) The environment scope for the WAF custom rule.
                            The environment scope for the WAF custom rule.
                          type: string
                        service:
                          description: |-
                            (String) The service scope for the WAF custom rule.
                            The service scope for the WAF custom rule.
                          type: string
                      type: object
                    type: array
                  tags:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Tags associated with the WAF custom rule. category and type tags are required. Supported categories include business_logic, attack_attempt and security_response.
                      Tags associated with the WAF custom rule. `category` and `type` tags are required. Supported categories include `business_logic`, `attack_attempt` and `security_response`.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  action:
                    description: (Block, Optional) (see below for nested schema)
                    properties:
                      action:
                        description: |-
                          (Block, Optional) (see below for nested schema)
                          Override the default action to take when the WAF custom rule would block.
                        type: string
                      parameters:
                        description: (Block, Optional) (see below for nested schema)
                        properties:
                          location:
                            description: |-
                              (String) The location to redirect to when the WAF custom rule triggers.
                              The location to redirect to when the WAF custom rule triggers.
                            type: string
                          statusCode:
                            description: |-
                              (Number) The status code to return when the WAF custom rule triggers.
                              The status code to return when the WAF custom rule triggers.
                            type: number
                        type: object
                    type: object
                  blocking:
                    description: |-
                      (Boolean) Indicates whether the WAF custom rule will block the request.
                      Indicates whether the WAF custom rule will block the request.
                    type: boolean
                  condition:
                    description: (Block List) (see below for nested schema)
                    items:
                      properties:
                        operator:
                          description: |-
                            (String) Operator to use for the WAF Condition.
                            Operator to use for the WAF Condition.
                          type: string
                        parameters:
                          description: (Block, Optional) (see below for nested schema)
                          properties:
                            data:
                              description: |-
                                (String) Identifier of a list of data from the denylist. Can only be used as substitution from the list parameter.
                                Identifier of a list of data from the denylist. Can only be used as substitution from the list parameter.
                              type: string
                            input:
                              description: (Block List) (see below for nested schema)
                              items:
                                properties:
                                  address:
                                    description: |-
                                      (String) Input from the request on which the condition should apply.
                                      Input from the request on which the condition should apply.
                                    type: string
                                  keyPath:
                                    description: |-
                                      (List of String) Specific path for the input.
                                      Specific path for the input.
                                    items:
                                      type: string
                                    type: array
                                type: object
                              type: array
                            list:
                              description: |-
                                (List of String) List of value to use with the condition. Only used with the phrase_match, !phrase_match, exact_match and !exact_match operator.
                                List of value to use with the condition. Only used with the phrase_match, !phrase_match, exact_match and !exact_match operator.
                              items:
                                type: string
                              type: array
                            options:
                              description: (Block, Optional) (see below for nested
                                schema)
                              properties:
                                caseSensitive:
                                  description: |-
                                    (Boolean) Evaluate the value as case sensitive.
                                    Evaluate the value as case sensitive.
                                  type: boolean
                                minLength:
                                  description: |-
                                    (Number) Only evaluate this condition if the value has a minimum amount of characters.
                                    Only evaluate this condition if the value has a minimum amount of characters.
                                  type: number
                              type: object
                            regex:
                              description: |-
                                (String) Regex to use with the condition. Only used with match_regex and !match_regex operator.
                                Regex to use with the condition. Only used with match_regex and !match_regex operator.
                              type: string
                            value:
                              description: |-
                                (String) Store the captured value in the specified tag name. Only used with the capture_data operator.
                                Store the captured value in the specified tag name. Only used with the capture_data operator.
                              type: string
                          type: object
                      type: object
                    type: array
                  enabled:
                    description: |-
                      (Boolean) Indicates whether the WAF custom rule is enabled.
                      Indicates whether the WAF custom rule is enabled.
                    type: boolean
                  name:
                    description: |-
                      (String) The Name of the WAF custom rule.
                      The Name of the WAF custom rule.
                    type: string
                  pathGlob:
                    description: |-
                      (String) The path glob for the WAF custom rule.
                      The path glob for the WAF custom rule.
                    type: string
                  scope:
                    description: (Block List) (see below for nested schema)
                    items:
                      properties:
                        env:
                          description: |-
                            (String) The environment scope for the WAF custom rule.
                            The environment scope for the WAF custom rule.
                          type: string
                        service:
                          description: |-
                            (String) The service scope for the WAF custom rule.
                            The service scope for the WAF custom rule.
                          type: string
                      type: object
                    type: array
                  tags:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Tags associated with the WAF custom rule. category and type tags are required. Supported categories include business_logic, attack_attempt and security_response.
                      Tags associated with the WAF custom rule. `category` and `type` tags are required. Supported categories include `business_logic`, `attack_attempt` and `security_response`.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.blocking is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.blocking)
                || (has(self.initProvider) && has(self.initProvider.blocking))'
            - message: spec.forProvider.enabled is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.enabled)
                || (has(self.initProvider) && has(self.initProvider.enabled))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.tags is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.tags)
                || (has(self.initProvider) && has(self.initProvider.tags))'
          status:
            description: WAFCustomRuleStatus defines the observed state of WAFCustomRule.
            properties:
              atProvider:
                properties:
                  action:
                    description: (Block, Optional) (see below for nested schema)
                    properties:
                      action:
                        description: |-
                          (Block, Optional) (see below for nested schema)
                          Override the default action to take when the WAF custom rule would block.
                        type: string
                      parameters:
                        description: (Block, Optional) (see below for nested schema)
                        properties:
                          location:
                            description: |-
                              (String) The location to redirect to when the WAF custom rule triggers.
                              The location to redirect to when the WAF custom rule triggers.
                            type: string
                          statusCode:
                            description: |-
                              (Number) The status code to return when the WAF custom rule triggers.
                              The status code to return when the WAF custom rule triggers.
                            type: number
                        type: object
                    type: object
                  blocking:
                    description: |-
                      (Boolean) Indicates whether the WAF custom rule will block the request.
                      Indicates whether the WAF custom rule will block the request.
                    type: boolean
                  condition:
                    description: (Block List) (see below for nested schema)
                    items:
                      properties:
                        operator:
                          description: |-
                            (String) Operator to use for the WAF Condition.
                            Operator to use for the WAF Condition.
                          type: string
                        parameters:
                          description: (Block, Optional) (see below for nested schema)
                          properties:
                            data:
                              description: |-
                                (String) Identifier of a list of data from the denylist. Can only be used as substitution from the list parameter.
                                Identifier of a list of data from the denylist. Can only be used as substitution from the list parameter.
                              type: string
                            input:
                              description: (Block List) (see below for nested schema)
                              items:
                                properties:
                                  address:
                                    description: |-
                                      (String) Input from the request on which the condition should apply.
                                      Input from the request on which the condition should apply.
                                    type: string
                                  keyPath:
                                    description: |-
                                      (List of String) Specific path for the input.
                                      Specific path for the input.
                                    items:
                                      type: string
                                    type: array
                                type: object
                              type: array
                            list:
                              description: |-
                                (List of String) List of value to use with the condition. Only used with the phrase_match, !phrase_match, exact_match and !exact_match operator.
                                List of value to use with the condition. Only used with the phrase_match, !phrase_match, exact_match and !exact_match operator.
                              items:
                                type: string
                              type: array
                            options:
                              description: (Block, Optional) (see below for nested
                                schema)
                              properties:
                                caseSensitive:
                                  description: |-
                                    (Boolean) Evaluate the value as case sensitive.
                                    Evaluate the value as case sensitive.
                                  type: boolean
                                minLength:
                                  description: |-
                                    (Number) Only evaluate this condition if the value has a minimum amount of characters.
                                    Only evaluate this condition if the value has a minimum amount of characters.
                                  type: number
                              type: object
                            regex:
                              description: |-
                                (String) Regex to use with the condition. Only used with match_regex and !match_regex operator.
                                Regex to use with the condition. Only used with match_regex and !match_regex operator.
                              type: string
                            value:
                              description: |-
                                (String) Store the captured value in the specified tag name. Only used with the capture_data operator.
                                Store the captured value in the specified tag name. Only used with the capture_data operator.
                              type: string
                          type: object
                      type: object
                    type: array
                  enabled:
                    description: |-
                      (Boolean) Indicates whether the WAF custom rule is enabled.
                      Indicates whether the WAF custom rule is enabled.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) The Name of the WAF custom rule.
                      The Name of the WAF custom rule.
                    type: string
                  pathGlob:
                    description: |-
                      (String) The path glob for the WAF custom rule.
                      The path glob for the WAF custom rule.
                    type: string
                  scope:
                    description: (Block List) (see below for nested schema)
                    items:
                      properties:
                        env:
                          description: |-
                            (String) The environment scope for the WAF custom rule.
                            The environment scope for the WAF custom rule.
                          type: string
                        service:
                          description: |-
                            (String) The service scope for the WAF custom rule.
                            The service scope for the WAF custom rule.
                          type: string
                      type: object
                    type: array
                  tags:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Tags associated with the WAF custom rule. category and type tags are required. Supported categories include business_logic, attack_attempt and security_response.
                      Tags associated with the WAF custom rule. `category` and `type` tags are required. Supported categories include `business_logic`, `attack_attempt` and `security_response`.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

