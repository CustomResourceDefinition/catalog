---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: pages.statuspage.datadog.m.upbound.io
spec:
  group: statuspage.datadog.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - datadog
    kind: Page
    listKind: PageList
    plural: pages
    singular: page
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Page is the Schema for the Pages API. Provides a Datadog status
          page resource. This can be used to create and manage Datadog status pages.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PageSpec defines the desired state of Page
            properties:
              forProvider:
                properties:
                  companyLogo:
                    description: |-
                      (String) The company logo displayed on the status page.
                      The company logo displayed on the status page.
                    type: string
                  domainPrefix:
                    description: |-
                      (String) The subdomain prefix used to build the status page's URL.
                      The subdomain prefix used to build the status page's URL.
                    type: string
                  emailHeaderImage:
                    description: |-
                      (String) The header image included in subscriber emails.
                      The header image included in subscriber emails.
                    type: string
                  favicon:
                    description: |-
                      (String) The favicon displayed for the status page.
                      The favicon displayed for the status page.
                    type: string
                  name:
                    description: |-
                      (String) The name of the status page.
                      The name of the status page.
                    type: string
                  slackAppIcon:
                    description: |-
                      (String) The icon used for the status page's Slack app integration.
                      The icon used for the status page's Slack app integration.
                    type: string
                  slackSubscriptionsEnabled:
                    description: |-
                      (Boolean) Whether Slack subscriber notifications are enabled for the status page.
                      Whether Slack subscriber notifications are enabled for the status page.
                    type: boolean
                  subscriptionsEnabled:
                    description: |-
                      (Boolean) Whether subscriber notifications are enabled for the status page.
                      Whether subscriber notifications are enabled for the status page.
                    type: boolean
                  type:
                    description: |-
                      (String) The type of the status page. Valid values are: public, internal.
                      The type of the status page. Valid values are: public, internal.
                    type: string
                  visualizationType:
                    description: |-
                      (String) How component statuses are visualized on the page. Valid values are: bars_and_uptime_percentage, bars_only, component_name_only.
                      How component statuses are visualized on the page. Valid values are: bars_and_uptime_percentage, bars_only, component_name_only.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  companyLogo:
                    description: |-
                      (String) The company logo displayed on the status page.
                      The company logo displayed on the status page.
                    type: string
                  domainPrefix:
                    description: |-
                      (String) The subdomain prefix used to build the status page's URL.
                      The subdomain prefix used to build the status page's URL.
                    type: string
                  emailHeaderImage:
                    description: |-
                      (String) The header image included in subscriber emails.
                      The header image included in subscriber emails.
                    type: string
                  favicon:
                    description: |-
                      (String) The favicon displayed for the status page.
                      The favicon displayed for the status page.
                    type: string
                  name:
                    description: |-
                      (String) The name of the status page.
                      The name of the status page.
                    type: string
                  slackAppIcon:
                    description: |-
                      (String) The icon used for the status page's Slack app integration.
                      The icon used for the status page's Slack app integration.
                    type: string
                  slackSubscriptionsEnabled:
                    description: |-
                      (Boolean) Whether Slack subscriber notifications are enabled for the status page.
                      Whether Slack subscriber notifications are enabled for the status page.
                    type: boolean
                  subscriptionsEnabled:
                    description: |-
                      (Boolean) Whether subscriber notifications are enabled for the status page.
                      Whether subscriber notifications are enabled for the status page.
                    type: boolean
                  type:
                    description: |-
                      (String) The type of the status page. Valid values are: public, internal.
                      The type of the status page. Valid values are: public, internal.
                    type: string
                  visualizationType:
                    description: |-
                      (String) How component statuses are visualized on the page. Valid values are: bars_and_uptime_percentage, bars_only, component_name_only.
                      How component statuses are visualized on the page. Valid values are: bars_and_uptime_percentage, bars_only, component_name_only.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.domainPrefix is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.domainPrefix)
                || (has(self.initProvider) && has(self.initProvider.domainPrefix))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
            - message: spec.forProvider.visualizationType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.visualizationType)
                || (has(self.initProvider) && has(self.initProvider.visualizationType))'
          status:
            description: PageStatus defines the observed state of Page.
            properties:
              atProvider:
                properties:
                  companyLogo:
                    description: |-
                      (String) The company logo displayed on the status page.
                      The company logo displayed on the status page.
                    type: string
                  createdAt:
                    description: |-
                      (String) Timestamp when the status page was created.
                      Timestamp when the status page was created.
                    type: string
                  customDomain:
                    description: |-
                      domain flow, not by this resource.
                      The custom domain configured for the status page, if any. Managed through a separate custom-domain flow, not by this resource.
                    type: string
                  customDomainEnabled:
                    description: |-
                      domain flow, not by this resource.
                      Whether the custom domain is enabled for the status page. Managed through a separate custom-domain flow, not by this resource.
                    type: boolean
                  domainPrefix:
                    description: |-
                      (String) The subdomain prefix used to build the status page's URL.
                      The subdomain prefix used to build the status page's URL.
                    type: string
                  emailHeaderImage:
                    description: |-
                      (String) The header image included in subscriber emails.
                      The header image included in subscriber emails.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the status page is published. Managed by the publish/unpublish API operations, not by this resource; always reflects the page's current state.
                      Whether the status page is published. Managed by the publish/unpublish API operations, not by this resource; always reflects the page's current state.
                    type: boolean
                  favicon:
                    description: |-
                      (String) The favicon displayed for the status page.
                      The favicon displayed for the status page.
                    type: string
                  id:
                    description: (String) The ID of the status page.
                    type: string
                  modifiedAt:
                    description: |-
                      (String) Timestamp when the status page was last modified.
                      Timestamp when the status page was last modified.
                    type: string
                  name:
                    description: |-
                      (String) The name of the status page.
                      The name of the status page.
                    type: string
                  pageUrl:
                    description: |-
                      (String) The URL of the status page.
                      The URL of the status page.
                    type: string
                  slackAppIcon:
                    description: |-
                      (String) The icon used for the status page's Slack app integration.
                      The icon used for the status page's Slack app integration.
                    type: string
                  slackSubscriptionsEnabled:
                    description: |-
                      (Boolean) Whether Slack subscriber notifications are enabled for the status page.
                      Whether Slack subscriber notifications are enabled for the status page.
                    type: boolean
                  subscriptionsEnabled:
                    description: |-
                      (Boolean) Whether subscriber notifications are enabled for the status page.
                      Whether subscriber notifications are enabled for the status page.
                    type: boolean
                  type:
                    description: |-
                      (String) The type of the status page. Valid values are: public, internal.
                      The type of the status page. Valid values are: public, internal.
                    type: string
                  visualizationType:
                    description: |-
                      (String) How component statuses are visualized on the page. Valid values are: bars_and_uptime_percentage, bars_only, component_name_only.
                      How component statuses are visualized on the page. Valid values are: bars_and_uptime_percentage, bars_only, component_name_only.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

