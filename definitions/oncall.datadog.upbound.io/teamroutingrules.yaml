---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: teamroutingrules.oncall.datadog.upbound.io
spec:
  group: oncall.datadog.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - datadog
    kind: TeamRoutingRules
    listKind: TeamRoutingRulesList
    plural: teamroutingrules
    singular: teamroutingrules
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TeamRoutingRules is the Schema for the TeamRoutingRuless API.
          Provides a Datadog On-Call team routing rules resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TeamRoutingRulesSpec defines the desired state of TeamRoutingRules
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  rule:
                    description: |-
                      (Block List) List of team routing rules. (see below for nested schema)
                      List of team routing rules.
                    items:
                      properties:
                        action:
                          description: |-
                            (Block List) Specifies the list of actions to perform when the routing rule is matched. (see below for nested schema)
                            Specifies the list of actions to perform when the routing rule is matched.
                          items:
                            properties:
                              escalationPolicy:
                                description: (String) ID of the policy to be applied
                                  when this routing rule matches. The last rule must
                                  define an escalation policy, either via this attribute
                                  or via an escalation_policy action.
                                properties:
                                  ackTimeoutMinutes:
                                    description: |-
                                      triggered. Value must be between 30 and 4320.
                                      Number of minutes before an acknowledged page is re-triggered. Value must be between 30 and 4320.
                                    type: number
                                  policyId:
                                    description: |-
                                      (String) Escalation policy ID.
                                      Escalation policy ID.
                                    type: string
                                  policyIdRef:
                                    description: Reference to a EscalationPolicy in
                                      oncall.datadog to populate policyId.
                                    properties:
                                      name:
                                        description: Name of the referenced object.
                                        type: string
                                      policy:
                                        description: Policies for referencing.
                                        properties:
                                          resolution:
                                            default: Required
                                            description: |-
                                              Resolution specifies whether resolution of this reference is required.
                                              The default is 'Required', which means the reconcile will fail if the
                                              reference cannot be resolved. 'Optional' means this reference will be
                                              a no-op if it cannot be resolved.
                                            enum:
                                            - Required
                                            - Optional
                                            type: string
                                          resolve:
                                            description: |-
                                              Resolve specifies when this reference should be resolved. The default
                                              is 'IfNotPresent', which will attempt to resolve the reference only when
                                              the corresponding field is not present. Use 'Always' to resolve the
                                              reference on every reconcile.
                                            enum:
                                            - Always
                                            - IfNotPresent
                                            type: string
                                        type: object
                                    required:
                                    - name
                                    type: object
                                  policyIdSelector:
                                    description: Selector for a EscalationPolicy in
                                      oncall.datadog to populate policyId.
                                    properties:
                                      matchControllerRef:
                                        description: |-
                                          MatchControllerRef ensures an object with the same controller reference
                                          as the selecting object is selected.
                                        type: boolean
                                      matchLabels:
                                        additionalProperties:
                                          type: string
                                        description: MatchLabels ensures an object
                                          with matching labels is selected.
                                        type: object
                                      policy:
                                        description: Policies for selection.
                                        properties:
                                          resolution:
                                            default: Required
                                            description: |-
                                              Resolution specifies whether resolution of this reference is required.
                                              The default is 'Required', which means the reconcile will fail if the
                                              reference cannot be resolved. 'Optional' means this reference will be
                                              a no-op if it cannot be resolved.
                                            enum:
                                            - Required
                                            - Optional
                                            type: string
                                          resolve:
                                            description: |-
                                              Resolve specifies when this reference should be resolved. The default
                                              is 'IfNotPresent', which will attempt to resolve the reference only when
                                              the corresponding field is not present. Use 'Always' to resolve the
                                              reference on every reconcile.
                                            enum:
                                            - Always
                                            - IfNotPresent
                                            type: string
                                        type: object
                                    type: object
                                  supportHours:
                                    description: |-
                                      (Block, Optional) Support hours during which the escalation policy will execute. (see below for nested schema)
                                      Support hours during which the escalation policy will trigger.
                                    properties:
                                      restriction:
                                        description: (Block List) (see below for nested
                                          schema)
                                        items:
                                          properties:
                                            endDay:
                                              description: |-
                                                (String) The weekday when the restriction period ends. Valid values are monday, tuesday, wednesday, thursday, friday, saturday, sunday.
                                                The weekday when the restriction period ends. Valid values are `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday`, `sunday`.
                                              type: string
                                            endTime:
                                              description: |-
                                                (String) The time of day when the restriction ends (hh:mm:ss).
                                                The time of day when the restriction ends (hh:mm:ss).
                                              type: string
                                            startDay:
                                              description: |-
                                                (String) The weekday when the restriction period starts. Valid values are monday, tuesday, wednesday, thursday, friday, saturday, sunday.
                                                The weekday when the restriction period starts. Valid values are `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday`, `sunday`.
                                              type: string
                                            startTime:
                                              description: |-
                                                (String) The time of day when the restriction begins (hh:mm:ss).
                                                The time of day when the restriction begins (hh:mm:ss).
                                              type: string
                                          type: object
                                        type: array
                                      timeZone:
                                        description: |-
                                          (String) Specifies the time zone applicable to the restrictions, e.g. America/New_York.
                                          Specifies the time zone applicable to the restrictions, e.g. `America/New_York`.
                                        type: string
                                    type: object
                                  urgency:
                                    description: |-
                                      (String) Defines the urgency for pages created via this rule. Only valid if escalation_policy is set. Valid values are high, low, dynamic.
                                      Urgency for pages created via this action. Valid values are `high`, `low`, `dynamic`.
                                    type: string
                                type: object
                              sendSlackMessage:
                                description: (Block, Optional) (see below for nested
                                  schema)
                                properties:
                                  channel:
                                    description: |-
                                      (String) Slack channel ID.
                                      Slack channel ID.
                                    type: string
                                  workspace:
                                    description: |-
                                      (String) Slack workspace ID.
                                      Slack workspace ID.
                                    type: string
                                type: object
                              sendTeamsMessage:
                                description: (Block, Optional) (see below for nested
                                  schema)
                                properties:
                                  channel:
                                    description: |-
                                      (String) Slack channel ID.
                                      Teams channel ID.
                                    type: string
                                  team:
                                    description: |-
                                      (String) Teams team ID.
                                      Teams team ID.
                                    type: string
                                  tenant:
                                    description: |-
                                      (String) Teams tenant ID.
                                      Teams tenant ID.
                                    type: string
                                type: object
                              triggerWorkflowAutomation:
                                description: (Block, Optional) (see below for nested
                                  schema)
                                properties:
                                  handle:
                                    description: |-
                                      (String) The handle of the Workflow Automation to trigger.
                                      The handle of the Workflow Automation to trigger.
                                    type: string
                                type: object
                            type: object
                          type: array
                        escalationPolicy:
                          description: |-
                            (String) ID of the policy to be applied when this routing rule matches. The last rule must define an escalation policy, either via this attribute or via an escalation_policy action.
                            ID of the policy to be applied when this routing rule matches. The last rule must define an escalation policy, either via this attribute or via an `escalation_policy` action.
                          type: string
                        query:
                          description: |-
                            all rule. Defaults to "".
                            Defines the query or condition that triggers this routing rule. Must not be set on the last rule, which acts as a catch-all rule. Defaults to `""`.
                          type: string
                        timeRestrictions:
                          description: |-
                            all rule. (see below for nested schema)
                            Holds time zone information and a list of time restrictions for a routing rule. Must not be set on the last rule, which acts as a catch-all rule.
                          properties:
                            restriction:
                              description: |-
                                (Block List) (see below for nested schema)
                                List of restrictions for the rule.
                              items:
                                properties:
                                  endDay:
                                    description: |-
                                      (String) The weekday when the restriction period ends. Valid values are monday, tuesday, wednesday, thursday, friday, saturday, sunday.
                                      The weekday when the restriction period ends. Valid values are `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday`, `sunday`.
                                    type: string
                                  endTime:
                                    description: |-
                                      (String) The time of day when the restriction ends (hh:mm:ss).
                                      The time of day when the restriction ends (hh:mm:ss).
                                    type: string
                                  startDay:
                                    description: |-
                                      (String) The weekday when the restriction period starts. Valid values are monday, tuesday, wednesday, thursday, friday, saturday, sunday.
                                      The weekday when the restriction period starts. Valid values are `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday`, `sunday`.
                                    type: string
                                  startTime:
                                    description: |-
                                      (String) The time of day when the restriction begins (hh:mm:ss).
                                      The time of day when the restriction begins (hh:mm:ss).
                                    type: string
                                type: object
                              type: array
                            timeZone:
                              description: |-
                                (String) Specifies the time zone applicable to the restrictions, e.g. America/New_York.
                                Specifies the time zone applicable to the restrictions, e.g. `America/New_York`.
                              type: string
                          type: object
                        urgency:
                          description: |-
                            (String) Defines the urgency for pages created via this rule. Only valid if escalation_policy is set. Valid values are high, low, dynamic.
                            Defines the urgency for pages created via this rule. Only valid if `escalation_policy` is set. Valid values are `high`, `low`, `dynamic`.
                          type: string
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  rule:
                    description: |-
                      (Block List) List of team routing rules. (see below for nested schema)
                      List of team routing rules.
                    items:
                      properties:
                        action:
                          description: |-
                            (Block List) Specifies the list of actions to perform when the routing rule is matched. (see below for nested schema)
                            Specifies the list of actions to perform when the routing rule is matched.
                          items:
                            properties:
                              escalationPolicy:
                                description: (String) ID of the policy to be applied
                                  when this routing rule matches. The last rule must
                                  define an escalation policy, either via this attribute
                                  or via an escalation_policy action.
                                properties:
                                  ackTimeoutMinutes:
                                    description: |-
                                      triggered. Value must be between 30 and 4320.
                                      Number of minutes before an acknowledged page is re-triggered. Value must be between 30 and 4320.
                                    type: number
                                  policyId:
                                    description: |-
                                      (String) Escalation policy ID.
                                      Escalation policy ID.
                                    type: string
                                  policyIdRef:
                                    description: Reference to a EscalationPolicy in
                                      oncall.datadog to populate policyId.
                                    properties:
                                      name:
                                        description: Name of the referenced object.
                                        type: string
                                      policy:
                                        description: Policies for referencing.
                                        properties:
                                          resolution:
                                            default: Required
                                            description: |-
                                              Resolution specifies whether resolution of this reference is required.
                                              The default is 'Required', which means the reconcile will fail if the
                                              reference cannot be resolved. 'Optional' means this reference will be
                                              a no-op if it cannot be resolved.
                                            enum:
                                            - Required
                                            - Optional
                                            type: string
                                          resolve:
                                            description: |-
                                              Resolve specifies when this reference should be resolved. The default
                                              is 'IfNotPresent', which will attempt to resolve the reference only when
                                              the corresponding field is not present. Use 'Always' to resolve the
                                              reference on every reconcile.
                                            enum:
                                            - Always
                                            - IfNotPresent
                                            type: string
                                        type: object
                                    required:
                                    - name
                                    type: object
                                  policyIdSelector:
                                    description: Selector for a EscalationPolicy in
                                      oncall.datadog to populate policyId.
                                    properties:
                                      matchControllerRef:
                                        description: |-
                                          MatchControllerRef ensures an object with the same controller reference
                                          as the selecting object is selected.
                                        type: boolean
                                      matchLabels:
                                        additionalProperties:
                                          type: string
                                        description: MatchLabels ensures an object
                                          with matching labels is selected.
                                        type: object
                                      policy:
                                        description: Policies for selection.
                                        properties:
                                          resolution:
                                            default: Required
                                            description: |-
                                              Resolution specifies whether resolution of this reference is required.
                                              The default is 'Required', which means the reconcile will fail if the
                                              reference cannot be resolved. 'Optional' means this reference will be
                                              a no-op if it cannot be resolved.
                                            enum:
                                            - Required
                                            - Optional
                                            type: string
                                          resolve:
                                            description: |-
                                              Resolve specifies when this reference should be resolved. The default
                                              is 'IfNotPresent', which will attempt to resolve the reference only when
                                              the corresponding field is not present. Use 'Always' to resolve the
                                              reference on every reconcile.
                                            enum:
                                            - Always
                                            - IfNotPresent
                                            type: string
                                        type: object
                                    type: object
                                  supportHours:
                                    description: |-
                                      (Block, Optional) Support hours during which the escalation policy will execute. (see below for nested schema)
                                      Support hours during which the escalation policy will trigger.
                                    properties:
                                      restriction:
                                        description: (Block List) (see below for nested
                                          schema)
                                        items:
                                          properties:
                                            endDay:
                                              description: |-
                                                (String) The weekday when the restriction period ends. Valid values are monday, tuesday, wednesday, thursday, friday, saturday, sunday.
                                                The weekday when the restriction period ends. Valid values are `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday`, `sunday`.
                                              type: string
                                            endTime:
                                              description: |-
                                                (String) The time of day when the restriction ends (hh:mm:ss).
                                                The time of day when the restriction ends (hh:mm:ss).
                                              type: string
                                            startDay:
                                              description: |-
                                                (String) The weekday when the restriction period starts. Valid values are monday, tuesday, wednesday, thursday, friday, saturday, sunday.
                                                The weekday when the restriction period starts. Valid values are `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday`, `sunday`.
                                              type: string
                                            startTime:
                                              description: |-
                                                (String) The time of day when the restriction begins (hh:mm:ss).
                                                The time of day when the restriction begins (hh:mm:ss).
                                              type: string
                                          type: object
                                        type: array
                                      timeZone:
                                        description: |-
                                          (String) Specifies the time zone applicable to the restrictions, e.g. America/New_York.
                                          Specifies the time zone applicable to the restrictions, e.g. `America/New_York`.
                                        type: string
                                    type: object
                                  urgency:
                                    description: |-
                                      (String) Defines the urgency for pages created via this rule. Only valid if escalation_policy is set. Valid values are high, low, dynamic.
                                      Urgency for pages created via this action. Valid values are `high`, `low`, `dynamic`.
                                    type: string
                                type: object
                              sendSlackMessage:
                                description: (Block, Optional) (see below for nested
                                  schema)
                                properties:
                                  channel:
                                    description: |-
                                      (String) Slack channel ID.
                                      Slack channel ID.
                                    type: string
                                  workspace:
                                    description: |-
                                      (String) Slack workspace ID.
                                      Slack workspace ID.
                                    type: string
                                type: object
                              sendTeamsMessage:
                                description: (Block, Optional) (see below for nested
                                  schema)
                                properties:
                                  channel:
                                    description: |-
                                      (String) Slack channel ID.
                                      Teams channel ID.
                                    type: string
                                  team:
                                    description: |-
                                      (String) Teams team ID.
                                      Teams team ID.
                                    type: string
                                  tenant:
                                    description: |-
                                      (String) Teams tenant ID.
                                      Teams tenant ID.
                                    type: string
                                type: object
                              triggerWorkflowAutomation:
                                description: (Block, Optional) (see below for nested
                                  schema)
                                properties:
                                  handle:
                                    description: |-
                                      (String) The handle of the Workflow Automation to trigger.
                                      The handle of the Workflow Automation to trigger.
                                    type: string
                                type: object
                            type: object
                          type: array
                        escalationPolicy:
                          description: |-
                            (String) ID of the policy to be applied when this routing rule matches. The last rule must define an escalation policy, either via this attribute or via an escalation_policy action.
                            ID of the policy to be applied when this routing rule matches. The last rule must define an escalation policy, either via this attribute or via an `escalation_policy` action.
                          type: string
                        query:
                          description: |-
                            all rule. Defaults to "".
                            Defines the query or condition that triggers this routing rule. Must not be set on the last rule, which acts as a catch-all rule. Defaults to `""`.
                          type: string
                        timeRestrictions:
                          description: |-
                            all rule. (see below for nested schema)
                            Holds time zone information and a list of time restrictions for a routing rule. Must not be set on the last rule, which acts as a catch-all rule.
                          properties:
                            restriction:
                              description: |-
                                (Block List) (see below for nested schema)
                                List of restrictions for the rule.
                              items:
                                properties:
                                  endDay:
                                    description: |-
                                      (String) The weekday when the restriction period ends. Valid values are monday, tuesday, wednesday, thursday, friday, saturday, sunday.
                                      The weekday when the restriction period ends. Valid values are `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday`, `sunday`.
                                    type: string
                                  endTime:
                                    description: |-
                                      (String) The time of day when the restriction ends (hh:mm:ss).
                                      The time of day when the restriction ends (hh:mm:ss).
                                    type: string
                                  startDay:
                                    description: |-
                                      (String) The weekday when the restriction period starts. Valid values are monday, tuesday, wednesday, thursday, friday, saturday, sunday.
                                      The weekday when the restriction period starts. Valid values are `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday`, `sunday`.
                                    type: string
                                  startTime:
                                    description: |-
                                      (String) The time of day when the restriction begins (hh:mm:ss).
                                      The time of day when the restriction begins (hh:mm:ss).
                                    type: string
                                type: object
                              type: array
                            timeZone:
                              description: |-
                                (String) Specifies the time zone applicable to the restrictions, e.g. America/New_York.
                                Specifies the time zone applicable to the restrictions, e.g. `America/New_York`.
                              type: string
                          type: object
                        urgency:
                          description: |-
                            (String) Defines the urgency for pages created via this rule. Only valid if escalation_policy is set. Valid values are high, low, dynamic.
                            Defines the urgency for pages created via this rule. Only valid if `escalation_policy` is set. Valid values are `high`, `low`, `dynamic`.
                          type: string
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: TeamRoutingRulesStatus defines the observed state of TeamRoutingRules.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) ID of the team to associate the routing
                      rules with.
                    type: string
                  rule:
                    description: |-
                      (Block List) List of team routing rules. (see below for nested schema)
                      List of team routing rules.
                    items:
                      properties:
                        action:
                          description: |-
                            (Block List) Specifies the list of actions to perform when the routing rule is matched. (see below for nested schema)
                            Specifies the list of actions to perform when the routing rule is matched.
                          items:
                            properties:
                              escalationPolicy:
                                description: (String) ID of the policy to be applied
                                  when this routing rule matches. The last rule must
                                  define an escalation policy, either via this attribute
                                  or via an escalation_policy action.
                                properties:
                                  ackTimeoutMinutes:
                                    description: |-
                                      triggered. Value must be between 30 and 4320.
                                      Number of minutes before an acknowledged page is re-triggered. Value must be between 30 and 4320.
                                    type: number
                                  policyId:
                                    description: |-
                                      (String) Escalation policy ID.
                                      Escalation policy ID.
                                    type: string
                                  supportHours:
                                    description: |-
                                      (Block, Optional) Support hours during which the escalation policy will execute. (see below for nested schema)
                                      Support hours during which the escalation policy will trigger.
                                    properties:
                                      restriction:
                                        description: (Block List) (see below for nested
                                          schema)
                                        items:
                                          properties:
                                            endDay:
                                              description: |-
                                                (String) The weekday when the restriction period ends. Valid values are monday, tuesday, wednesday, thursday, friday, saturday, sunday.
                                                The weekday when the restriction period ends. Valid values are `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday`, `sunday`.
                                              type: string
                                            endTime:
                                              description: |-
                                                (String) The time of day when the restriction ends (hh:mm:ss).
                                                The time of day when the restriction ends (hh:mm:ss).
                                              type: string
                                            startDay:
                                              description: |-
                                                (String) The weekday when the restriction period starts. Valid values are monday, tuesday, wednesday, thursday, friday, saturday, sunday.
                                                The weekday when the restriction period starts. Valid values are `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday`, `sunday`.
                                              type: string
                                            startTime:
                                              description: |-
                                                (String) The time of day when the restriction begins (hh:mm:ss).
                                                The time of day when the restriction begins (hh:mm:ss).
                                              type: string
                                          type: object
                                        type: array
                                      timeZone:
                                        description: |-
                                          (String) Specifies the time zone applicable to the restrictions, e.g. America/New_York.
                                          Specifies the time zone applicable to the restrictions, e.g. `America/New_York`.
                                        type: string
                                    type: object
                                  urgency:
                                    description: |-
                                      (String) Defines the urgency for pages created via this rule. Only valid if escalation_policy is set. Valid values are high, low, dynamic.
                                      Urgency for pages created via this action. Valid values are `high`, `low`, `dynamic`.
                                    type: string
                                type: object
                              sendSlackMessage:
                                description: (Block, Optional) (see below for nested
                                  schema)
                                properties:
                                  channel:
                                    description: |-
                                      (String) Slack channel ID.
                                      Slack channel ID.
                                    type: string
                                  workspace:
                                    description: |-
                                      (String) Slack workspace ID.
                                      Slack workspace ID.
                                    type: string
                                type: object
                              sendTeamsMessage:
                                description: (Block, Optional) (see below for nested
                                  schema)
                                properties:
                                  channel:
                                    description: |-
                                      (String) Slack channel ID.
                                      Teams channel ID.
                                    type: string
                                  team:
                                    description: |-
                                      (String) Teams team ID.
                                      Teams team ID.
                                    type: string
                                  tenant:
                                    description: |-
                                      (String) Teams tenant ID.
                                      Teams tenant ID.
                                    type: string
                                type: object
                              triggerWorkflowAutomation:
                                description: (Block, Optional) (see below for nested
                                  schema)
                                properties:
                                  handle:
                                    description: |-
                                      (String) The handle of the Workflow Automation to trigger.
                                      The handle of the Workflow Automation to trigger.
                                    type: string
                                type: object
                            type: object
                          type: array
                        escalationPolicy:
                          description: |-
                            (String) ID of the policy to be applied when this routing rule matches. The last rule must define an escalation policy, either via this attribute or via an escalation_policy action.
                            ID of the policy to be applied when this routing rule matches. The last rule must define an escalation policy, either via this attribute or via an `escalation_policy` action.
                          type: string
                        id:
                          description: |-
                            (String) ID of the team to associate the routing rules with.
                            The ID of this rule.
                          type: string
                        query:
                          description: |-
                            all rule. Defaults to "".
                            Defines the query or condition that triggers this routing rule. Must not be set on the last rule, which acts as a catch-all rule. Defaults to `""`.
                          type: string
                        timeRestrictions:
                          description: |-
                            all rule. (see below for nested schema)
                            Holds time zone information and a list of time restrictions for a routing rule. Must not be set on the last rule, which acts as a catch-all rule.
                          properties:
                            restriction:
                              description: |-
                                (Block List) (see below for nested schema)
                                List of restrictions for the rule.
                              items:
                                properties:
                                  endDay:
                                    description: |-
                                      (String) The weekday when the restriction period ends. Valid values are monday, tuesday, wednesday, thursday, friday, saturday, sunday.
                                      The weekday when the restriction period ends. Valid values are `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday`, `sunday`.
                                    type: string
                                  endTime:
                                    description: |-
                                      (String) The time of day when the restriction ends (hh:mm:ss).
                                      The time of day when the restriction ends (hh:mm:ss).
                                    type: string
                                  startDay:
                                    description: |-
                                      (String) The weekday when the restriction period starts. Valid values are monday, tuesday, wednesday, thursday, friday, saturday, sunday.
                                      The weekday when the restriction period starts. Valid values are `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday`, `sunday`.
                                    type: string
                                  startTime:
                                    description: |-
                                      (String) The time of day when the restriction begins (hh:mm:ss).
                                      The time of day when the restriction begins (hh:mm:ss).
                                    type: string
                                type: object
                              type: array
                            timeZone:
                              description: |-
                                (String) Specifies the time zone applicable to the restrictions, e.g. America/New_York.
                                Specifies the time zone applicable to the restrictions, e.g. `America/New_York`.
                              type: string
                          type: object
                        urgency:
                          description: |-
                            (String) Defines the urgency for pages created via this rule. Only valid if escalation_policy is set. Valid values are high, low, dynamic.
                            Defines the urgency for pages created via this rule. Only valid if `escalation_policy` is set. Valid values are `high`, `low`, `dynamic`.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

