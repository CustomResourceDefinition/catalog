---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: confluentcloudsets.core.cpc.platform.confluent.io
spec:
  group: core.cpc.platform.confluent.io
  names:
    categories:
    - all
    - confluent
    - cpc
    - cpccore
    kind: ConfluentCloudSet
    listKind: ConfluentCloudSetList
    plural: confluentcloudsets
    shortNames:
    - ccs
    - confluentcloudset
    singular: confluentcloudset
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: ConfluentCloudSet is the Schema for the ConfluentCloudSets API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ConfluentCloudSetSpec defines the desired state of ConfluentCloudSet
            properties:
              clientConfigs:
                description: clientConfigs configures clients to clusters in confluent
                  cloud.
                properties:
                  connector:
                    description: connector configures a client for a confluent cloud
                      connector.
                    properties:
                      authentication:
                        description: credentialStoreConfigRef configures credentials
                          to connect to the cluster.
                        properties:
                          basic:
                            description: Basic specifies basic authentication configuration.
                            properties:
                              blueprint:
                                description: blueprintKey defines the key at the blueprint
                                  layer and cannot be overridden by deployments. This
                                  key will be used by all the deployments using this
                                  blueprint
                                properties:
                                  credentialStoreRef:
                                    description: credentialStoreRef specifies the
                                      key in the credential store
                                    properties:
                                      key:
                                        description: key defines the name of the key
                                          in secret the CredentialStoreConfigRef refers
                                          to
                                        type: string
                                      name:
                                        description: name defines the name of the
                                          CredentialStoreConfigRef
                                        type: string
                                    type: object
                                required:
                                - credentialStoreRef
                                type: object
                              providerType:
                                allOf:
                                - enum:
                                  - deployment
                                  - blueprint
                                - enum:
                                  - blueprint
                                description: providerType defines the provider for
                                  the Credential. Valid options are `blueprint`
                                type: string
                            required:
                            - blueprint
                            - providerType
                            type: object
                          type:
                            description: type defines the authentication method. Valid
                              options are `basic`.
                            enum:
                            - basic
                            type: string
                        required:
                        - basic
                        - type
                        type: object
                      clusterID:
                        description: clusterId is the ID of the cluster.
                        minLength: 1
                        type: string
                      name:
                        description: name is the name of the cluster connected to.
                        minLength: 1
                        type: string
                      url:
                        description: url is the external endpoint of the cluster.
                        minLength: 1
                        type: string
                    required:
                    - clusterID
                    - name
                    - url
                    type: object
                  kafka:
                    description: kafka configures a client for a confluent cloud kafka
                      cluster.
                    properties:
                      cluster:
                        description: cluster configures a client for a confluent cloud
                          kafka cluster
                        properties:
                          authentication:
                            description: authentication configures the confluent cloud
                              kafka authentication.
                            properties:
                              plain:
                                description: plain configures plain auth for confluent
                                  cloud kafka authentication.
                                properties:
                                  blueprint:
                                    description: blueprintKey defines the key at the
                                      blueprint layer and cannot be overridden by
                                      deployments. This key will be used by all the
                                      deployments using this blueprint
                                    properties:
                                      credentialStoreRef:
                                        description: credentialStoreRef specifies
                                          the key in the credential store
                                        properties:
                                          key:
                                            description: key defines the name of the
                                              key in secret the CredentialStoreConfigRef
                                              refers to
                                            type: string
                                          name:
                                            description: name defines the name of
                                              the CredentialStoreConfigRef
                                            type: string
                                        type: object
                                    required:
                                    - credentialStoreRef
                                    type: object
                                  providerType:
                                    allOf:
                                    - enum:
                                      - deployment
                                      - blueprint
                                    - enum:
                                      - blueprint
                                    description: providerType defines the provider
                                      for the Credential. Valid options are `blueprint`
                                    type: string
                                required:
                                - blueprint
                                - providerType
                                type: object
                              type:
                                description: type defines the type of ccloud kafka
                                  authentication.
                                enum:
                                - plain
                                minLength: 1
                                type: string
                            required:
                            - type
                            type: object
                          bootstrapEndpoint:
                            description: bootstrapEndpointList is a list of endpoints
                              for the kafkacluster.
                            minLength: 1
                            type: string
                          clusterID:
                            description: clusterId is the ID of the cluster.
                            minLength: 1
                            type: string
                          name:
                            description: name is the name of the cluster connected
                              to.
                            minLength: 1
                            type: string
                          tls:
                            description: tls configures the confluent cloud kafka
                              tls.
                            properties:
                              enabled:
                                default: false
                                description: enabled specifies if tls is required.
                                type: boolean
                            required:
                            - enabled
                            type: object
                        required:
                        - bootstrapEndpoint
                        - clusterID
                        - name
                        type: object
                      restAPI:
                        description: restAPI configures a client for a confluent cloud
                          kafka rest API.
                        properties:
                          authentication:
                            description: credentialStoreConfigRef configures credentials
                              to connect to the cluster.
                            properties:
                              basic:
                                description: Basic specifies basic authentication
                                  configuration.
                                properties:
                                  blueprint:
                                    description: blueprintKey defines the key at the
                                      blueprint layer and cannot be overridden by
                                      deployments. This key will be used by all the
                                      deployments using this blueprint
                                    properties:
                                      credentialStoreRef:
                                        description: credentialStoreRef specifies
                                          the key in the credential store
                                        properties:
                                          key:
                                            description: key defines the name of the
                                              key in secret the CredentialStoreConfigRef
                                              refers to
                                            type: string
                                          name:
                                            description: name defines the name of
                                              the CredentialStoreConfigRef
                                            type: string
                                        type: object
                                    required:
                                    - credentialStoreRef
                                    type: object
                                  providerType:
                                    allOf:
                                    - enum:
                                      - deployment
                                      - blueprint
                                    - enum:
                                      - blueprint
                                    description: providerType defines the provider
                                      for the Credential. Valid options are `blueprint`
                                    type: string
                                required:
                                - blueprint
                                - providerType
                                type: object
                              type:
                                description: type defines the authentication method.
                                  Valid options are `basic`.
                                enum:
                                - basic
                                type: string
                            required:
                            - basic
                            - type
                            type: object
                          url:
                            description: url is the url of the kafka Rest API endpoint.
                            minLength: 1
                            type: string
                        required:
                        - url
                        type: object
                    required:
                    - cluster
                    - restAPI
                    type: object
                  ksqlApps:
                    description: ksqlApps configures a cloud ksqlApps client.
                    properties:
                      authentication:
                        description: credentialStoreConfigRef configures credentials
                          to connect to the cluster.
                        properties:
                          basic:
                            description: Basic specifies basic authentication configuration.
                            properties:
                              blueprint:
                                description: blueprintKey defines the key at the blueprint
                                  layer and cannot be overridden by deployments. This
                                  key will be used by all the deployments using this
                                  blueprint
                                properties:
                                  credentialStoreRef:
                                    description: credentialStoreRef specifies the
                                      key in the credential store
                                    properties:
                                      key:
                                        description: key defines the name of the key
                                          in secret the CredentialStoreConfigRef refers
                                          to
                                        type: string
                                      name:
                                        description: name defines the name of the
                                          CredentialStoreConfigRef
                                        type: string
                                    type: object
                                required:
                                - credentialStoreRef
                                type: object
                              providerType:
                                allOf:
                                - enum:
                                  - deployment
                                  - blueprint
                                - enum:
                                  - blueprint
                                description: providerType defines the provider for
                                  the Credential. Valid options are `blueprint`
                                type: string
                            required:
                            - blueprint
                            - providerType
                            type: object
                          type:
                            description: type defines the authentication method. Valid
                              options are `basic`.
                            enum:
                            - basic
                            type: string
                        required:
                        - basic
                        - type
                        type: object
                      clusterID:
                        description: clusterId is the ID of the cluster.
                        minLength: 1
                        type: string
                      name:
                        description: name is the name of the cluster connected to.
                        minLength: 1
                        type: string
                      url:
                        description: url is the external endpoint of the cluster.
                        minLength: 1
                        type: string
                    required:
                    - clusterID
                    - name
                    - url
                    type: object
                  schemaRegistry:
                    description: schemaRegistry configures a client for a confluent
                      cloud schemaRegistry cluster.
                    properties:
                      authentication:
                        description: credentialStoreConfigRef configures credentials
                          to connect to the cluster.
                        properties:
                          basic:
                            description: Basic specifies basic authentication configuration.
                            properties:
                              blueprint:
                                description: blueprintKey defines the key at the blueprint
                                  layer and cannot be overridden by deployments. This
                                  key will be used by all the deployments using this
                                  blueprint
                                properties:
                                  credentialStoreRef:
                                    description: credentialStoreRef specifies the
                                      key in the credential store
                                    properties:
                                      key:
                                        description: key defines the name of the key
                                          in secret the CredentialStoreConfigRef refers
                                          to
                                        type: string
                                      name:
                                        description: name defines the name of the
                                          CredentialStoreConfigRef
                                        type: string
                                    type: object
                                required:
                                - credentialStoreRef
                                type: object
                              providerType:
                                allOf:
                                - enum:
                                  - deployment
                                  - blueprint
                                - enum:
                                  - blueprint
                                description: providerType defines the provider for
                                  the Credential. Valid options are `blueprint`
                                type: string
                            required:
                            - blueprint
                            - providerType
                            type: object
                          type:
                            description: type defines the authentication method. Valid
                              options are `basic`.
                            enum:
                            - basic
                            type: string
                        required:
                        - basic
                        - type
                        type: object
                      clusterID:
                        description: clusterId is the ID of the cluster.
                        minLength: 1
                        type: string
                      name:
                        description: name is the name of the cluster connected to.
                        minLength: 1
                        type: string
                      url:
                        description: url is the external endpoint of the cluster.
                        minLength: 1
                        type: string
                    required:
                    - clusterID
                    - name
                    - url
                    type: object
                type: object
              credentialStoreConfigRef:
                description: credentialStoreConfigRef references the CPCredentialConfig
                  CR that manages credentials for connecting to ConfluentCloud.
                properties:
                  name:
                    description: Name is the name of the referenced CR
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  namespace:
                    description: Namespace of the CR. Defaults to the current namespace
                      of the CR making the reference
                    maxLength: 255
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                required:
                - name
                type: object
              environmentName:
                description: environmentName is the name of the confluentCloud environment.
                minLength: 1
                type: string
              region:
                description: region is where the region of the ConfluentCloud cluster.
                minLength: 1
                type: string
              serviceAccount:
                description: serviceAccount is the service account used to access
                type: object
              type:
                description: type configures the type of confluent cloud cluster used.
                enum:
                - basic
                - standard
                - dedicated
                minLength: 1
                type: string
              zones:
                description: zone describes the zone of a ConfluentCloud cluster.
                items:
                  description: TODO ConfluentCloudZone describes the zone of a ConfluentCloud
                    cluster.
                  enum:
                  - single
                  minLength: 1
                  type: string
                minItems: 1
                type: array
            required:
            - clientConfigs
            - environmentName
            - region
            - serviceAccount
            - type
            - zones
            type: object
          status:
            description: ConfluentCloudSetStatus defines the observed state of ConfluentCloudSet
            properties:
              kafkaClientState:
                description: kafkaClientState is whether the client config for confluentCloud
                  kafka is successful.
                type: string
              schemaRegistryClientState:
                description: schemaRegistryClientState is whether the client config
                  for confluentCloud schemaRegistry is successful.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

