---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: awsaccounts.integration.datadog.upbound.io
spec:
  group: integration.datadog.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - datadog
    kind: AWSAccount
    listKind: AWSAccountList
    plural: awsaccounts
    singular: awsaccount
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AWSAccount is the Schema for the AWSAccounts API. Provides a
          Datadog - Amazon Web Services integration resource. This can be used to
          create and manage Datadog - Amazon Web Services integration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AWSAccountSpec defines the desired state of AWSAccount
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  accountTags:
                    description: |-
                      (List of String) Tags to apply to all metrics in the account. Defaults to [].
                      Tags to apply to all metrics in the account.
                    items:
                      type: string
                    type: array
                  authConfig:
                    description: |-
                      (Block) Configure how Datadog authenticates to your AWS account. Either aws_auth_config_keys or aws_auth_config_role block is required within. (see below for nested schema)
                      Configure how Datadog authenticates to your AWS Account. Either `aws_auth_config_keys` or `aws_auth_config_role` block is required within.
                    properties:
                      awsAuthConfigKeys:
                        description: |-
                          (Block, Optional) (see below for nested schema)
                          Datadog will use the provided AWS Access Key ID and Secret Access Key to authenticate to your account.
                        properties:
                          accessKeyId:
                            description: |-
                              (String) AWS Access Key ID
                              AWS Access Key ID. Invalid access_key_id.
                            type: string
                          secretAccessKeySecretRef:
                            description: AWS Secret Access Key. Secret_access_key
                              must be non-empty and not contain whitespace.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                      awsAuthConfigRole:
                        description: (Block, Optional) (see below for nested schema)
                        properties:
                          externalId:
                            description: |-
                              (String) AWS IAM external ID for associated role. If omitted, one is generated.
                              AWS IAM External ID for associated role. If omitted, one will be generated.
                            type: string
                          roleName:
                            description: |-
                              (String) AWS IAM role name.
                              AWS IAM Role name.
                            type: string
                        type: object
                    type: object
                  awsAccountId:
                    description: |-
                      (String) Your AWS Account ID without dashes.
                      Your AWS Account ID without dashes. Invalid aws_account_id.
                    type: string
                  awsPartition:
                    description: |-
                      (String) AWS Account partition.
                      AWS Account partition. Valid values are `aws`, `aws-cn`, `aws-us-gov`.
                    type: string
                  awsRegions:
                    description: |-
                      (Block) AWS regions to collect data from. Defaults to include_all if block is empty. (see below for nested schema)
                      AWS Regions to collect data from. Defaults to `include_all` if block is empty.
                    properties:
                      includeAll:
                        description: |-
                          (Boolean) Include all regions. Defaults to true.
                          Include all regions. Defaults to `true`.
                        type: boolean
                      includeOnly:
                        description: |-
                          (List of String) Include only these regions.
                          Include only these regions.
                        items:
                          type: string
                        type: array
                    type: object
                  logsConfig:
                    description: |-
                      (Block) Configure log autosubscription for your Datadog Forwarder Lambda functions. The lambda_forwarder block is required within, but may be empty to use defaults. (see below for nested schema)
                      Configure log autosubscription for your Datadog Forwarder Lambda functions. The `lambda_forwarder` block is required within, but may be empty to use defaults.
                    properties:
                      lambdaForwarder:
                        description: (Block) Leave empty to omit logs config. (see
                          below for nested schema)
                        properties:
                          lambdas:
                            description: |-
                              (List of String) List of Datadog Lambda Log Forwarder ARNs in your AWS account. Defaults to [].
                              List of Datadog Lambda Log Forwarder ARNs in your AWS account. Defaults to `[]`.
                            items:
                              type: string
                            type: array
                          logSourceConfig:
                            description: |-
                              (Block, Optional) Configure log source collection for your Datadog Forwarder Lambda functions. (see below for nested schema)
                              Configure log source collection for your Datadog Forwarder Lambda functions.
                            properties:
                              tagFilters:
                                description: |-
                                  (Block List) AWS Logs Collection tag filters list. (see below for nested schema)
                                  AWS Logs Collection tag filters list.
                                items:
                                  properties:
                                    source:
                                      description: |-
                                        (String) The AWS service for which the tag filters defined in tags will be applied.
                                        The AWS service for which the tag filters defined in `tags` will be applied.
                                      type: string
                                    tags:
                                      description: |-
                                        (List of String) The AWS resource tags to filter on for the service specified by source.
                                        The AWS resource tags to filter on for the service specified by `source`.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                type: array
                            type: object
                          sources:
                            description: |-
                              (List of String) List of service IDs set to enable automatic log collection. Use datadog_integration_aws_available_logs_services data source or the AWS Logs Integration API to get allowed values. Defaults to [].
                              List of service IDs set to enable automatic log collection. Use [`datadog_integration_aws_available_logs_services` data source](https://registry.io/providers/DataDog/datadog/latest/docs/data-sources/integration_aws_available_logs_services) or [the AWS Logs Integration API](https://docs.datadoghq.com/api/latest/aws-logs-integration/?#get-list-of-aws-log-ready-services) to get allowed values. Defaults to `[]`.
                            items:
                              type: string
                            type: array
                        type: object
                    type: object
                  metricsConfig:
                    description: |-
                      (Block) Configure metrics collection from AWS CloudWatch. The namespace_filters block is required within, but may be empty to use defaults. (see below for nested schema)
                      Configure metrics collection from AWS CloudWatch. The `namespace_filters` block is required within, but may be empty to use defaults.
                    properties:
                      automuteEnabled:
                        description: |-
                          (Boolean) Enable EC2 automute for AWS metrics Defaults to true.
                          Enable EC2 automute for AWS metrics Defaults to `true`.
                        type: boolean
                      collectCloudwatchAlarms:
                        description: |-
                          (Boolean) Enable CloudWatch alarms collection Defaults to false.
                          Enable CloudWatch alarms collection Defaults to `false`.
                        type: boolean
                      collectCustomMetrics:
                        description: |-
                          (Boolean) Enable custom metrics collection Defaults to false.
                          Enable custom metrics collection Defaults to `false`.
                        type: boolean
                      enabled:
                        description: |-
                          (Boolean) Enable AWS metrics collection Defaults to true.
                          Enable AWS metrics collection Defaults to `true`.
                        type: boolean
                      metricNameFilters:
                        description: |-
                          (Block List) AWS CloudWatch metric name filters. Each filter applies to a single namespace and must define exactly one of include_only or exclude_only. (see below for nested schema)
                          AWS CloudWatch metric name filters. Each filter applies to a single namespace and must define exactly one of `include_only` or `exclude_only`.
                        items:
                          properties:
                            excludeOnly:
                              description: |-
                                (List of String) Exclude only these namespaces from metrics collection. Use datadog_integration_aws_available_namespaces data source to get allowed values. Defaults to ["AWS/SQS", "AWS/ElasticMapReduce", "AWS/Usage"]. AWS/SQS, AWS/ElasticMapReduce, and AWS/Usage are excluded by default to reduce your AWS CloudWatch costs from GetMetricData API calls.
                                Exclude metric names matching one of these patterns.
                              items:
                                type: string
                              type: array
                            includeOnly:
                              description: |-
                                (List of String) Include only these regions.
                                Include only metric names matching one of these patterns.
                              items:
                                type: string
                              type: array
                            namespace:
                              description: |-
                                (String) The AWS CloudWatch namespace to which this metric name filter applies.
                                The AWS CloudWatch namespace to which this metric name filter applies.
                              type: string
                          type: object
                        type: array
                      namespaceFilters:
                        description: |-
                          set exclude_only list if block is empty. (see below for nested schema)
                          AWS Metrics namespace filters. Defaults to a pre-set `exclude_only` list if block is empty.
                        properties:
                          excludeOnly:
                            description: |-
                              (List of String) Exclude only these namespaces from metrics collection. Use datadog_integration_aws_available_namespaces data source to get allowed values. Defaults to ["AWS/SQS", "AWS/ElasticMapReduce", "AWS/Usage"]. AWS/SQS, AWS/ElasticMapReduce, and AWS/Usage are excluded by default to reduce your AWS CloudWatch costs from GetMetricData API calls.
                              Exclude only these namespaces from metrics collection. Use [`datadog_integration_aws_available_namespaces` data source](https://registry.io/providers/DataDog/datadog/latest/docs/data-sources/integration_aws_available_namespaces) to get allowed values. Defaults to `["AWS/SQS", "AWS/ElasticMapReduce", "AWS/Usage"]`. `AWS/SQS`, `AWS/ElasticMapReduce`, and `AWS/Usage` are excluded by default to reduce your AWS CloudWatch costs from `GetMetricData` API calls.
                            items:
                              type: string
                            type: array
                          includeOnly:
                            description: |-
                              (List of String) Include only these regions.
                              Include only these namespaces for metrics collection. Use [`datadog_integration_aws_available_namespaces` data source](https://registry.io/providers/DataDog/datadog/latest/docs/data-sources/integration_aws_available_namespaces) to get allowed values.
                            items:
                              type: string
                            type: array
                        type: object
                      tagFilters:
                        description: |-
                          (Block List) AWS Logs Collection tag filters list. (see below for nested schema)
                          AWS Metrics Collection tag filters list. The array of custom AWS resource tags (in the form `key:value`) defines a filter that Datadog uses when collecting metrics from a specified service. Wildcards, such as `?` (match a single character) and `*` (match multiple characters), and exclusion using `!` before the tag are supported. For EC2, only hosts that match one of the defined tags will be imported into Datadog. The rest will be ignored. For example, `env:production,instance-type:c?.*,!region:us-east-1`.
                        items:
                          properties:
                            namespace:
                              description: |-
                                (String) The AWS CloudWatch namespace to which this metric name filter applies.
                                The AWS service for which the tag filters defined in `tags` will be applied.
                              type: string
                            tags:
                              description: |-
                                (List of String) The AWS resource tags to filter on for the service specified by source.
                                The AWS resource tags to filter on for the service specified by `namespace`. Defaults to `[]`.
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                    type: object
                  resourcesConfig:
                    description: |-
                      (Block) AWS resources collection config. May be empty to use defaults. (see below for nested schema)
                      AWS Resources Collection config. May be empty to use defaults.
                    properties:
                      cloudSecurityPostureManagementCollection:
                        description: |-
                          (Boolean) Enable Cloud Security Management to scan AWS resources for vulnerabilities, misconfigurations, identity risks, and compliance violations. Requires extended_collection to be set to true. Defaults to false.
                          Enable Cloud Security Management to scan AWS resources for vulnerabilities, misconfigurations, identity risks, and compliance violations. Requires `extended_collection` to be set to `true`. Defaults to `false`.
                        type: boolean
                      extendedCollection:
                        description: |-
                          (Boolean) Whether Datadog collects additional attributes and configuration information about the resources in your AWS account. Required for cloud_security_posture_management_collection. Defaults to true.
                          Whether Datadog collects additional attributes and configuration information about the resources in your AWS account. Required for `cloud_security_posture_management_collection`. Defaults to `true`.
                        type: boolean
                    type: object
                  tracesConfig:
                    description: |-
                      (Block) AWS traces collection config. The xray_services block is required within, but may be empty to use defaults. (see below for nested schema)
                      AWS Traces Collection config. The `xray_services` block is required within, but may be empty to use defaults.
                    properties:
                      xrayServices:
                        description: |-
                          Ray services to collect traces from. Defaults to include_only. (see below for nested schema)
                          AWS X-Ray services to collect traces from. Defaults to `include_only`.
                        properties:
                          includeAll:
                            description: |-
                              (Boolean) Include all regions. Defaults to true.
                              Include all services.
                            type: boolean
                          includeOnly:
                            description: |-
                              (List of String) Include only these regions.
                              Include only these services. Defaults to `[]`.
                            items:
                              type: string
                            type: array
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accountTags:
                    description: |-
                      (List of String) Tags to apply to all metrics in the account. Defaults to [].
                      Tags to apply to all metrics in the account.
                    items:
                      type: string
                    type: array
                  authConfig:
                    description: |-
                      (Block) Configure how Datadog authenticates to your AWS account. Either aws_auth_config_keys or aws_auth_config_role block is required within. (see below for nested schema)
                      Configure how Datadog authenticates to your AWS Account. Either `aws_auth_config_keys` or `aws_auth_config_role` block is required within.
                    properties:
                      awsAuthConfigKeys:
                        description: |-
                          (Block, Optional) (see below for nested schema)
                          Datadog will use the provided AWS Access Key ID and Secret Access Key to authenticate to your account.
                        properties:
                          accessKeyId:
                            description: |-
                              (String) AWS Access Key ID
                              AWS Access Key ID. Invalid access_key_id.
                            type: string
                          secretAccessKeySecretRef:
                            description: AWS Secret Access Key. Secret_access_key
                              must be non-empty and not contain whitespace.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                      awsAuthConfigRole:
                        description: (Block, Optional) (see below for nested schema)
                        properties:
                          externalId:
                            description: |-
                              (String) AWS IAM external ID for associated role. If omitted, one is generated.
                              AWS IAM External ID for associated role. If omitted, one will be generated.
                            type: string
                          roleName:
                            description: |-
                              (String) AWS IAM role name.
                              AWS IAM Role name.
                            type: string
                        type: object
                    type: object
                  awsAccountId:
                    description: |-
                      (String) Your AWS Account ID without dashes.
                      Your AWS Account ID without dashes. Invalid aws_account_id.
                    type: string
                  awsPartition:
                    description: |-
                      (String) AWS Account partition.
                      AWS Account partition. Valid values are `aws`, `aws-cn`, `aws-us-gov`.
                    type: string
                  awsRegions:
                    description: |-
                      (Block) AWS regions to collect data from. Defaults to include_all if block is empty. (see below for nested schema)
                      AWS Regions to collect data from. Defaults to `include_all` if block is empty.
                    properties:
                      includeAll:
                        description: |-
                          (Boolean) Include all regions. Defaults to true.
                          Include all regions. Defaults to `true`.
                        type: boolean
                      includeOnly:
                        description: |-
                          (List of String) Include only these regions.
                          Include only these regions.
                        items:
                          type: string
                        type: array
                    type: object
                  logsConfig:
                    description: |-
                      (Block) Configure log autosubscription for your Datadog Forwarder Lambda functions. The lambda_forwarder block is required within, but may be empty to use defaults. (see below for nested schema)
                      Configure log autosubscription for your Datadog Forwarder Lambda functions. The `lambda_forwarder` block is required within, but may be empty to use defaults.
                    properties:
                      lambdaForwarder:
                        description: (Block) Leave empty to omit logs config. (see
                          below for nested schema)
                        properties:
                          lambdas:
                            description: |-
                              (List of String) List of Datadog Lambda Log Forwarder ARNs in your AWS account. Defaults to [].
                              List of Datadog Lambda Log Forwarder ARNs in your AWS account. Defaults to `[]`.
                            items:
                              type: string
                            type: array
                          logSourceConfig:
                            description: |-
                              (Block, Optional) Configure log source collection for your Datadog Forwarder Lambda functions. (see below for nested schema)
                              Configure log source collection for your Datadog Forwarder Lambda functions.
                            properties:
                              tagFilters:
                                description: |-
                                  (Block List) AWS Logs Collection tag filters list. (see below for nested schema)
                                  AWS Logs Collection tag filters list.
                                items:
                                  properties:
                                    source:
                                      description: |-
                                        (String) The AWS service for which the tag filters defined in tags will be applied.
                                        The AWS service for which the tag filters defined in `tags` will be applied.
                                      type: string
                                    tags:
                                      description: |-
                                        (List of String) The AWS resource tags to filter on for the service specified by source.
                                        The AWS resource tags to filter on for the service specified by `source`.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                type: array
                            type: object
                          sources:
                            description: |-
                              (List of String) List of service IDs set to enable automatic log collection. Use datadog_integration_aws_available_logs_services data source or the AWS Logs Integration API to get allowed values. Defaults to [].
                              List of service IDs set to enable automatic log collection. Use [`datadog_integration_aws_available_logs_services` data source](https://registry.io/providers/DataDog/datadog/latest/docs/data-sources/integration_aws_available_logs_services) or [the AWS Logs Integration API](https://docs.datadoghq.com/api/latest/aws-logs-integration/?#get-list-of-aws-log-ready-services) to get allowed values. Defaults to `[]`.
                            items:
                              type: string
                            type: array
                        type: object
                    type: object
                  metricsConfig:
                    description: |-
                      (Block) Configure metrics collection from AWS CloudWatch. The namespace_filters block is required within, but may be empty to use defaults. (see below for nested schema)
                      Configure metrics collection from AWS CloudWatch. The `namespace_filters` block is required within, but may be empty to use defaults.
                    properties:
                      automuteEnabled:
                        description: |-
                          (Boolean) Enable EC2 automute for AWS metrics Defaults to true.
                          Enable EC2 automute for AWS metrics Defaults to `true`.
                        type: boolean
                      collectCloudwatchAlarms:
                        description: |-
                          (Boolean) Enable CloudWatch alarms collection Defaults to false.
                          Enable CloudWatch alarms collection Defaults to `false`.
                        type: boolean
                      collectCustomMetrics:
                        description: |-
                          (Boolean) Enable custom metrics collection Defaults to false.
                          Enable custom metrics collection Defaults to `false`.
                        type: boolean
                      enabled:
                        description: |-
                          (Boolean) Enable AWS metrics collection Defaults to true.
                          Enable AWS metrics collection Defaults to `true`.
                        type: boolean
                      metricNameFilters:
                        description: |-
                          (Block List) AWS CloudWatch metric name filters. Each filter applies to a single namespace and must define exactly one of include_only or exclude_only. (see below for nested schema)
                          AWS CloudWatch metric name filters. Each filter applies to a single namespace and must define exactly one of `include_only` or `exclude_only`.
                        items:
                          properties:
                            excludeOnly:
                              description: |-
                                (List of String) Exclude only these namespaces from metrics collection. Use datadog_integration_aws_available_namespaces data source to get allowed values. Defaults to ["AWS/SQS", "AWS/ElasticMapReduce", "AWS/Usage"]. AWS/SQS, AWS/ElasticMapReduce, and AWS/Usage are excluded by default to reduce your AWS CloudWatch costs from GetMetricData API calls.
                                Exclude metric names matching one of these patterns.
                              items:
                                type: string
                              type: array
                            includeOnly:
                              description: |-
                                (List of String) Include only these regions.
                                Include only metric names matching one of these patterns.
                              items:
                                type: string
                              type: array
                            namespace:
                              description: |-
                                (String) The AWS CloudWatch namespace to which this metric name filter applies.
                                The AWS CloudWatch namespace to which this metric name filter applies.
                              type: string
                          type: object
                        type: array
                      namespaceFilters:
                        description: |-
                          set exclude_only list if block is empty. (see below for nested schema)
                          AWS Metrics namespace filters. Defaults to a pre-set `exclude_only` list if block is empty.
                        properties:
                          excludeOnly:
                            description: |-
                              (List of String) Exclude only these namespaces from metrics collection. Use datadog_integration_aws_available_namespaces data source to get allowed values. Defaults to ["AWS/SQS", "AWS/ElasticMapReduce", "AWS/Usage"]. AWS/SQS, AWS/ElasticMapReduce, and AWS/Usage are excluded by default to reduce your AWS CloudWatch costs from GetMetricData API calls.
                              Exclude only these namespaces from metrics collection. Use [`datadog_integration_aws_available_namespaces` data source](https://registry.io/providers/DataDog/datadog/latest/docs/data-sources/integration_aws_available_namespaces) to get allowed values. Defaults to `["AWS/SQS", "AWS/ElasticMapReduce", "AWS/Usage"]`. `AWS/SQS`, `AWS/ElasticMapReduce`, and `AWS/Usage` are excluded by default to reduce your AWS CloudWatch costs from `GetMetricData` API calls.
                            items:
                              type: string
                            type: array
                          includeOnly:
                            description: |-
                              (List of String) Include only these regions.
                              Include only these namespaces for metrics collection. Use [`datadog_integration_aws_available_namespaces` data source](https://registry.io/providers/DataDog/datadog/latest/docs/data-sources/integration_aws_available_namespaces) to get allowed values.
                            items:
                              type: string
                            type: array
                        type: object
                      tagFilters:
                        description: |-
                          (Block List) AWS Logs Collection tag filters list. (see below for nested schema)
                          AWS Metrics Collection tag filters list. The array of custom AWS resource tags (in the form `key:value`) defines a filter that Datadog uses when collecting metrics from a specified service. Wildcards, such as `?` (match a single character) and `*` (match multiple characters), and exclusion using `!` before the tag are supported. For EC2, only hosts that match one of the defined tags will be imported into Datadog. The rest will be ignored. For example, `env:production,instance-type:c?.*,!region:us-east-1`.
                        items:
                          properties:
                            namespace:
                              description: |-
                                (String) The AWS CloudWatch namespace to which this metric name filter applies.
                                The AWS service for which the tag filters defined in `tags` will be applied.
                              type: string
                            tags:
                              description: |-
                                (List of String) The AWS resource tags to filter on for the service specified by source.
                                The AWS resource tags to filter on for the service specified by `namespace`. Defaults to `[]`.
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                    type: object
                  resourcesConfig:
                    description: |-
                      (Block) AWS resources collection config. May be empty to use defaults. (see below for nested schema)
                      AWS Resources Collection config. May be empty to use defaults.
                    properties:
                      cloudSecurityPostureManagementCollection:
                        description: |-
                          (Boolean) Enable Cloud Security Management to scan AWS resources for vulnerabilities, misconfigurations, identity risks, and compliance violations. Requires extended_collection to be set to true. Defaults to false.
                          Enable Cloud Security Management to scan AWS resources for vulnerabilities, misconfigurations, identity risks, and compliance violations. Requires `extended_collection` to be set to `true`. Defaults to `false`.
                        type: boolean
                      extendedCollection:
                        description: |-
                          (Boolean) Whether Datadog collects additional attributes and configuration information about the resources in your AWS account. Required for cloud_security_posture_management_collection. Defaults to true.
                          Whether Datadog collects additional attributes and configuration information about the resources in your AWS account. Required for `cloud_security_posture_management_collection`. Defaults to `true`.
                        type: boolean
                    type: object
                  tracesConfig:
                    description: |-
                      (Block) AWS traces collection config. The xray_services block is required within, but may be empty to use defaults. (see below for nested schema)
                      AWS Traces Collection config. The `xray_services` block is required within, but may be empty to use defaults.
                    properties:
                      xrayServices:
                        description: |-
                          Ray services to collect traces from. Defaults to include_only. (see below for nested schema)
                          AWS X-Ray services to collect traces from. Defaults to `include_only`.
                        properties:
                          includeAll:
                            description: |-
                              (Boolean) Include all regions. Defaults to true.
                              Include all services.
                            type: boolean
                          includeOnly:
                            description: |-
                              (List of String) Include only these regions.
                              Include only these services. Defaults to `[]`.
                            items:
                              type: string
                            type: array
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.awsAccountId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.awsAccountId)
                || (has(self.initProvider) && has(self.initProvider.awsAccountId))'
            - message: spec.forProvider.awsPartition is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.awsPartition)
                || (has(self.initProvider) && has(self.initProvider.awsPartition))'
          status:
            description: AWSAccountStatus defines the observed state of AWSAccount.
            properties:
              atProvider:
                properties:
                  accountTags:
                    description: |-
                      (List of String) Tags to apply to all metrics in the account. Defaults to [].
                      Tags to apply to all metrics in the account.
                    items:
                      type: string
                    type: array
                  authConfig:
                    description: |-
                      (Block) Configure how Datadog authenticates to your AWS account. Either aws_auth_config_keys or aws_auth_config_role block is required within. (see below for nested schema)
                      Configure how Datadog authenticates to your AWS Account. Either `aws_auth_config_keys` or `aws_auth_config_role` block is required within.
                    properties:
                      awsAuthConfigKeys:
                        description: |-
                          (Block, Optional) (see below for nested schema)
                          Datadog will use the provided AWS Access Key ID and Secret Access Key to authenticate to your account.
                        properties:
                          accessKeyId:
                            description: |-
                              (String) AWS Access Key ID
                              AWS Access Key ID. Invalid access_key_id.
                            type: string
                        type: object
                      awsAuthConfigRole:
                        description: (Block, Optional) (see below for nested schema)
                        properties:
                          externalId:
                            description: |-
                              (String) AWS IAM external ID for associated role. If omitted, one is generated.
                              AWS IAM External ID for associated role. If omitted, one will be generated.
                            type: string
                          roleName:
                            description: |-
                              (String) AWS IAM role name.
                              AWS IAM Role name.
                            type: string
                        type: object
                    type: object
                  awsAccountId:
                    description: |-
                      (String) Your AWS Account ID without dashes.
                      Your AWS Account ID without dashes. Invalid aws_account_id.
                    type: string
                  awsPartition:
                    description: |-
                      (String) AWS Account partition.
                      AWS Account partition. Valid values are `aws`, `aws-cn`, `aws-us-gov`.
                    type: string
                  awsRegions:
                    description: |-
                      (Block) AWS regions to collect data from. Defaults to include_all if block is empty. (see below for nested schema)
                      AWS Regions to collect data from. Defaults to `include_all` if block is empty.
                    properties:
                      includeAll:
                        description: |-
                          (Boolean) Include all regions. Defaults to true.
                          Include all regions. Defaults to `true`.
                        type: boolean
                      includeOnly:
                        description: |-
                          (List of String) Include only these regions.
                          Include only these regions.
                        items:
                          type: string
                        type: array
                    type: object
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  logsConfig:
                    description: |-
                      (Block) Configure log autosubscription for your Datadog Forwarder Lambda functions. The lambda_forwarder block is required within, but may be empty to use defaults. (see below for nested schema)
                      Configure log autosubscription for your Datadog Forwarder Lambda functions. The `lambda_forwarder` block is required within, but may be empty to use defaults.
                    properties:
                      lambdaForwarder:
                        description: (Block) Leave empty to omit logs config. (see
                          below for nested schema)
                        properties:
                          lambdas:
                            description: |-
                              (List of String) List of Datadog Lambda Log Forwarder ARNs in your AWS account. Defaults to [].
                              List of Datadog Lambda Log Forwarder ARNs in your AWS account. Defaults to `[]`.
                            items:
                              type: string
                            type: array
                          logSourceConfig:
                            description: |-
                              (Block, Optional) Configure log source collection for your Datadog Forwarder Lambda functions. (see below for nested schema)
                              Configure log source collection for your Datadog Forwarder Lambda functions.
                            properties:
                              tagFilters:
                                description: |-
                                  (Block List) AWS Logs Collection tag filters list. (see below for nested schema)
                                  AWS Logs Collection tag filters list.
                                items:
                                  properties:
                                    source:
                                      description: |-
                                        (String) The AWS service for which the tag filters defined in tags will be applied.
                                        The AWS service for which the tag filters defined in `tags` will be applied.
                                      type: string
                                    tags:
                                      description: |-
                                        (List of String) The AWS resource tags to filter on for the service specified by source.
                                        The AWS resource tags to filter on for the service specified by `source`.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                type: array
                            type: object
                          sources:
                            description: |-
                              (List of String) List of service IDs set to enable automatic log collection. Use datadog_integration_aws_available_logs_services data source or the AWS Logs Integration API to get allowed values. Defaults to [].
                              List of service IDs set to enable automatic log collection. Use [`datadog_integration_aws_available_logs_services` data source](https://registry.io/providers/DataDog/datadog/latest/docs/data-sources/integration_aws_available_logs_services) or [the AWS Logs Integration API](https://docs.datadoghq.com/api/latest/aws-logs-integration/?#get-list-of-aws-log-ready-services) to get allowed values. Defaults to `[]`.
                            items:
                              type: string
                            type: array
                        type: object
                    type: object
                  metricsConfig:
                    description: |-
                      (Block) Configure metrics collection from AWS CloudWatch. The namespace_filters block is required within, but may be empty to use defaults. (see below for nested schema)
                      Configure metrics collection from AWS CloudWatch. The `namespace_filters` block is required within, but may be empty to use defaults.
                    properties:
                      automuteEnabled:
                        description: |-
                          (Boolean) Enable EC2 automute for AWS metrics Defaults to true.
                          Enable EC2 automute for AWS metrics Defaults to `true`.
                        type: boolean
                      collectCloudwatchAlarms:
                        description: |-
                          (Boolean) Enable CloudWatch alarms collection Defaults to false.
                          Enable CloudWatch alarms collection Defaults to `false`.
                        type: boolean
                      collectCustomMetrics:
                        description: |-
                          (Boolean) Enable custom metrics collection Defaults to false.
                          Enable custom metrics collection Defaults to `false`.
                        type: boolean
                      enabled:
                        description: |-
                          (Boolean) Enable AWS metrics collection Defaults to true.
                          Enable AWS metrics collection Defaults to `true`.
                        type: boolean
                      metricNameFilters:
                        description: |-
                          (Block List) AWS CloudWatch metric name filters. Each filter applies to a single namespace and must define exactly one of include_only or exclude_only. (see below for nested schema)
                          AWS CloudWatch metric name filters. Each filter applies to a single namespace and must define exactly one of `include_only` or `exclude_only`.
                        items:
                          properties:
                            excludeOnly:
                              description: |-
                                (List of String) Exclude only these namespaces from metrics collection. Use datadog_integration_aws_available_namespaces data source to get allowed values. Defaults to ["AWS/SQS", "AWS/ElasticMapReduce", "AWS/Usage"]. AWS/SQS, AWS/ElasticMapReduce, and AWS/Usage are excluded by default to reduce your AWS CloudWatch costs from GetMetricData API calls.
                                Exclude metric names matching one of these patterns.
                              items:
                                type: string
                              type: array
                            includeOnly:
                              description: |-
                                (List of String) Include only these regions.
                                Include only metric names matching one of these patterns.
                              items:
                                type: string
                              type: array
                            namespace:
                              description: |-
                                (String) The AWS CloudWatch namespace to which this metric name filter applies.
                                The AWS CloudWatch namespace to which this metric name filter applies.
                              type: string
                          type: object
                        type: array
                      namespaceFilters:
                        description: |-
                          set exclude_only list if block is empty. (see below for nested schema)
                          AWS Metrics namespace filters. Defaults to a pre-set `exclude_only` list if block is empty.
                        properties:
                          excludeOnly:
                            description: |-
                              (List of String) Exclude only these namespaces from metrics collection. Use datadog_integration_aws_available_namespaces data source to get allowed values. Defaults to ["AWS/SQS", "AWS/ElasticMapReduce", "AWS/Usage"]. AWS/SQS, AWS/ElasticMapReduce, and AWS/Usage are excluded by default to reduce your AWS CloudWatch costs from GetMetricData API calls.
                              Exclude only these namespaces from metrics collection. Use [`datadog_integration_aws_available_namespaces` data source](https://registry.io/providers/DataDog/datadog/latest/docs/data-sources/integration_aws_available_namespaces) to get allowed values. Defaults to `["AWS/SQS", "AWS/ElasticMapReduce", "AWS/Usage"]`. `AWS/SQS`, `AWS/ElasticMapReduce`, and `AWS/Usage` are excluded by default to reduce your AWS CloudWatch costs from `GetMetricData` API calls.
                            items:
                              type: string
                            type: array
                          includeOnly:
                            description: |-
                              (List of String) Include only these regions.
                              Include only these namespaces for metrics collection. Use [`datadog_integration_aws_available_namespaces` data source](https://registry.io/providers/DataDog/datadog/latest/docs/data-sources/integration_aws_available_namespaces) to get allowed values.
                            items:
                              type: string
                            type: array
                        type: object
                      tagFilters:
                        description: |-
                          (Block List) AWS Logs Collection tag filters list. (see below for nested schema)
                          AWS Metrics Collection tag filters list. The array of custom AWS resource tags (in the form `key:value`) defines a filter that Datadog uses when collecting metrics from a specified service. Wildcards, such as `?` (match a single character) and `*` (match multiple characters), and exclusion using `!` before the tag are supported. For EC2, only hosts that match one of the defined tags will be imported into Datadog. The rest will be ignored. For example, `env:production,instance-type:c?.*,!region:us-east-1`.
                        items:
                          properties:
                            namespace:
                              description: |-
                                (String) The AWS CloudWatch namespace to which this metric name filter applies.
                                The AWS service for which the tag filters defined in `tags` will be applied.
                              type: string
                            tags:
                              description: |-
                                (List of String) The AWS resource tags to filter on for the service specified by source.
                                The AWS resource tags to filter on for the service specified by `namespace`. Defaults to `[]`.
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                    type: object
                  resourcesConfig:
                    description: |-
                      (Block) AWS resources collection config. May be empty to use defaults. (see below for nested schema)
                      AWS Resources Collection config. May be empty to use defaults.
                    properties:
                      cloudSecurityPostureManagementCollection:
                        description: |-
                          (Boolean) Enable Cloud Security Management to scan AWS resources for vulnerabilities, misconfigurations, identity risks, and compliance violations. Requires extended_collection to be set to true. Defaults to false.
                          Enable Cloud Security Management to scan AWS resources for vulnerabilities, misconfigurations, identity risks, and compliance violations. Requires `extended_collection` to be set to `true`. Defaults to `false`.
                        type: boolean
                      extendedCollection:
                        description: |-
                          (Boolean) Whether Datadog collects additional attributes and configuration information about the resources in your AWS account. Required for cloud_security_posture_management_collection. Defaults to true.
                          Whether Datadog collects additional attributes and configuration information about the resources in your AWS account. Required for `cloud_security_posture_management_collection`. Defaults to `true`.
                        type: boolean
                    type: object
                  tracesConfig:
                    description: |-
                      (Block) AWS traces collection config. The xray_services block is required within, but may be empty to use defaults. (see below for nested schema)
                      AWS Traces Collection config. The `xray_services` block is required within, but may be empty to use defaults.
                    properties:
                      xrayServices:
                        description: |-
                          Ray services to collect traces from. Defaults to include_only. (see below for nested schema)
                          AWS X-Ray services to collect traces from. Defaults to `include_only`.
                        properties:
                          includeAll:
                            description: |-
                              (Boolean) Include all regions. Defaults to true.
                              Include all services.
                            type: boolean
                          includeOnly:
                            description: |-
                              (List of String) Include only these regions.
                              Include only these services. Defaults to `[]`.
                            items:
                              type: string
                            type: array
                        type: object
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

