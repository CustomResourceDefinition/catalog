---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: connections.action.datadog.upbound.io
spec:
  group: action.datadog.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - datadog
    kind: Connection
    listKind: ConnectionList
    plural: connections
    singular: connection
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Connection is the Schema for the Connections API. A connection
          that can be used in Actions, including in the Workflow Automation and App
          Builder products. This resource requires a registered application key https://registry.io/providers/DataDog/datadog/latest/docs/resources/app_key_registration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ConnectionSpec defines the desired state of Connection
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  anthropic:
                    description: |-
                      (Block, Optional) Configuration for an Anthropic connection (see below for nested schema)
                      Configuration for an Anthropic connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Anthropic API key authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              Anthropic API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  asana:
                    description: |-
                      (Block, Optional) Configuration for an Asana connection (see below for nested schema)
                      Configuration for an Asana connection
                    properties:
                      accessToken:
                        description: |-
                          (Block, Optional) Configuration for Asana access token authentication (see below for nested schema)
                          Configuration for Asana access token authentication
                        properties:
                          accessTokenSecretRef:
                            description: |-
                              (Block, Optional) Configuration for Asana access token authentication (see below for nested schema)
                              Asana access token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  aws:
                    description: |-
                      (Block, Optional) Configuration for an AWS connection (see below for nested schema)
                      Configuration for an AWS connection
                    properties:
                      assumeRole:
                        description: |-
                          (Block, Optional) Configuration for an assume role AWS connection (see below for nested schema)
                          Configuration for an assume role AWS connection
                        properties:
                          accountId:
                            description: |-
                              (String) AWS account that the connection is created for. String length must be at least 1.
                              AWS account that the connection is created for. String length must be at least 1.
                            type: string
                          role:
                            description: |-
                              (String) Role to assume. String length must be at least 1.
                              Role to assume. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  azure:
                    description: |-
                      (Block, Optional) Configuration for an Azure connection (see below for nested schema)
                      Configuration for an Azure connection
                    properties:
                      tenant:
                        description: |-
                          (Block, Optional) Configuration for Azure tenant authentication (see below for nested schema)
                          Configuration for Azure tenant authentication
                        properties:
                          appClientId:
                            description: |-
                              (String) Azure application client ID. String length must be at least 1.
                              Azure application client ID. String length must be at least 1.
                            type: string
                          clientSecretSecretRef:
                            description: |-
                              (String, Sensitive) Azure application client secret. String length must be at least 1.
                              Azure application client secret. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                          customScopes:
                            description: |-
                              (String) Custom scope requested when acquiring an OAuth 2 access token. String length must be at least 1.
                              Custom scope requested when acquiring an OAuth 2 access token. String length must be at least 1.
                            type: string
                          tenantId:
                            description: |-
                              (String) Azure Active Directory tenant ID. String length must be at least 1.
                              Azure Active Directory tenant ID. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  circleCi:
                    description: |-
                      (Block, Optional) Configuration for a CircleCI connection (see below for nested schema)
                      Configuration for a CircleCI connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for CircleCI API key authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              CircleCI API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  clickup:
                    description: |-
                      (Block, Optional) Configuration for a ClickUp connection (see below for nested schema)
                      Configuration for a ClickUp connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for ClickUp API key authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              ClickUp API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  cloudflare:
                    description: |-
                      (Block, Optional) Configuration for a Cloudflare connection (see below for nested schema)
                      Configuration for a Cloudflare connection
                    properties:
                      apiToken:
                        description: |-
                          (String, Sensitive) Anthropic API token. String length must be at least 1.
                          Configuration for Cloudflare API token authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              Cloudflare API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                      globalApiToken:
                        description: |-
                          (Block, Optional) Configuration for Cloudflare global API token authentication (see below for nested schema)
                          Configuration for Cloudflare global API token authentication
                        properties:
                          authEmail:
                            description: |-
                              (String) Email address associated with the Cloudflare account. String length must be at least 1.
                              Email address associated with the Cloudflare account. String length must be at least 1.
                            type: string
                          globalApiKeySecretRef:
                            description: |-
                              (String, Sensitive) Cloudflare global API key. String length must be at least 1.
                              Cloudflare global API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  configCat:
                    description: |-
                      (Block, Optional) Configuration for a ConfigCat connection (see below for nested schema)
                      Configuration for a ConfigCat connection
                    properties:
                      sdkKey:
                        description: |-
                          (Block, Optional) Configuration for ConfigCat SDK key authentication (see below for nested schema)
                          Configuration for ConfigCat SDK key authentication
                        properties:
                          apiPasswordSecretRef:
                            description: |-
                              (String, Sensitive) ConfigCat Public Management API password. String length must be at least 1.
                              ConfigCat Public Management API password. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                          apiUsername:
                            description: |-
                              (String) ConfigCat Public Management API username. String length must be at least 1.
                              ConfigCat Public Management API username. String length must be at least 1.
                            type: string
                          sdkKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for ConfigCat SDK key authentication (see below for nested schema)
                              ConfigCat SDK key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  datadog:
                    description: |-
                      (Block, Optional) Configuration for a Datadog connection (see below for nested schema)
                      Configuration for a Datadog connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Datadog API and application key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              Datadog API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                          appKeySecretRef:
                            description: |-
                              (String, Sensitive) Datadog application key. String length must be at least 1.
                              Datadog application key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                          datacenter:
                            description: |-
                              (String) Datadog site data center. String length must be at least 1.
                              Datadog site data center. String length must be at least 1.
                            type: string
                          subdomain:
                            description: |-
                              (String) Custom subdomain used for URLs generated with this connection. String length must be at least 1.
                              Custom subdomain used for URLs generated with this connection. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  fastly:
                    description: |-
                      (Block, Optional) Configuration for a Fastly connection (see below for nested schema)
                      Configuration for a Fastly connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Fastly API key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              Fastly API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  freshservice:
                    description: |-
                      (Block, Optional) Configuration for a Freshservice connection (see below for nested schema)
                      Configuration for a Freshservice connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Freshservice API key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              Freshservice API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                          domain:
                            description: |-
                              (String) Freshservice domain. String length must be at least 1.
                              Freshservice domain. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  gcp:
                    description: |-
                      (Block, Optional) Configuration for a Google Cloud connection (see below for nested schema)
                      Configuration for a Google Cloud connection
                    properties:
                      serviceAccount:
                        description: |-
                          (Block, Optional) Configuration for Google Cloud service account authentication (see below for nested schema)
                          Configuration for Google Cloud service account authentication
                        properties:
                          privateKeySecretRef:
                            description: |-
                              (String, Sensitive) Google Cloud service account private key. String length must be at least 1.
                              Google Cloud service account private key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                          serviceAccountEmail:
                            description: |-
                              (String) Google Cloud service account email. String length must be at least 1.
                              Google Cloud service account email. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  gemini:
                    description: |-
                      (Block, Optional) Configuration for a Gemini connection (see below for nested schema)
                      Configuration for a Gemini connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Gemini API key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              Gemini API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  gitlab:
                    description: |-
                      (Block, Optional) Configuration for a GitLab connection (see below for nested schema)
                      Configuration for a GitLab connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for GitLab API key authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              GitLab API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  greyNoise:
                    description: |-
                      (Block, Optional) Configuration for a GreyNoise connection (see below for nested schema)
                      Configuration for a GreyNoise connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for GreyNoise API key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              GreyNoise API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  http:
                    description: |-
                      (Block, Optional) Configuration for an HTTP connection (see below for nested schema)
                      Configuration for an HTTP connection
                    properties:
                      baseUrl:
                        description: |-
                          (String) Base HTTP url for the integration. String length must be at least 1.
                          Base HTTP url for the integration. String length must be at least 1.
                        type: string
                      tokenAuth:
                        description: |-
                          (Block, Optional) Configuration for an HTTP connection that uses token auth (see below for nested schema)
                          Configuration for an HTTP connection that uses token auth
                        properties:
                          body:
                            description: |-
                              (Block, Optional) Body for HTTP authentication (see below for nested schema)
                              Body for HTTP authentication
                            properties:
                              content:
                                description: |-
                                  (String) Serialized body content. String length must be at least 1.
                                  Serialized body content. String length must be at least 1.
                                type: string
                              contentType:
                                description: |-
                                  (String) Content type of the body. String length must be at least 1.
                                  Content type of the body. String length must be at least 1.
                                type: string
                            type: object
                          header:
                            description: |-
                              (Block List) Header for HTTP authentication (see below for nested schema)
                              Header for HTTP authentication
                            items:
                              properties:
                                name:
                                  description: |-
                                    (String) Name of the connection
                                    Header name. String length must be at least 1.
                                  type: string
                                value:
                                  description: |-
                                    (String) String length must be at least 1.
                                    String length must be at least 1.
                                  type: string
                              type: object
                            type: array
                          token:
                            description: |-
                              (Block List) Token for HTTP authentication (see below for nested schema)
                              Token for HTTP authentication
                            items:
                              properties:
                                name:
                                  description: |-
                                    (String) Name of the connection
                                    Token name. String length must be at least 1.
                                  type: string
                                type:
                                  description: |-
                                    (String) Token type Valid values are SECRET.
                                    Token type Valid values are `SECRET`.
                                  type: string
                                valueSecretRef:
                                  description: |-
                                    (String) String length must be at least 1.
                                    Token value. String length must be at least 1.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      description: Name of the secret.
                                      type: string
                                    namespace:
                                      description: Namespace of the secret.
                                      type: string
                                  required:
                                  - key
                                  - name
                                  - namespace
                                  type: object
                              type: object
                            type: array
                          urlParameter:
                            description: |-
                              (Block List) URL parameter for HTTP authentication (see below for nested schema)
                              URL parameter for HTTP authentication
                            items:
                              properties:
                                name:
                                  description: |-
                                    (String) Name of the connection
                                    URL parameter name. String length must be at least 1.
                                  type: string
                                value:
                                  description: |-
                                    (String) String length must be at least 1.
                                    URL parameter value. String length must be at least 1.
                                  type: string
                              type: object
                            type: array
                        type: object
                    type: object
                  launchDarkly:
                    description: |-
                      (Block, Optional) Configuration for a LaunchDarkly connection (see below for nested schema)
                      Configuration for a LaunchDarkly connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for LaunchDarkly API key authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              LaunchDarkly API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  name:
                    description: |-
                      (String) Name of the connection
                      Name of the connection
                    type: string
                  notion:
                    description: |-
                      (Block, Optional) Configuration for a Notion connection (see below for nested schema)
                      Configuration for a Notion connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Notion API key authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              Notion API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  okta:
                    description: |-
                      (Block, Optional) Configuration for an Okta connection (see below for nested schema)
                      Configuration for an Okta connection
                    properties:
                      apiToken:
                        description: |-
                          (String, Sensitive) Anthropic API token. String length must be at least 1.
                          Configuration for Okta API token authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              Okta API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                          domain:
                            description: |-
                              (String) Freshservice domain. String length must be at least 1.
                              Okta domain. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  openai:
                    description: |-
                      (Block, Optional) Configuration for an OpenAI connection (see below for nested schema)
                      Configuration for an OpenAI connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for OpenAI API key authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              OpenAI API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  serviceNow:
                    description: |-
                      (Block, Optional) Configuration for a ServiceNow connection (see below for nested schema)
                      Configuration for a ServiceNow connection
                    properties:
                      basicAuth:
                        description: |-
                          (Block, Optional) Configuration for ServiceNow basic authentication (see below for nested schema)
                          Configuration for ServiceNow basic authentication
                        properties:
                          instance:
                            description: |-
                              (String) ServiceNow instance. String length must be at least 1.
                              ServiceNow instance. String length must be at least 1.
                            type: string
                          passwordSecretRef:
                            description: |-
                              (String, Sensitive) ServiceNow password. String length must be at least 1.
                              ServiceNow password. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                          username:
                            description: |-
                              (String) ServiceNow username. String length must be at least 1.
                              ServiceNow username. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  split:
                    description: |-
                      (Block, Optional) Configuration for a Split connection (see below for nested schema)
                      Configuration for a Split connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Split API key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              Split API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  statsig:
                    description: |-
                      (Block, Optional) Configuration for a Statsig connection (see below for nested schema)
                      Configuration for a Statsig connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Statsig API key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              Statsig API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  tags:
                    description: |-
                      defined tags associated with the connection. Each tag must follow the key:value format. The default tag key is reserved. See also effective_tags, which includes provider-level default_tags.
                      User-defined tags associated with the connection. Each tag must follow the `key:value` format. The `default` tag key is reserved. See also `effective_tags`, which includes provider-level `default_tags`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  virusTotal:
                    description: |-
                      (Block, Optional) Configuration for a VirusTotal connection (see below for nested schema)
                      Configuration for a VirusTotal connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for VirusTotal API key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              VirusTotal API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  anthropic:
                    description: |-
                      (Block, Optional) Configuration for an Anthropic connection (see below for nested schema)
                      Configuration for an Anthropic connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Anthropic API key authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              Anthropic API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  asana:
                    description: |-
                      (Block, Optional) Configuration for an Asana connection (see below for nested schema)
                      Configuration for an Asana connection
                    properties:
                      accessToken:
                        description: |-
                          (Block, Optional) Configuration for Asana access token authentication (see below for nested schema)
                          Configuration for Asana access token authentication
                        properties:
                          accessTokenSecretRef:
                            description: |-
                              (Block, Optional) Configuration for Asana access token authentication (see below for nested schema)
                              Asana access token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  aws:
                    description: |-
                      (Block, Optional) Configuration for an AWS connection (see below for nested schema)
                      Configuration for an AWS connection
                    properties:
                      assumeRole:
                        description: |-
                          (Block, Optional) Configuration for an assume role AWS connection (see below for nested schema)
                          Configuration for an assume role AWS connection
                        properties:
                          accountId:
                            description: |-
                              (String) AWS account that the connection is created for. String length must be at least 1.
                              AWS account that the connection is created for. String length must be at least 1.
                            type: string
                          role:
                            description: |-
                              (String) Role to assume. String length must be at least 1.
                              Role to assume. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  azure:
                    description: |-
                      (Block, Optional) Configuration for an Azure connection (see below for nested schema)
                      Configuration for an Azure connection
                    properties:
                      tenant:
                        description: |-
                          (Block, Optional) Configuration for Azure tenant authentication (see below for nested schema)
                          Configuration for Azure tenant authentication
                        properties:
                          appClientId:
                            description: |-
                              (String) Azure application client ID. String length must be at least 1.
                              Azure application client ID. String length must be at least 1.
                            type: string
                          clientSecretSecretRef:
                            description: |-
                              (String, Sensitive) Azure application client secret. String length must be at least 1.
                              Azure application client secret. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                          customScopes:
                            description: |-
                              (String) Custom scope requested when acquiring an OAuth 2 access token. String length must be at least 1.
                              Custom scope requested when acquiring an OAuth 2 access token. String length must be at least 1.
                            type: string
                          tenantId:
                            description: |-
                              (String) Azure Active Directory tenant ID. String length must be at least 1.
                              Azure Active Directory tenant ID. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  circleCi:
                    description: |-
                      (Block, Optional) Configuration for a CircleCI connection (see below for nested schema)
                      Configuration for a CircleCI connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for CircleCI API key authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              CircleCI API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  clickup:
                    description: |-
                      (Block, Optional) Configuration for a ClickUp connection (see below for nested schema)
                      Configuration for a ClickUp connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for ClickUp API key authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              ClickUp API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  cloudflare:
                    description: |-
                      (Block, Optional) Configuration for a Cloudflare connection (see below for nested schema)
                      Configuration for a Cloudflare connection
                    properties:
                      apiToken:
                        description: |-
                          (String, Sensitive) Anthropic API token. String length must be at least 1.
                          Configuration for Cloudflare API token authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              Cloudflare API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                      globalApiToken:
                        description: |-
                          (Block, Optional) Configuration for Cloudflare global API token authentication (see below for nested schema)
                          Configuration for Cloudflare global API token authentication
                        properties:
                          authEmail:
                            description: |-
                              (String) Email address associated with the Cloudflare account. String length must be at least 1.
                              Email address associated with the Cloudflare account. String length must be at least 1.
                            type: string
                          globalApiKeySecretRef:
                            description: |-
                              (String, Sensitive) Cloudflare global API key. String length must be at least 1.
                              Cloudflare global API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  configCat:
                    description: |-
                      (Block, Optional) Configuration for a ConfigCat connection (see below for nested schema)
                      Configuration for a ConfigCat connection
                    properties:
                      sdkKey:
                        description: |-
                          (Block, Optional) Configuration for ConfigCat SDK key authentication (see below for nested schema)
                          Configuration for ConfigCat SDK key authentication
                        properties:
                          apiPasswordSecretRef:
                            description: |-
                              (String, Sensitive) ConfigCat Public Management API password. String length must be at least 1.
                              ConfigCat Public Management API password. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                          apiUsername:
                            description: |-
                              (String) ConfigCat Public Management API username. String length must be at least 1.
                              ConfigCat Public Management API username. String length must be at least 1.
                            type: string
                          sdkKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for ConfigCat SDK key authentication (see below for nested schema)
                              ConfigCat SDK key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  datadog:
                    description: |-
                      (Block, Optional) Configuration for a Datadog connection (see below for nested schema)
                      Configuration for a Datadog connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Datadog API and application key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              Datadog API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                          appKeySecretRef:
                            description: |-
                              (String, Sensitive) Datadog application key. String length must be at least 1.
                              Datadog application key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                          datacenter:
                            description: |-
                              (String) Datadog site data center. String length must be at least 1.
                              Datadog site data center. String length must be at least 1.
                            type: string
                          subdomain:
                            description: |-
                              (String) Custom subdomain used for URLs generated with this connection. String length must be at least 1.
                              Custom subdomain used for URLs generated with this connection. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  fastly:
                    description: |-
                      (Block, Optional) Configuration for a Fastly connection (see below for nested schema)
                      Configuration for a Fastly connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Fastly API key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              Fastly API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  freshservice:
                    description: |-
                      (Block, Optional) Configuration for a Freshservice connection (see below for nested schema)
                      Configuration for a Freshservice connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Freshservice API key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              Freshservice API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                          domain:
                            description: |-
                              (String) Freshservice domain. String length must be at least 1.
                              Freshservice domain. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  gcp:
                    description: |-
                      (Block, Optional) Configuration for a Google Cloud connection (see below for nested schema)
                      Configuration for a Google Cloud connection
                    properties:
                      serviceAccount:
                        description: |-
                          (Block, Optional) Configuration for Google Cloud service account authentication (see below for nested schema)
                          Configuration for Google Cloud service account authentication
                        properties:
                          privateKeySecretRef:
                            description: |-
                              (String, Sensitive) Google Cloud service account private key. String length must be at least 1.
                              Google Cloud service account private key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                          serviceAccountEmail:
                            description: |-
                              (String) Google Cloud service account email. String length must be at least 1.
                              Google Cloud service account email. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  gemini:
                    description: |-
                      (Block, Optional) Configuration for a Gemini connection (see below for nested schema)
                      Configuration for a Gemini connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Gemini API key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              Gemini API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  gitlab:
                    description: |-
                      (Block, Optional) Configuration for a GitLab connection (see below for nested schema)
                      Configuration for a GitLab connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for GitLab API key authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              GitLab API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  greyNoise:
                    description: |-
                      (Block, Optional) Configuration for a GreyNoise connection (see below for nested schema)
                      Configuration for a GreyNoise connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for GreyNoise API key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              GreyNoise API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  http:
                    description: |-
                      (Block, Optional) Configuration for an HTTP connection (see below for nested schema)
                      Configuration for an HTTP connection
                    properties:
                      baseUrl:
                        description: |-
                          (String) Base HTTP url for the integration. String length must be at least 1.
                          Base HTTP url for the integration. String length must be at least 1.
                        type: string
                      tokenAuth:
                        description: |-
                          (Block, Optional) Configuration for an HTTP connection that uses token auth (see below for nested schema)
                          Configuration for an HTTP connection that uses token auth
                        properties:
                          body:
                            description: |-
                              (Block, Optional) Body for HTTP authentication (see below for nested schema)
                              Body for HTTP authentication
                            properties:
                              content:
                                description: |-
                                  (String) Serialized body content. String length must be at least 1.
                                  Serialized body content. String length must be at least 1.
                                type: string
                              contentType:
                                description: |-
                                  (String) Content type of the body. String length must be at least 1.
                                  Content type of the body. String length must be at least 1.
                                type: string
                            type: object
                          header:
                            description: |-
                              (Block List) Header for HTTP authentication (see below for nested schema)
                              Header for HTTP authentication
                            items:
                              properties:
                                name:
                                  description: |-
                                    (String) Name of the connection
                                    Header name. String length must be at least 1.
                                  type: string
                                value:
                                  description: |-
                                    (String) String length must be at least 1.
                                    String length must be at least 1.
                                  type: string
                              type: object
                            type: array
                          token:
                            description: |-
                              (Block List) Token for HTTP authentication (see below for nested schema)
                              Token for HTTP authentication
                            items:
                              properties:
                                name:
                                  description: |-
                                    (String) Name of the connection
                                    Token name. String length must be at least 1.
                                  type: string
                                type:
                                  description: |-
                                    (String) Token type Valid values are SECRET.
                                    Token type Valid values are `SECRET`.
                                  type: string
                                valueSecretRef:
                                  description: |-
                                    (String) String length must be at least 1.
                                    Token value. String length must be at least 1.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      description: Name of the secret.
                                      type: string
                                    namespace:
                                      description: Namespace of the secret.
                                      type: string
                                  required:
                                  - key
                                  - name
                                  - namespace
                                  type: object
                              type: object
                            type: array
                          urlParameter:
                            description: |-
                              (Block List) URL parameter for HTTP authentication (see below for nested schema)
                              URL parameter for HTTP authentication
                            items:
                              properties:
                                name:
                                  description: |-
                                    (String) Name of the connection
                                    URL parameter name. String length must be at least 1.
                                  type: string
                                value:
                                  description: |-
                                    (String) String length must be at least 1.
                                    URL parameter value. String length must be at least 1.
                                  type: string
                              type: object
                            type: array
                        type: object
                    type: object
                  launchDarkly:
                    description: |-
                      (Block, Optional) Configuration for a LaunchDarkly connection (see below for nested schema)
                      Configuration for a LaunchDarkly connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for LaunchDarkly API key authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              LaunchDarkly API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  name:
                    description: |-
                      (String) Name of the connection
                      Name of the connection
                    type: string
                  notion:
                    description: |-
                      (Block, Optional) Configuration for a Notion connection (see below for nested schema)
                      Configuration for a Notion connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Notion API key authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              Notion API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  okta:
                    description: |-
                      (Block, Optional) Configuration for an Okta connection (see below for nested schema)
                      Configuration for an Okta connection
                    properties:
                      apiToken:
                        description: |-
                          (String, Sensitive) Anthropic API token. String length must be at least 1.
                          Configuration for Okta API token authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              Okta API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                          domain:
                            description: |-
                              (String) Freshservice domain. String length must be at least 1.
                              Okta domain. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  openai:
                    description: |-
                      (Block, Optional) Configuration for an OpenAI connection (see below for nested schema)
                      Configuration for an OpenAI connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for OpenAI API key authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              OpenAI API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  serviceNow:
                    description: |-
                      (Block, Optional) Configuration for a ServiceNow connection (see below for nested schema)
                      Configuration for a ServiceNow connection
                    properties:
                      basicAuth:
                        description: |-
                          (Block, Optional) Configuration for ServiceNow basic authentication (see below for nested schema)
                          Configuration for ServiceNow basic authentication
                        properties:
                          instance:
                            description: |-
                              (String) ServiceNow instance. String length must be at least 1.
                              ServiceNow instance. String length must be at least 1.
                            type: string
                          passwordSecretRef:
                            description: |-
                              (String, Sensitive) ServiceNow password. String length must be at least 1.
                              ServiceNow password. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                          username:
                            description: |-
                              (String) ServiceNow username. String length must be at least 1.
                              ServiceNow username. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  split:
                    description: |-
                      (Block, Optional) Configuration for a Split connection (see below for nested schema)
                      Configuration for a Split connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Split API key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              Split API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  statsig:
                    description: |-
                      (Block, Optional) Configuration for a Statsig connection (see below for nested schema)
                      Configuration for a Statsig connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Statsig API key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              Statsig API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  tags:
                    description: |-
                      defined tags associated with the connection. Each tag must follow the key:value format. The default tag key is reserved. See also effective_tags, which includes provider-level default_tags.
                      User-defined tags associated with the connection. Each tag must follow the `key:value` format. The `default` tag key is reserved. See also `effective_tags`, which includes provider-level `default_tags`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  virusTotal:
                    description: |-
                      (Block, Optional) Configuration for a VirusTotal connection (see below for nested schema)
                      Configuration for a VirusTotal connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for VirusTotal API key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              VirusTotal API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: ConnectionStatus defines the observed state of Connection.
            properties:
              atProvider:
                properties:
                  anthropic:
                    description: |-
                      (Block, Optional) Configuration for an Anthropic connection (see below for nested schema)
                      Configuration for an Anthropic connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Anthropic API key authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              Anthropic API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  asana:
                    description: |-
                      (Block, Optional) Configuration for an Asana connection (see below for nested schema)
                      Configuration for an Asana connection
                    properties:
                      accessToken:
                        description: |-
                          (Block, Optional) Configuration for Asana access token authentication (see below for nested schema)
                          Configuration for Asana access token authentication
                        properties:
                          accessTokenSecretRef:
                            description: |-
                              (Block, Optional) Configuration for Asana access token authentication (see below for nested schema)
                              Asana access token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  aws:
                    description: |-
                      (Block, Optional) Configuration for an AWS connection (see below for nested schema)
                      Configuration for an AWS connection
                    properties:
                      assumeRole:
                        description: |-
                          (Block, Optional) Configuration for an assume role AWS connection (see below for nested schema)
                          Configuration for an assume role AWS connection
                        properties:
                          accountId:
                            description: |-
                              (String) AWS account that the connection is created for. String length must be at least 1.
                              AWS account that the connection is created for. String length must be at least 1.
                            type: string
                          externalId:
                            description: |-
                              (String) External ID that specifies which connection can be used to assume the role
                              External ID that specifies which connection can be used to assume the role
                            type: string
                          principalId:
                            description: |-
                              (String) AWS account that will assume the role
                              AWS account that will assume the role
                            type: string
                          role:
                            description: |-
                              (String) Role to assume. String length must be at least 1.
                              Role to assume. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  azure:
                    description: |-
                      (Block, Optional) Configuration for an Azure connection (see below for nested schema)
                      Configuration for an Azure connection
                    properties:
                      tenant:
                        description: |-
                          (Block, Optional) Configuration for Azure tenant authentication (see below for nested schema)
                          Configuration for Azure tenant authentication
                        properties:
                          appClientId:
                            description: |-
                              (String) Azure application client ID. String length must be at least 1.
                              Azure application client ID. String length must be at least 1.
                            type: string
                          customScopes:
                            description: |-
                              (String) Custom scope requested when acquiring an OAuth 2 access token. String length must be at least 1.
                              Custom scope requested when acquiring an OAuth 2 access token. String length must be at least 1.
                            type: string
                          tenantId:
                            description: |-
                              (String) Azure Active Directory tenant ID. String length must be at least 1.
                              Azure Active Directory tenant ID. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  circleCi:
                    description: |-
                      (Block, Optional) Configuration for a CircleCI connection (see below for nested schema)
                      Configuration for a CircleCI connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for CircleCI API key authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              CircleCI API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  clickup:
                    description: |-
                      (Block, Optional) Configuration for a ClickUp connection (see below for nested schema)
                      Configuration for a ClickUp connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for ClickUp API key authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              ClickUp API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  cloudflare:
                    description: |-
                      (Block, Optional) Configuration for a Cloudflare connection (see below for nested schema)
                      Configuration for a Cloudflare connection
                    properties:
                      apiToken:
                        description: |-
                          (String, Sensitive) Anthropic API token. String length must be at least 1.
                          Configuration for Cloudflare API token authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              Cloudflare API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                      globalApiToken:
                        description: |-
                          (Block, Optional) Configuration for Cloudflare global API token authentication (see below for nested schema)
                          Configuration for Cloudflare global API token authentication
                        properties:
                          authEmail:
                            description: |-
                              (String) Email address associated with the Cloudflare account. String length must be at least 1.
                              Email address associated with the Cloudflare account. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  configCat:
                    description: |-
                      (Block, Optional) Configuration for a ConfigCat connection (see below for nested schema)
                      Configuration for a ConfigCat connection
                    properties:
                      sdkKey:
                        description: |-
                          (Block, Optional) Configuration for ConfigCat SDK key authentication (see below for nested schema)
                          Configuration for ConfigCat SDK key authentication
                        properties:
                          apiUsername:
                            description: |-
                              (String) ConfigCat Public Management API username. String length must be at least 1.
                              ConfigCat Public Management API username. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  datadog:
                    description: |-
                      (Block, Optional) Configuration for a Datadog connection (see below for nested schema)
                      Configuration for a Datadog connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Datadog API and application key authentication
                        properties:
                          datacenter:
                            description: |-
                              (String) Datadog site data center. String length must be at least 1.
                              Datadog site data center. String length must be at least 1.
                            type: string
                          subdomain:
                            description: |-
                              (String) Custom subdomain used for URLs generated with this connection. String length must be at least 1.
                              Custom subdomain used for URLs generated with this connection. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  effectiveTags:
                    description: |-
                      (Set of String) Tags associated with the connection, including those inherited from the provider's default_tags configuration.
                      Tags associated with the connection, including those inherited from the provider's `default_tags` configuration.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  fastly:
                    description: |-
                      (Block, Optional) Configuration for a Fastly connection (see below for nested schema)
                      Configuration for a Fastly connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Fastly API key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              Fastly API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  freshservice:
                    description: |-
                      (Block, Optional) Configuration for a Freshservice connection (see below for nested schema)
                      Configuration for a Freshservice connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Freshservice API key authentication
                        properties:
                          domain:
                            description: |-
                              (String) Freshservice domain. String length must be at least 1.
                              Freshservice domain. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  gcp:
                    description: |-
                      (Block, Optional) Configuration for a Google Cloud connection (see below for nested schema)
                      Configuration for a Google Cloud connection
                    properties:
                      serviceAccount:
                        description: |-
                          (Block, Optional) Configuration for Google Cloud service account authentication (see below for nested schema)
                          Configuration for Google Cloud service account authentication
                        properties:
                          serviceAccountEmail:
                            description: |-
                              (String) Google Cloud service account email. String length must be at least 1.
                              Google Cloud service account email. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  gemini:
                    description: |-
                      (Block, Optional) Configuration for a Gemini connection (see below for nested schema)
                      Configuration for a Gemini connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Gemini API key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              Gemini API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  gitlab:
                    description: |-
                      (Block, Optional) Configuration for a GitLab connection (see below for nested schema)
                      Configuration for a GitLab connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for GitLab API key authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              GitLab API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  greyNoise:
                    description: |-
                      (Block, Optional) Configuration for a GreyNoise connection (see below for nested schema)
                      Configuration for a GreyNoise connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for GreyNoise API key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              GreyNoise API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  http:
                    description: |-
                      (Block, Optional) Configuration for an HTTP connection (see below for nested schema)
                      Configuration for an HTTP connection
                    properties:
                      baseUrl:
                        description: |-
                          (String) Base HTTP url for the integration. String length must be at least 1.
                          Base HTTP url for the integration. String length must be at least 1.
                        type: string
                      tokenAuth:
                        description: |-
                          (Block, Optional) Configuration for an HTTP connection that uses token auth (see below for nested schema)
                          Configuration for an HTTP connection that uses token auth
                        properties:
                          body:
                            description: |-
                              (Block, Optional) Body for HTTP authentication (see below for nested schema)
                              Body for HTTP authentication
                            properties:
                              content:
                                description: |-
                                  (String) Serialized body content. String length must be at least 1.
                                  Serialized body content. String length must be at least 1.
                                type: string
                              contentType:
                                description: |-
                                  (String) Content type of the body. String length must be at least 1.
                                  Content type of the body. String length must be at least 1.
                                type: string
                            type: object
                          header:
                            description: |-
                              (Block List) Header for HTTP authentication (see below for nested schema)
                              Header for HTTP authentication
                            items:
                              properties:
                                name:
                                  description: |-
                                    (String) Name of the connection
                                    Header name. String length must be at least 1.
                                  type: string
                                value:
                                  description: |-
                                    (String) String length must be at least 1.
                                    String length must be at least 1.
                                  type: string
                              type: object
                            type: array
                          token:
                            description: |-
                              (Block List) Token for HTTP authentication (see below for nested schema)
                              Token for HTTP authentication
                            items:
                              properties:
                                name:
                                  description: |-
                                    (String) Name of the connection
                                    Token name. String length must be at least 1.
                                  type: string
                                type:
                                  description: |-
                                    (String) Token type Valid values are SECRET.
                                    Token type Valid values are `SECRET`.
                                  type: string
                              type: object
                            type: array
                          urlParameter:
                            description: |-
                              (Block List) URL parameter for HTTP authentication (see below for nested schema)
                              URL parameter for HTTP authentication
                            items:
                              properties:
                                name:
                                  description: |-
                                    (String) Name of the connection
                                    URL parameter name. String length must be at least 1.
                                  type: string
                                value:
                                  description: |-
                                    (String) String length must be at least 1.
                                    URL parameter value. String length must be at least 1.
                                  type: string
                              type: object
                            type: array
                        type: object
                    type: object
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  launchDarkly:
                    description: |-
                      (Block, Optional) Configuration for a LaunchDarkly connection (see below for nested schema)
                      Configuration for a LaunchDarkly connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for LaunchDarkly API key authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              LaunchDarkly API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  name:
                    description: |-
                      (String) Name of the connection
                      Name of the connection
                    type: string
                  notion:
                    description: |-
                      (Block, Optional) Configuration for a Notion connection (see below for nested schema)
                      Configuration for a Notion connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Notion API key authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              Notion API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  okta:
                    description: |-
                      (Block, Optional) Configuration for an Okta connection (see below for nested schema)
                      Configuration for an Okta connection
                    properties:
                      apiToken:
                        description: |-
                          (String, Sensitive) Anthropic API token. String length must be at least 1.
                          Configuration for Okta API token authentication
                        properties:
                          domain:
                            description: |-
                              (String) Freshservice domain. String length must be at least 1.
                              Okta domain. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  openai:
                    description: |-
                      (Block, Optional) Configuration for an OpenAI connection (see below for nested schema)
                      Configuration for an OpenAI connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for OpenAI API key authentication
                        properties:
                          apiTokenSecretRef:
                            description: |-
                              (String, Sensitive) Anthropic API token. String length must be at least 1.
                              OpenAI API token. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  serviceNow:
                    description: |-
                      (Block, Optional) Configuration for a ServiceNow connection (see below for nested schema)
                      Configuration for a ServiceNow connection
                    properties:
                      basicAuth:
                        description: |-
                          (Block, Optional) Configuration for ServiceNow basic authentication (see below for nested schema)
                          Configuration for ServiceNow basic authentication
                        properties:
                          instance:
                            description: |-
                              (String) ServiceNow instance. String length must be at least 1.
                              ServiceNow instance. String length must be at least 1.
                            type: string
                          username:
                            description: |-
                              (String) ServiceNow username. String length must be at least 1.
                              ServiceNow username. String length must be at least 1.
                            type: string
                        type: object
                    type: object
                  split:
                    description: |-
                      (Block, Optional) Configuration for a Split connection (see below for nested schema)
                      Configuration for a Split connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Split API key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              Split API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  statsig:
                    description: |-
                      (Block, Optional) Configuration for a Statsig connection (see below for nested schema)
                      Configuration for a Statsig connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for Statsig API key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              Statsig API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                  tags:
                    description: |-
                      defined tags associated with the connection. Each tag must follow the key:value format. The default tag key is reserved. See also effective_tags, which includes provider-level default_tags.
                      User-defined tags associated with the connection. Each tag must follow the `key:value` format. The `default` tag key is reserved. See also `effective_tags`, which includes provider-level `default_tags`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  virusTotal:
                    description: |-
                      (Block, Optional) Configuration for a VirusTotal connection (see below for nested schema)
                      Configuration for a VirusTotal connection
                    properties:
                      apiKey:
                        description: |-
                          (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                          Configuration for VirusTotal API key authentication
                        properties:
                          apiKeySecretRef:
                            description: |-
                              (Block, Optional) Configuration for Anthropic API key authentication (see below for nested schema)
                              VirusTotal API key. String length must be at least 1.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                              namespace:
                                description: Namespace of the secret.
                                type: string
                            required:
                            - key
                            - name
                            - namespace
                            type: object
                        type: object
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

