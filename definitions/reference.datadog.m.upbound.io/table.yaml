---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: tables.reference.datadog.m.upbound.io
spec:
  group: reference.datadog.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - datadog
    kind: Table
    listKind: TableList
    plural: tables
    singular: table
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Table is the Schema for the Tables API. Provides a Datadog Reference
          Table resource for cloud storage sources (S3, GCS, Azure). This can be used
          to create and manage Datadog reference tables that sync data from cloud
          storage. For setup instructions including granting Datadog read access to
          your cloud storage bucket, see the Reference Tables documentation https://docs.datadoghq.com/reference_tables/?tab=cloudstorage#create-a-reference-table.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TableSpec defines the desired state of Table
            properties:
              forProvider:
                properties:
                  description:
                    description: |-
                      (String) The description of the reference table.
                      The description of the reference table.
                    type: string
                  fileMetadata:
                    description: |-
                      (Block, Optional) Configuration for cloud storage file access and sync settings. (see below for nested schema)
                      Configuration for cloud storage file access and sync settings.
                    properties:
                      accessDetails:
                        description: |-
                          (Block, Optional) Cloud storage access configuration. Exactly one of aws_detail, gcp_detail, or azure_detail must be specified. (see below for nested schema)
                          Cloud storage access configuration. Exactly one of aws_detail, gcp_detail, or azure_detail must be specified.
                        properties:
                          awsDetail:
                            description: |-
                              (Block, Optional) AWS S3 access configuration. Required when source is S3. (see below for nested schema)
                              AWS S3 access configuration. Required when source is S3.
                            properties:
                              awsAccountId:
                                description: |-
                                  (String) The ID of the AWS account.
                                  The ID of the AWS account.
                                type: string
                              awsBucketName:
                                description: |-
                                  (String) The name of the AWS S3 bucket.
                                  The name of the AWS S3 bucket.
                                type: string
                              filePath:
                                description: |-
                                  (String) The relative file path from the AWS S3 bucket root to the CSV file.
                                  The relative file path from the AWS S3 bucket root to the CSV file.
                                type: string
                            type: object
                          azureDetail:
                            description: |-
                              (Block, Optional) Azure Blob Storage access configuration. Required when source is AZURE. (see below for nested schema)
                              Azure Blob Storage access configuration. Required when source is AZURE.
                            properties:
                              azureClientId:
                                description: |-
                                  (String) The Azure client ID (application ID).
                                  The Azure client ID (application ID).
                                type: string
                              azureContainerName:
                                description: |-
                                  (String) The name of the Azure container.
                                  The name of the Azure container.
                                type: string
                              azureStorageAccountName:
                                description: |-
                                  (String) The name of the Azure storage account.
                                  The name of the Azure storage account.
                                type: string
                              azureTenantId:
                                description: |-
                                  (String) The ID of the Azure tenant.
                                  The ID of the Azure tenant.
                                type: string
                              filePath:
                                description: |-
                                  (String) The relative file path from the AWS S3 bucket root to the CSV file.
                                  The relative file path from the Azure container root to the CSV file.
                                type: string
                            type: object
                          gcpDetail:
                            description: |-
                              (Block, Optional) Google Cloud Storage access configuration. Required when source is GCS. (see below for nested schema)
                              Google Cloud Storage access configuration. Required when source is GCS.
                            properties:
                              filePath:
                                description: |-
                                  (String) The relative file path from the AWS S3 bucket root to the CSV file.
                                  The relative file path from the GCS bucket root to the CSV file.
                                type: string
                              gcpBucketName:
                                description: |-
                                  (String) The name of the GCP bucket.
                                  The name of the GCP bucket.
                                type: string
                              gcpProjectId:
                                description: |-
                                  (String) The ID of the GCP project.
                                  The ID of the GCP project.
                                type: string
                              gcpServiceAccountEmail:
                                description: |-
                                  (String) The email of the GCP service account used to access the bucket.
                                  The email of the GCP service account used to access the bucket.
                                type: string
                            type: object
                        type: object
                      syncEnabled:
                        description: |-
                          (Boolean) Whether this table should automatically sync with the cloud storage source.
                          Whether this table should automatically sync with the cloud storage source.
                        type: boolean
                    type: object
                  schema:
                    description: |-
                      (Block, Optional) The schema definition for the reference table, including field definitions and primary keys. This block is required. Schema is only set on create; updates are derived from the file asynchronously. (see below for nested schema)
                      The schema definition for the reference table, including field definitions and primary keys. This block is required. Schema is only set on create; updates are derived from the file asynchronously.
                    properties:
                      fields:
                        description: |-
                          (Block List) List of fields in the table schema. At least one field is required. Schema is only set on create. (see below for nested schema)
                          List of fields in the table schema. At least one field is required. Schema is only set on create.
                        items:
                          properties:
                            name:
                              description: |-
                                (String) The name of the field.
                                The name of the field.
                              type: string
                            type:
                              description: |-
                                (String) The data type of the field. Must be one of: STRING, INT32. Valid values are STRING, INT32.
                                The data type of the field. Must be one of: STRING, INT32. Valid values are `STRING`, `INT32`.
                              type: string
                          type: object
                        type: array
                      primaryKeys:
                        description: |-
                          (List of String) List of field names that serve as primary keys for the table. Currently only one primary key is supported.
                          List of field names that serve as primary keys for the table. Currently only one primary key is supported.
                        items:
                          type: string
                        type: array
                    type: object
                  source:
                    description: |-
                      (String) The source type for the reference table. Valid values are S3, GCS, AZURE.
                      The source type for the reference table. Valid values are `S3`, `GCS`, `AZURE`.
                    type: string
                  tableName:
                    description: |-
                      (String) The name of the reference table. This must be unique within your organization.
                      The name of the reference table. This must be unique within your organization.
                    type: string
                  tags:
                    description: |-
                      (List of String) A list of tags to associate with the reference table.
                      A list of tags to associate with the reference table.
                    items:
                      type: string
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  description:
                    description: |-
                      (String) The description of the reference table.
                      The description of the reference table.
                    type: string
                  fileMetadata:
                    description: |-
                      (Block, Optional) Configuration for cloud storage file access and sync settings. (see below for nested schema)
                      Configuration for cloud storage file access and sync settings.
                    properties:
                      accessDetails:
                        description: |-
                          (Block, Optional) Cloud storage access configuration. Exactly one of aws_detail, gcp_detail, or azure_detail must be specified. (see below for nested schema)
                          Cloud storage access configuration. Exactly one of aws_detail, gcp_detail, or azure_detail must be specified.
                        properties:
                          awsDetail:
                            description: |-
                              (Block, Optional) AWS S3 access configuration. Required when source is S3. (see below for nested schema)
                              AWS S3 access configuration. Required when source is S3.
                            properties:
                              awsAccountId:
                                description: |-
                                  (String) The ID of the AWS account.
                                  The ID of the AWS account.
                                type: string
                              awsBucketName:
                                description: |-
                                  (String) The name of the AWS S3 bucket.
                                  The name of the AWS S3 bucket.
                                type: string
                              filePath:
                                description: |-
                                  (String) The relative file path from the AWS S3 bucket root to the CSV file.
                                  The relative file path from the AWS S3 bucket root to the CSV file.
                                type: string
                            type: object
                          azureDetail:
                            description: |-
                              (Block, Optional) Azure Blob Storage access configuration. Required when source is AZURE. (see below for nested schema)
                              Azure Blob Storage access configuration. Required when source is AZURE.
                            properties:
                              azureClientId:
                                description: |-
                                  (String) The Azure client ID (application ID).
                                  The Azure client ID (application ID).
                                type: string
                              azureContainerName:
                                description: |-
                                  (String) The name of the Azure container.
                                  The name of the Azure container.
                                type: string
                              azureStorageAccountName:
                                description: |-
                                  (String) The name of the Azure storage account.
                                  The name of the Azure storage account.
                                type: string
                              azureTenantId:
                                description: |-
                                  (String) The ID of the Azure tenant.
                                  The ID of the Azure tenant.
                                type: string
                              filePath:
                                description: |-
                                  (String) The relative file path from the AWS S3 bucket root to the CSV file.
                                  The relative file path from the Azure container root to the CSV file.
                                type: string
                            type: object
                          gcpDetail:
                            description: |-
                              (Block, Optional) Google Cloud Storage access configuration. Required when source is GCS. (see below for nested schema)
                              Google Cloud Storage access configuration. Required when source is GCS.
                            properties:
                              filePath:
                                description: |-
                                  (String) The relative file path from the AWS S3 bucket root to the CSV file.
                                  The relative file path from the GCS bucket root to the CSV file.
                                type: string
                              gcpBucketName:
                                description: |-
                                  (String) The name of the GCP bucket.
                                  The name of the GCP bucket.
                                type: string
                              gcpProjectId:
                                description: |-
                                  (String) The ID of the GCP project.
                                  The ID of the GCP project.
                                type: string
                              gcpServiceAccountEmail:
                                description: |-
                                  (String) The email of the GCP service account used to access the bucket.
                                  The email of the GCP service account used to access the bucket.
                                type: string
                            type: object
                        type: object
                      syncEnabled:
                        description: |-
                          (Boolean) Whether this table should automatically sync with the cloud storage source.
                          Whether this table should automatically sync with the cloud storage source.
                        type: boolean
                    type: object
                  schema:
                    description: |-
                      (Block, Optional) The schema definition for the reference table, including field definitions and primary keys. This block is required. Schema is only set on create; updates are derived from the file asynchronously. (see below for nested schema)
                      The schema definition for the reference table, including field definitions and primary keys. This block is required. Schema is only set on create; updates are derived from the file asynchronously.
                    properties:
                      fields:
                        description: |-
                          (Block List) List of fields in the table schema. At least one field is required. Schema is only set on create. (see below for nested schema)
                          List of fields in the table schema. At least one field is required. Schema is only set on create.
                        items:
                          properties:
                            name:
                              description: |-
                                (String) The name of the field.
                                The name of the field.
                              type: string
                            type:
                              description: |-
                                (String) The data type of the field. Must be one of: STRING, INT32. Valid values are STRING, INT32.
                                The data type of the field. Must be one of: STRING, INT32. Valid values are `STRING`, `INT32`.
                              type: string
                          type: object
                        type: array
                      primaryKeys:
                        description: |-
                          (List of String) List of field names that serve as primary keys for the table. Currently only one primary key is supported.
                          List of field names that serve as primary keys for the table. Currently only one primary key is supported.
                        items:
                          type: string
                        type: array
                    type: object
                  source:
                    description: |-
                      (String) The source type for the reference table. Valid values are S3, GCS, AZURE.
                      The source type for the reference table. Valid values are `S3`, `GCS`, `AZURE`.
                    type: string
                  tableName:
                    description: |-
                      (String) The name of the reference table. This must be unique within your organization.
                      The name of the reference table. This must be unique within your organization.
                    type: string
                  tags:
                    description: |-
                      (List of String) A list of tags to associate with the reference table.
                      A list of tags to associate with the reference table.
                    items:
                      type: string
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.source is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.source)
                || (has(self.initProvider) && has(self.initProvider.source))'
            - message: spec.forProvider.tableName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.tableName)
                || (has(self.initProvider) && has(self.initProvider.tableName))'
          status:
            description: TableStatus defines the observed state of Table.
            properties:
              atProvider:
                properties:
                  createdBy:
                    description: |-
                      (String) UUID of the user who created the reference table.
                      UUID of the user who created the reference table.
                    type: string
                  description:
                    description: |-
                      (String) The description of the reference table.
                      The description of the reference table.
                    type: string
                  fileMetadata:
                    description: |-
                      (Block, Optional) Configuration for cloud storage file access and sync settings. (see below for nested schema)
                      Configuration for cloud storage file access and sync settings.
                    properties:
                      accessDetails:
                        description: |-
                          (Block, Optional) Cloud storage access configuration. Exactly one of aws_detail, gcp_detail, or azure_detail must be specified. (see below for nested schema)
                          Cloud storage access configuration. Exactly one of aws_detail, gcp_detail, or azure_detail must be specified.
                        properties:
                          awsDetail:
                            description: |-
                              (Block, Optional) AWS S3 access configuration. Required when source is S3. (see below for nested schema)
                              AWS S3 access configuration. Required when source is S3.
                            properties:
                              awsAccountId:
                                description: |-
                                  (String) The ID of the AWS account.
                                  The ID of the AWS account.
                                type: string
                              awsBucketName:
                                description: |-
                                  (String) The name of the AWS S3 bucket.
                                  The name of the AWS S3 bucket.
                                type: string
                              filePath:
                                description: |-
                                  (String) The relative file path from the AWS S3 bucket root to the CSV file.
                                  The relative file path from the AWS S3 bucket root to the CSV file.
                                type: string
                            type: object
                          azureDetail:
                            description: |-
                              (Block, Optional) Azure Blob Storage access configuration. Required when source is AZURE. (see below for nested schema)
                              Azure Blob Storage access configuration. Required when source is AZURE.
                            properties:
                              azureClientId:
                                description: |-
                                  (String) The Azure client ID (application ID).
                                  The Azure client ID (application ID).
                                type: string
                              azureContainerName:
                                description: |-
                                  (String) The name of the Azure container.
                                  The name of the Azure container.
                                type: string
                              azureStorageAccountName:
                                description: |-
                                  (String) The name of the Azure storage account.
                                  The name of the Azure storage account.
                                type: string
                              azureTenantId:
                                description: |-
                                  (String) The ID of the Azure tenant.
                                  The ID of the Azure tenant.
                                type: string
                              filePath:
                                description: |-
                                  (String) The relative file path from the AWS S3 bucket root to the CSV file.
                                  The relative file path from the Azure container root to the CSV file.
                                type: string
                            type: object
                          gcpDetail:
                            description: |-
                              (Block, Optional) Google Cloud Storage access configuration. Required when source is GCS. (see below for nested schema)
                              Google Cloud Storage access configuration. Required when source is GCS.
                            properties:
                              filePath:
                                description: |-
                                  (String) The relative file path from the AWS S3 bucket root to the CSV file.
                                  The relative file path from the GCS bucket root to the CSV file.
                                type: string
                              gcpBucketName:
                                description: |-
                                  (String) The name of the GCP bucket.
                                  The name of the GCP bucket.
                                type: string
                              gcpProjectId:
                                description: |-
                                  (String) The ID of the GCP project.
                                  The ID of the GCP project.
                                type: string
                              gcpServiceAccountEmail:
                                description: |-
                                  (String) The email of the GCP service account used to access the bucket.
                                  The email of the GCP service account used to access the bucket.
                                type: string
                            type: object
                        type: object
                      errorMessage:
                        description: |-
                          (String) Error message from the last sync attempt, if any.
                          Error message from the last sync attempt, if any.
                        type: string
                      errorRowCount:
                        description: |-
                          (Number) The number of rows that failed to sync.
                          The number of rows that failed to sync.
                        type: number
                      errorType:
                        description: |-
                          (String) The type of error that occurred during file processing.
                          The type of error that occurred during file processing.
                        type: string
                      syncEnabled:
                        description: |-
                          (Boolean) Whether this table should automatically sync with the cloud storage source.
                          Whether this table should automatically sync with the cloud storage source.
                        type: boolean
                    type: object
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  lastUpdatedBy:
                    description: |-
                      (String) UUID of the user who last updated the reference table.
                      UUID of the user who last updated the reference table.
                    type: string
                  rowCount:
                    description: |-
                      (Number) The number of successfully processed rows in the reference table.
                      The number of successfully processed rows in the reference table.
                    type: number
                  schema:
                    description: |-
                      (Block, Optional) The schema definition for the reference table, including field definitions and primary keys. This block is required. Schema is only set on create; updates are derived from the file asynchronously. (see below for nested schema)
                      The schema definition for the reference table, including field definitions and primary keys. This block is required. Schema is only set on create; updates are derived from the file asynchronously.
                    properties:
                      fields:
                        description: |-
                          (Block List) List of fields in the table schema. At least one field is required. Schema is only set on create. (see below for nested schema)
                          List of fields in the table schema. At least one field is required. Schema is only set on create.
                        items:
                          properties:
                            name:
                              description: |-
                                (String) The name of the field.
                                The name of the field.
                              type: string
                            type:
                              description: |-
                                (String) The data type of the field. Must be one of: STRING, INT32. Valid values are STRING, INT32.
                                The data type of the field. Must be one of: STRING, INT32. Valid values are `STRING`, `INT32`.
                              type: string
                          type: object
                        type: array
                      primaryKeys:
                        description: |-
                          (List of String) List of field names that serve as primary keys for the table. Currently only one primary key is supported.
                          List of field names that serve as primary keys for the table. Currently only one primary key is supported.
                        items:
                          type: string
                        type: array
                    type: object
                  source:
                    description: |-
                      (String) The source type for the reference table. Valid values are S3, GCS, AZURE.
                      The source type for the reference table. Valid values are `S3`, `GCS`, `AZURE`.
                    type: string
                  status:
                    description: |-
                      (String) The status of the reference table (e.g., DONE, PROCESSING, ERROR).
                      The status of the reference table (e.g., DONE, PROCESSING, ERROR).
                    type: string
                  tableName:
                    description: |-
                      (String) The name of the reference table. This must be unique within your organization.
                      The name of the reference table. This must be unique within your organization.
                    type: string
                  tags:
                    description: |-
                      (List of String) A list of tags to associate with the reference table.
                      A list of tags to associate with the reference table.
                    items:
                      type: string
                    type: array
                  updatedAt:
                    description: |-
                      (String) The timestamp of the last update to the reference table in ISO 8601 format.
                      The timestamp of the last update to the reference table in ISO 8601 format.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

