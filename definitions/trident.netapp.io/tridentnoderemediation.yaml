apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  name: tridentnoderemediations.trident.netapp.io
spec:
  group: trident.netapp.io
  names:
    kind: TridentNodeRemediation
    listKind: TridentNodeRemediationList
    plural: tridentnoderemediations
    singular: tridentnoderemediation
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            type: object
          status:
            properties:
              completionTime:
                format: date-time
                type: string
              message:
                type: string
              state:
                type: string
              volumeAttachments:
                additionalProperties:
                  type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
