apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  name: tridentgroupsnapshots.trident.netapp.io
spec:
  group: trident.netapp.io
  names:
    kind: TridentGroupSnapshot
    listKind: TridentGroupSnapshotList
    plural: tridentgroupsnapshots
    singular: tridentgroupsnapshot
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          dateCreated:
            type: string
          kind:
            type: string
          metadata:
            type: object
          snapshotIDs:
            items:
              type: string
            type: array
          spec:
            type: object
            x-kubernetes-preserve-unknown-fields: true
        required:
        - snapshotIDs
        - spec
        type: object
    served: true
    storage: true
