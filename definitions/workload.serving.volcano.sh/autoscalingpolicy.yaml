---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: autoscalingpolicies.workload.serving.volcano.sh
spec:
  group: workload.serving.volcano.sh
  names:
    kind: AutoscalingPolicy
    listKind: AutoscalingPolicyList
    plural: autoscalingpolicies
    singular: autoscalingpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AutoscalingPolicy defines the autoscaling policy configuration for model serving workloads.
          It specifies scaling rules, metrics, and behavior for automatic replica adjustment.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AutoscalingPolicySpec defines the desired state of AutoscalingPolicy.

              At most one of HomogeneousTarget, HeterogeneousTarget, or DisaggregatedTarget
              may be set. When the spec is used standalone (as an AutoscalingPolicy custom
              resource), exactly one target must be set; this is enforced by the
              autoscalingpolicy validating webhook rather than a CEL rule.
            properties:
              behavior:
                description: Behavior defines the scaling behavior configuration for
                  both scale up and scale down operations.
                properties:
                  scaleDown:
                    description: ScaleDown defines the policy configuration for scaling
                      down (decreasing replicas).
                    properties:
                      instances:
                        default: 1
                        description: Instances defines the maximum absolute number
                          of instances to scale per period.
                        format: int32
                        minimum: 0
                        type: integer
                      percent:
                        default: 100
                        description: Percent defines the maximum percentage of current
                          instances to scale per period.
                        format: int32
                        maximum: 1000
                        minimum: 0
                        type: integer
                      period:
                        default: 15s
                        description: Period defines the time duration over which scaling
                          metrics are evaluated.
                        type: string
                      selectPolicy:
                        default: Or
                        description: |-
                          SelectPolicy determines the selection strategy for scaling operations.
                          'Or' means scaling is performed if either the Percent or Instances requirement is met.
                          'And' means scaling is performed only if both Percent and Instances requirements are met.
                        enum:
                        - Or
                        - And
                        type: string
                      stabilizationWindow:
                        description: StabilizationWindow defines the time window to
                          stabilize scaling actions and prevent rapid oscillations.
                        type: string
                    type: object
                  scaleUp:
                    description: ScaleUp defines the policy configuration for scaling
                      up (increasing replicas).
                    properties:
                      panicPolicy:
                        description: |-
                          PanicPolicy defines the emergency scaling policy for handling sudden traffic spikes.
                          This policy activates during rapid load surges to prevent service degradation or timeouts.
                        properties:
                          panicModeHold:
                            default: 60s
                            description: PanicModeHold defines the duration to remain
                              in panic mode before returning to normal scaling.
                            type: string
                          panicThresholdPercent:
                            default: 200
                            description: |-
                              PanicThresholdPercent defines the metric threshold percentage that triggers panic mode.
                              When metrics exceed this percentage of target values, panic mode is activated.
                            format: int32
                            maximum: 1000
                            minimum: 110
                            type: integer
                          percent:
                            default: 1000
                            description: Percent defines the maximum percentage of
                              current instances to scale up during panic mode.
                            format: int32
                            maximum: 1000
                            minimum: 0
                            type: integer
                          period:
                            description: Period defines the evaluation period for
                              panic mode scaling decisions.
                            type: string
                        required:
                        - period
                        type: object
                      stablePolicy:
                        description: |-
                          StablePolicy defines the stable scaling policy that uses average metric values over time windows.
                          This policy smooths out short-term fluctuations and avoids unnecessary frequent scaling operations.
                        properties:
                          instances:
                            default: 1
                            description: Instances defines the maximum absolute number
                              of instances to scale per period.
                            format: int32
                            minimum: 0
                            type: integer
                          percent:
                            default: 100
                            description: Percent defines the maximum percentage of
                              current instances to scale per period.
                            format: int32
                            maximum: 1000
                            minimum: 0
                            type: integer
                          period:
                            default: 15s
                            description: Period defines the time duration over which
                              scaling metrics are evaluated.
                            type: string
                          selectPolicy:
                            default: Or
                            description: |-
                              SelectPolicy determines the selection strategy for scaling operations.
                              'Or' means scaling is performed if either the Percent or Instances requirement is met.
                              'And' means scaling is performed only if both Percent and Instances requirements are met.
                            enum:
                            - Or
                            - And
                            type: string
                          stabilizationWindow:
                            description: StabilizationWindow defines the time window
                              to stabilize scaling actions and prevent rapid oscillations.
                            type: string
                        type: object
                    type: object
                type: object
              disaggregatedTarget:
                description: |-
                  DisaggregatedTarget enables coordinated autoscaling of roles within a
                  single ModelServing that uses disaggregated serving.
                properties:
                  ratioConstraint:
                    description: |-
                      RatioConstraint defines the acceptable ratio range of a single role pair.
                      It enforces that replicas[numeratorRole] / replicas[denominatorRole] stays
                      within [minRatio, maxRatio] when denominator replica is non-zero.
                    properties:
                      denominatorRole:
                        description: DenominatorRole is the role on the denominator
                          side of the ratio.
                        type: string
                      maxRatio:
                        anyOf:
                        - type: integer
                        - type: string
                        description: |-
                          MaxRatio is the maximum allowed value of
                          replicas[numeratorRole] / replicas[denominatorRole].
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      minRatio:
                        anyOf:
                        - type: integer
                        - type: string
                        description: |-
                          MinRatio is the minimum allowed value of
                          replicas[numeratorRole] / replicas[denominatorRole].
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      numeratorRole:
                        description: NumeratorRole is the role on the numerator side
                          of the ratio.
                        type: string
                    required:
                    - denominatorRole
                    - maxRatio
                    - minRatio
                    - numeratorRole
                    type: object
                    x-kubernetes-validations:
                    - message: numeratorRole and denominatorRole must differ
                      rule: self.numeratorRole != self.denominatorRole
                  roles:
                    additionalProperties:
                      description: RoleScalingParam defines the scaling configuration
                        for one role.
                      properties:
                        maxReplicas:
                          description: MaxReplicas defines the maximum number of replicas
                            for this role.
                          format: int32
                          maximum: 1000000
                          minimum: 1
                          type: integer
                        metricSources:
                          additionalProperties:
                            description: "MetricSource is a discriminated union selecting
                              the metric backend.\n\nExactly one backend config must
                              be provided:\n  - Pod        -> set the pod field only.\n
                              \ - Prometheus -> set the prometheus field only.\n\nExample
                              (scrape the metric directly from each pod's /metrics
                              endpoint):\n\n\tmetricSources:\n\t  gpu_cache_usage:\n\t
                              \   pod:\n\t      name: vllm:gpu_cache_usage_perc\n\t
                              \     uri: /metrics\n\t      port: 8000\n\nExample (read
                              the metric from an external Prometheus server):\n\n\tmetricSources:\n\t
                              \ http_rps:\n\t    prometheus:\n\t      serverURL: http://prometheus.monitoring.svc:9090\n\t
                              \     query: sum(rate(http_requests_total[2m]))"
                            properties:
                              pod:
                                description: Pod configures direct pod endpoint scraping.
                                properties:
                                  labelSelector:
                                    description: |-
                                      LabelSelector defines additional filtering for pods exposing this metric.
                                      Only pods matching both the target and this selector are scraped, e.g.
                                      matchLabels with role=decode to scrape only the decode role's pods.
                                    properties:
                                      matchExpressions:
                                        description: matchExpressions is a list of
                                          label selector requirements. The requirements
                                          are ANDed.
                                        items:
                                          description: |-
                                            A label selector requirement is a selector that contains values, a key, and an operator that
                                            relates the key and values.
                                          properties:
                                            key:
                                              description: key is the label key that
                                                the selector applies to.
                                              type: string
                                            operator:
                                              description: |-
                                                operator represents a key's relationship to a set of values.
                                                Valid operators are In, NotIn, Exists and DoesNotExist.
                                              type: string
                                            values:
                                              description: |-
                                                values is an array of string values. If the operator is In or NotIn,
                                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                the values array must be empty. This array is replaced during a strategic
                                                merge patch.
                                              items:
                                                type: string
                                              type: array
                                              x-kubernetes-list-type: atomic
                                          required:
                                          - key
                                          - operator
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      matchLabels:
                                        additionalProperties:
                                          type: string
                                        description: |-
                                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                                        type: object
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  name:
                                    description: |-
                                      Name is the Prometheus metric name matched against labels in the pod's scraped output.
                                      Defaults to the policy metric key when omitted.
                                      For example, set it to "vllm:gpu_cache_usage_perc" to read that exact series.
                                    type: string
                                  port:
                                    default: 8100
                                    description: Port defines the network port where
                                      metrics are exposed by the pods (e.g., 8000).
                                    format: int32
                                    type: integer
                                  uri:
                                    default: /metrics
                                    description: Uri defines the HTTP path where metrics
                                      are exposed (e.g., "/metrics").
                                    type: string
                                type: object
                              prometheus:
                                description: Prometheus configures an external Prometheus
                                  server as the metric source.
                                properties:
                                  auth:
                                    description: Auth holds optional authentication
                                      configuration for the Prometheus server.
                                    type: object
                                  query:
                                    description: |-
                                      Query is a PromQL instant-query expression. It must evaluate to a single
                                      scalar or a one-element vector, e.g. "avg(rate(vllm:request_latency[1m]))".
                                      More Query details refer to https://prometheus.io/docs/prometheus/latest/querying/basics
                                    minLength: 1
                                    type: string
                                  serverURL:
                                    description: |-
                                      ServerURL is the base URL of the Prometheus HTTP API server.
                                      Example: "http://prometheus.monitoring.svc:9090".
                                    format: uri
                                    minLength: 1
                                    type: string
                                required:
                                - query
                                - serverURL
                                type: object
                            type: object
                            x-kubernetes-validations:
                            - message: one metric source backend config is required
                              rule: has(self.pod) || has(self.prometheus)
                            - message: pod and prometheus configs are mutually exclusive
                              rule: '!(has(self.pod) && has(self.prometheus))'
                          description: |-
                            MetricSources declares how each metric is fetched for this role.
                            Keys must match role-level metrics when present, otherwise top-level
                            spec.metrics[].name.
                            Missing keys are treated as missing metrics for that reconcile loop.
                          type: object
                        metrics:
                          description: |-
                            Metrics defines the list of metrics used to evaluate scaling decisions
                            for this role, allowing different roles to scale on different signals.

                            When set, these metrics override spec.metrics for this role. When omitted,
                            the role inherits spec.metrics. A fixed role (minReplicas == maxReplicas)
                            may omit metrics; the autoscaler keeps it at that fixed size and does not
                            collect metrics for it.
                          items:
                            description: AutoscalingPolicyMetric defines a metric
                              and its target value for scaling decisions.
                            properties:
                              name:
                                description: Name defines the metric key used by the
                                  scaling algorithm.
                                minLength: 1
                                type: string
                              targetValue:
                                anyOf:
                                - type: integer
                                - type: string
                                description: TargetValue defines the target value
                                  for the metric that triggers scaling operations.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            required:
                            - name
                            - targetValue
                            type: object
                          minItems: 1
                          type: array
                        minReplicas:
                          description: MinReplicas defines the minimum number of replicas
                            for this role.
                          format: int32
                          maximum: 1000000
                          minimum: 0
                          type: integer
                      required:
                      - maxReplicas
                      - minReplicas
                      type: object
                    description: |-
                      Roles defines per-role scaling parameters. The map key is roleName
                      from ModelServing.spec.template.roles[].name. A single role is allowed so
                      users can autoscale one role independently without configuring a P/D pair.
                      RatioConstraint, when set, still requires two distinct roles.
                    maxProperties: 2
                    minProperties: 1
                    type: object
                  targetRef:
                    description: |-
                      TargetRef references the ModelServing deployment that contains
                      all scalable roles.
                    properties:
                      apiVersion:
                        description: API version of the referent.
                        type: string
                      fieldPath:
                        description: |-
                          If referring to a piece of an object instead of an entire object, this string
                          should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                          For example, if the object reference is to a container within a pod, this would take on a value like:
                          "spec.containers{name}" (where "name" refers to the name of the container that triggered
                          the event) or if no container name is specified "spec.containers[2]" (container with
                          index 2 in this pod). This syntax is chosen only to have some well-defined way of
                          referencing a part of an object.
                        type: string
                      kind:
                        description: |-
                          Kind of the referent.
                          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                        type: string
                      name:
                        description: |-
                          Name of the referent.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      namespace:
                        description: |-
                          Namespace of the referent.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                        type: string
                      resourceVersion:
                        description: |-
                          Specific resourceVersion to which this reference is made, if any.
                          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                        type: string
                      uid:
                        description: |-
                          UID of the referent.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - roles
                - targetRef
                type: object
                x-kubernetes-validations:
                - message: roles must contain at least two entries when ratioConstraint
                    is configured
                  rule: '!has(self.ratioConstraint) || size(self.roles) >= 2'
              heterogeneousTarget:
                description: |-
                  HeterogeneousTarget enables optimization-based scaling across multiple
                  ModelServing deployments with different hardware capabilities.
                properties:
                  costExpansionRatePercent:
                    default: 200
                    description: |-
                      CostExpansionRatePercent defines the percentage rate at which the cost expands during optimization calculations.
                      For example, 200 allows the optimizer to spend up to 2x the minimal cost to
                      meet performance targets before refusing to scale further.
                    format: int32
                    minimum: 0
                    type: integer
                  params:
                    description: Params defines the configuration parameters for multiple
                      ModelServing groups to be optimized.
                    items:
                      description: "HeterogeneousTargetParam defines the configuration
                        parameters for a specific deployment type in heterogeneous
                        scaling.\n\nExample (one expensive H100 group within a HeterogeneousTarget):\n\n\tcost:
                        100\n\tminReplicas: 0\n\tmaxReplicas: 4\n\ttarget:\n\t  targetRef:\n\t
                        \   kind: ModelServing\n\t    name: llama-h100"
                      properties:
                        cost:
                          description: |-
                            Cost defines the relative cost factor used in optimization calculations.
                            This factor balances performance requirements against deployment costs.
                            Values are relative across params, e.g. 100 for an H100 group and 60 for a
                            cheaper A100 group makes the optimizer prefer A100 replicas when adequate.
                          format: int32
                          minimum: 0
                          type: integer
                        maxReplicas:
                          description: MaxReplicas defines the maximum number of replicas
                            allowed for this deployment type.
                          format: int32
                          maximum: 1000000
                          minimum: 1
                          type: integer
                        minReplicas:
                          description: MinReplicas defines the minimum number of replicas
                            to maintain for this deployment type.
                          format: int32
                          maximum: 1000000
                          minimum: 0
                          type: integer
                        target:
                          description: Target defines the scaling instance configuration
                            for this deployment type.
                          properties:
                            metricSources:
                              additionalProperties:
                                description: "MetricSource is a discriminated union
                                  selecting the metric backend.\n\nExactly one backend
                                  config must be provided:\n  - Pod        -> set
                                  the pod field only.\n  - Prometheus -> set the prometheus
                                  field only.\n\nExample (scrape the metric directly
                                  from each pod's /metrics endpoint):\n\n\tmetricSources:\n\t
                                  \ gpu_cache_usage:\n\t    pod:\n\t      name: vllm:gpu_cache_usage_perc\n\t
                                  \     uri: /metrics\n\t      port: 8000\n\nExample
                                  (read the metric from an external Prometheus server):\n\n\tmetricSources:\n\t
                                  \ http_rps:\n\t    prometheus:\n\t      serverURL:
                                  http://prometheus.monitoring.svc:9090\n\t      query:
                                  sum(rate(http_requests_total[2m]))"
                                properties:
                                  pod:
                                    description: Pod configures direct pod endpoint
                                      scraping.
                                    properties:
                                      labelSelector:
                                        description: |-
                                          LabelSelector defines additional filtering for pods exposing this metric.
                                          Only pods matching both the target and this selector are scraped, e.g.
                                          matchLabels with role=decode to scrape only the decode role's pods.
                                        properties:
                                          matchExpressions:
                                            description: matchExpressions is a list
                                              of label selector requirements. The
                                              requirements are ANDed.
                                            items:
                                              description: |-
                                                A label selector requirement is a selector that contains values, a key, and an operator that
                                                relates the key and values.
                                              properties:
                                                key:
                                                  description: key is the label key
                                                    that the selector applies to.
                                                  type: string
                                                operator:
                                                  description: |-
                                                    operator represents a key's relationship to a set of values.
                                                    Valid operators are In, NotIn, Exists and DoesNotExist.
                                                  type: string
                                                values:
                                                  description: |-
                                                    values is an array of string values. If the operator is In or NotIn,
                                                    the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                    the values array must be empty. This array is replaced during a strategic
                                                    merge patch.
                                                  items:
                                                    type: string
                                                  type: array
                                                  x-kubernetes-list-type: atomic
                                              required:
                                              - key
                                              - operator
                                              type: object
                                            type: array
                                            x-kubernetes-list-type: atomic
                                          matchLabels:
                                            additionalProperties:
                                              type: string
                                            description: |-
                                              matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                              map is equivalent to an element of matchExpressions, whose key field is "key", the
                                              operator is "In", and the values array contains only "value". The requirements are ANDed.
                                            type: object
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      name:
                                        description: |-
                                          Name is the Prometheus metric name matched against labels in the pod's scraped output.
                                          Defaults to the policy metric key when omitted.
                                          For example, set it to "vllm:gpu_cache_usage_perc" to read that exact series.
                                        type: string
                                      port:
                                        default: 8100
                                        description: Port defines the network port
                                          where metrics are exposed by the pods (e.g.,
                                          8000).
                                        format: int32
                                        type: integer
                                      uri:
                                        default: /metrics
                                        description: Uri defines the HTTP path where
                                          metrics are exposed (e.g., "/metrics").
                                        type: string
                                    type: object
                                  prometheus:
                                    description: Prometheus configures an external
                                      Prometheus server as the metric source.
                                    properties:
                                      auth:
                                        description: Auth holds optional authentication
                                          configuration for the Prometheus server.
                                        type: object
                                      query:
                                        description: |-
                                          Query is a PromQL instant-query expression. It must evaluate to a single
                                          scalar or a one-element vector, e.g. "avg(rate(vllm:request_latency[1m]))".
                                          More Query details refer to https://prometheus.io/docs/prometheus/latest/querying/basics
                                        minLength: 1
                                        type: string
                                      serverURL:
                                        description: |-
                                          ServerURL is the base URL of the Prometheus HTTP API server.
                                          Example: "http://prometheus.monitoring.svc:9090".
                                        format: uri
                                        minLength: 1
                                        type: string
                                    required:
                                    - query
                                    - serverURL
                                    type: object
                                type: object
                                x-kubernetes-validations:
                                - message: one metric source backend config is required
                                  rule: has(self.pod) || has(self.prometheus)
                                - message: pod and prometheus configs are mutually
                                    exclusive
                                  rule: '!(has(self.pod) && has(self.prometheus))'
                              description: |-
                                MetricSources declares how to fetch specific metrics for this target.
                                Keys must match AutoscalingPolicy.spec.metrics[].name.
                                Missing keys are treated as missing metrics for that reconcile loop.
                                For example, a key "podinfo_rps" here must correspond to a metric named
                                "podinfo_rps" in the referenced AutoscalingPolicy.
                              type: object
                            targetRef:
                              description: |-
                                TargetRef references the target object to be monitored and scaled.
                                Default target GVK is ModelServing. Currently supported kinds: ModelServing.
                                Example: kind=ModelServing, name=podinfo-ms.
                              properties:
                                apiVersion:
                                  description: API version of the referent.
                                  type: string
                                fieldPath:
                                  description: |-
                                    If referring to a piece of an object instead of an entire object, this string
                                    should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                                    For example, if the object reference is to a container within a pod, this would take on a value like:
                                    "spec.containers{name}" (where "name" refers to the name of the container that triggered
                                    the event) or if no container name is specified "spec.containers[2]" (container with
                                    index 2 in this pod). This syntax is chosen only to have some well-defined way of
                                    referencing a part of an object.
                                  type: string
                                kind:
                                  description: |-
                                    Kind of the referent.
                                    More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                                  type: string
                                name:
                                  description: |-
                                    Name of the referent.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                namespace:
                                  description: |-
                                    Namespace of the referent.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                                  type: string
                                resourceVersion:
                                  description: |-
                                    Specific resourceVersion to which this reference is made, if any.
                                    More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                                  type: string
                                uid:
                                  description: |-
                                    UID of the referent.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                                  type: string
                              type: object
                              x-kubernetes-map-type: atomic
                          required:
                          - targetRef
                          type: object
                      required:
                      - maxReplicas
                      - minReplicas
                      type: object
                    minItems: 1
                    type: array
                type: object
              homogeneousTarget:
                description: |-
                  HomogeneousTarget enables traditional metric-based scaling for a single
                  ModelServing deployment (whole-deployment granularity).
                properties:
                  maxReplicas:
                    description: MaxReplicas defines the maximum number of replicas
                      allowed (e.g., 6).
                    format: int32
                    maximum: 1000000
                    minimum: 1
                    type: integer
                  minReplicas:
                    description: MinReplicas defines the minimum number of replicas
                      to maintain (e.g., 1).
                    format: int32
                    maximum: 1000000
                    minimum: 0
                    type: integer
                  target:
                    description: Target defines the object to be monitored and scaled.
                    properties:
                      metricSources:
                        additionalProperties:
                          description: "MetricSource is a discriminated union selecting
                            the metric backend.\n\nExactly one backend config must
                            be provided:\n  - Pod        -> set the pod field only.\n
                            \ - Prometheus -> set the prometheus field only.\n\nExample
                            (scrape the metric directly from each pod's /metrics endpoint):\n\n\tmetricSources:\n\t
                            \ gpu_cache_usage:\n\t    pod:\n\t      name: vllm:gpu_cache_usage_perc\n\t
                            \     uri: /metrics\n\t      port: 8000\n\nExample (read
                            the metric from an external Prometheus server):\n\n\tmetricSources:\n\t
                            \ http_rps:\n\t    prometheus:\n\t      serverURL: http://prometheus.monitoring.svc:9090\n\t
                            \     query: sum(rate(http_requests_total[2m]))"
                          properties:
                            pod:
                              description: Pod configures direct pod endpoint scraping.
                              properties:
                                labelSelector:
                                  description: |-
                                    LabelSelector defines additional filtering for pods exposing this metric.
                                    Only pods matching both the target and this selector are scraped, e.g.
                                    matchLabels with role=decode to scrape only the decode role's pods.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                name:
                                  description: |-
                                    Name is the Prometheus metric name matched against labels in the pod's scraped output.
                                    Defaults to the policy metric key when omitted.
                                    For example, set it to "vllm:gpu_cache_usage_perc" to read that exact series.
                                  type: string
                                port:
                                  default: 8100
                                  description: Port defines the network port where
                                    metrics are exposed by the pods (e.g., 8000).
                                  format: int32
                                  type: integer
                                uri:
                                  default: /metrics
                                  description: Uri defines the HTTP path where metrics
                                    are exposed (e.g., "/metrics").
                                  type: string
                              type: object
                            prometheus:
                              description: Prometheus configures an external Prometheus
                                server as the metric source.
                              properties:
                                auth:
                                  description: Auth holds optional authentication
                                    configuration for the Prometheus server.
                                  type: object
                                query:
                                  description: |-
                                    Query is a PromQL instant-query expression. It must evaluate to a single
                                    scalar or a one-element vector, e.g. "avg(rate(vllm:request_latency[1m]))".
                                    More Query details refer to https://prometheus.io/docs/prometheus/latest/querying/basics
                                  minLength: 1
                                  type: string
                                serverURL:
                                  description: |-
                                    ServerURL is the base URL of the Prometheus HTTP API server.
                                    Example: "http://prometheus.monitoring.svc:9090".
                                  format: uri
                                  minLength: 1
                                  type: string
                              required:
                              - query
                              - serverURL
                              type: object
                          type: object
                          x-kubernetes-validations:
                          - message: one metric source backend config is required
                            rule: has(self.pod) || has(self.prometheus)
                          - message: pod and prometheus configs are mutually exclusive
                            rule: '!(has(self.pod) && has(self.prometheus))'
                        description: |-
                          MetricSources declares how to fetch specific metrics for this target.
                          Keys must match AutoscalingPolicy.spec.metrics[].name.
                          Missing keys are treated as missing metrics for that reconcile loop.
                          For example, a key "podinfo_rps" here must correspond to a metric named
                          "podinfo_rps" in the referenced AutoscalingPolicy.
                        type: object
                      targetRef:
                        description: |-
                          TargetRef references the target object to be monitored and scaled.
                          Default target GVK is ModelServing. Currently supported kinds: ModelServing.
                          Example: kind=ModelServing, name=podinfo-ms.
                        properties:
                          apiVersion:
                            description: API version of the referent.
                            type: string
                          fieldPath:
                            description: |-
                              If referring to a piece of an object instead of an entire object, this string
                              should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                              For example, if the object reference is to a container within a pod, this would take on a value like:
                              "spec.containers{name}" (where "name" refers to the name of the container that triggered
                              the event) or if no container name is specified "spec.containers[2]" (container with
                              index 2 in this pod). This syntax is chosen only to have some well-defined way of
                              referencing a part of an object.
                            type: string
                          kind:
                            description: |-
                              Kind of the referent.
                              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                            type: string
                          name:
                            description: |-
                              Name of the referent.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          namespace:
                            description: |-
                              Namespace of the referent.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                            type: string
                          resourceVersion:
                            description: |-
                              Specific resourceVersion to which this reference is made, if any.
                              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                            type: string
                          uid:
                            description: |-
                              UID of the referent.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                    required:
                    - targetRef
                    type: object
                required:
                - maxReplicas
                - minReplicas
                type: object
              metrics:
                description: |-
                  Metrics defines the list of metrics used to evaluate scaling decisions.
                  This is the default metric list applied to scalable units. For
                  DisaggregatedTarget, role-level metrics override this list for that role.
                items:
                  description: AutoscalingPolicyMetric defines a metric and its target
                    value for scaling decisions.
                  properties:
                    name:
                      description: Name defines the metric key used by the scaling
                        algorithm.
                      minLength: 1
                      type: string
                    targetValue:
                      anyOf:
                      - type: integer
                      - type: string
                      description: TargetValue defines the target value for the metric
                        that triggers scaling operations.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                  required:
                  - name
                  - targetValue
                  type: object
                type: array
              tolerancePercent:
                default: 10
                description: |-
                  TolerancePercent defines the percentage of deviation tolerated before scaling actions are triggered.
                  current_replicas represents the current number of instances, while target_replicas represents the expected number of instances calculated from monitoring metrics.
                  Scaling operations are performed only when |current_replicas - target_replicas| >= current_replicas * TolerancePercent / 100.
                format: int32
                maximum: 100
                minimum: 0
                type: integer
            type: object
          status:
            description: AutoscalingPolicyStatus defines the observed state of AutoscalingPolicy.
            properties:
              conditions:
                description: |-
                  Conditions represents the latest available observations of the policy's state.
                  Well-known condition types include:
                    - "Ready":                   the policy is actively reconciled.
                    - "TargetFound":             the referenced ModelServing (and roles) exist.
                    - "RatioConstraintViolated": the desired counts could not satisfy ratioConstraint
                                                 given the per-role min/max bounds.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              disaggregatedStatus:
                description: DisaggregatedStatus reports the observed state when DisaggregatedTarget
                  is used.
                properties:
                  ratioAdjusted:
                    description: |-
                      RatioAdjusted is true when the most recent reconcile had to override the
                      metric-derived replica counts to satisfy the ratio constraint.
                    type: boolean
                  ratioStatus:
                    description: RatioStatus reports the observed value of the configured
                      ratio constraint.
                    properties:
                      currentRatio:
                        type: string
                      denominatorRole:
                        type: string
                      numeratorRole:
                        type: string
                    required:
                    - denominatorRole
                    - numeratorRole
                    type: object
                  roles:
                    description: Roles reports the observed scaling state per role.
                    items:
                      description: |-
                        TargetScalingStatus reports the observed scaling state of a single scalable
                        unit (a whole ModelServing, or one role within it).
                      properties:
                        currentReplicas:
                          description: CurrentReplicas is the number of replicas currently
                            observed.
                          format: int32
                          type: integer
                        desiredReplicas:
                          description: |-
                            DesiredReplicas is the number of replicas the controller computed from
                            metrics, before ratio enforcement.
                          format: int32
                          type: integer
                        lastScaleTime:
                          description: LastScaleTime is the last time the unit was
                            scaled by the controller.
                          format: date-time
                          type: string
                        mode:
                          description: Mode reports whether the unit is currently
                            in "Stable" or "Panic" mode.
                          type: string
                        name:
                          description: |-
                            Name identifies the unit when this status appears in a list.
                            It is required for HeterogeneousStatus entries and DisaggregatedStatus roles,
                            and may be empty for HomogeneousStatus because the target is implied.
                          type: string
                      required:
                      - currentReplicas
                      - desiredReplicas
                      type: object
                    type: array
                required:
                - roles
                type: object
              heterogeneousStatus:
                description: |-
                  HeterogeneousStatus reports the per-target observed state when
                  HeterogeneousTarget is used.
                items:
                  description: |-
                    TargetScalingStatus reports the observed scaling state of a single scalable
                    unit (a whole ModelServing, or one role within it).
                  properties:
                    currentReplicas:
                      description: CurrentReplicas is the number of replicas currently
                        observed.
                      format: int32
                      type: integer
                    desiredReplicas:
                      description: |-
                        DesiredReplicas is the number of replicas the controller computed from
                        metrics, before ratio enforcement.
                      format: int32
                      type: integer
                    lastScaleTime:
                      description: LastScaleTime is the last time the unit was scaled
                        by the controller.
                      format: date-time
                      type: string
                    mode:
                      description: Mode reports whether the unit is currently in "Stable"
                        or "Panic" mode.
                      type: string
                    name:
                      description: |-
                        Name identifies the unit when this status appears in a list.
                        It is required for HeterogeneousStatus entries and DisaggregatedStatus roles,
                        and may be empty for HomogeneousStatus because the target is implied.
                      type: string
                  required:
                  - currentReplicas
                  - desiredReplicas
                  type: object
                type: array
              homogeneousStatus:
                description: HomogeneousStatus reports the observed state when HomogeneousTarget
                  is used.
                properties:
                  currentReplicas:
                    description: CurrentReplicas is the number of replicas currently
                      observed.
                    format: int32
                    type: integer
                  desiredReplicas:
                    description: |-
                      DesiredReplicas is the number of replicas the controller computed from
                      metrics, before ratio enforcement.
                    format: int32
                    type: integer
                  lastScaleTime:
                    description: LastScaleTime is the last time the unit was scaled
                      by the controller.
                    format: date-time
                    type: string
                  mode:
                    description: Mode reports whether the unit is currently in "Stable"
                      or "Panic" mode.
                    type: string
                  name:
                    description: |-
                      Name identifies the unit when this status appears in a list.
                      It is required for HeterogeneousStatus entries and DisaggregatedStatus roles,
                      and may be empty for HomogeneousStatus because the target is implied.
                    type: string
                required:
                - currentReplicas
                - desiredReplicas
                type: object
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

