---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: autoscalingpolicybindings.workload.serving.volcano.sh
spec:
  group: workload.serving.volcano.sh
  names:
    kind: AutoscalingPolicyBinding
    listKind: AutoscalingPolicyBindingList
    plural: autoscalingpolicybindings
    singular: autoscalingpolicybinding
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AutoscalingPolicyBinding binds AutoscalingPolicy rules to specific ModelServing deployments.
          It enables either traditional metric-based scaling or multi-target optimization across heterogeneous hardware deployments.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AutoscalingPolicyBindingSpec defines the desired state of
              AutoscalingPolicyBinding.
            properties:
              heterogeneousTarget:
                description: |-
                  HeterogeneousTarget enables optimization-based scaling across multiple ModelServing deployments with different hardware capabilities.
                  This approach dynamically adjusts replica distribution across heterogeneous resources (e.g., H100/A100 GPUs) based on overall computing requirements.
                properties:
                  costExpansionRatePercent:
                    default: 200
                    description: CostExpansionRatePercent defines the percentage rate
                      at which the cost expands during optimization calculations.
                    format: int32
                    minimum: 0
                    type: integer
                  params:
                    description: Params defines the configuration parameters for multiple
                      ModelServing groups to be optimized.
                    items:
                      description: HeterogeneousTargetParam defines the configuration
                        parameters for a specific deployment type in heterogeneous
                        scaling.
                      properties:
                        cost:
                          description: |-
                            Cost defines the relative cost factor used in optimization calculations.
                            This factor balances performance requirements against deployment costs.
                          format: int32
                          minimum: 0
                          type: integer
                        maxReplicas:
                          description: MaxReplicas defines the maximum number of replicas
                            allowed for this deployment type.
                          format: int32
                          maximum: 1000000
                          minimum: 1
                          type: integer
                        minReplicas:
                          description: MinReplicas defines the minimum number of replicas
                            to maintain for this deployment type.
                          format: int32
                          maximum: 1000000
                          minimum: 0
                          type: integer
                        target:
                          description: Target defines the scaling instance configuration
                            for this deployment type.
                          properties:
                            metricEndpoint:
                              description: MetricEndpoint defines the configuration
                                for scraping metrics from the target pods.
                              properties:
                                labelSelector:
                                  description: |-
                                    LabelSelector defines additional label-based filtering for pods that expose metric endpoints.
                                    For example: Ray Leader Pods expose metrics but worker pods don't, so use `ray.io/ray-node-type: 'raylet'`.
                                    When targetRef kind is `ModelServing` or `ModelServing/Role`, `modelserving.volcano.sh/entry: 'true'` is added by default.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                port:
                                  default: 8100
                                  description: Port defines the network port where
                                    metrics are exposed by the pods.
                                  format: int32
                                  type: integer
                                uri:
                                  default: /metrics
                                  description: Uri defines the HTTP path where metrics
                                    are exposed (e.g., "/metrics").
                                  type: string
                              type: object
                            subTargets:
                              description: |-
                                SubTarget defines the sub-target object to be monitored and scaled.
                                Currently supported kinds: `Role` when TargetRef kind is ModelServing.
                              properties:
                                kind:
                                  type: string
                                name:
                                  type: string
                              type: object
                            targetRef:
                              description: |-
                                TargetRef references the target object to be monitored and scaled.
                                Default target GVK is ModelServing. Currently supported kinds: ModelServing.
                              properties:
                                apiVersion:
                                  description: API version of the referent.
                                  type: string
                                fieldPath:
                                  description: |-
                                    If referring to a piece of an object instead of an entire object, this string
                                    should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                                    For example, if the object reference is to a container within a pod, this would take on a value like:
                                    "spec.containers{name}" (where "name" refers to the name of the container that triggered
                                    the event) or if no container name is specified "spec.containers[2]" (container with
                                    index 2 in this pod). This syntax is chosen only to have some well-defined way of
                                    referencing a part of an object.
                                  type: string
                                kind:
                                  description: |-
                                    Kind of the referent.
                                    More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                                  type: string
                                name:
                                  description: |-
                                    Name of the referent.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                namespace:
                                  description: |-
                                    Namespace of the referent.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                                  type: string
                                resourceVersion:
                                  description: |-
                                    Specific resourceVersion to which this reference is made, if any.
                                    More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                                  type: string
                                uid:
                                  description: |-
                                    UID of the referent.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                                  type: string
                              type: object
                              x-kubernetes-map-type: atomic
                          required:
                          - targetRef
                          type: object
                      required:
                      - maxReplicas
                      - minReplicas
                      type: object
                    minItems: 1
                    type: array
                type: object
              homogeneousTarget:
                description: |-
                  HomogeneousTarget enables traditional metric-based scaling for a single ModelServing deployment.
                  This approach adjusts replica count based on monitoring metrics and their target values.
                properties:
                  maxReplicas:
                    description: MaxReplicas defines the maximum number of replicas
                      allowed.
                    format: int32
                    maximum: 1000000
                    minimum: 1
                    type: integer
                  minReplicas:
                    description: MinReplicas defines the minimum number of replicas
                      to maintain.
                    format: int32
                    maximum: 1000000
                    minimum: 0
                    type: integer
                  target:
                    description: Target defines the object to be monitored and scaled.
                    properties:
                      metricEndpoint:
                        description: MetricEndpoint defines the configuration for
                          scraping metrics from the target pods.
                        properties:
                          labelSelector:
                            description: |-
                              LabelSelector defines additional label-based filtering for pods that expose metric endpoints.
                              For example: Ray Leader Pods expose metrics but worker pods don't, so use `ray.io/ray-node-type: 'raylet'`.
                              When targetRef kind is `ModelServing` or `ModelServing/Role`, `modelserving.volcano.sh/entry: 'true'` is added by default.
                            properties:
                              matchExpressions:
                                description: matchExpressions is a list of label selector
                                  requirements. The requirements are ANDed.
                                items:
                                  description: |-
                                    A label selector requirement is a selector that contains values, a key, and an operator that
                                    relates the key and values.
                                  properties:
                                    key:
                                      description: key is the label key that the selector
                                        applies to.
                                      type: string
                                    operator:
                                      description: |-
                                        operator represents a key's relationship to a set of values.
                                        Valid operators are In, NotIn, Exists and DoesNotExist.
                                      type: string
                                    values:
                                      description: |-
                                        values is an array of string values. If the operator is In or NotIn,
                                        the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                        the values array must be empty. This array is replaced during a strategic
                                        merge patch.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  required:
                                  - key
                                  - operator
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              matchLabels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                  map is equivalent to an element of matchExpressions, whose key field is "key", the
                                  operator is "In", and the values array contains only "value". The requirements are ANDed.
                                type: object
                            type: object
                            x-kubernetes-map-type: atomic
                          port:
                            default: 8100
                            description: Port defines the network port where metrics
                              are exposed by the pods.
                            format: int32
                            type: integer
                          uri:
                            default: /metrics
                            description: Uri defines the HTTP path where metrics are
                              exposed (e.g., "/metrics").
                            type: string
                        type: object
                      subTargets:
                        description: |-
                          SubTarget defines the sub-target object to be monitored and scaled.
                          Currently supported kinds: `Role` when TargetRef kind is ModelServing.
                        properties:
                          kind:
                            type: string
                          name:
                            type: string
                        type: object
                      targetRef:
                        description: |-
                          TargetRef references the target object to be monitored and scaled.
                          Default target GVK is ModelServing. Currently supported kinds: ModelServing.
                        properties:
                          apiVersion:
                            description: API version of the referent.
                            type: string
                          fieldPath:
                            description: |-
                              If referring to a piece of an object instead of an entire object, this string
                              should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                              For example, if the object reference is to a container within a pod, this would take on a value like:
                              "spec.containers{name}" (where "name" refers to the name of the container that triggered
                              the event) or if no container name is specified "spec.containers[2]" (container with
                              index 2 in this pod). This syntax is chosen only to have some well-defined way of
                              referencing a part of an object.
                            type: string
                          kind:
                            description: |-
                              Kind of the referent.
                              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                            type: string
                          name:
                            description: |-
                              Name of the referent.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          namespace:
                            description: |-
                              Namespace of the referent.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                            type: string
                          resourceVersion:
                            description: |-
                              Specific resourceVersion to which this reference is made, if any.
                              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                            type: string
                          uid:
                            description: |-
                              UID of the referent.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                    required:
                    - targetRef
                    type: object
                required:
                - maxReplicas
                - minReplicas
                type: object
              policyRef:
                description: PolicyRef references the AutoscalingPolicy that defines
                  the scaling rules and metrics.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - policyRef
            type: object
            x-kubernetes-validations:
            - message: Either heterogeneousTarget or homogeneousTarget must be set,
                but not both.
              rule: has(self.heterogeneousTarget) != has(self.homogeneousTarget)
          status:
            description: AutoscalingPolicyBindingStatus defines the observed state
              of AutoscalingPolicyBinding.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

