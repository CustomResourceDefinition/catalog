---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: enrichments.coralogix.com
spec:
  group: coralogix.com
  names:
    kind: Enrichment
    listKind: EnrichmentList
    plural: enrichments
    singular: enrichment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.printableStatus
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Enrichment is the Schema for the enrichments API.
          Will overwrite the existing enrichments on the Coralogix side,
          so it should contain all enrichments that should be applied, not just the new ones.
          See also https://coralogix.com/docs/user-guides/data-transformation/enrichments/custom-enrichment/#configuration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EnrichmentSpec defines the desired state of Enrichment.
            properties:
              enrichments:
                description: |-
                  List of enrichments to apply. Each enrichment must have exactly one of GeoIp, SuspiciousIp, Aws, or Custom set.
                  Will overwrite the existing enrichments on the Coralogix side,
                  so it should contain all enrichments that should be applied, not just the new ones.
                items:
                  description: EnrichmentType must have exactly one of GeoIp, SuspiciousIp,
                    Aws, or Custom set.
                  properties:
                    aws:
                      description: |-
                        Coralogix allows you to enrich your logs with the data from a chosen AWS resource.
                        The feature enriches every log that contains a particular resourceId,
                        associated with the metadata of a chosen AWS resource.
                      properties:
                        enrichedFieldName:
                          type: string
                        fieldName:
                          type: string
                        resourceType:
                          type: string
                        selectedColumns:
                          items:
                            type: string
                          type: array
                      required:
                      - fieldName
                      - resourceType
                      type: object
                    custom:
                      description: Custom Log Enrichment with Coralogix enables you
                        to easily enrich your log data.
                      properties:
                        customEnrichmentRef:
                          properties:
                            backendRef:
                              description: BackendRef is a reference to a CustomEnrichment
                                in the backend.
                              properties:
                                id:
                                  description: ID of the CustomEnrichment in the backend.
                                  format: int32
                                  type: integer
                              required:
                              - id
                              type: object
                            resourceRef:
                              description: ResourceRef is a reference to a CustomEnrichment
                                resource in the cluster.
                              properties:
                                name:
                                  description: Name of the resource (not id).
                                  type: string
                                namespace:
                                  description: Kubernetes namespace.
                                  type: string
                              required:
                              - name
                              type: object
                          type: object
                          x-kubernetes-validations:
                          - message: Exactly one of backendRef or resourceRef must
                              be set
                            rule: has(self.backendRef) != has(self.resourceRef)
                        enrichedFieldName:
                          type: string
                        fieldName:
                          type: string
                        selectedColumns:
                          items:
                            type: string
                          type: array
                      required:
                      - customEnrichmentRef
                      - fieldName
                      type: object
                    geoIp:
                      description: Set of fields to enrich with geo_ip information.
                      properties:
                        enrichedFieldName:
                          type: string
                        fieldName:
                          type: string
                        selectedColumns:
                          items:
                            type: string
                          type: array
                        withAsn:
                          type: boolean
                      required:
                      - fieldName
                      type: object
                    suspiciousIp:
                      description: |-
                        Coralogix allows you to automatically discover threats on your web servers
                        by enriching your logs with the most updated IP blacklists.
                      properties:
                        enrichedFieldName:
                          type: string
                        fieldName:
                          type: string
                        selectedColumns:
                          items:
                            type: string
                          type: array
                      required:
                      - fieldName
                      type: object
                  type: object
                  x-kubernetes-validations:
                  - message: Exactly one of geoIp, suspiciousIp, aws, or custom must
                      be set
                    rule: '(has(self.geoIp) ? 1 : 0) + (has(self.suspiciousIp) ? 1
                      : 0) + (has(self.aws) ? 1 : 0) + (has(self.custom) ? 1 : 0)
                      == 1'
                maxItems: 100
                minItems: 1
                type: array
            required:
            - enrichments
            type: object
          status:
            description: EnrichmentStatus defines the observed state of Enrichment.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              id:
                type: string
              printableStatus:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

