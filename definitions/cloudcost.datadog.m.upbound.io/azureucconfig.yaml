---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: azureucconfigs.cloudcost.datadog.m.upbound.io
spec:
  group: cloudcost.datadog.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - datadog
    kind: AzureUCConfig
    listKind: AzureUCConfigList
    plural: azureucconfigs
    singular: azureucconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AzureUCConfig is the Schema for the AzureUCConfigs API. Provides
          a Datadog Azure Usage Cost configuration resource. This can be used to create
          and manage Azure Cost Export configurations for Cloud Cost Management. Azure
          configurations require both actual and amortized cost export settings.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AzureUCConfigSpec defines the desired state of AzureUCConfig
            properties:
              forProvider:
                properties:
                  accountId:
                    description: |-
                      (String) The tenant ID of the Azure account.
                      The tenant ID of the Azure account.
                    type: string
                  actualBillConfig:
                    description: |-
                      (Block, Optional) Configuration for the actual cost export. (see below for nested schema)
                      Configuration for the actual cost export.
                    properties:
                      exportName:
                        description: |-
                          (String) The name of the configured Azure Export.
                          The name of the configured Azure Export.
                        type: string
                      exportPath:
                        description: |-
                          (String) The path where the Azure Export is saved.
                          The path where the Azure Export is saved.
                        type: string
                      storageAccount:
                        description: |-
                          (String) The name of the storage account where the Azure Export is saved.
                          The name of the storage account where the Azure Export is saved.
                        type: string
                      storageContainer:
                        description: |-
                          (String) The name of the storage container where the Azure Export is saved.
                          The name of the storage container where the Azure Export is saved.
                        type: string
                    type: object
                  amortizedBillConfig:
                    description: |-
                      (Block, Optional) Configuration for the amortized cost export. (see below for nested schema)
                      Configuration for the amortized cost export.
                    properties:
                      exportName:
                        description: |-
                          (String) The name of the configured Azure Export.
                          The name of the configured Azure Export.
                        type: string
                      exportPath:
                        description: |-
                          (String) The path where the Azure Export is saved.
                          The path where the Azure Export is saved.
                        type: string
                      storageAccount:
                        description: |-
                          (String) The name of the storage account where the Azure Export is saved.
                          The name of the storage account where the Azure Export is saved.
                        type: string
                      storageContainer:
                        description: |-
                          (String) The name of the storage container where the Azure Export is saved.
                          The name of the storage container where the Azure Export is saved.
                        type: string
                    type: object
                  clientId:
                    description: |-
                      (String) The client ID of the Azure account.
                      The client ID of the Azure account.
                    type: string
                  scope:
                    description: |-
                      (String) The scope of your observed subscription.
                      The scope of your observed subscription.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accountId:
                    description: |-
                      (String) The tenant ID of the Azure account.
                      The tenant ID of the Azure account.
                    type: string
                  actualBillConfig:
                    description: |-
                      (Block, Optional) Configuration for the actual cost export. (see below for nested schema)
                      Configuration for the actual cost export.
                    properties:
                      exportName:
                        description: |-
                          (String) The name of the configured Azure Export.
                          The name of the configured Azure Export.
                        type: string
                      exportPath:
                        description: |-
                          (String) The path where the Azure Export is saved.
                          The path where the Azure Export is saved.
                        type: string
                      storageAccount:
                        description: |-
                          (String) The name of the storage account where the Azure Export is saved.
                          The name of the storage account where the Azure Export is saved.
                        type: string
                      storageContainer:
                        description: |-
                          (String) The name of the storage container where the Azure Export is saved.
                          The name of the storage container where the Azure Export is saved.
                        type: string
                    type: object
                  amortizedBillConfig:
                    description: |-
                      (Block, Optional) Configuration for the amortized cost export. (see below for nested schema)
                      Configuration for the amortized cost export.
                    properties:
                      exportName:
                        description: |-
                          (String) The name of the configured Azure Export.
                          The name of the configured Azure Export.
                        type: string
                      exportPath:
                        description: |-
                          (String) The path where the Azure Export is saved.
                          The path where the Azure Export is saved.
                        type: string
                      storageAccount:
                        description: |-
                          (String) The name of the storage account where the Azure Export is saved.
                          The name of the storage account where the Azure Export is saved.
                        type: string
                      storageContainer:
                        description: |-
                          (String) The name of the storage container where the Azure Export is saved.
                          The name of the storage container where the Azure Export is saved.
                        type: string
                    type: object
                  clientId:
                    description: |-
                      (String) The client ID of the Azure account.
                      The client ID of the Azure account.
                    type: string
                  scope:
                    description: |-
                      (String) The scope of your observed subscription.
                      The scope of your observed subscription.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.accountId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.accountId)
                || (has(self.initProvider) && has(self.initProvider.accountId))'
            - message: spec.forProvider.clientId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.clientId)
                || (has(self.initProvider) && has(self.initProvider.clientId))'
            - message: spec.forProvider.scope is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.scope)
                || (has(self.initProvider) && has(self.initProvider.scope))'
          status:
            description: AzureUCConfigStatus defines the observed state of AzureUCConfig.
            properties:
              atProvider:
                properties:
                  accountId:
                    description: |-
                      (String) The tenant ID of the Azure account.
                      The tenant ID of the Azure account.
                    type: string
                  actualBillConfig:
                    description: |-
                      (Block, Optional) Configuration for the actual cost export. (see below for nested schema)
                      Configuration for the actual cost export.
                    properties:
                      exportName:
                        description: |-
                          (String) The name of the configured Azure Export.
                          The name of the configured Azure Export.
                        type: string
                      exportPath:
                        description: |-
                          (String) The path where the Azure Export is saved.
                          The path where the Azure Export is saved.
                        type: string
                      storageAccount:
                        description: |-
                          (String) The name of the storage account where the Azure Export is saved.
                          The name of the storage account where the Azure Export is saved.
                        type: string
                      storageContainer:
                        description: |-
                          (String) The name of the storage container where the Azure Export is saved.
                          The name of the storage container where the Azure Export is saved.
                        type: string
                    type: object
                  amortizedBillConfig:
                    description: |-
                      (Block, Optional) Configuration for the amortized cost export. (see below for nested schema)
                      Configuration for the amortized cost export.
                    properties:
                      exportName:
                        description: |-
                          (String) The name of the configured Azure Export.
                          The name of the configured Azure Export.
                        type: string
                      exportPath:
                        description: |-
                          (String) The path where the Azure Export is saved.
                          The path where the Azure Export is saved.
                        type: string
                      storageAccount:
                        description: |-
                          (String) The name of the storage account where the Azure Export is saved.
                          The name of the storage account where the Azure Export is saved.
                        type: string
                      storageContainer:
                        description: |-
                          (String) The name of the storage container where the Azure Export is saved.
                          The name of the storage container where the Azure Export is saved.
                        type: string
                    type: object
                  clientId:
                    description: |-
                      (String) The client ID of the Azure account.
                      The client ID of the Azure account.
                    type: string
                  createdAt:
                    description: |-
                      (String) The timestamp when the Azure Usage Cost configuration was created.
                      The timestamp when the Azure Usage Cost configuration was created.
                    type: string
                  errorMessages:
                    description: |-
                      (List of String) List of error messages if the Azure Usage Cost configuration encountered any issues during setup or data processing.
                      List of error messages if the Azure Usage Cost configuration encountered any issues during setup or data processing.
                    items:
                      type: string
                    type: array
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  scope:
                    description: |-
                      (String) The scope of your observed subscription.
                      The scope of your observed subscription.
                    type: string
                  status:
                    description: |-
                      (String) The current status of the Azure Usage Cost configuration.
                      The current status of the Azure Usage Cost configuration.
                    type: string
                  statusUpdatedAt:
                    description: |-
                      (String) The timestamp when the configuration status was last updated.
                      The timestamp when the configuration status was last updated.
                    type: string
                  updatedAt:
                    description: |-
                      (String) The timestamp when the Azure Usage Cost configuration was last modified.
                      The timestamp when the Azure Usage Cost configuration was last modified.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

