---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: allocationrules.cloudcost.datadog.m.upbound.io
spec:
  group: cloudcost.datadog.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - datadog
    kind: AllocationRule
    listKind: AllocationRuleList
    plural: allocationrules
    singular: allocationrule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AllocationRule is the Schema for the AllocationRules API. Provides
          a Datadog Custom Allocation Rule Order API resource. This can be used to
          manage the order of Datadog Custom Allocation Rules.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AllocationRuleSpec defines the desired state of AllocationRule
            properties:
              forProvider:
                properties:
                  costsToAllocate:
                    items:
                      properties:
                        condition:
                          description: The condition to match. Valid values are `=`,
                            `!=`, `is`, `is not`, `like`, `in`, `not in`.
                          type: string
                        tag:
                          description: The tag key to filter on (e.g., `aws_product`,
                            `team`, `environment`).
                          type: string
                        value:
                          description: The single tag value to match. Use this field
                            for conditions like `=`, `!=`, `is`, `is not`, `like`.
                            Do not use with `in` or `not in` conditions.
                          type: string
                        values:
                          description: A list of tag values to match. Use this field
                            for `in` or `not in` conditions only. Do not use with
                            single-value conditions.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  enabled:
                    description: Whether the custom allocation rule is enabled.
                    type: boolean
                  providernames:
                    description: List of cloud providers the rule applies to. Valid
                      values include `aws`, `azure`, and `gcp`.
                    items:
                      type: string
                    type: array
                  ruleName:
                    description: The name of the custom allocation rule. This field
                      is immutable - changing it will force replacement of the resource.
                    type: string
                  strategy:
                    properties:
                      allocatedBy:
                        items:
                          properties:
                            allocatedTags:
                              items:
                                properties:
                                  key:
                                    description: The tag key to allocate costs to
                                      (e.g., `team`, `environment`).
                                    type: string
                                  value:
                                    description: The tag value to allocate costs to
                                      (e.g., `backend`, `production`).
                                    type: string
                                type: object
                              type: array
                            percentage:
                              description: The percentage of costs to allocate to
                                this target as a decimal (e.g., 0.33 for 33%). Used
                                when `method` is `percent`.
                              type: number
                          type: object
                        type: array
                      allocatedByFilters:
                        items:
                          properties:
                            condition:
                              description: The condition to match. Valid values are
                                `=`, `!=`, `is`, `is not`, `like`, `in`, `not in`.
                              type: string
                            tag:
                              description: The tag key to filter on for allocation
                                targets.
                              type: string
                            value:
                              description: The single tag value to match for allocation.
                                Use with conditions like `=`, `!=`, `is`, `is not`,
                                `like`.
                              type: string
                            values:
                              description: A list of tag values to match for allocation.
                                Use with `in` or `not in` conditions.
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                      allocatedByTagKeys:
                        description: List of tag keys used to allocate costs (e.g.,
                          `["team", "project"]`). Costs will be distributed across
                          unique values of these tags.
                        items:
                          type: string
                        type: array
                      basedOnCosts:
                        items:
                          properties:
                            condition:
                              description: The condition to match. Valid values are
                                `=`, `!=`, `is`, `is not`, `like`, `in`, `not in`.
                              type: string
                            tag:
                              description: The tag key to use as the basis for cost
                                allocation calculations.
                              type: string
                            value:
                              description: The single tag value to use for cost calculations.
                                Use with conditions like `=`, `!=`, `is`, `is not`,
                                `like`.
                              type: string
                            values:
                              description: A list of tag values to use for cost calculations.
                                Use with `in` or `not in` conditions.
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                      basedOnTimeseries:
                        properties:
                          json:
                            description: The timeseries query that determines the
                              allocation proportions, encoded as a JSON object. Required
                              when `method` is `proportional_timeseries`. Uses Datadog's
                              formulas-and-functions request format with `queries`,
                              `formulas`, and `response_format` keys. Build it with
                              `jsonencode()`. The set of supported `data_source` values
                              is defined by the API, not by this provider.
                            type: string
                        type: object
                      evaluateGroupedByFilters:
                        items:
                          properties:
                            condition:
                              description: The condition to match. Valid values are
                                `=`, `!=`, `is`, `is not`, `like`, `in`, `not in`.
                              type: string
                            tag:
                              description: The tag key to filter on when grouping
                                costs for evaluation.
                              type: string
                            value:
                              description: The single tag value to match when grouping.
                                Use with conditions like `=`, `!=`, `is`, `is not`,
                                `like`.
                              type: string
                            values:
                              description: A list of tag values to match when grouping.
                                Use with `in` or `not in` conditions.
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                      evaluateGroupedByTagKeys:
                        description: List of tag keys used to group costs before allocation.
                          Costs are grouped by these tag values before applying the
                          allocation strategy.
                        items:
                          type: string
                        type: array
                      granularity:
                        description: The granularity level for cost allocation. Valid
                          values are `daily` or `monthly`.
                        type: string
                      method:
                        description: The allocation method. Valid values are `even`,
                          `percent`, `proportional`, `proportional_timeseries`.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  costsToAllocate:
                    items:
                      properties:
                        condition:
                          description: The condition to match. Valid values are `=`,
                            `!=`, `is`, `is not`, `like`, `in`, `not in`.
                          type: string
                        tag:
                          description: The tag key to filter on (e.g., `aws_product`,
                            `team`, `environment`).
                          type: string
                        value:
                          description: The single tag value to match. Use this field
                            for conditions like `=`, `!=`, `is`, `is not`, `like`.
                            Do not use with `in` or `not in` conditions.
                          type: string
                        values:
                          description: A list of tag values to match. Use this field
                            for `in` or `not in` conditions only. Do not use with
                            single-value conditions.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  enabled:
                    description: Whether the custom allocation rule is enabled.
                    type: boolean
                  providernames:
                    description: List of cloud providers the rule applies to. Valid
                      values include `aws`, `azure`, and `gcp`.
                    items:
                      type: string
                    type: array
                  ruleName:
                    description: The name of the custom allocation rule. This field
                      is immutable - changing it will force replacement of the resource.
                    type: string
                  strategy:
                    properties:
                      allocatedBy:
                        items:
                          properties:
                            allocatedTags:
                              items:
                                properties:
                                  key:
                                    description: The tag key to allocate costs to
                                      (e.g., `team`, `environment`).
                                    type: string
                                  value:
                                    description: The tag value to allocate costs to
                                      (e.g., `backend`, `production`).
                                    type: string
                                type: object
                              type: array
                            percentage:
                              description: The percentage of costs to allocate to
                                this target as a decimal (e.g., 0.33 for 33%). Used
                                when `method` is `percent`.
                              type: number
                          type: object
                        type: array
                      allocatedByFilters:
                        items:
                          properties:
                            condition:
                              description: The condition to match. Valid values are
                                `=`, `!=`, `is`, `is not`, `like`, `in`, `not in`.
                              type: string
                            tag:
                              description: The tag key to filter on for allocation
                                targets.
                              type: string
                            value:
                              description: The single tag value to match for allocation.
                                Use with conditions like `=`, `!=`, `is`, `is not`,
                                `like`.
                              type: string
                            values:
                              description: A list of tag values to match for allocation.
                                Use with `in` or `not in` conditions.
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                      allocatedByTagKeys:
                        description: List of tag keys used to allocate costs (e.g.,
                          `["team", "project"]`). Costs will be distributed across
                          unique values of these tags.
                        items:
                          type: string
                        type: array
                      basedOnCosts:
                        items:
                          properties:
                            condition:
                              description: The condition to match. Valid values are
                                `=`, `!=`, `is`, `is not`, `like`, `in`, `not in`.
                              type: string
                            tag:
                              description: The tag key to use as the basis for cost
                                allocation calculations.
                              type: string
                            value:
                              description: The single tag value to use for cost calculations.
                                Use with conditions like `=`, `!=`, `is`, `is not`,
                                `like`.
                              type: string
                            values:
                              description: A list of tag values to use for cost calculations.
                                Use with `in` or `not in` conditions.
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                      basedOnTimeseries:
                        properties:
                          json:
                            description: The timeseries query that determines the
                              allocation proportions, encoded as a JSON object. Required
                              when `method` is `proportional_timeseries`. Uses Datadog's
                              formulas-and-functions request format with `queries`,
                              `formulas`, and `response_format` keys. Build it with
                              `jsonencode()`. The set of supported `data_source` values
                              is defined by the API, not by this provider.
                            type: string
                        type: object
                      evaluateGroupedByFilters:
                        items:
                          properties:
                            condition:
                              description: The condition to match. Valid values are
                                `=`, `!=`, `is`, `is not`, `like`, `in`, `not in`.
                              type: string
                            tag:
                              description: The tag key to filter on when grouping
                                costs for evaluation.
                              type: string
                            value:
                              description: The single tag value to match when grouping.
                                Use with conditions like `=`, `!=`, `is`, `is not`,
                                `like`.
                              type: string
                            values:
                              description: A list of tag values to match when grouping.
                                Use with `in` or `not in` conditions.
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                      evaluateGroupedByTagKeys:
                        description: List of tag keys used to group costs before allocation.
                          Costs are grouped by these tag values before applying the
                          allocation strategy.
                        items:
                          type: string
                        type: array
                      granularity:
                        description: The granularity level for cost allocation. Valid
                          values are `daily` or `monthly`.
                        type: string
                      method:
                        description: The allocation method. Valid values are `even`,
                          `percent`, `proportional`, `proportional_timeseries`.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.enabled is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.enabled)
                || (has(self.initProvider) && has(self.initProvider.enabled))'
            - message: spec.forProvider.providernames is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.providernames)
                || (has(self.initProvider) && has(self.initProvider.providernames))'
            - message: spec.forProvider.ruleName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.ruleName)
                || (has(self.initProvider) && has(self.initProvider.ruleName))'
          status:
            description: AllocationRuleStatus defines the observed state of AllocationRule.
            properties:
              atProvider:
                properties:
                  costsToAllocate:
                    items:
                      properties:
                        condition:
                          description: The condition to match. Valid values are `=`,
                            `!=`, `is`, `is not`, `like`, `in`, `not in`.
                          type: string
                        tag:
                          description: The tag key to filter on (e.g., `aws_product`,
                            `team`, `environment`).
                          type: string
                        value:
                          description: The single tag value to match. Use this field
                            for conditions like `=`, `!=`, `is`, `is not`, `like`.
                            Do not use with `in` or `not in` conditions.
                          type: string
                        values:
                          description: A list of tag values to match. Use this field
                            for `in` or `not in` conditions only. Do not use with
                            single-value conditions.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  created:
                    description: The timestamp (in ISO 8601 format) when the rule
                      was created.
                    type: string
                  enabled:
                    description: Whether the custom allocation rule is enabled.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  lastModifiedUserUuid:
                    description: |-
                      (String) The ID of this resource.
                      The UUID of the user who last modified the rule.
                    type: string
                  orderId:
                    description: |-
                      (String) The ID of this resource.
                      The order of the rule in the list of custom allocation rules. This field is read-only. Use the `datadog_custom_allocation_rules` resource to manage rule order.
                    type: number
                  providernames:
                    description: List of cloud providers the rule applies to. Valid
                      values include `aws`, `azure`, and `gcp`.
                    items:
                      type: string
                    type: array
                  rejected:
                    description: Whether the rule was rejected by the API during creation
                      due to validation errors. This field is read-only.
                    type: boolean
                  ruleName:
                    description: The name of the custom allocation rule. This field
                      is immutable - changing it will force replacement of the resource.
                    type: string
                  strategy:
                    properties:
                      allocatedBy:
                        items:
                          properties:
                            allocatedTags:
                              items:
                                properties:
                                  key:
                                    description: The tag key to allocate costs to
                                      (e.g., `team`, `environment`).
                                    type: string
                                  value:
                                    description: The tag value to allocate costs to
                                      (e.g., `backend`, `production`).
                                    type: string
                                type: object
                              type: array
                            percentage:
                              description: The percentage of costs to allocate to
                                this target as a decimal (e.g., 0.33 for 33%). Used
                                when `method` is `percent`.
                              type: number
                          type: object
                        type: array
                      allocatedByFilters:
                        items:
                          properties:
                            condition:
                              description: The condition to match. Valid values are
                                `=`, `!=`, `is`, `is not`, `like`, `in`, `not in`.
                              type: string
                            tag:
                              description: The tag key to filter on for allocation
                                targets.
                              type: string
                            value:
                              description: The single tag value to match for allocation.
                                Use with conditions like `=`, `!=`, `is`, `is not`,
                                `like`.
                              type: string
                            values:
                              description: A list of tag values to match for allocation.
                                Use with `in` or `not in` conditions.
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                      allocatedByTagKeys:
                        description: List of tag keys used to allocate costs (e.g.,
                          `["team", "project"]`). Costs will be distributed across
                          unique values of these tags.
                        items:
                          type: string
                        type: array
                      basedOnCosts:
                        items:
                          properties:
                            condition:
                              description: The condition to match. Valid values are
                                `=`, `!=`, `is`, `is not`, `like`, `in`, `not in`.
                              type: string
                            tag:
                              description: The tag key to use as the basis for cost
                                allocation calculations.
                              type: string
                            value:
                              description: The single tag value to use for cost calculations.
                                Use with conditions like `=`, `!=`, `is`, `is not`,
                                `like`.
                              type: string
                            values:
                              description: A list of tag values to use for cost calculations.
                                Use with `in` or `not in` conditions.
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                      basedOnTimeseries:
                        properties:
                          json:
                            description: The timeseries query that determines the
                              allocation proportions, encoded as a JSON object. Required
                              when `method` is `proportional_timeseries`. Uses Datadog's
                              formulas-and-functions request format with `queries`,
                              `formulas`, and `response_format` keys. Build it with
                              `jsonencode()`. The set of supported `data_source` values
                              is defined by the API, not by this provider.
                            type: string
                        type: object
                      evaluateGroupedByFilters:
                        items:
                          properties:
                            condition:
                              description: The condition to match. Valid values are
                                `=`, `!=`, `is`, `is not`, `like`, `in`, `not in`.
                              type: string
                            tag:
                              description: The tag key to filter on when grouping
                                costs for evaluation.
                              type: string
                            value:
                              description: The single tag value to match when grouping.
                                Use with conditions like `=`, `!=`, `is`, `is not`,
                                `like`.
                              type: string
                            values:
                              description: A list of tag values to match when grouping.
                                Use with `in` or `not in` conditions.
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                      evaluateGroupedByTagKeys:
                        description: List of tag keys used to group costs before allocation.
                          Costs are grouped by these tag values before applying the
                          allocation strategy.
                        items:
                          type: string
                        type: array
                      granularity:
                        description: The granularity level for cost allocation. Valid
                          values are `daily` or `monthly`.
                        type: string
                      method:
                        description: The allocation method. Valid values are `even`,
                          `percent`, `proportional`, `proportional_timeseries`.
                        type: string
                    type: object
                  updated:
                    description: The timestamp (in ISO 8601 format) when the rule
                      was last updated.
                    type: string
                  version:
                    description: The version number of the rule. This increments each
                      time the rule is updated.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

