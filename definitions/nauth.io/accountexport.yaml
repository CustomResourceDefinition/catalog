apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
    helm.sh/resource-policy: keep
  name: accountexports.nauth.io
spec:
  group: nauth.io
  names:
    kind: AccountExport
    listKind: AccountExportList
    plural: accountexports
    singular: accountexport
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="ValidRules")].reason
      name: Rules
      type: string
    - jsonPath: .status.conditions[?(@.type=="BoundToAccount")].reason
      name: Bound
      type: string
    - jsonPath: .status.conditions[?(@.type=="AdoptedByAccount")].reason
      name: Adopted
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AccountExport is a component resource for exports in the accounts
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AccountExportSpec defines the desired state of AccountExport.
            properties:
              accountName:
                description: AccountName refers to the Account in the same namespace
                  to which this export applies.
                type: string
              rules:
                description: Rules defines the export rules for this account export.
                  Must have at least one rule.
                items:
                  properties:
                    accountTokenPosition:
                      type: integer
                    advertise:
                      type: boolean
                    allowTrace:
                      type: boolean
                    name:
                      type: string
                    responseThreshold:
                      description: |-
                        A Duration represents the elapsed time between two instants
                        as an int64 nanosecond count. The representation limits the
                        largest representable duration to approximately 290 years.
                      format: int64
                      type: integer
                    responseType:
                      description: ResponseType is used to store an export response
                        type
                      enum:
                      - Singleton
                      - Stream
                      - Chunked
                      type: string
                    serviceLatency:
                      properties:
                        results:
                          description: Subject is a string that represents a NATS
                            subject
                          type: string
                        sampling:
                          type: integer
                      required:
                      - results
                      - sampling
                      type: object
                    subject:
                      description: Subject is a string that represents a NATS subject
                      type: string
                    type:
                      description: ExportType defines the type of import/export.
                      enum:
                      - stream
                      - service
                      type: string
                  required:
                  - subject
                  - type
                  type: object
                minItems: 1
                type: array
            required:
            - accountName
            - rules
            type: object
          status:
            description: AccountExportStatus defines the observed state of AccountExport.
            properties:
              accountID:
                description: AccountID is the ID of the account that this export is
                  bound to.
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              desiredClaim:
                description: Normalized claim for account to use
                properties:
                  observedGeneration:
                    format: int64
                    type: integer
                  rules:
                    description: Rules contains export rules that have been validated
                      and are ready to be used by Account
                    items:
                      properties:
                        accountTokenPosition:
                          type: integer
                        advertise:
                          type: boolean
                        allowTrace:
                          type: boolean
                        name:
                          type: string
                        responseThreshold:
                          description: |-
                            A Duration represents the elapsed time between two instants
                            as an int64 nanosecond count. The representation limits the
                            largest representable duration to approximately 290 years.
                          format: int64
                          type: integer
                        responseType:
                          description: ResponseType is used to store an export response
                            type
                          enum:
                          - Singleton
                          - Stream
                          - Chunked
                          type: string
                        serviceLatency:
                          properties:
                            results:
                              description: Subject is a string that represents a NATS
                                subject
                              type: string
                            sampling:
                              type: integer
                          required:
                          - results
                          - sampling
                          type: object
                        subject:
                          description: Subject is a string that represents a NATS
                            subject
                          type: string
                        type:
                          description: ExportType defines the type of import/export.
                          enum:
                          - stream
                          - service
                          type: string
                      required:
                      - subject
                      - type
                      type: object
                    minItems: 1
                    type: array
                required:
                - observedGeneration
                - rules
                type: object
              observedGeneration:
                format: int64
                type: integer
              operatorVersion:
                type: string
              reconcileTimestamp:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
