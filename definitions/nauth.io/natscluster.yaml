apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
    helm.sh/resource-policy: keep
  name: natsclusters.nauth.io
spec:
  group: nauth.io
  names:
    kind: NatsCluster
    listKind: NatsClusterList
    plural: natsclusters
    singular: natscluster
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NatsCluster is the Schema for the natsclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NatsClusterSpec defines the desired state of NatsCluster
            properties:
              operatorSigningKeySecretRef:
                description: SecretKeyReference contains information to locate a secret
                  in the same namespace
                properties:
                  key:
                    description: Key in the Secret, when not specified an implementation-specific
                      default key is used.
                    type: string
                  name:
                    description: Name of the Secret.
                    type: string
                required:
                - name
                type: object
              systemAccountUserCredsSecretRef:
                description: SecretKeyReference contains information to locate a secret
                  in the same namespace
                properties:
                  key:
                    description: Key in the Secret, when not specified an implementation-specific
                      default key is used.
                    type: string
                  name:
                    description: Name of the Secret.
                    type: string
                required:
                - name
                type: object
              url:
                description: URL is the NATS server URL for this cluster. Mutually
                  exclusive with urlFrom.
                type: string
              urlFrom:
                description: URLFrom loads the NATS URL from a ConfigMap or Secret.
                  Mutually exclusive with url.
                properties:
                  key:
                    description: Key in the ConfigMap or Secret whose value is the
                      NATS URL.
                    type: string
                  kind:
                    description: 'Kind is the type of resource to load from: ConfigMap
                      or Secret.'
                    enum:
                    - ConfigMap
                    - Secret
                    type: string
                  name:
                    description: Name of the ConfigMap or Secret.
                    type: string
                  namespace:
                    description: Namespace of the resource. When empty, defaults to
                      the NatsCluster's namespace.
                    type: string
                required:
                - key
                - kind
                - name
                type: object
            required:
            - operatorSigningKeySecretRef
            - systemAccountUserCredsSecretRef
            type: object
            x-kubernetes-validations:
            - message: exactly one of url or urlFrom must be specified
              rule: has(self.url) != has(self.urlFrom)
        type: object
    served: true
    storage: true
