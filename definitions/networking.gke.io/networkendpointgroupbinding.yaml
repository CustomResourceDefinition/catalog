apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  name: networkendpointgroupbindings.networking.gke.io
spec:
  group: networking.gke.io
  names:
    kind: NetworkEndpointGroupBinding
    listKind: NetworkEndpointGroupBindingList
    plural: networkendpointgroupbindings
    singular: networkendpointgroupbinding
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              backendRef:
                properties:
                  group:
                    type: string
                  kind:
                    type: string
                  name:
                    type: string
                  port:
                    format: int32
                    type: integer
                required:
                - group
                type: object
              networkEndpointGroups:
                items:
                  properties:
                    name:
                      type: string
                    subnet:
                      type: string
                    zones:
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: set
                  required:
                  - name
                  - subnet
                  - zones
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
            required:
            - backendRef
            - networkEndpointGroups
            type: object
          status:
            properties:
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncTime:
                format: date-time
                type: string
              networkEndpointGroups:
                items:
                  properties:
                    resourceURL:
                      type: string
                    subnetURL:
                      type: string
                  required:
                  - resourceURL
                  - subnetURL
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - resourceURL
                x-kubernetes-list-type: map
            type: object
        type: object
    served: true
    storage: true
