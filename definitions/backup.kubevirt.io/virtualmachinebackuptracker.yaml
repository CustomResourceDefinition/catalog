apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  name: virtualmachinebackuptrackers.backup.kubevirt.io
spec:
  group: backup.kubevirt.io
  names:
    kind: VirtualMachineBackupTracker
    listKind: VirtualMachineBackupTrackerList
    plural: virtualmachinebackuptrackers
    singular: virtualmachinebackuptracker
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              source:
                properties:
                  apiGroup:
                    type: string
                  kind:
                    type: string
                  name:
                    type: string
                required:
                - kind
                - name
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: apiGroup must be kubevirt.io
                  rule: has(self.apiGroup) && self.apiGroup == 'kubevirt.io'
                - message: kind must be VirtualMachine
                  rule: self.kind == 'VirtualMachine'
                - message: name is required
                  rule: self.name != ''
            required:
            - source
            type: object
            x-kubernetes-validations:
            - message: spec is immutable after creation
              rule: self == oldSelf
          status:
            properties:
              checkpointRedefinitionRequired:
                type: boolean
              latestCheckpoint:
                properties:
                  creationTime:
                    format: date-time
                    type: string
                  name:
                    type: string
                  volumes:
                    items:
                      properties:
                        dataEndpoint:
                          type: string
                        diskTarget:
                          type: string
                        mapEndpoint:
                          type: string
                        volumeName:
                          type: string
                      required:
                      - diskTarget
                      - volumeName
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
