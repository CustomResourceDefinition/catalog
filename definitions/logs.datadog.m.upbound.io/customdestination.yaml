---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: customdestinations.logs.datadog.m.upbound.io
spec:
  group: logs.datadog.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - datadog
    kind: CustomDestination
    listKind: CustomDestinationList
    plural: customdestinations
    singular: customdestination
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CustomDestination is the Schema for the CustomDestinations API.
          Provides a Datadog Logs Custom Destination API resource, which is used to
          create and manage Datadog log forwarding.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CustomDestinationSpec defines the desired state of CustomDestination
            properties:
              forProvider:
                properties:
                  elasticsearchDestination:
                    description: |-
                      (Block List) The Elasticsearch destination. (see below for nested schema)
                      The Elasticsearch destination.
                    items:
                      properties:
                        basicAuth:
                          description: |-
                            (Block List) Basic access authentication. (see below for nested schema)
                            Basic access authentication.
                          items:
                            properties:
                              passwordSecretRef:
                                description: |-
                                  (String, Sensitive) The password of the authentication. This field is not returned by the API.
                                  The password of the authentication. This field is not returned by the API.
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              usernameSecretRef:
                                description: |-
                                  (String, Sensitive) The username of the authentication. This field is not returned by the API.
                                  The username of the authentication. This field is not returned by the API.
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                            type: object
                          type: array
                        endpoint:
                          description: |-
                            (String) The destination for which logs will be forwarded to. Must have HTTPS scheme. Forwarding back to Datadog is not allowed.
                            The destination for which logs will be forwarded to. Must have HTTPS scheme. Forwarding back to Datadog is not allowed.
                          type: string
                        indexName:
                          description: |-
                            (String) Name of the Elasticsearch index (must follow Elasticsearch's criteria).
                            Name of the Elasticsearch index (must follow [Elasticsearch's criteria](https://www.elastic.co/guide/en/elasticsearch/reference/8.11/indices-create-index.html#indices-create-api-path-params)).
                          type: string
                        indexRotation:
                          description: |-
                            '
                            (that is, '${index_name}-${indexPattern}').
                            You can customize the index rotation naming pattern by choosing one of these options:
                            - Hourly: 'yyyy-MM-dd-HH' (as an example, it would render: '2022-10-19-09')
                            - Daily: 'yyyy-MM-dd' (as an example, it would render: '2022-10-19')
                            - Weekly: 'yyyy-'W'ww' (as an example, it would render: '2022-W42')
                            - Monthly: 'yyyy-MM' (as an example, it would render: '2022-10')
                            If this field is missing or is blank, it means that the index name will always be the same
                            (that is, no rotation).
                            Date pattern with US locale and UTC timezone to be appended to the index name after adding '-'
                            (that is, '${index_name}-${indexPattern}').
                            You can customize the index rotation naming pattern by choosing one of these options:
                            - Hourly: 'yyyy-MM-dd-HH' (as an example, it would render: '2022-10-19-09')
                            - Daily: 'yyyy-MM-dd' (as an example, it would render: '2022-10-19')
                            - Weekly: 'yyyy-'W'ww' (as an example, it would render: '2022-W42')
                            - Monthly: 'yyyy-MM' (as an example, it would render: '2022-10')
                            If this field is missing or is blank, it means that the index name will always be the same
                            (that is, no rotation).
                          type: string
                      type: object
                    type: array
                  enabled:
                    description: |-
                      (Boolean) Whether logs matching this custom destination should be forwarded or not.
                      Whether logs matching this custom destination should be forwarded or not.
                    type: boolean
                  forwardTags:
                    description: |-
                      (Boolean) Whether tags from the forwarded logs should be forwarded or not.
                      Whether tags from the forwarded logs should be forwarded or not.
                    type: boolean
                  forwardTagsRestrictionList:
                    description: |-
                      (List of String) List of tag keys to be filtered.
                      An empty list represents no restriction is in place and either all or no tags will be
                      forwarded depending on forward_tags_restriction_list_type parameter.
                      List of [tag keys](https://docs.datadoghq.com/getting_started/tagging/#define-tags) to be filtered.
                      An empty list represents no restriction is in place and either all or no tags will be
                      forwarded depending on `forward_tags_restriction_list_type` parameter.
                    items:
                      type: string
                    type: array
                  forwardTagsRestrictionListType:
                    description: |-
                      (String) How the forward_tags_restriction_list parameter should be interpreted.
                      If ALLOW_LIST, then only tags whose keys on the forwarded logs match the ones on the restriction list
                      are forwarded.
                      BLOCK_LIST works the opposite way. It does not forward the tags matching the ones on the list. Valid values are ALLOW_LIST, BLOCK_LIST.
                      How the `forward_tags_restriction_list` parameter should be interpreted.
                      If `ALLOW_LIST`, then only tags whose keys on the forwarded logs match the ones on the restriction list
                      are forwarded.
                      `BLOCK_LIST` works the opposite way. It does not forward the tags matching the ones on the list. Valid values are `ALLOW_LIST`, `BLOCK_LIST`.
                    type: string
                  httpDestination:
                    description: |-
                      (Block List) The HTTP destination. (see below for nested schema)
                      The HTTP destination.
                    items:
                      properties:
                        basicAuth:
                          description: |-
                            (Block List) Basic access authentication. (see below for nested schema)
                            Basic access authentication.
                          items:
                            properties:
                              passwordSecretRef:
                                description: |-
                                  (String, Sensitive) The password of the authentication. This field is not returned by the API.
                                  The password of the authentication. This field is not returned by the API.
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              usernameSecretRef:
                                description: |-
                                  (String, Sensitive) The username of the authentication. This field is not returned by the API.
                                  The username of the authentication. This field is not returned by the API.
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                            type: object
                          type: array
                        customHeaderAuth:
                          description: |-
                            (Block List) Custom header access authentication. (see below for nested schema)
                            Custom header access authentication.
                          items:
                            properties:
                              headerName:
                                description: |-
                                  (String) The header name of the authentication.
                                  The header name of the authentication.
                                type: string
                              headerValueSecretRef:
                                description: |-
                                  (String, Sensitive) The header value of the authentication. This field is not returned by the API.
                                  The header value of the authentication. This field is not returned by the API.
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                            type: object
                          type: array
                        endpoint:
                          description: |-
                            (String) The destination for which logs will be forwarded to. Must have HTTPS scheme. Forwarding back to Datadog is not allowed.
                            The destination for which logs will be forwarded to. Must have HTTPS scheme. Forwarding back to Datadog is not allowed.
                          type: string
                      type: object
                    type: array
                  microsoftSentinelDestination:
                    description: |-
                      (Block List) The Microsoft Sentinel destination. (see below for nested schema)
                      The Microsoft Sentinel destination.
                    items:
                      properties:
                        clientId:
                          description: |-
                            (String) Client ID from the Datadog Azure Integration.
                            Client ID from the Datadog Azure Integration.
                          type: string
                        dataCollectionEndpoint:
                          description: |-
                            (String) Azure Data Collection Endpoint.
                            Azure Data Collection Endpoint.
                          type: string
                        dataCollectionRuleId:
                          description: |-
                            (String) Azure Data Collection Rule ID.
                            Azure Data Collection Rule ID.
                          type: string
                        streamName:
                          description: |-
                            (String) Azure stream name.
                            Azure stream name.
                          type: string
                        tenantId:
                          description: |-
                            (String) Tenant ID from the Datadog Azure Integration.
                            Tenant ID from the Datadog Azure Integration.
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The custom destination name.
                      The custom destination name.
                    type: string
                  query:
                    description: |-
                      (String) The custom destination query filter. Logs matching this query are forwarded to the destination.
                      The custom destination query filter. Logs matching this query are forwarded to the destination.
                    type: string
                  splunkDestination:
                    description: |-
                      (Block List) The Splunk HTTP Event Collector (HEC) destination. (see below for nested schema)
                      The Splunk HTTP Event Collector (HEC) destination.
                    items:
                      properties:
                        accessTokenSecretRef:
                          description: |-
                            (String, Sensitive) Access token of the Splunk HTTP Event Collector. This field is not returned by the API.
                            Access token of the Splunk HTTP Event Collector. This field is not returned by the API.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        endpoint:
                          description: |-
                            (String) The destination for which logs will be forwarded to. Must have HTTPS scheme. Forwarding back to Datadog is not allowed.
                            The destination for which logs will be forwarded to. Must have HTTPS scheme. Forwarding back to Datadog is not allowed.
                          type: string
                        sourcetype:
                          description: |-
                            (Block List) The Splunk source type for forwarded events. Omitting sourcetype when you create a custom destination leaves sourcetype unconfigured, and events are forwarded to Splunk with _json as the source type. When updating a custom destination, omitting sourcetype preserves the previously set value; if none was set, the sourcetype field stays unconfigured and events are forwarded with _json (same as when creating a custom destination). Setting value = null omits the sourcetype field in the forwarded events entirely. (see below for nested schema)
                            The Splunk source type for forwarded events. Omitting `sourcetype` when you create a custom destination leaves `sourcetype` unconfigured, and events are forwarded to Splunk with `_json` as the source type. When updating a custom destination, omitting `sourcetype` preserves the previously set value; if none was set, the `sourcetype` field stays unconfigured and events are forwarded with `_json` (same as when creating a custom destination). Setting `value = null` omits the `sourcetype` field in the forwarded events entirely.
                          items:
                            properties:
                              value:
                                description: |-
                                  (String) The source type string. Set to null to omit the sourcetype from forwarded events.
                                  The source type string. Set to `null` to omit the sourcetype from forwarded events.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  elasticsearchDestination:
                    description: |-
                      (Block List) The Elasticsearch destination. (see below for nested schema)
                      The Elasticsearch destination.
                    items:
                      properties:
                        basicAuth:
                          description: |-
                            (Block List) Basic access authentication. (see below for nested schema)
                            Basic access authentication.
                          items:
                            properties:
                              passwordSecretRef:
                                description: |-
                                  (String, Sensitive) The password of the authentication. This field is not returned by the API.
                                  The password of the authentication. This field is not returned by the API.
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              usernameSecretRef:
                                description: |-
                                  (String, Sensitive) The username of the authentication. This field is not returned by the API.
                                  The username of the authentication. This field is not returned by the API.
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                            required:
                            - passwordSecretRef
                            - usernameSecretRef
                            type: object
                          type: array
                        endpoint:
                          description: |-
                            (String) The destination for which logs will be forwarded to. Must have HTTPS scheme. Forwarding back to Datadog is not allowed.
                            The destination for which logs will be forwarded to. Must have HTTPS scheme. Forwarding back to Datadog is not allowed.
                          type: string
                        indexName:
                          description: |-
                            (String) Name of the Elasticsearch index (must follow Elasticsearch's criteria).
                            Name of the Elasticsearch index (must follow [Elasticsearch's criteria](https://www.elastic.co/guide/en/elasticsearch/reference/8.11/indices-create-index.html#indices-create-api-path-params)).
                          type: string
                        indexRotation:
                          description: |-
                            '
                            (that is, '${index_name}-${indexPattern}').
                            You can customize the index rotation naming pattern by choosing one of these options:
                            - Hourly: 'yyyy-MM-dd-HH' (as an example, it would render: '2022-10-19-09')
                            - Daily: 'yyyy-MM-dd' (as an example, it would render: '2022-10-19')
                            - Weekly: 'yyyy-'W'ww' (as an example, it would render: '2022-W42')
                            - Monthly: 'yyyy-MM' (as an example, it would render: '2022-10')
                            If this field is missing or is blank, it means that the index name will always be the same
                            (that is, no rotation).
                            Date pattern with US locale and UTC timezone to be appended to the index name after adding '-'
                            (that is, '${index_name}-${indexPattern}').
                            You can customize the index rotation naming pattern by choosing one of these options:
                            - Hourly: 'yyyy-MM-dd-HH' (as an example, it would render: '2022-10-19-09')
                            - Daily: 'yyyy-MM-dd' (as an example, it would render: '2022-10-19')
                            - Weekly: 'yyyy-'W'ww' (as an example, it would render: '2022-W42')
                            - Monthly: 'yyyy-MM' (as an example, it would render: '2022-10')
                            If this field is missing or is blank, it means that the index name will always be the same
                            (that is, no rotation).
                          type: string
                      type: object
                    type: array
                  enabled:
                    description: |-
                      (Boolean) Whether logs matching this custom destination should be forwarded or not.
                      Whether logs matching this custom destination should be forwarded or not.
                    type: boolean
                  forwardTags:
                    description: |-
                      (Boolean) Whether tags from the forwarded logs should be forwarded or not.
                      Whether tags from the forwarded logs should be forwarded or not.
                    type: boolean
                  forwardTagsRestrictionList:
                    description: |-
                      (List of String) List of tag keys to be filtered.
                      An empty list represents no restriction is in place and either all or no tags will be
                      forwarded depending on forward_tags_restriction_list_type parameter.
                      List of [tag keys](https://docs.datadoghq.com/getting_started/tagging/#define-tags) to be filtered.
                      An empty list represents no restriction is in place and either all or no tags will be
                      forwarded depending on `forward_tags_restriction_list_type` parameter.
                    items:
                      type: string
                    type: array
                  forwardTagsRestrictionListType:
                    description: |-
                      (String) How the forward_tags_restriction_list parameter should be interpreted.
                      If ALLOW_LIST, then only tags whose keys on the forwarded logs match the ones on the restriction list
                      are forwarded.
                      BLOCK_LIST works the opposite way. It does not forward the tags matching the ones on the list. Valid values are ALLOW_LIST, BLOCK_LIST.
                      How the `forward_tags_restriction_list` parameter should be interpreted.
                      If `ALLOW_LIST`, then only tags whose keys on the forwarded logs match the ones on the restriction list
                      are forwarded.
                      `BLOCK_LIST` works the opposite way. It does not forward the tags matching the ones on the list. Valid values are `ALLOW_LIST`, `BLOCK_LIST`.
                    type: string
                  httpDestination:
                    description: |-
                      (Block List) The HTTP destination. (see below for nested schema)
                      The HTTP destination.
                    items:
                      properties:
                        basicAuth:
                          description: |-
                            (Block List) Basic access authentication. (see below for nested schema)
                            Basic access authentication.
                          items:
                            properties:
                              passwordSecretRef:
                                description: |-
                                  (String, Sensitive) The password of the authentication. This field is not returned by the API.
                                  The password of the authentication. This field is not returned by the API.
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              usernameSecretRef:
                                description: |-
                                  (String, Sensitive) The username of the authentication. This field is not returned by the API.
                                  The username of the authentication. This field is not returned by the API.
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                            required:
                            - passwordSecretRef
                            - usernameSecretRef
                            type: object
                          type: array
                        customHeaderAuth:
                          description: |-
                            (Block List) Custom header access authentication. (see below for nested schema)
                            Custom header access authentication.
                          items:
                            properties:
                              headerName:
                                description: |-
                                  (String) The header name of the authentication.
                                  The header name of the authentication.
                                type: string
                              headerValueSecretRef:
                                description: |-
                                  (String, Sensitive) The header value of the authentication. This field is not returned by the API.
                                  The header value of the authentication. This field is not returned by the API.
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                            required:
                            - headerValueSecretRef
                            type: object
                          type: array
                        endpoint:
                          description: |-
                            (String) The destination for which logs will be forwarded to. Must have HTTPS scheme. Forwarding back to Datadog is not allowed.
                            The destination for which logs will be forwarded to. Must have HTTPS scheme. Forwarding back to Datadog is not allowed.
                          type: string
                      type: object
                    type: array
                  microsoftSentinelDestination:
                    description: |-
                      (Block List) The Microsoft Sentinel destination. (see below for nested schema)
                      The Microsoft Sentinel destination.
                    items:
                      properties:
                        clientId:
                          description: |-
                            (String) Client ID from the Datadog Azure Integration.
                            Client ID from the Datadog Azure Integration.
                          type: string
                        dataCollectionEndpoint:
                          description: |-
                            (String) Azure Data Collection Endpoint.
                            Azure Data Collection Endpoint.
                          type: string
                        dataCollectionRuleId:
                          description: |-
                            (String) Azure Data Collection Rule ID.
                            Azure Data Collection Rule ID.
                          type: string
                        streamName:
                          description: |-
                            (String) Azure stream name.
                            Azure stream name.
                          type: string
                        tenantId:
                          description: |-
                            (String) Tenant ID from the Datadog Azure Integration.
                            Tenant ID from the Datadog Azure Integration.
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The custom destination name.
                      The custom destination name.
                    type: string
                  query:
                    description: |-
                      (String) The custom destination query filter. Logs matching this query are forwarded to the destination.
                      The custom destination query filter. Logs matching this query are forwarded to the destination.
                    type: string
                  splunkDestination:
                    description: |-
                      (Block List) The Splunk HTTP Event Collector (HEC) destination. (see below for nested schema)
                      The Splunk HTTP Event Collector (HEC) destination.
                    items:
                      properties:
                        accessTokenSecretRef:
                          description: |-
                            (String, Sensitive) Access token of the Splunk HTTP Event Collector. This field is not returned by the API.
                            Access token of the Splunk HTTP Event Collector. This field is not returned by the API.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        endpoint:
                          description: |-
                            (String) The destination for which logs will be forwarded to. Must have HTTPS scheme. Forwarding back to Datadog is not allowed.
                            The destination for which logs will be forwarded to. Must have HTTPS scheme. Forwarding back to Datadog is not allowed.
                          type: string
                        sourcetype:
                          description: |-
                            (Block List) The Splunk source type for forwarded events. Omitting sourcetype when you create a custom destination leaves sourcetype unconfigured, and events are forwarded to Splunk with _json as the source type. When updating a custom destination, omitting sourcetype preserves the previously set value; if none was set, the sourcetype field stays unconfigured and events are forwarded with _json (same as when creating a custom destination). Setting value = null omits the sourcetype field in the forwarded events entirely. (see below for nested schema)
                            The Splunk source type for forwarded events. Omitting `sourcetype` when you create a custom destination leaves `sourcetype` unconfigured, and events are forwarded to Splunk with `_json` as the source type. When updating a custom destination, omitting `sourcetype` preserves the previously set value; if none was set, the `sourcetype` field stays unconfigured and events are forwarded with `_json` (same as when creating a custom destination). Setting `value = null` omits the `sourcetype` field in the forwarded events entirely.
                          items:
                            properties:
                              value:
                                description: |-
                                  (String) The source type string. Set to null to omit the sourcetype from forwarded events.
                                  The source type string. Set to `null` to omit the sourcetype from forwarded events.
                                type: string
                            type: object
                          type: array
                      required:
                      - accessTokenSecretRef
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: CustomDestinationStatus defines the observed state of CustomDestination.
            properties:
              atProvider:
                properties:
                  elasticsearchDestination:
                    description: |-
                      (Block List) The Elasticsearch destination. (see below for nested schema)
                      The Elasticsearch destination.
                    items:
                      properties:
                        basicAuth:
                          description: |-
                            (Block List) Basic access authentication. (see below for nested schema)
                            Basic access authentication.
                          items:
                            properties:
                              passwordSecretRef:
                                description: |-
                                  (String, Sensitive) The password of the authentication. This field is not returned by the API.
                                  The password of the authentication. This field is not returned by the API.
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              usernameSecretRef:
                                description: |-
                                  (String, Sensitive) The username of the authentication. This field is not returned by the API.
                                  The username of the authentication. This field is not returned by the API.
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                            type: object
                          type: array
                        endpoint:
                          description: |-
                            (String) The destination for which logs will be forwarded to. Must have HTTPS scheme. Forwarding back to Datadog is not allowed.
                            The destination for which logs will be forwarded to. Must have HTTPS scheme. Forwarding back to Datadog is not allowed.
                          type: string
                        indexName:
                          description: |-
                            (String) Name of the Elasticsearch index (must follow Elasticsearch's criteria).
                            Name of the Elasticsearch index (must follow [Elasticsearch's criteria](https://www.elastic.co/guide/en/elasticsearch/reference/8.11/indices-create-index.html#indices-create-api-path-params)).
                          type: string
                        indexRotation:
                          description: |-
                            '
                            (that is, '${index_name}-${indexPattern}').
                            You can customize the index rotation naming pattern by choosing one of these options:
                            - Hourly: 'yyyy-MM-dd-HH' (as an example, it would render: '2022-10-19-09')
                            - Daily: 'yyyy-MM-dd' (as an example, it would render: '2022-10-19')
                            - Weekly: 'yyyy-'W'ww' (as an example, it would render: '2022-W42')
                            - Monthly: 'yyyy-MM' (as an example, it would render: '2022-10')
                            If this field is missing or is blank, it means that the index name will always be the same
                            (that is, no rotation).
                            Date pattern with US locale and UTC timezone to be appended to the index name after adding '-'
                            (that is, '${index_name}-${indexPattern}').
                            You can customize the index rotation naming pattern by choosing one of these options:
                            - Hourly: 'yyyy-MM-dd-HH' (as an example, it would render: '2022-10-19-09')
                            - Daily: 'yyyy-MM-dd' (as an example, it would render: '2022-10-19')
                            - Weekly: 'yyyy-'W'ww' (as an example, it would render: '2022-W42')
                            - Monthly: 'yyyy-MM' (as an example, it would render: '2022-10')
                            If this field is missing or is blank, it means that the index name will always be the same
                            (that is, no rotation).
                          type: string
                      type: object
                    type: array
                  enabled:
                    description: |-
                      (Boolean) Whether logs matching this custom destination should be forwarded or not.
                      Whether logs matching this custom destination should be forwarded or not.
                    type: boolean
                  forwardTags:
                    description: |-
                      (Boolean) Whether tags from the forwarded logs should be forwarded or not.
                      Whether tags from the forwarded logs should be forwarded or not.
                    type: boolean
                  forwardTagsRestrictionList:
                    description: |-
                      (List of String) List of tag keys to be filtered.
                      An empty list represents no restriction is in place and either all or no tags will be
                      forwarded depending on forward_tags_restriction_list_type parameter.
                      List of [tag keys](https://docs.datadoghq.com/getting_started/tagging/#define-tags) to be filtered.
                      An empty list represents no restriction is in place and either all or no tags will be
                      forwarded depending on `forward_tags_restriction_list_type` parameter.
                    items:
                      type: string
                    type: array
                  forwardTagsRestrictionListType:
                    description: |-
                      (String) How the forward_tags_restriction_list parameter should be interpreted.
                      If ALLOW_LIST, then only tags whose keys on the forwarded logs match the ones on the restriction list
                      are forwarded.
                      BLOCK_LIST works the opposite way. It does not forward the tags matching the ones on the list. Valid values are ALLOW_LIST, BLOCK_LIST.
                      How the `forward_tags_restriction_list` parameter should be interpreted.
                      If `ALLOW_LIST`, then only tags whose keys on the forwarded logs match the ones on the restriction list
                      are forwarded.
                      `BLOCK_LIST` works the opposite way. It does not forward the tags matching the ones on the list. Valid values are `ALLOW_LIST`, `BLOCK_LIST`.
                    type: string
                  httpDestination:
                    description: |-
                      (Block List) The HTTP destination. (see below for nested schema)
                      The HTTP destination.
                    items:
                      properties:
                        basicAuth:
                          description: |-
                            (Block List) Basic access authentication. (see below for nested schema)
                            Basic access authentication.
                          items:
                            properties:
                              passwordSecretRef:
                                description: |-
                                  (String, Sensitive) The password of the authentication. This field is not returned by the API.
                                  The password of the authentication. This field is not returned by the API.
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              usernameSecretRef:
                                description: |-
                                  (String, Sensitive) The username of the authentication. This field is not returned by the API.
                                  The username of the authentication. This field is not returned by the API.
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                            type: object
                          type: array
                        customHeaderAuth:
                          description: |-
                            (Block List) Custom header access authentication. (see below for nested schema)
                            Custom header access authentication.
                          items:
                            properties:
                              headerName:
                                description: |-
                                  (String) The header name of the authentication.
                                  The header name of the authentication.
                                type: string
                            type: object
                          type: array
                        endpoint:
                          description: |-
                            (String) The destination for which logs will be forwarded to. Must have HTTPS scheme. Forwarding back to Datadog is not allowed.
                            The destination for which logs will be forwarded to. Must have HTTPS scheme. Forwarding back to Datadog is not allowed.
                          type: string
                      type: object
                    type: array
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  microsoftSentinelDestination:
                    description: |-
                      (Block List) The Microsoft Sentinel destination. (see below for nested schema)
                      The Microsoft Sentinel destination.
                    items:
                      properties:
                        clientId:
                          description: |-
                            (String) Client ID from the Datadog Azure Integration.
                            Client ID from the Datadog Azure Integration.
                          type: string
                        dataCollectionEndpoint:
                          description: |-
                            (String) Azure Data Collection Endpoint.
                            Azure Data Collection Endpoint.
                          type: string
                        dataCollectionRuleId:
                          description: |-
                            (String) Azure Data Collection Rule ID.
                            Azure Data Collection Rule ID.
                          type: string
                        streamName:
                          description: |-
                            (String) Azure stream name.
                            Azure stream name.
                          type: string
                        tenantId:
                          description: |-
                            (String) Tenant ID from the Datadog Azure Integration.
                            Tenant ID from the Datadog Azure Integration.
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The custom destination name.
                      The custom destination name.
                    type: string
                  query:
                    description: |-
                      (String) The custom destination query filter. Logs matching this query are forwarded to the destination.
                      The custom destination query filter. Logs matching this query are forwarded to the destination.
                    type: string
                  splunkDestination:
                    description: |-
                      (Block List) The Splunk HTTP Event Collector (HEC) destination. (see below for nested schema)
                      The Splunk HTTP Event Collector (HEC) destination.
                    items:
                      properties:
                        endpoint:
                          description: |-
                            (String) The destination for which logs will be forwarded to. Must have HTTPS scheme. Forwarding back to Datadog is not allowed.
                            The destination for which logs will be forwarded to. Must have HTTPS scheme. Forwarding back to Datadog is not allowed.
                          type: string
                        sourcetype:
                          description: |-
                            (Block List) The Splunk source type for forwarded events. Omitting sourcetype when you create a custom destination leaves sourcetype unconfigured, and events are forwarded to Splunk with _json as the source type. When updating a custom destination, omitting sourcetype preserves the previously set value; if none was set, the sourcetype field stays unconfigured and events are forwarded with _json (same as when creating a custom destination). Setting value = null omits the sourcetype field in the forwarded events entirely. (see below for nested schema)
                            The Splunk source type for forwarded events. Omitting `sourcetype` when you create a custom destination leaves `sourcetype` unconfigured, and events are forwarded to Splunk with `_json` as the source type. When updating a custom destination, omitting `sourcetype` preserves the previously set value; if none was set, the `sourcetype` field stays unconfigured and events are forwarded with `_json` (same as when creating a custom destination). Setting `value = null` omits the `sourcetype` field in the forwarded events entirely.
                          items:
                            properties:
                              value:
                                description: |-
                                  (String) The source type string. Set to null to omit the sourcetype from forwarded events.
                                  The source type string. Set to `null` to omit the sourcetype from forwarded events.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

