---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: rules.tag.datadog.upbound.io
spec:
  group: tag.datadog.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - datadog
    kind: Rule
    listKind: RuleList
    plural: rules
    singular: rule
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Rule is the Schema for the Rules API. Provides a Datadog tag
          rule resource. This can be used to create and manage Datadog governance
          tag rules.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RuleSpec defines the desired state of Rule
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  enabled:
                    description: |-
                      (Boolean) Whether the rule is currently enforced. Defaults to true.
                      Whether the rule is currently enforced. Defaults to `true`.
                    type: boolean
                  forceBlockingOnCreate:
                    description: |-
                      atomic: if the update fails, a surfacing rule is left behind and the resource is marked tainted. This field is only read during creation and is not sent to the API; changing it afterwards produces a diff that has no effect.
                      Set to `true` to allow creating a rule with `rule_type` set to `blocking`. The Datadog API only accepts `surfacing` at creation time, so the provider creates the rule as `surfacing` and then immediately updates it to `blocking`, which makes the create non-atomic: if the update fails, a `surfacing` rule is left behind and the resource is marked tainted. This field is only read during creation and is not sent to the API; changing it afterwards produces a diff that has no effect.
                    type: boolean
                  hardDelete:
                    description: |-
                      deleted instead, which keeps it recoverable and preserves its historical compliance score data. This field is only read during deletion and is not sent to the API on create or update. Defaults to true.
                      Whether destroying this resource permanently deletes the tag rule. When set to `false` the rule is soft-deleted instead, which keeps it recoverable and preserves its historical compliance score data. This field is only read during deletion and is not sent to the API on create or update. Defaults to `true`.
                    type: boolean
                  name:
                    description: |-
                      readable name for the tag rule.
                      Human-readable name for the tag rule.
                    type: string
                  negated:
                    description: |-
                      (Boolean) When true, the rule matches tag values that do NOT match any of the supplied patterns. Defaults to false.
                      When `true`, the rule matches tag values that do NOT match any of the supplied patterns. Defaults to `false`.
                    type: boolean
                  required:
                    description: |-
                      (Boolean) When true, telemetry without this tag is treated as a violation. Defaults to false.
                      When `true`, telemetry without this tag is treated as a violation. Defaults to `false`.
                    type: boolean
                  ruleType:
                    description: |-
                      compliant telemetry, while blocking rejects telemetry that violates the rule. The API only accepts surfacing at creation time, so creating a rule directly as blocking requires force_blocking_on_create to be set to true. Using blocking at all requires blocking tag rules to be enabled for your organization; otherwise the API returns 403 permission denied. Valid values are blocking, surfacing.
                      How the rule is enforced. `surfacing` only highlights non-compliant telemetry, while `blocking` rejects telemetry that violates the rule. The API only accepts `surfacing` at creation time, so creating a rule directly as `blocking` requires `force_blocking_on_create` to be set to `true`. Using `blocking` at all requires blocking tag rules to be enabled for your organization; otherwise the API returns `403 permission denied`. Valid values are `blocking`, `surfacing`.
                    type: string
                  scope:
                    description: |-
                      level identifier used to limit where the rule is enforced.
                      The scope the rule applies within. Typically an environment, team, or organization-level identifier used to limit where the rule is enforced.
                    type: string
                  source:
                    description: |-
                      (String) The telemetry source that the tag rule applies to. This field cannot be updated after creation; changing it forces a new resource. Valid values are logs, spans, metrics, rum, feed.
                      The telemetry source that the tag rule applies to. This field cannot be updated after creation; changing it forces a new resource. Valid values are `logs`, `spans`, `metrics`, `rum`, `feed`.
                    type: string
                  tagKey:
                    description: |-
                      (String) The tag key that the rule governs (for example, service).
                      The tag key that the rule governs (for example, `service`).
                    type: string
                  tagValuePatterns:
                    description: |-
                      Za-z0-9_:-.,/*, with * acting as a wildcard. A pattern made up only of wildcards (for example a bare *) is rejected by the API.
                      One or more patterns that valid values for the tag key must match. At least one pattern is required. These are not regular expressions: the API restricts pattern characters to `A-Za-z0-9_:-.,/*`, with `*` acting as a wildcard. A pattern made up only of wildcards (for example a bare `*`) is rejected by the API.
                    items:
                      type: string
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  enabled:
                    description: |-
                      (Boolean) Whether the rule is currently enforced. Defaults to true.
                      Whether the rule is currently enforced. Defaults to `true`.
                    type: boolean
                  forceBlockingOnCreate:
                    description: |-
                      atomic: if the update fails, a surfacing rule is left behind and the resource is marked tainted. This field is only read during creation and is not sent to the API; changing it afterwards produces a diff that has no effect.
                      Set to `true` to allow creating a rule with `rule_type` set to `blocking`. The Datadog API only accepts `surfacing` at creation time, so the provider creates the rule as `surfacing` and then immediately updates it to `blocking`, which makes the create non-atomic: if the update fails, a `surfacing` rule is left behind and the resource is marked tainted. This field is only read during creation and is not sent to the API; changing it afterwards produces a diff that has no effect.
                    type: boolean
                  hardDelete:
                    description: |-
                      deleted instead, which keeps it recoverable and preserves its historical compliance score data. This field is only read during deletion and is not sent to the API on create or update. Defaults to true.
                      Whether destroying this resource permanently deletes the tag rule. When set to `false` the rule is soft-deleted instead, which keeps it recoverable and preserves its historical compliance score data. This field is only read during deletion and is not sent to the API on create or update. Defaults to `true`.
                    type: boolean
                  name:
                    description: |-
                      readable name for the tag rule.
                      Human-readable name for the tag rule.
                    type: string
                  negated:
                    description: |-
                      (Boolean) When true, the rule matches tag values that do NOT match any of the supplied patterns. Defaults to false.
                      When `true`, the rule matches tag values that do NOT match any of the supplied patterns. Defaults to `false`.
                    type: boolean
                  required:
                    description: |-
                      (Boolean) When true, telemetry without this tag is treated as a violation. Defaults to false.
                      When `true`, telemetry without this tag is treated as a violation. Defaults to `false`.
                    type: boolean
                  ruleType:
                    description: |-
                      compliant telemetry, while blocking rejects telemetry that violates the rule. The API only accepts surfacing at creation time, so creating a rule directly as blocking requires force_blocking_on_create to be set to true. Using blocking at all requires blocking tag rules to be enabled for your organization; otherwise the API returns 403 permission denied. Valid values are blocking, surfacing.
                      How the rule is enforced. `surfacing` only highlights non-compliant telemetry, while `blocking` rejects telemetry that violates the rule. The API only accepts `surfacing` at creation time, so creating a rule directly as `blocking` requires `force_blocking_on_create` to be set to `true`. Using `blocking` at all requires blocking tag rules to be enabled for your organization; otherwise the API returns `403 permission denied`. Valid values are `blocking`, `surfacing`.
                    type: string
                  scope:
                    description: |-
                      level identifier used to limit where the rule is enforced.
                      The scope the rule applies within. Typically an environment, team, or organization-level identifier used to limit where the rule is enforced.
                    type: string
                  source:
                    description: |-
                      (String) The telemetry source that the tag rule applies to. This field cannot be updated after creation; changing it forces a new resource. Valid values are logs, spans, metrics, rum, feed.
                      The telemetry source that the tag rule applies to. This field cannot be updated after creation; changing it forces a new resource. Valid values are `logs`, `spans`, `metrics`, `rum`, `feed`.
                    type: string
                  tagKey:
                    description: |-
                      (String) The tag key that the rule governs (for example, service).
                      The tag key that the rule governs (for example, `service`).
                    type: string
                  tagValuePatterns:
                    description: |-
                      Za-z0-9_:-.,/*, with * acting as a wildcard. A pattern made up only of wildcards (for example a bare *) is rejected by the API.
                      One or more patterns that valid values for the tag key must match. At least one pattern is required. These are not regular expressions: the API restricts pattern characters to `A-Za-z0-9_:-.,/*`, with `*` acting as a wildcard. A pattern made up only of wildcards (for example a bare `*`) is rejected by the API.
                    items:
                      type: string
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.ruleType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.ruleType)
                || (has(self.initProvider) && has(self.initProvider.ruleType))'
            - message: spec.forProvider.scope is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.scope)
                || (has(self.initProvider) && has(self.initProvider.scope))'
            - message: spec.forProvider.source is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.source)
                || (has(self.initProvider) && has(self.initProvider.source))'
            - message: spec.forProvider.tagKey is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.tagKey)
                || (has(self.initProvider) && has(self.initProvider.tagKey))'
            - message: spec.forProvider.tagValuePatterns is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.tagValuePatterns)
                || (has(self.initProvider) && has(self.initProvider.tagValuePatterns))'
          status:
            description: RuleStatus defines the observed state of Rule.
            properties:
              atProvider:
                properties:
                  createdAt:
                    description: |-
                      (String) The RFC 3339 timestamp at which the rule was created.
                      The RFC 3339 timestamp at which the rule was created.
                    type: string
                  createdBy:
                    description: |-
                      (String) The identifier of the user who created the rule.
                      The identifier of the user who created the rule.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the rule is currently enforced. Defaults to true.
                      Whether the rule is currently enforced. Defaults to `true`.
                    type: boolean
                  forceBlockingOnCreate:
                    description: |-
                      atomic: if the update fails, a surfacing rule is left behind and the resource is marked tainted. This field is only read during creation and is not sent to the API; changing it afterwards produces a diff that has no effect.
                      Set to `true` to allow creating a rule with `rule_type` set to `blocking`. The Datadog API only accepts `surfacing` at creation time, so the provider creates the rule as `surfacing` and then immediately updates it to `blocking`, which makes the create non-atomic: if the update fails, a `surfacing` rule is left behind and the resource is marked tainted. This field is only read during creation and is not sent to the API; changing it afterwards produces a diff that has no effect.
                    type: boolean
                  hardDelete:
                    description: |-
                      deleted instead, which keeps it recoverable and preserves its historical compliance score data. This field is only read during deletion and is not sent to the API on create or update. Defaults to true.
                      Whether destroying this resource permanently deletes the tag rule. When set to `false` the rule is soft-deleted instead, which keeps it recoverable and preserves its historical compliance score data. This field is only read during deletion and is not sent to the API on create or update. Defaults to `true`.
                    type: boolean
                  id:
                    description: (String) The ID of the tag rule.
                    type: string
                  modifiedAt:
                    description: |-
                      (String) The RFC 3339 timestamp at which the rule was last modified.
                      The RFC 3339 timestamp at which the rule was last modified.
                    type: string
                  modifiedBy:
                    description: |-
                      (String) The identifier of the user who last modified the rule.
                      The identifier of the user who last modified the rule.
                    type: string
                  name:
                    description: |-
                      readable name for the tag rule.
                      Human-readable name for the tag rule.
                    type: string
                  negated:
                    description: |-
                      (Boolean) When true, the rule matches tag values that do NOT match any of the supplied patterns. Defaults to false.
                      When `true`, the rule matches tag values that do NOT match any of the supplied patterns. Defaults to `false`.
                    type: boolean
                  required:
                    description: |-
                      (Boolean) When true, telemetry without this tag is treated as a violation. Defaults to false.
                      When `true`, telemetry without this tag is treated as a violation. Defaults to `false`.
                    type: boolean
                  ruleType:
                    description: |-
                      compliant telemetry, while blocking rejects telemetry that violates the rule. The API only accepts surfacing at creation time, so creating a rule directly as blocking requires force_blocking_on_create to be set to true. Using blocking at all requires blocking tag rules to be enabled for your organization; otherwise the API returns 403 permission denied. Valid values are blocking, surfacing.
                      How the rule is enforced. `surfacing` only highlights non-compliant telemetry, while `blocking` rejects telemetry that violates the rule. The API only accepts `surfacing` at creation time, so creating a rule directly as `blocking` requires `force_blocking_on_create` to be set to `true`. Using `blocking` at all requires blocking tag rules to be enabled for your organization; otherwise the API returns `403 permission denied`. Valid values are `blocking`, `surfacing`.
                    type: string
                  scope:
                    description: |-
                      level identifier used to limit where the rule is enforced.
                      The scope the rule applies within. Typically an environment, team, or organization-level identifier used to limit where the rule is enforced.
                    type: string
                  source:
                    description: |-
                      (String) The telemetry source that the tag rule applies to. This field cannot be updated after creation; changing it forces a new resource. Valid values are logs, spans, metrics, rum, feed.
                      The telemetry source that the tag rule applies to. This field cannot be updated after creation; changing it forces a new resource. Valid values are `logs`, `spans`, `metrics`, `rum`, `feed`.
                    type: string
                  tagKey:
                    description: |-
                      (String) The tag key that the rule governs (for example, service).
                      The tag key that the rule governs (for example, `service`).
                    type: string
                  tagValuePatterns:
                    description: |-
                      Za-z0-9_:-.,/*, with * acting as a wildcard. A pattern made up only of wildcards (for example a bare *) is rejected by the API.
                      One or more patterns that valid values for the tag key must match. At least one pattern is required. These are not regular expressions: the API restricts pattern characters to `A-Za-z0-9_:-.,/*`, with `*` acting as a wildcard. A pattern made up only of wildcards (for example a bare `*`) is rejected by the API.
                    items:
                      type: string
                    type: array
                  version:
                    description: |-
                      (Number) A monotonically increasing version counter that is incremented on each update.
                      A monotonically increasing version counter that is incremented on each update.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

