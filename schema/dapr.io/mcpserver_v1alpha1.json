{
  "description": "MCPServer describes a Dapr MCPServer resource that declares a connection to\nan MCP (Model Context Protocol) server for durable tool execution.",
  "type": "object",
  "properties": {
    "apiVersion": {
      "description": "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources",
      "type": "string"
    },
    "kind": {
      "description": "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds",
      "type": "string"
    },
    "metadata": {
      "type": "object"
    },
    "scopes": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "spec": {
      "description": "MCPServerSpec is the full configuration for an MCP server connection.",
      "type": "object",
      "required": [
        "endpoint"
      ],
      "properties": {
        "catalog": {
          "description": "Catalog holds user-facing governance metadata (display name, description,\nowner, tags, links). It is purely informational and not used at runtime.",
          "type": "object",
          "properties": {
            "description": {
              "type": "string"
            },
            "displayName": {
              "type": "string"
            },
            "links": {
              "description": "Links is a free-form map of named URLs (e.g. \"docs\", \"runbook\", \"dashboard\").",
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            },
            "owner": {
              "description": "MCPCatalogOwner identifies the team responsible for the MCP server.",
              "type": "object",
              "properties": {
                "contact": {
                  "type": "string"
                },
                "team": {
                  "type": "string"
                }
              },
              "additionalProperties": false
            },
            "tags": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "additionalProperties": false
        },
        "endpoint": {
          "description": "Endpoint describes the transport and target of the MCP server.",
          "type": "object",
          "properties": {
            "sse": {
              "description": "SSE holds configuration for the legacy SSE transport.",
              "type": "object",
              "required": [
                "url"
              ],
              "properties": {
                "auth": {
                  "description": "Auth configures authentication for the MCP server connection.",
                  "type": "object",
                  "properties": {
                    "oauth2": {
                      "description": "OAuth2 configures OAuth2 client credentials flow for the MCP server.",
                      "type": "object",
                      "required": [
                        "issuer"
                      ],
                      "properties": {
                        "audience": {
                          "type": "string"
                        },
                        "clientID": {
                          "description": "ClientID is the OAuth2 client identifier sent to the token endpoint.\nRequired by RFC 6749 for the standard client_credentials flow (form parameter\nor HTTP Basic username). May be left empty for non-standard flows (e.g. JWT-bearer\nassertions or token endpoints that key solely on the client_secret). Client IDs\nare typically public identifiers; use spec.headers with a secretKeyRef if your\nenvironment requires sourcing the value from a secret store.",
                          "type": "string"
                        },
                        "issuer": {
                          "description": "Issuer is the token endpoint of the authorization server.",
                          "type": "string",
                          "minLength": 1
                        },
                        "scopes": {
                          "type": "array",
                          "items": {
                            "type": "string"
                          }
                        },
                        "secretKeyRef": {
                          "description": "SecretKeyRef references the client secret in the configured secret store.",
                          "type": "object",
                          "required": [
                            "name"
                          ],
                          "properties": {
                            "key": {
                              "description": "Field in the secret.",
                              "type": "string"
                            },
                            "name": {
                              "description": "Secret name.",
                              "type": "string"
                            }
                          },
                          "additionalProperties": false
                        }
                      },
                      "additionalProperties": false
                    },
                    "secretStore": {
                      "description": "SecretStore names the Dapr secret store used to resolve secretKeyRef entries\nin spec.endpoint.http.headers. auth.oauth2.secretKeyRef is resolved separately\nat call time by the MCP worker. Defaults to \"kubernetes\".",
                      "type": "string"
                    },
                    "spiffe": {
                      "description": "SPIFFE configures workload identity JWT injection via Dapr's Sentry CA.\nNo secret is required; Sentry issues the SVID automatically.",
                      "type": "object",
                      "properties": {
                        "jwt": {
                          "description": "JWT configures how the SPIFFE SVID JWT is attached to outbound requests.",
                          "type": "object",
                          "required": [
                            "audience",
                            "header"
                          ],
                          "properties": {
                            "audience": {
                              "description": "Audience is the intended audience for the JWT (e.g. \"mcp://payments\").",
                              "type": "string",
                              "minLength": 1
                            },
                            "header": {
                              "description": "Header is the HTTP header name to inject the JWT into (e.g. \"Authorization\").",
                              "type": "string",
                              "minLength": 1
                            },
                            "headerValuePrefix": {
                              "description": "HeaderValuePrefix is an optional string prepended to the JWT value (e.g. \"Bearer \").",
                              "type": "string"
                            }
                          },
                          "additionalProperties": false
                        }
                      },
                      "additionalProperties": false
                    }
                  },
                  "additionalProperties": false
                },
                "headers": {
                  "description": "Headers are injected on all outbound HTTP requests to the MCP server.",
                  "type": "array",
                  "items": {
                    "description": "NameValuePair is a name/value pair.",
                    "type": "object",
                    "required": [
                      "name"
                    ],
                    "properties": {
                      "envRef": {
                        "description": "EnvRef is the name of an environmental variable to read the value from.",
                        "type": "string"
                      },
                      "name": {
                        "description": "Name of the property.",
                        "type": "string"
                      },
                      "secretKeyRef": {
                        "description": "SecretKeyRef is the reference of a value in a secret store component.",
                        "type": "object",
                        "required": [
                          "name"
                        ],
                        "properties": {
                          "key": {
                            "description": "Field in the secret.",
                            "type": "string"
                          },
                          "name": {
                            "description": "Secret name.",
                            "type": "string"
                          }
                        },
                        "additionalProperties": false
                      },
                      "value": {
                        "description": "Value of the property, in plaintext.",
                        "x-kubernetes-preserve-unknown-fields": true
                      }
                    },
                    "additionalProperties": false
                  }
                },
                "protocolVersion": {
                  "description": "ProtocolVersion pins the MCP spec version the server implements,\nusing the date-based format defined by the MCP specification (e.g. \"2025-06-18\").\nWhen unset, the go-sdk negotiates the latest version supported by both sides.",
                  "type": "string"
                },
                "timeout": {
                  "description": "Timeout is the per-call deadline for MCP requests.",
                  "type": "string"
                },
                "url": {
                  "description": "URL is the endpoint URL of the MCP server.",
                  "type": "string",
                  "minLength": 1
                }
              },
              "additionalProperties": false
            },
            "stdio": {
              "description": "Stdio holds configuration for the stdio subprocess transport.",
              "type": "object",
              "required": [
                "command"
              ],
              "properties": {
                "args": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "command": {
                  "description": "Command is the executable to run.",
                  "type": "string",
                  "minLength": 1
                },
                "env": {
                  "description": "Env are environment variables injected into the subprocess.\nSupports plain value, secretKeyRef, and envRef.",
                  "type": "array",
                  "items": {
                    "description": "NameValuePair is a name/value pair.",
                    "type": "object",
                    "required": [
                      "name"
                    ],
                    "properties": {
                      "envRef": {
                        "description": "EnvRef is the name of an environmental variable to read the value from.",
                        "type": "string"
                      },
                      "name": {
                        "description": "Name of the property.",
                        "type": "string"
                      },
                      "secretKeyRef": {
                        "description": "SecretKeyRef is the reference of a value in a secret store component.",
                        "type": "object",
                        "required": [
                          "name"
                        ],
                        "properties": {
                          "key": {
                            "description": "Field in the secret.",
                            "type": "string"
                          },
                          "name": {
                            "description": "Secret name.",
                            "type": "string"
                          }
                        },
                        "additionalProperties": false
                      },
                      "value": {
                        "description": "Value of the property, in plaintext.",
                        "x-kubernetes-preserve-unknown-fields": true
                      }
                    },
                    "additionalProperties": false
                  }
                }
              },
              "additionalProperties": false
            },
            "streamableHTTP": {
              "description": "StreamableHTTP holds configuration for the streamable_http transport.",
              "type": "object",
              "required": [
                "url"
              ],
              "properties": {
                "auth": {
                  "description": "Auth configures authentication for the MCP server connection.",
                  "type": "object",
                  "properties": {
                    "oauth2": {
                      "description": "OAuth2 configures OAuth2 client credentials flow for the MCP server.",
                      "type": "object",
                      "required": [
                        "issuer"
                      ],
                      "properties": {
                        "audience": {
                          "type": "string"
                        },
                        "clientID": {
                          "description": "ClientID is the OAuth2 client identifier sent to the token endpoint.\nRequired by RFC 6749 for the standard client_credentials flow (form parameter\nor HTTP Basic username). May be left empty for non-standard flows (e.g. JWT-bearer\nassertions or token endpoints that key solely on the client_secret). Client IDs\nare typically public identifiers; use spec.headers with a secretKeyRef if your\nenvironment requires sourcing the value from a secret store.",
                          "type": "string"
                        },
                        "issuer": {
                          "description": "Issuer is the token endpoint of the authorization server.",
                          "type": "string",
                          "minLength": 1
                        },
                        "scopes": {
                          "type": "array",
                          "items": {
                            "type": "string"
                          }
                        },
                        "secretKeyRef": {
                          "description": "SecretKeyRef references the client secret in the configured secret store.",
                          "type": "object",
                          "required": [
                            "name"
                          ],
                          "properties": {
                            "key": {
                              "description": "Field in the secret.",
                              "type": "string"
                            },
                            "name": {
                              "description": "Secret name.",
                              "type": "string"
                            }
                          },
                          "additionalProperties": false
                        }
                      },
                      "additionalProperties": false
                    },
                    "secretStore": {
                      "description": "SecretStore names the Dapr secret store used to resolve secretKeyRef entries\nin spec.endpoint.http.headers. auth.oauth2.secretKeyRef is resolved separately\nat call time by the MCP worker. Defaults to \"kubernetes\".",
                      "type": "string"
                    },
                    "spiffe": {
                      "description": "SPIFFE configures workload identity JWT injection via Dapr's Sentry CA.\nNo secret is required; Sentry issues the SVID automatically.",
                      "type": "object",
                      "properties": {
                        "jwt": {
                          "description": "JWT configures how the SPIFFE SVID JWT is attached to outbound requests.",
                          "type": "object",
                          "required": [
                            "audience",
                            "header"
                          ],
                          "properties": {
                            "audience": {
                              "description": "Audience is the intended audience for the JWT (e.g. \"mcp://payments\").",
                              "type": "string",
                              "minLength": 1
                            },
                            "header": {
                              "description": "Header is the HTTP header name to inject the JWT into (e.g. \"Authorization\").",
                              "type": "string",
                              "minLength": 1
                            },
                            "headerValuePrefix": {
                              "description": "HeaderValuePrefix is an optional string prepended to the JWT value (e.g. \"Bearer \").",
                              "type": "string"
                            }
                          },
                          "additionalProperties": false
                        }
                      },
                      "additionalProperties": false
                    }
                  },
                  "additionalProperties": false
                },
                "headers": {
                  "description": "Headers are injected on all outbound HTTP requests to the MCP server.\nEach entry follows the same NameValuePair contract used by HTTPEndpoint:\nplain value, secretKeyRef, or envRef. Secret resolution uses auth.secretStore.",
                  "type": "array",
                  "items": {
                    "description": "NameValuePair is a name/value pair.",
                    "type": "object",
                    "required": [
                      "name"
                    ],
                    "properties": {
                      "envRef": {
                        "description": "EnvRef is the name of an environmental variable to read the value from.",
                        "type": "string"
                      },
                      "name": {
                        "description": "Name of the property.",
                        "type": "string"
                      },
                      "secretKeyRef": {
                        "description": "SecretKeyRef is the reference of a value in a secret store component.",
                        "type": "object",
                        "required": [
                          "name"
                        ],
                        "properties": {
                          "key": {
                            "description": "Field in the secret.",
                            "type": "string"
                          },
                          "name": {
                            "description": "Secret name.",
                            "type": "string"
                          }
                        },
                        "additionalProperties": false
                      },
                      "value": {
                        "description": "Value of the property, in plaintext.",
                        "x-kubernetes-preserve-unknown-fields": true
                      }
                    },
                    "additionalProperties": false
                  }
                },
                "protocolVersion": {
                  "description": "ProtocolVersion pins the MCP spec version the server implements,\nusing the date-based format defined by the MCP specification (e.g. \"2025-06-18\").\nWhen unset, the go-sdk negotiates the latest version supported by both sides.",
                  "type": "string"
                },
                "timeout": {
                  "description": "Timeout is the per-call deadline for MCP requests.",
                  "type": "string"
                },
                "url": {
                  "description": "URL is the endpoint URL of the MCP server.",
                  "type": "string",
                  "minLength": 1
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false,
          "x-kubernetes-validations": [
            {
              "rule": "(has(self.streamableHTTP) ? 1 : 0) + (has(self.sse) ? 1 : 0) + (has(self.stdio) ? 1 : 0) == 1",
              "message": "exactly one of streamableHTTP, sse, or stdio must be set"
            }
          ]
        },
        "ignoreErrors": {
          "description": "IgnoreErrors, when true, allows daprd to continue running if validation\nor secret resolution fails for this MCPServer. When false (default),\nsuch failures cause daprd to exit gracefully.",
          "type": "boolean"
        },
        "middleware": {
          "description": "Middleware defines optional workflow hooks invoked around each tool call.",
          "type": "object",
          "properties": {
            "afterCallTool": {
              "description": "AfterCallTool hooks are invoked in order after each CallTool.\nReceives {mcpServer, toolName, arguments, result} as input.\nErrors fail the workflow — after-hooks act as authorization gates.",
              "type": "array",
              "items": {
                "description": "MutatingMCPMiddlewareHook extends MCPMiddlewareHook with the ability to\nreplace the data flowing through the pipeline when Mutate is true.",
                "type": "object",
                "required": [
                  "workflow"
                ],
                "properties": {
                  "mutate": {
                    "description": "Mutate, when true, causes the hook's return value to replace the data\nflowing through the pipeline:\n  - beforeCallTool: replaces the arguments sent to the tool call\n    (e.g. redact PII, inject defaults).\n  - afterCallTool / afterListTools: replaces the result returned to the caller.\nWhen false (default), the hook validates/observes only — its output is discarded.",
                    "type": "boolean"
                  },
                  "workflow": {
                    "description": "Workflow is the Dapr workflow to invoke as the hook.",
                    "type": "object",
                    "required": [
                      "workflowName"
                    ],
                    "properties": {
                      "appID": {
                        "description": "AppID targets the workflow on a remote Dapr app via service invocation.\nWhen unset, the workflow is invoked locally.",
                        "type": "string"
                      },
                      "workflowName": {
                        "description": "WorkflowName is the name of the workflow to invoke.",
                        "type": "string",
                        "minLength": 1
                      }
                    },
                    "additionalProperties": false
                  }
                },
                "additionalProperties": false
              }
            },
            "afterListTools": {
              "description": "AfterListTools hooks are invoked in order after each ListTools.\nReceives {mcpServer, result} as input.\nErrors are logged but do not affect the result.",
              "type": "array",
              "items": {
                "description": "MutatingMCPMiddlewareHook extends MCPMiddlewareHook with the ability to\nreplace the data flowing through the pipeline when Mutate is true.",
                "type": "object",
                "required": [
                  "workflow"
                ],
                "properties": {
                  "mutate": {
                    "description": "Mutate, when true, causes the hook's return value to replace the data\nflowing through the pipeline:\n  - beforeCallTool: replaces the arguments sent to the tool call\n    (e.g. redact PII, inject defaults).\n  - afterCallTool / afterListTools: replaces the result returned to the caller.\nWhen false (default), the hook validates/observes only — its output is discarded.",
                    "type": "boolean"
                  },
                  "workflow": {
                    "description": "Workflow is the Dapr workflow to invoke as the hook.",
                    "type": "object",
                    "required": [
                      "workflowName"
                    ],
                    "properties": {
                      "appID": {
                        "description": "AppID targets the workflow on a remote Dapr app via service invocation.\nWhen unset, the workflow is invoked locally.",
                        "type": "string"
                      },
                      "workflowName": {
                        "description": "WorkflowName is the name of the workflow to invoke.",
                        "type": "string",
                        "minLength": 1
                      }
                    },
                    "additionalProperties": false
                  }
                },
                "additionalProperties": false
              }
            },
            "beforeCallTool": {
              "description": "BeforeCallTool hooks are invoked in order before each CallTool.\nReceives {mcpServer, toolName, arguments} as input.\nIf any hook returns an error, the chain stops and the workflow completes\nwith CallToolResult{isError: true} so the agent/LLM can self-correct.",
              "type": "array",
              "items": {
                "description": "MutatingMCPMiddlewareHook extends MCPMiddlewareHook with the ability to\nreplace the data flowing through the pipeline when Mutate is true.",
                "type": "object",
                "required": [
                  "workflow"
                ],
                "properties": {
                  "mutate": {
                    "description": "Mutate, when true, causes the hook's return value to replace the data\nflowing through the pipeline:\n  - beforeCallTool: replaces the arguments sent to the tool call\n    (e.g. redact PII, inject defaults).\n  - afterCallTool / afterListTools: replaces the result returned to the caller.\nWhen false (default), the hook validates/observes only — its output is discarded.",
                    "type": "boolean"
                  },
                  "workflow": {
                    "description": "Workflow is the Dapr workflow to invoke as the hook.",
                    "type": "object",
                    "required": [
                      "workflowName"
                    ],
                    "properties": {
                      "appID": {
                        "description": "AppID targets the workflow on a remote Dapr app via service invocation.\nWhen unset, the workflow is invoked locally.",
                        "type": "string"
                      },
                      "workflowName": {
                        "description": "WorkflowName is the name of the workflow to invoke.",
                        "type": "string",
                        "minLength": 1
                      }
                    },
                    "additionalProperties": false
                  }
                },
                "additionalProperties": false
              }
            },
            "beforeListTools": {
              "description": "BeforeListTools hooks are invoked in order before each ListTools.\nReceives {mcpServer} as input.\nIf any hook returns an error, the chain stops and the error is returned.",
              "type": "array",
              "items": {
                "description": "MCPMiddlewareHook is a single middleware hook.",
                "type": "object",
                "required": [
                  "workflow"
                ],
                "properties": {
                  "workflow": {
                    "description": "Workflow is the Dapr workflow to invoke as the hook.",
                    "type": "object",
                    "required": [
                      "workflowName"
                    ],
                    "properties": {
                      "appID": {
                        "description": "AppID targets the workflow on a remote Dapr app via service invocation.\nWhen unset, the workflow is invoked locally.",
                        "type": "string"
                      },
                      "workflowName": {
                        "description": "WorkflowName is the name of the workflow to invoke.",
                        "type": "string",
                        "minLength": 1
                      }
                    },
                    "additionalProperties": false
                  }
                },
                "additionalProperties": false
              }
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    }
  }
}
