{
  "description": "HostNetworkAttachment defines switchport configuration for BMH network interfaces.\nSpec fields are mutable when no BMH references the attachment, immutable when in use.",
  "type": "object",
  "properties": {
    "apiVersion": {
      "description": "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources",
      "type": "string"
    },
    "kind": {
      "description": "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds",
      "type": "string"
    },
    "metadata": {
      "type": "object"
    },
    "spec": {
      "description": "HostNetworkAttachmentSpec defines the desired switchport configuration.",
      "type": "object",
      "required": [
        "mode",
        "nativeVLAN"
      ],
      "properties": {
        "allowedVLANs": {
          "description": "AllowedVLANs specifies the VLAN IDs allowed on this network attachment.\nEach entry is either a single VLAN ID (e.g., \"100\") or a range (e.g., \"100-200\").\nThis is typically used in trunk or hybrid mode to specify which tagged VLANs can be carried on the interface.",
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^\\d+(-\\d+)?$"
          }
        },
        "mode": {
          "description": "Mode specifies the network attachment mode.",
          "type": "string",
          "default": "access",
          "enum": [
            "access",
            "trunk",
            "hybrid"
          ]
        },
        "mtu": {
          "description": "MTU specifies the Maximum Transmission Unit size for the network attachment.\nIf not specified, the default MTU for the underlying network will be used.",
          "type": "integer",
          "maximum": 9216,
          "minimum": 68
        },
        "nativeVLAN": {
          "description": "NativeVLAN specifies the native VLAN ID for the network attachment.\nThis is the untagged VLAN used on the interface.",
          "type": "integer",
          "maximum": 4094,
          "minimum": 1
        }
      },
      "additionalProperties": false
    }
  }
}
