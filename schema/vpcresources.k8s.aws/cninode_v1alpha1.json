{
  "type": "object",
  "properties": {
    "apiVersion": {
      "description": "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources",
      "type": "string"
    },
    "kind": {
      "description": "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds",
      "type": "string"
    },
    "metadata": {
      "type": "object"
    },
    "spec": {
      "description": "Important: Run \"make\" to regenerate code after modifying this file\nCNINodeSpec defines the desired state of CNINode",
      "type": "object",
      "properties": {
        "features": {
          "type": "array",
          "items": {
            "description": "Feature is a type of feature being supported by VPC resource controller and other AWS Services",
            "type": "object",
            "properties": {
              "name": {
                "description": "FeatureName is a type of feature name supported by AWS VPC CNI. It can be Security Group for Pods, custom networking, or others",
                "type": "string"
              },
              "value": {
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        },
        "managedBy": {
          "description": "ManagedBy identifies the controller that owns this CNINode and its\nstatus. Empty is equivalent to \"vpc-resource-controller\" for backward\ncompatibility with objects created before this field existed.\nControllers must ignore CNINodes managed by another controller.\nImmutable once set: a node can never legitimately change between\ncompute types, so a managedBy flip is always a bug or tampering.\nSetting it on an existing object that has never had a value remains\nallowed (adoption of pre-existing objects).",
          "type": "string",
          "enum": [
            "vpc-resource-controller",
            "eks-auto-mode"
          ],
          "x-kubernetes-validations": [
            {
              "rule": "self == oldSelf",
              "message": "managedBy is immutable once set"
            }
          ]
        },
        "tags": {
          "description": "Additional tag key/value added to all network interfaces provisioned by the vpc-resource-controller and VPC-CNI",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false,
      "x-kubernetes-validations": [
        {
          "rule": "!has(oldSelf.managedBy) || has(self.managedBy)",
          "message": "managedBy cannot be removed once set"
        }
      ]
    },
    "status": {
      "description": "CNINodeStatus defines the managed VPC resources.",
      "type": "object",
      "properties": {
        "nodeNetworkState": {
          "description": "Supports EC2-free controller restart recovery.",
          "type": "object",
          "required": [
            "instanceID",
            "primaryNetworkInterfaceSecurityGroups",
            "subnetCIDRBlock",
            "subnetID"
          ],
          "properties": {
            "connectionTracking": {
              "description": "Preserves primary ENI settings for new branch ENIs.",
              "type": "object",
              "properties": {
                "tcpEstablishedTimeout": {
                  "type": "integer",
                  "format": "int32"
                },
                "udpStreamTimeout": {
                  "type": "integer",
                  "format": "int32"
                },
                "udpTimeout": {
                  "type": "integer",
                  "format": "int32"
                }
              },
              "additionalProperties": false
            },
            "instanceID": {
              "type": "string",
              "maxLength": 19,
              "pattern": "^i-([0-9a-f]{8}|[0-9a-f]{17})$"
            },
            "primaryNetworkInterfaceSecurityGroups": {
              "type": "array",
              "minItems": 1,
              "items": {
                "type": "string",
                "maxLength": 20,
                "pattern": "^sg-([0-9a-f]{8}|[0-9a-f]{17})$"
              },
              "x-kubernetes-list-type": "atomic"
            },
            "subnetCIDRBlock": {
              "type": "string"
            },
            "subnetID": {
              "description": "May differ from the trunk subnet with ENIConfig.",
              "type": "string",
              "maxLength": 24,
              "pattern": "^subnet-([0-9a-f]{8}|[0-9a-f]{17})$"
            },
            "subnetV6CIDRBlock": {
              "type": "string"
            }
          },
          "additionalProperties": false
        },
        "trunkInterface": {
          "description": "TrunkInterface describes the trunk network interface attached to the\nnode and the branch interfaces associated with it, persisted by the\nmanaging controller (see spec.managedBy) for visibility and recovery.",
          "type": "object",
          "required": [
            "id"
          ],
          "properties": {
            "branches": {
              "description": "Branches are the branch ENIs associated with this trunk ENI.\nListed as a map keyed by id so distinct field managers can own\nindividual entries under Server-Side Apply without conflicting.",
              "type": "array",
              "items": {
                "description": "BranchInterface describes a branch ENI associated with a trunk ENI.",
                "type": "object",
                "required": [
                  "id",
                  "vlanID"
                ],
                "properties": {
                  "associationID": {
                    "description": "AssociationID is the id of the association between the branch and trunk ENI.",
                    "type": "string"
                  },
                  "id": {
                    "description": "ID is the EC2 network interface id of the branch ENI.",
                    "type": "string",
                    "minLength": 1
                  },
                  "ipv4CIDRs": {
                    "description": "IPv4CIDRs are the IPv4 addresses or prefixes assigned to the branch ENI,\nin CIDR notation (a single address is expressed as a /32).",
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "x-kubernetes-list-type": "atomic"
                  },
                  "ipv6CIDRs": {
                    "description": "IPv6CIDRs are the IPv6 addresses or prefixes assigned to the branch ENI,\nin CIDR notation (a single address is expressed as a /128).",
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "x-kubernetes-list-type": "atomic"
                  },
                  "macAddress": {
                    "description": "MacAddress is the MAC address of the branch ENI.",
                    "type": "string"
                  },
                  "securityGroups": {
                    "description": "SecurityGroups are the security group ids attached to the branch ENI.",
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "x-kubernetes-list-type": "atomic"
                  },
                  "subnetCIDR": {
                    "description": "SubnetCIDR is the IPv4 CIDR block of the subnet the branch ENI belongs to.",
                    "type": "string"
                  },
                  "subnetV6CIDR": {
                    "description": "SubnetV6CIDR is the IPv6 CIDR block of the subnet the branch ENI belongs to.",
                    "type": "string"
                  },
                  "vlanID": {
                    "description": "VlanID is the vlan id of the trunk-to-branch association.",
                    "type": "integer",
                    "format": "int32"
                  }
                },
                "additionalProperties": false
              },
              "x-kubernetes-list-map-keys": [
                "id"
              ],
              "x-kubernetes-list-type": "map"
            },
            "deviceIndex": {
              "description": "DeviceIndex is the attachment device index of the trunk ENI on the instance.",
              "type": "integer",
              "format": "int32"
            },
            "id": {
              "description": "ID is the EC2 network interface id of the trunk ENI.",
              "type": "string",
              "minLength": 1
            },
            "macAddress": {
              "description": "MacAddress is the MAC address of the trunk ENI.",
              "type": "string"
            },
            "subnetID": {
              "description": "SubnetID is the id of the EC2 subnet the trunk ENI resides in, which is\nalso the subnet its branch ENIs are created in. Persisted with the trunk\nso a controller can create a branch ENI straight from the CNINode,\nwithout a DescribeNetworkInterfaces call on the pod path, and so the\nvalue survives a controller restart that drops in-memory node state.",
              "type": "string",
              "maxLength": 24,
              "pattern": "^subnet-([0-9a-f]{8}|[0-9a-f]{17})$"
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    }
  }
}
