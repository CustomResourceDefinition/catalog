{
  "type": "object",
  "required": [
    "name",
    "levels"
  ],
  "properties": {
    "levels": {
      "type": "array",
      "maxItems": 8,
      "minItems": 1,
      "items": {
        "type": "string"
      },
      "x-kubernetes-list-type": "atomic"
    },
    "name": {
      "type": "string",
      "maxLength": 253,
      "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$"
    }
  }
}
