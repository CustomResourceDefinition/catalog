{
  "type": "object",
  "required": [
    "spec"
  ],
  "properties": {
    "metadata": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "pattern": "^default$"
        }
      },
      "additionalProperties": false
    },
    "spec": {
      "type": "object",
      "required": [
        "pingIntervalSeconds"
      ],
      "properties": {
        "pingIntervalSeconds": {
          "description": "Ping interval in seconds, must be at least 1.",
          "type": "integer",
          "format": "int32",
          "default": 60,
          "minimum": 1
        }
      },
      "additionalProperties": false
    }
  }
}
