{
  "description": "NodeShard is a collection of nodes dedicated to a specific scheduler",
  "type": "object",
  "required": [
    "spec"
  ],
  "properties": {
    "apiVersion": {
      "description": "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources",
      "type": "string"
    },
    "kind": {
      "description": "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds",
      "type": "string"
    },
    "metadata": {
      "type": "object"
    },
    "spec": {
      "description": "Specification of the desired behavior of the NodeShard.",
      "type": "object",
      "required": [
        "nodesDesired"
      ],
      "properties": {
        "nodesDesired": {
          "description": "NodesDesired defines the list of nodes desired to be included in this NodeShard.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "status": {
      "description": "Status represents the current information about a NodeShard.\nThis data may not be up to date.",
      "type": "object",
      "properties": {
        "lastUpdateTime": {
          "description": "LastUpdateTime is the last time the status was updated.",
          "type": "string",
          "format": "date-time"
        },
        "nodesInUse": {
          "description": "NodesInUse is the list of nodes currently in use by the NodeShard.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "nodesToAdd": {
          "description": "NodesToAdd is the list of nodes preparing to be added to the NodeShard.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "nodesToRemove": {
          "description": "NodesToRemove is the list of nodes preparing to be removed from the NodeShard.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    }
  }
}
