{
  "description": "ValidationRules is a specification for a ValidationRules resource",
  "type": "object",
  "required": [
    "spec"
  ],
  "properties": {
    "apiVersion": {
      "description": "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources",
      "type": "string"
    },
    "kind": {
      "description": "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds",
      "type": "string"
    },
    "metadata": {
      "type": "object"
    },
    "spec": {
      "type": "object",
      "required": [
        "validation_rules"
      ],
      "properties": {
        "prefix": {
          "type": "string"
        },
        "validation_rules": {
          "type": "object",
          "additionalProperties": {
            "description": "Rule defines the structure for a single validation rule in the YAML.",
            "type": "object",
            "required": [
              "rule",
              "type"
            ],
            "properties": {
              "ingresses": {
                "description": "ingresses where this rule can apply (ingress names)",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "namespaces": {
                "description": "namespaces where this rule can apply (namespace names)",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "order_priority": {
                "description": "Optional priority for sorting rules, bigger value has higher priority",
                "type": "integer"
              },
              "resources": {
                "description": "resources where this rule can apply (service, frontend)",
                "type": "array",
                "maxItems": 42,
                "items": {
                  "type": "string"
                }
              },
              "rule": {
                "type": "string"
              },
              "section": {
                "type": "string",
                "default": "backend",
                "enum": [
                  "frontend",
                  "backend",
                  "all"
                ]
              },
              "template": {
                "type": "string"
              },
              "type": {
                "description": "AnnType defines the type of annotation used in the validation rules.",
                "type": "string",
                "enum": [
                  "duration",
                  "int",
                  "uint",
                  "bool",
                  "string",
                  "float",
                  "json"
                ]
              }
            }
          }
        }
      },
      "additionalProperties": false
    }
  }
}
