{
  "description": "Represents a remote participating cluster.",
  "type": "object",
  "properties": {
    "apiVersion": {
      "type": "string"
    },
    "kind": {
      "type": "string"
    },
    "metadata": {
      "type": "object"
    },
    "spec": {
      "type": "object",
      "required": [
        "apiFqdnUrl",
        "recName",
        "recNamespace"
      ],
      "properties": {
        "apiFqdnUrl": {
          "description": "URL of the cluster. Used for the Active-Active database URL.",
          "type": "string"
        },
        "apiPort": {
          "description": "Port number of the cluster's URL. Used for connectivity and synchronization.",
          "type": "integer"
        },
        "dbFqdnSuffix": {
          "description": "Database URL suffix. Used for the Active-Active database replication endpoint and replication endpoint SNI.",
          "type": "string"
        },
        "recName": {
          "description": "Name of the REC that this RERC points to.",
          "type": "string"
        },
        "recNamespace": {
          "description": "Namespace of the REC that this RERC points to.",
          "type": "string"
        },
        "secretName": {
          "description": "Name of the secret containing cluster credentials. Must use the following format: \"redis-enterprise-\u003cRERC name\u003e\".",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "status": {
      "type": "object",
      "properties": {
        "internalObservedSecretResourceVersion": {
          "description": "Observed secret resource version. For internal use only.",
          "type": "string"
        },
        "local": {
          "description": "Indicates whether this object represents a local or remote cluster.",
          "type": "boolean"
        },
        "observedGeneration": {
          "description": "Most recent generation observed for this RERC. Corresponds to the RERC's generation, which is updated by the API Server.",
          "type": "integer"
        },
        "specStatus": {
          "description": "Indicates whether the desired specification is valid.",
          "type": "string"
        },
        "status": {
          "description": "Status of the remote cluster.",
          "type": "string"
        }
      },
      "additionalProperties": false
    }
  }
}
