{
  "description": "AutoscalingPolicy defines the autoscaling policy configuration for model serving workloads.\nIt specifies scaling rules, metrics, and behavior for automatic replica adjustment.",
  "type": "object",
  "properties": {
    "apiVersion": {
      "description": "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources",
      "type": "string"
    },
    "kind": {
      "description": "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds",
      "type": "string"
    },
    "metadata": {
      "type": "object"
    },
    "spec": {
      "description": "AutoscalingPolicySpec defines the desired state of AutoscalingPolicy.",
      "type": "object",
      "required": [
        "metrics"
      ],
      "properties": {
        "behavior": {
          "description": "Behavior defines the scaling behavior configuration for both scale up and scale down operations.",
          "type": "object",
          "properties": {
            "scaleDown": {
              "description": "ScaleDown defines the policy configuration for scaling down (decreasing replicas).",
              "type": "object",
              "properties": {
                "instances": {
                  "description": "Instances defines the maximum absolute number of instances to scale per period.",
                  "type": "integer",
                  "format": "int32",
                  "default": 1,
                  "minimum": 0
                },
                "percent": {
                  "description": "Percent defines the maximum percentage of current instances to scale per period.",
                  "type": "integer",
                  "format": "int32",
                  "default": 100,
                  "maximum": 1000,
                  "minimum": 0
                },
                "period": {
                  "description": "Period defines the time duration over which scaling metrics are evaluated.",
                  "type": "string",
                  "default": "15s"
                },
                "selectPolicy": {
                  "description": "SelectPolicy determines the selection strategy for scaling operations.\n'Or' means scaling is performed if either the Percent or Instances requirement is met.\n'And' means scaling is performed only if both Percent and Instances requirements are met.",
                  "type": "string",
                  "default": "Or",
                  "enum": [
                    "Or",
                    "And"
                  ]
                },
                "stabilizationWindow": {
                  "description": "StabilizationWindow defines the time window to stabilize scaling actions and prevent rapid oscillations.",
                  "type": "string"
                }
              },
              "additionalProperties": false
            },
            "scaleUp": {
              "description": "ScaleUp defines the policy configuration for scaling up (increasing replicas).",
              "type": "object",
              "properties": {
                "panicPolicy": {
                  "description": "PanicPolicy defines the emergency scaling policy for handling sudden traffic spikes.\nThis policy activates during rapid load surges to prevent service degradation or timeouts.",
                  "type": "object",
                  "required": [
                    "period"
                  ],
                  "properties": {
                    "panicModeHold": {
                      "description": "PanicModeHold defines the duration to remain in panic mode before returning to normal scaling.",
                      "type": "string",
                      "default": "60s"
                    },
                    "panicThresholdPercent": {
                      "description": "PanicThresholdPercent defines the metric threshold percentage that triggers panic mode.\nWhen metrics exceed this percentage of target values, panic mode is activated.",
                      "type": "integer",
                      "format": "int32",
                      "default": 200,
                      "maximum": 1000,
                      "minimum": 110
                    },
                    "percent": {
                      "description": "Percent defines the maximum percentage of current instances to scale up during panic mode.",
                      "type": "integer",
                      "format": "int32",
                      "default": 1000,
                      "maximum": 1000,
                      "minimum": 0
                    },
                    "period": {
                      "description": "Period defines the evaluation period for panic mode scaling decisions.",
                      "type": "string"
                    }
                  },
                  "additionalProperties": false
                },
                "stablePolicy": {
                  "description": "StablePolicy defines the stable scaling policy that uses average metric values over time windows.\nThis policy smooths out short-term fluctuations and avoids unnecessary frequent scaling operations.",
                  "type": "object",
                  "properties": {
                    "instances": {
                      "description": "Instances defines the maximum absolute number of instances to scale per period.",
                      "type": "integer",
                      "format": "int32",
                      "default": 1,
                      "minimum": 0
                    },
                    "percent": {
                      "description": "Percent defines the maximum percentage of current instances to scale per period.",
                      "type": "integer",
                      "format": "int32",
                      "default": 100,
                      "maximum": 1000,
                      "minimum": 0
                    },
                    "period": {
                      "description": "Period defines the time duration over which scaling metrics are evaluated.",
                      "type": "string",
                      "default": "15s"
                    },
                    "selectPolicy": {
                      "description": "SelectPolicy determines the selection strategy for scaling operations.\n'Or' means scaling is performed if either the Percent or Instances requirement is met.\n'And' means scaling is performed only if both Percent and Instances requirements are met.",
                      "type": "string",
                      "default": "Or",
                      "enum": [
                        "Or",
                        "And"
                      ]
                    },
                    "stabilizationWindow": {
                      "description": "StabilizationWindow defines the time window to stabilize scaling actions and prevent rapid oscillations.",
                      "type": "string"
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        },
        "metrics": {
          "description": "Metrics defines the list of metrics used to evaluate scaling decisions.",
          "type": "array",
          "minItems": 1,
          "items": {
            "description": "AutoscalingPolicyMetric defines a metric and its target value for scaling decisions.",
            "type": "object",
            "required": [
              "metricName",
              "targetValue"
            ],
            "properties": {
              "metricName": {
                "description": "MetricName defines the name of the metric to monitor for scaling decisions.",
                "type": "string"
              },
              "targetValue": {
                "description": "TargetValue defines the target value for the metric that triggers scaling operations.",
                "pattern": "^(\\+|-)?(([0-9]+(\\.[0-9]*)?)|(\\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\\+|-)?(([0-9]+(\\.[0-9]*)?)|(\\.[0-9]+))))?$",
                "anyOf": [
                  {
                    "type": "integer"
                  },
                  {
                    "type": "string"
                  }
                ],
                "x-kubernetes-int-or-string": true
              }
            },
            "additionalProperties": false
          }
        },
        "tolerancePercent": {
          "description": "TolerancePercent defines the percentage of deviation tolerated before scaling actions are triggered.\ncurrent_replicas represents the current number of instances, while target_replicas represents the expected number of instances calculated from monitoring metrics.\nScaling operations are performed only when |current_replicas - target_replicas| \u003e= current_replicas * TolerancePercent / 100.",
          "type": "integer",
          "format": "int32",
          "default": 10,
          "maximum": 100,
          "minimum": 0
        }
      },
      "additionalProperties": false
    },
    "status": {
      "description": "AutoscalingPolicyStatus defines the observed state of AutoscalingPolicy.",
      "type": "object"
    }
  }
}
