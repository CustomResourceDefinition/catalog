{
  "type": "object",
  "properties": {
    "apiVersion": {
      "description": "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources",
      "type": "string"
    },
    "kind": {
      "description": "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds",
      "type": "string"
    },
    "metadata": {
      "type": "object"
    },
    "spec": {
      "description": "Spec is the specification of the desired configuration for the oidc client.",
      "type": "object",
      "required": [
        "refreshTokenExpirationSeconds",
        "tokenExpirationSeconds"
      ],
      "properties": {
        "description": {
          "description": "Description provides additional context about the OIDC client.",
          "type": "string"
        },
        "redirectURIs": {
          "description": "RedirectURIs defines the allowed redirect URIs for the OIDC client.\nThese URIs must be registered and used during the authentication flow.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "refreshTokenExpirationSeconds": {
          "description": "RefreshTokenExpirationSeconds defines how long (in seconds)\na refresh token remains valid before expiration.",
          "type": "integer",
          "format": "int64",
          "minimum": 1
        },
        "tokenExpirationSeconds": {
          "description": "TokenExpirationSeconds specifies the duration (in seconds) before\nan access token and ID token expire.",
          "type": "integer",
          "format": "int64",
          "minimum": 1
        }
      },
      "additionalProperties": false
    },
    "status": {
      "description": "Status is the most recently observed status of the oidc client.",
      "type": "object",
      "properties": {
        "clientID": {
          "description": "ClientID represents the ID of the client",
          "type": "string"
        },
        "clientSecrets": {
          "description": "ClientSecrets represents the observed status of the client secrets",
          "type": "object",
          "additionalProperties": {
            "description": "OIDCClientSecretStatus represent the observed status of a client secret.",
            "type": "object",
            "properties": {
              "createdAt": {
                "description": "CreatedAt represents when this client secret was created.",
                "type": "string"
              },
              "lastFiveCharacters": {
                "description": "LastFiveCharacters are the 5 last characters of the client secret",
                "type": "string"
              },
              "lastUsedAt": {
                "description": "LastUsedAt represent when this client secret was used.",
                "type": "string"
              }
            }
          }
        }
      },
      "additionalProperties": false
    }
  }
}
