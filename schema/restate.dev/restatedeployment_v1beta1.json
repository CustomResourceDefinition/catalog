{
  "description": "Auto-generated derived type for RestateDeploymentSpec via `CustomResource`",
  "type": "object",
  "title": "RestateDeployment",
  "required": [
    "spec"
  ],
  "properties": {
    "spec": {
      "description": "RestateDeployment is similar to a Kubernetes Deployment but tailored for Restate services.\nIt maintains ReplicaSets and Services for each version to support Restate's versioning requirements,\nensuring old versions remain available until all invocations against them are complete.",
      "type": "object",
      "required": [
        "restate",
        "template"
      ],
      "properties": {
        "deploymentMode": {
          "description": "Deployment mode: replicaset (default) or knative.\nThis field is immutable after creation.",
          "type": "string",
          "enum": [
            "replicaset",
            "knative"
          ],
          "nullable": true,
          "x-kubernetes-validations": [
            {
              "rule": "self == oldSelf",
              "message": "deploymentMode is immutable after creation"
            }
          ]
        },
        "knative": {
          "description": "Knative-specific configuration.\nWhen specified, enables Knative Serving mode.",
          "type": "object",
          "properties": {
            "maxScale": {
              "description": "Maximum number of replicas (default: unlimited)",
              "type": "integer",
              "format": "int32",
              "minimum": 0,
              "nullable": true
            },
            "minScale": {
              "description": "Minimum number of replicas (default: 0 for scale-to-zero)",
              "type": "integer",
              "format": "int32",
              "minimum": 0,
              "nullable": true
            },
            "tag": {
              "description": "Deployment tag - determines Restate deployment identity.\n\nA Restate deployment is a specific, versioned instance of your service code.\nEach deployment is immutable: once registered with Restate, its endpoint and\nidentity (deployment ID) must not change.\n\nThe tag acts as a stable label that groups multiple Knative Revisions under\na single Restate deployment:\n- **Same tag**: In-place updates create new Knative Revisions within the same\n  Restate deployment (no new registration)\n- **Changed tag**: Creates a new Restate deployment with a new deployment ID\n  (versioned update)\n- **No tag specified**: Uses template hash as tag, causing every template change\n  to create a new Restate deployment\n\nThe tag must be a valid DNS-1035 label: a lowercase RFC 1123 label that consists of\nlower case alphanumeric characters or '-', and must start and end with an alphanumeric\ncharacter (e.g. 'my-name',  or '123-abc', regex used for validation is\n'[a-z]([-a-z0-9]*[a-z0-9])?').\n\nExample: tag \"v1-0\" → Configuration \"my-service-v1-0\" → Restate deployment \"dp_abc123\"\n         Multiple Knative Revisions (00001, 00002, ...) all serve this deployment.",
              "type": "string",
              "pattern": "^[a-z]([-a-z0-9]*[a-z0-9])?$",
              "nullable": true
            },
            "target": {
              "description": "Target concurrent requests per replica (default: 100)",
              "type": "integer",
              "format": "int32",
              "minimum": 0,
              "nullable": true
            }
          },
          "additionalProperties": false,
          "nullable": true
        },
        "minReadySeconds": {
          "description": "Minimum number of seconds for which a newly created pod should be ready.\nOnly used in ReplicaSet mode.",
          "type": "integer",
          "format": "int32",
          "minimum": 0,
          "nullable": true
        },
        "replicas": {
          "description": "Number of desired pods. Defaults to 1.\nOnly used in ReplicaSet mode.",
          "type": "integer",
          "format": "int32",
          "default": 1,
          "minimum": 0
        },
        "restate": {
          "description": "Restate specific configuration",
          "type": "object",
          "required": [
            "register"
          ],
          "properties": {
            "register": {
              "description": "The location of the Restate Admin API to register this deployment against",
              "type": "object",
              "oneOf": [
                {
                  "required": [
                    "cluster"
                  ]
                },
                {
                  "required": [
                    "cloud"
                  ]
                },
                {
                  "required": [
                    "service"
                  ]
                },
                {
                  "required": [
                    "url"
                  ]
                }
              ],
              "properties": {
                "cloud": {
                  "description": "The name of a RestateCloudEnvironment against which to register the deployment. Exactly one of `cluster`, `cloud`, `service` or `url` must be specified",
                  "type": "string"
                },
                "cluster": {
                  "description": "The name of a RestateCluster against which to register the deployment. Exactly one of `cluster`, `cloud`, `service` or `url` must be specified",
                  "type": "string"
                },
                "service": {
                  "description": "A reference to a Service pointing against which to register the deployment. Exactly one of `cluster`, `cloud`, `service` or `url` must be specified",
                  "type": "object",
                  "required": [
                    "name",
                    "namespace"
                  ],
                  "properties": {
                    "name": {
                      "description": "`name` is the name of the service. Required",
                      "type": "string"
                    },
                    "namespace": {
                      "description": "`namespace` is the namespace of the service. Required",
                      "type": "string"
                    },
                    "path": {
                      "description": "`path` is an optional URL path which will be prepended before admin api paths. Should not end in a /.",
                      "type": "string",
                      "nullable": true
                    },
                    "port": {
                      "description": "If specified, the port on the service that hosts the admin api. Defaults to 9070. `port` should be a valid port number (1-65535, inclusive).",
                      "type": "integer",
                      "format": "int32",
                      "maximum": 65535,
                      "minimum": 1,
                      "nullable": true
                    }
                  },
                  "additionalProperties": false
                },
                "url": {
                  "description": "A url of the restate admin endpoint against which to register the deployment Exactly one of `cluster`, `cloud`, `service` or `url` must be specified",
                  "type": "string"
                }
              },
              "additionalProperties": false
            },
            "servicePath": {
              "description": "Optional path to append to the Service url when registering with Restate.\nIf not provided, the service will be registered at the root path \"/\".",
              "type": "string",
              "nullable": true
            },
            "useHttp11": {
              "description": "Force the use of HTTP/1.1 when registering with Restate",
              "type": "boolean",
              "nullable": true
            }
          },
          "additionalProperties": false
        },
        "revisionHistoryLimit": {
          "description": "The number of old ReplicaSets to retain to allow rollback. Defaults to 10.\nOnly used in ReplicaSet mode.",
          "type": "integer",
          "format": "int32",
          "default": 10,
          "minimum": 0
        },
        "selector": {
          "description": "Label selector for pods. Must match the pod template's labels.\nOnly used in ReplicaSet mode.",
          "type": "object",
          "properties": {
            "matchExpressions": {
              "description": "matchExpressions is a list of label selector requirements. The requirements are ANDed.",
              "type": "array",
              "items": {
                "description": "A label selector requirement is a selector that contains values, a key, and an operator that\nrelates the key and values.",
                "type": "object",
                "required": [
                  "key",
                  "operator"
                ],
                "properties": {
                  "key": {
                    "description": "key is the label key that the selector applies to.",
                    "type": "string"
                  },
                  "operator": {
                    "description": "operator represents a key's relationship to a set of values.\nValid operators are In, NotIn, Exists and DoesNotExist.",
                    "type": "string"
                  },
                  "values": {
                    "description": "values is an array of string values. If the operator is In or NotIn,\nthe values array must be non-empty. If the operator is Exists or DoesNotExist,\nthe values array must be empty. This array is replaced during a strategic\nmerge patch.",
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "x-kubernetes-list-type": "atomic"
                  }
                },
                "additionalProperties": false
              },
              "x-kubernetes-list-type": "atomic"
            },
            "matchLabels": {
              "description": "matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels\nmap is equivalent to an element of matchExpressions, whose key field is \"key\", the\noperator is \"In\", and the values array contains only \"value\". The requirements are ANDed.",
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            }
          },
          "additionalProperties": false,
          "x-kubernetes-map-type": "atomic"
        },
        "template": {
          "description": "Template describes the pods that will be created.",
          "type": "object",
          "required": [
            "spec"
          ],
          "properties": {
            "metadata": {
              "description": "Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata",
              "type": "object",
              "properties": {
                "annotations": {
                  "description": "Annotations is an unstructured key value map stored with a resource that may be set by external tools to store and retrieve arbitrary metadata. They are not queryable and should be preserved when modifying objects. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations",
                  "type": "object",
                  "additionalProperties": {
                    "type": "string"
                  },
                  "nullable": true
                },
                "labels": {
                  "description": "Map of string keys and values that can be used to organize and categorize (scope and select) objects. May match selectors of replication controllers and services. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels",
                  "type": "object",
                  "additionalProperties": {
                    "type": "string"
                  },
                  "nullable": true
                }
              },
              "additionalProperties": false,
              "nullable": true
            },
            "spec": {
              "description": "Specification of the desired behavior of the pod. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status.\n\nThe contents of this field are passed through directly from the operator to the created workload:\n- **ReplicaSet mode**: Passed to the ReplicaSet's pod template spec.\n- **Knative mode**: Passed to the Configuration's revision template spec. This supports standard PodSpec fields (containers, serviceAccountName, volumes, etc.) as well as Knative-specific fields (timeoutSeconds, containerConcurrency, etc.).",
              "x-kubernetes-preserve-unknown-fields": true
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "status": {
      "description": "Status of the RestateDeployment\nThis is set and managed automatically by the controller",
      "type": "object",
      "required": [
        "replicas"
      ],
      "properties": {
        "availableReplicas": {
          "description": "Total number of updated available pods (ready for at least minReadySeconds)",
          "type": "integer",
          "format": "int32",
          "nullable": true
        },
        "collisionCount": {
          "description": "Count of hash collisions for the RestateDeployment. The controller uses this field as a collision avoidance mechanism when it needs to create the name for the newest ReplicaSet or Configuration.",
          "type": "integer",
          "format": "int32",
          "nullable": true
        },
        "conditions": {
          "description": "Represents the latest available observations of current state",
          "type": "array",
          "items": {
            "description": "Conditions for the RestateDeployment status",
            "type": "object",
            "required": [
              "status",
              "type"
            ],
            "properties": {
              "lastTransitionTime": {
                "description": "Last time the condition transitioned from one status to another",
                "type": "string",
                "format": "date-time",
                "nullable": true
              },
              "message": {
                "description": "Human-readable message indicating details about last transition",
                "type": "string",
                "nullable": true
              },
              "reason": {
                "description": "Reason for the condition's last transition",
                "type": "string",
                "nullable": true
              },
              "status": {
                "description": "Status is the status of the condition (True, False, Unknown)",
                "type": "string"
              },
              "type": {
                "description": "Type of condition (Ready, Progressing, Available)",
                "type": "string"
              }
            },
            "additionalProperties": false
          },
          "nullable": true
        },
        "deploymentId": {
          "description": "Restate deployment ID for the current tag",
          "type": "string",
          "nullable": true
        },
        "desiredReplicas": {
          "description": "Desired number of replicas.\n- For ReplicaSet mode: reflects spec.replicas\n- For Knative mode: reflects revision.status.desired_replicas from latest revision",
          "type": "integer",
          "format": "int32",
          "nullable": true
        },
        "knative": {
          "description": "Knative-specific status",
          "type": "object",
          "properties": {
            "configurationName": {
              "description": "Name of the active Configuration for the current tag",
              "type": "string",
              "nullable": true
            },
            "latestRevision": {
              "description": "Latest ready revision name for the current Configuration",
              "type": "string",
              "nullable": true
            },
            "routeName": {
              "description": "Name of the active Route for the current tag",
              "type": "string",
              "nullable": true
            },
            "url": {
              "description": "Default URL for the current deployment",
              "type": "string",
              "nullable": true
            }
          },
          "additionalProperties": false,
          "nullable": true
        },
        "labelSelector": {
          "description": "The label selector of the RestateDeployment as a string, for `kubectl get rsd -o wide`",
          "type": "string",
          "nullable": true
        },
        "observedGeneration": {
          "description": "The generation observed by the controller",
          "type": "integer",
          "format": "int64",
          "nullable": true
        },
        "readyReplicas": {
          "description": "Total number of updated ready pods",
          "type": "integer",
          "format": "int32",
          "nullable": true
        },
        "replicas": {
          "description": "Total number of updated non-terminated pods targeted by this RestateDeployment",
          "type": "integer",
          "format": "int32"
        },
        "unavailableReplicas": {
          "description": "Total number of updated unavailable pods",
          "type": "integer",
          "format": "int32",
          "nullable": true
        }
      },
      "additionalProperties": false,
      "nullable": true
    }
  }
}
