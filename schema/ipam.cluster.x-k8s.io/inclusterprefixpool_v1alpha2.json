{
  "description": "InClusterPrefixPool is the Schema for the inclusterprefixpools API.",
  "type": "object",
  "properties": {
    "apiVersion": {
      "description": "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources",
      "type": "string"
    },
    "kind": {
      "description": "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds",
      "type": "string"
    },
    "metadata": {
      "type": "object"
    },
    "spec": {
      "description": "InClusterPrefixPoolSpec defines the desired state of InClusterPrefixPool.",
      "type": "object",
      "required": [
        "prefixes"
      ],
      "properties": {
        "allocationPrefixLength": {
          "description": "AllocationPrefixLength is the prefix length of each allocated\nsub-prefix (e.g. 64 means /64 allocations). Defaults to 64.\nMust be \u003e= the prefix length of each entry in Prefixes and \u003c= 128.",
          "type": "integer",
          "default": 64,
          "maximum": 128,
          "minimum": 1
        },
        "excludedPrefixes": {
          "description": "ExcludedPrefixes lists IPs/CIDRs/ranges to exclude from allocation.\nAny candidate sub-prefix overlapping an excluded entry is skipped.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "gateway": {
          "description": "Gateway is the next-hop IPv6 address carried unchanged to every\nallocated IPAddress.Spec.Gateway. Unlike IP pools, the gateway does\nNOT block any prefix from being allocated — it is informational only.",
          "type": "string"
        },
        "prefixes": {
          "description": "Prefixes is a list of IPv6 CIDRs from which sub-prefixes are allocated.\nEach entry must be a network-aligned CIDR (e.g. \"fd00::/48\").",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "status": {
      "description": "InClusterPrefixPoolStatus defines the observed state of InClusterPrefixPool.",
      "type": "object",
      "properties": {
        "ipAddresses": {
          "description": "Addresses reports the count of total, free, and used prefixes in the pool.\nThe JSON tag is \"ipAddresses\" to match the InClusterIPPool status shape,\nkeeping a consistent status contract across pool types.",
          "type": "object",
          "required": [
            "free",
            "outOfRange",
            "total",
            "used"
          ],
          "properties": {
            "free": {
              "description": "Free is the count of unallocated sub-prefixes.\nCounts greater than int can contain will report as math.MaxInt.",
              "type": "integer"
            },
            "outOfRange": {
              "description": "OutOfRange is the count of allocated prefixes currently non-allocatable\nunder spec (outside spec.Prefixes, overlapped by spec.ExcludedPrefixes,\nor with the wrong prefix length).\nCounts greater than int can contain will report as math.MaxInt.",
              "type": "integer"
            },
            "total": {
              "description": "Total is the total number of allocatable sub-prefixes in the pool.\nCounts greater than int can contain will report as math.MaxInt.",
              "type": "integer"
            },
            "used": {
              "description": "Used is the count of allocated sub-prefixes.\nCounts greater than int can contain will report as math.MaxInt.",
              "type": "integer"
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    }
  }
}
