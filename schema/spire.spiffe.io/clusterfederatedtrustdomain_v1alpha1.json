{
  "description": "ClusterFederatedTrustDomain is the Schema for the clusterfederatedtrustdomains API",
  "type": "object",
  "required": [
    "metadata",
    "spec"
  ],
  "properties": {
    "apiVersion": {
      "description": "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources",
      "type": "string"
    },
    "kind": {
      "description": "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds",
      "type": "string"
    },
    "metadata": {
      "type": "object"
    },
    "spec": {
      "description": "ClusterFederatedTrustDomainSpec defines the desired state of ClusterFederatedTrustDomain",
      "type": "object",
      "required": [
        "bundleEndpointProfile",
        "bundleEndpointURL",
        "trustDomain"
      ],
      "properties": {
        "bundleEndpointProfile": {
          "description": "BundleEndpointProfile is the profile for the bundle endpoint.",
          "type": "object",
          "required": [
            "type"
          ],
          "properties": {
            "endpointSPIFFEID": {
              "description": "EndpointSPIFFEID is the SPIFFE ID of the bundle endpoint. It is\nrequired for the \"https_spiffe\" profile.",
              "type": "string"
            },
            "type": {
              "description": "Type is the type of the bundle endpoint profile.",
              "type": "string",
              "enum": [
                "https_spiffe",
                "https_web"
              ]
            }
          },
          "additionalProperties": false
        },
        "bundleEndpointURL": {
          "description": "BundleEndpointURL is the URL of the bundle endpoint. It must be an\nHTTPS URL and cannot contain userinfo (i.e. username/password).",
          "type": "string"
        },
        "className": {
          "description": "Set which Controller Class will act on this object",
          "type": "string"
        },
        "trustDomain": {
          "description": "TrustDomain is the name of the trust domain to federate with (e.g. example.org)",
          "type": "string",
          "pattern": "[a-z0-9._-]{1,255}"
        },
        "trustDomainBundle": {
          "description": "TrustDomainBundle is the contents of the bundle for the referenced trust\ndomain. This field is optional when the resource is created.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "status": {
      "description": "ClusterFederatedTrustDomainStatus defines the observed state of ClusterFederatedTrustDomain",
      "type": "object"
    }
  }
}
