{
  "description": "ModelServer is the Schema for the modelservers API.",
  "type": "object",
  "properties": {
    "apiVersion": {
      "description": "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources",
      "type": "string"
    },
    "kind": {
      "description": "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds",
      "type": "string"
    },
    "metadata": {
      "type": "object"
    },
    "spec": {
      "description": "ModelServerSpec defines the desired state of ModelServer.",
      "type": "object",
      "required": [
        "inferenceEngine",
        "workloadSelector"
      ],
      "properties": {
        "inferenceEngine": {
          "description": "The inference engine used to serve the model.",
          "type": "string",
          "enum": [
            "vLLM",
            "SGLang"
          ]
        },
        "kvConnector": {
          "description": "KVConnector specifies the KV connector configuration for PD disaggregated routing",
          "type": "object",
          "properties": {
            "type": {
              "description": "Type specifies the connector type.\nIf you do not know which type to use, please use \"http\" as default.",
              "type": "string",
              "default": "http",
              "enum": [
                "http",
                "lmcache",
                "nixl",
                "mooncake"
              ]
            }
          },
          "additionalProperties": false
        },
        "model": {
          "description": "The real model that the modelServers are running.\nIf the `model` in LLM inference request is different from this field, it should be overwritten by this field.\nOtherwise, the `model` in LLM inference request will not be mutated.",
          "type": "string",
          "maxLength": 256
        },
        "trafficPolicy": {
          "description": "Traffic Policy for accessing the model server instance.",
          "type": "object",
          "properties": {
            "retry": {
              "description": "The retry policy for the inference request.",
              "type": "object",
              "properties": {
                "attempts": {
                  "description": "The maximum number of times an individual inference request to a model server should be retried.\nIf the maximum number of retries has been done without a successgful response, the request will be considered failed.",
                  "type": "integer",
                  "format": "int32"
                },
                "retryInterval": {
                  "description": "RetryInterval is the interval between retries.",
                  "type": "string",
                  "default": "100ms"
                }
              },
              "additionalProperties": false
            },
            "timeout": {
              "description": "The request timeout for the inference request.\nBy default, there is no timeout.",
              "type": "string"
            }
          },
          "additionalProperties": false
        },
        "workloadPort": {
          "description": "WorkloadPort defines the port and protocol configuration for the model server.",
          "type": "object",
          "required": [
            "port"
          ],
          "properties": {
            "port": {
              "description": "The port of the model server. The number must be between 1 and 65535.",
              "type": "integer",
              "format": "int32",
              "maximum": 65535,
              "minimum": 1
            },
            "protocol": {
              "description": "The protocol of the model server. Supported values are \"http\" and \"https\".",
              "type": "string",
              "default": "http",
              "enum": [
                "http",
                "https"
              ]
            }
          },
          "additionalProperties": false
        },
        "workloadSelector": {
          "description": "WorkloadSelector is used to match the model serving instances.\nCurrently, they must be pods within the same namespace as modelServer object.",
          "type": "object",
          "properties": {
            "matchLabels": {
              "description": "The base labels to match the model serving instances.\nAll serving instances must match these labels.",
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            },
            "pdGroup": {
              "description": "PDGroup is used to further match different roles of the model serving instances,\nmainly used in case like PD disaggregation.",
              "type": "object",
              "required": [
                "decodeLabels",
                "groupKey",
                "prefillLabels"
              ],
              "properties": {
                "decodeLabels": {
                  "description": "The labels to match the model serving instances for decode.",
                  "type": "object",
                  "additionalProperties": {
                    "type": "string"
                  }
                },
                "groupKey": {
                  "description": "GroupKey is the key to distinguish different PD groups.\nOnly PD instances with the same group key and value could be paired.",
                  "type": "string"
                },
                "prefillLabels": {
                  "description": "The labels to match the model serving instances for prefill.",
                  "type": "object",
                  "additionalProperties": {
                    "type": "string"
                  }
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "status": {
      "description": "ModelServerStatus defines the observed state of ModelServer.",
      "type": "object"
    }
  }
}
